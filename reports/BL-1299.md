# BL-1299 — the send file is rebuilt and the 583 reconciles exactly; the dashboard half is held by a live round

**Round:** BL-1299 · **Date:** 2026-08-15 · **Interpreter:** `.venv\Scripts\python.exe` 3.11.9, by full
path, for every number · **Vendor spend: $0.000000.** MX probing is stdlib DNS — 2,155 domains in
4.9 s, no API, no emails sent. Cap was $0.20 and none was needed.

---

## 0. The six lines

1. **The 583 is real, and it reconciles to the record.** The guard stored `master_rows: 62092`
   at build time; master now holds **62,675**. **62,675 − 62,092 = 583.**
2. **Rebuilt in ALL → MEME → TIKTOK order with MX refreshed first.** The primary went
   **3,978 → 4,270 (+292)**.
3. **New vs merged, separated: 292 genuinely new addresses, 9 merged onto existing leads, 50
   held back** — and all 50 for one reason.
4. **Partition held at zero overlap on disk**, and the n8n mirror is **byte-identical** to the
   primary by sha256.
5. **`master_leads.csv` is byte-identical** — same sha256 before and after.
6. **Section 2 was not attempted.** Live round **BL-1295** holds all five dashboard files and
   its stated intent is verbatim the same task. Reported, not worked around.

---

## 1. Where the 583 came from

The red suite was `tests/test_send_list_rebuild.py::test_master_rows_is_records_not_lines`,
asserting `62092 != 62675`. That is the guard doing exactly its job: the meta stores
`master_rows` **at build time** plus a `stale_rule`, and staleness is computed at READ time
rather than baked as a boolean — because *"a stored `stale: false` reads false forever while
master moves underneath it"*.

| | before | after |
|---|---:|---:|
| `ALL_BOT_READY.meta.json` `master_rows` | 62,092 | **62,675** |
| primary rows | 3,978 | **4,270** |
| meta `generated_at` | 2026-08-14 21:57:11 | **2026-08-15 21:15:32** |

> **A second staleness nobody had noticed:** the primary CSV carried an mtime of 2026-08-15
> **16:31:35** while its meta was still dated **2026-08-14 21:57:11** — the file had been
> touched without its meta being rewritten. Both are now consistent.

---

## 2. MX first — and one premise corrected

Refreshed **from master**, not from the send file, so the domains arriving with new leads are
covered too:

| | before | after |
|---|---:|---:|
| domains in `suppress_mx.json` | 1,993 | **2,152** |
| probed this run | — | 2,155 in **4.9 s**, **$0.00** |
| certain-bounce entries | 48 | **49** |

Verdict spread: MX_OK 2,072 · NXDOMAIN 44 · NO_MX_A_ONLY 30 · UNRESOLVED 4 · NULL_MX 3 ·
NO_RECORDS 2. **The dry run reported "would CHANGE 0 existing entries"** — the table was
already right about what it knew; what it lacked was **159 domains it had never seen**.

> **The brief's "9 of 13 domains UNPROBED" does not describe the current primary.** Measured
> before touching anything: `ALL_BOT_READY.csv` had **866 domains, 866 in the table, 0
> unprobed**. After the rebuild the primary carries **992 domains, 100.0% covered**. The
> vacuous-suppression risk was real historically; it is not the state today, and saying
> otherwise would have justified a fix that was not needed.

**One schema change, checked rather than assumed.** `refresh_mx.py` rewrote the table from a
flat `{domain: status}` map into `{_source, _rule, _verdicts, …, domains: {…}}`. I verified
the shipped reader handles both: `send_suppress.load_mx_table()` prefers `blob["domains"]` and
falls back to a bare map *ignoring `_`-prefixed metadata keys* (`send_suppress.py:139-144`).
No reader was broken. The one domain the resolver could not settle (`massdrop.com`) is
**retained, never cut** — the table's own rule is *"a domain ABSENT from this table is
UNKNOWN, never dead."*

---

## 3. The rebuild, in the order that matters

ALL → MEME → TIKTOK, because the later builders **skip addresses already present in the
earlier files**. Run in any other order, rows land in two files and the partition breaks in
the direction nobody checks.

**1/3 — ALL** → 3,978 → **4,270 deliverable editor emails (+292)**

| suppressed | n | | flagged, not cut | n |
|---|---:|---|---|---:|
| role_inbox | 369 | | agency | 383 |
| fossil_fit_test_day | 107 | | over 180 days | 111 |
| dead_mx | 31 | | | |
| no_mx_route | 17 | | **sendable by kind** | client 2,686 · clipper 1,584 |

**2/3 — MEME** → **874 rows.** MX coverage 99.7% of its 395 domains. Cuts: 19 invalid, 19
bio-link custom, 4 no_mx_route, 2 dead_mx, 33 duplicate, and **1 "already in primary
(superset property)"** — the order working, visibly. Re-verified on the finished set: 0
dead_mx, 0 invalid, 0 no_route. 151 role inboxes kept by the operator's own BL-1186 decision,
each flagged.

**3/3 — TIKTOK** → **123 rows.** MX coverage 100.0%. **1 excluded as already in another send
file**; 2 role inboxes kept and flagged. Independent re-check: CLEAN.

Every builder reported **"master: opened read-only. Zero API calls."**

---

## 4. New versus merged — counted separately, as asked

Splitting by `date_added` against the previous build's `generated_at` (2026-08-14T21:57:11):

| | n |
|---|---:|
| master records added since the last build | **583** ← the guard's number, exactly |
| …of which carry an email | 352 |
| …distinct addresses among those | 351 |
| **NEW — addresses never seen before, now in the primary** | **292** |
| **MERGED — on a new record, but the address already existed on an older lead** | **9** |
| held back, not exported | **50** |

**292 is exactly the `+292` the builder reported independently**, which is the cross-check
that the split is right rather than a plausible-looking decomposition.

### All 50 held-back addresses have one cause, and it is not suppression

Not MX, not role, not duplication: **50 of 50 carry no declared `lead_kind`.** They are part
of the 60 rows the builder itself refused, in its own words:

> *"UNROUTABLE: 60 row(s) carry no declared lead_kind and were NOT exported. Their funnel
> never said whether they are someone to hire (clipper) or someone to sell to (client). That
> is a funnel to fix, not a filter to loosen."*

All 50 are `platform=instagram` with an empty `verdict`. **This is the single highest-value
follow-up in the round: 50 paid-for addresses are one funnel field away from being sendable.**

---

## 5. The partition and the mirror, checked on disk

| file | unique addresses |
|---|---:|
| `ALL_BOT_READY.csv` | **4,270** |
| `MEME_PAGES_BOT_READY.csv` | 874 |
| `TIKTOK_PAGES_BOT_READY.csv` | 123 |
| `LATEST_BOT_READY.csv` (n8n mirror) | 4,270 |
| **union of the three** | **5,267** (was 4,975) |

| pair | overlap |
|---|---:|
| ALL × MEME | **0** |
| ALL × TIKTOK | **0** |
| MEME × TIKTOK | **0** |

**PARTITION HELD.** And the mirror is **byte-identical to the primary by sha256**, not merely
equal as a set — the CLI path refreshes it from the same rows
(`all_bot_ready.py:388`), and BL-1283 fixed the `[b]` menu path to match
(`control.py:3524-3531`).

> ⚠️ **The 16 top-level campaign CSVs remain subsets of the primary and must never be sent
> beside it.** `PANICBABY_SEND.csv` alone is 1,396 people; sending it alongside
> `ALL_BOT_READY.csv` double-mails everyone in the overlap. This round did not touch them and
> they are still there.

---

## 6. FLAGGED, NOT FIXED — the mirror's companion meta is two weeks stale

The mirror **CSV** is current (589,321 bytes, 4,270 rows, 21:15:31). Its **companion meta is
not**:

```
output/LATEST_BOT_READY.meta.json   (mtime 2026-08-01 22:55:08)
  { "timestamp": "2026-08-01T22:53:39", "campaign": "DAYLIGHT",
    "row_count": 2, "source_csv": "DAYLIGHT_2026-08-01_225339_BOT_READY.csv" }
```

**A companion file saying "2 rows, DAYLIGHT" sits beside a 4,270-row cumulative send list.**
That is precisely the pathology this brief names for the charts — a stale picture under a
current heading — living in the send path instead.

- **Owner:** `clippershq/writer.py:1837` declares it the *"companion status/freshness file"*
  and writes it atomically on a real funnel run; `recover_ig_emails.py:250` reads it.
- **The hole:** the mirror refresh in `all_bot_ready.py:388` writes **only the CSV**. BL-787
  and BL-1283 made the CSV/mirror pair correct and left the meta out of the pair.

**I did not hand-patch it, deliberately.** Writing a correct meta by hand fixes it once and
lets it re-rot on the very next CLI run, while making the defect invisible. The fix belongs in
`all_bot_ready.py` — write the companion beside the mirror — and that file is outside my
declared paths.

---

## 7. Section 3 — the leak-ratchet hit was REAL, and its owner has already fixed it

**Classification: a real silent-zero defect, not a checker artefact.**

The ratchet flagged `tools/brief_leakcheck.py:read_records`, created 20:38 by live round
BL-1297. That function is now fixed and **`tests/test_silent_zero_shape.py` is green (19
tests, OK)**. The remediation is in the file, and its author agrees with the ratchet in
writing:

> *"NARROWED TO FileNotFoundError, and `tests/test_silent_zero_shape.py` is why — it flagged
> this function the first time it ran, **and it was right**. A broad `except OSError: return
> []` makes a permission error, a locked file or a bad encode produce EXACTLY the value a
> genuinely empty set produces, so an unreadable mark set would report as 'no handles' and
> every brief measured against it would read clean."*

So: **the guard caught a genuine hole in a brand-new file within minutes of its creation, and
the owning round closed it.**

### And then it caught a second one, from a different round, inside the hour

Twenty minutes after I verified it green, `test_silent_zero_shape` went red again — on a
**different target**:

```
AssertionError: Lists differ: ['clippershq/meme_finder.py:load_page_verdicts'] != []
```

`load_page_verdicts` **does not exist at HEAD** (confirmed: `git show HEAD:… | grep -c` → 0).
It is brand-new work by live round **BL-1300**, whose own docstring calls it *"the whole of
judge-first on the funnel side"* — the change BL-1284 recommended, now being built.

**Both hits were real, neither was a checker artefact, and they came from two different
rounds within about an hour of each other.** The ratchet is doing precisely the job it was
built for: catching a silent zero in the window between a function being written and anyone
noticing. BL-1300 owns the fix; I did not touch `meme_finder.py`.

---

## 8. SECTION 2 NOT ATTEMPTED — the five chart elements are held and mid-edit

**Stop-and-report, per the preconditions.** Live round **BL-1295** (filed 20:24:32, not stale)
declares every file the task needs:

| file | claimed by BL-1295 | working tree |
|---|:--:|---|
| `dashboard/server.py` | yes | **` M` mid-edit** |
| `dashboard/static/app.js` | yes | **` M` mid-edit** |
| `dashboard/static/index.html` | yes | **` M` mid-edit** |
| `dashboard/static/app.css` | yes | **` M` mid-edit** |
| `dashboard/static/charts.js` | yes | clean |
| `tests/test_bl1295_dashboard_fields.py` | yes | — |

And its recorded intent, item (2), is the same task in the same words:

> *"fix the four charts that discard the reason the server already computes, **with an AST
> audit of every field the page reads against what its writer writes**"*

Its item (4) also covers the rebuild: *"confirm [B] rebuilds ALL→MEME→TIKTOK with the n8n
mirror refreshed and the partition held at zero overlap."*

**Two things worth passing to that round rather than duplicating:**

1. **It is FIVE elements, not four.** This brief corrects the count, and BL-1295's claim still
   says four.
2. **The rebuild it intends to *confirm* has now been *done* here**, from the CLI, at
   21:15:31–21:15:39. If BL-1295 presses `[b]` it will rebuild the same three files again;
   that is safe (the builders are deterministic against master and re-assert the partition)
   but it will move the mtimes, and its own before/after deltas will read `+0` rather than
   `+292`.

I did not touch a single dashboard file. Nothing in this round makes BL-1295's work harder
except that the send files it planned to inspect are now current.

---

## 9. Verification

| check | result |
|---|---|
| interpreter | `.venv\Scripts\python.exe` **3.11.9**, by full path, every number |
| preconditions | `claims_read.py` + `git status --porcelain` read **before** any write |
| claims at start | BL-1295, BL-1296, BL-1297, BL-1298 — **none stale** |
| claims at end | BL-1300 and BL-1301 appeared mid-round; **BL-1300 took `clippershq/meme_finder.py` at 21:15:42** |
| **master byte-identical** | **YES** — `9a95267b…b7a845` before and after, 26,188,667 bytes, mtime unchanged at 20:34:27 |
| vendor spend | **$0.000000.** MX is stdlib DNS. **No email sent.** |
| partition | ALL×MEME 0, ALL×TIKTOK 0, MEME×TIKTOK 0 — **held on disk** |
| n8n mirror | **byte-identical to the primary by sha256** |
| campaigns unchanged | `7a029ee5447cddd8` (separators) and `8e02f8d6f6307ae8` (defaults) reproduce from `cfg["campaigns"]` — same object, two serialisations |
| send guards | `test_send_list_rebuild` **OK** (was the red one), `test_send_superset` OK, `test_send_file_counts` OK, `test_send_identity` OK, `test_mx_probe` OK |
| my file footprint | six scratch data files + the rebuilt outputs. **I created no `.py` file at all** — every analysis ran inline |

### Suites — 322 green of 328, 802.8 s, **6 rounds in flight at 21:34**

**Not green, and none of the six is mine.** Each was re-run in isolation rather than excused:

| suite | isolated re-run | cause | owner |
|---|---|---|---|
| `test_config_contract.py` | FAIL | `RULE B: 1 undocumented key — ['page_verdicts_path']`, from `meme_finder.py:2987` | **BL-1300** |
| `test_silent_zero_shape.py` | FAIL | `['clippershq/meme_finder.py:load_page_verdicts']` — absent at HEAD | **BL-1300** |
| `test_dashboard_redesign.py` | FAIL | `dashboard/static/index.html`, ` M` | **BL-1295** |
| `test_run_announce.py` | FAIL | `'<details class="fold" open'` in `index.html`, ` M` | **BL-1295** |
| `test_suites_parse.py` | FAIL | a `DeprecationWarning: invalid escape sequence '\P'` while parsing the tools tree | not mine — I created no `.py` |
| `test_send_superset.py` | **OK** | passed alone; the suite red was churn under a concurrent rebuild | artefact |

**`clippershq/meme_finder.py` was clean at my round's start** and BL-1300 claimed it at
**21:15:42**, ten seconds after my rebuild finished. Every dashboard file is still ` M`.
The campaigns fingerprints and every other config check pass.

> With six rounds editing shipped modules underneath the runner, **a repo-wide run measures
> the tree's motion as much as HEAD.** The suites this round can actually affect — the send
> path and MX — are individually green above, and the one that flickered passes alone.

---

## 10. What should happen next

1. **Give the 50 unroutable leads a `lead_kind`.** They are paid for, deliverable, and one
   funnel field short of sendable. `platform=instagram`, empty `verdict`.
2. **Write the companion meta beside the mirror** in `all_bot_ready.py` — one line, and it
   closes a two-week-old lie in the send path.
3. **BL-1295: it is five chart elements, not four**, and the rebuild it planned to confirm is
   already done.
4. **Retire or clearly quarantine the 16 top-level campaign CSVs.** They are subsets of the
   primary covering 5,560 addresses and one of them is 1,396 people.

---

*BL-1299 · every figure from `.venv\Scripts\python.exe` 3.11.9 · vendor spend $0.000000 · no
email sent · master byte-identical · no dashboard file touched.*
