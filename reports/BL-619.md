# BL-619 — Merge BL-618: multiposting opened to every clipper

**Status: SHIPPED.** Merge commit `adfef927` is on `origin/main` (verified `origin/main == local HEAD`). Merge only; no new code authored.

## STEP 0 — TRUTH (with SHA)
* `checkpoint/BL-618` tip = `9f7e1c41c4807e29659b4d861109204bb0ae3cf9`.
* **Genuinely unmerged:** `git merge-base --is-ancestor origin/checkpoint/BL-618 origin/main` returned 1 (not an ancestor). Exactly one commit was on the branch and not on main: `9f7e1c4` itself.
* **The merge-base WAS main HEAD** (`c784ec33`), so BL-618 = main + 1 commit with zero divergence. The `--no-ff` merge had **zero conflicts** and the diff is exactly the 4 files BL-618 changed; nothing a live round holds was swept in (the working tree held only untracked `.claude/` + two stray docs, never staged).
* Merge commit `adfef927` parents: `c784ec33` (main) + `9f7e1c41` (BL-618). Correct.

## Conflicts
**None.** merge-base == main HEAD, so git did a clean three-way merge with no manual edits. No conflict markers in any changed file. BACKLOG.md took BL-618's single appended entry cleanly (1 `BL-618` occurrence, no `$2` capture-group corruption, since nothing had to be resolved).

## Confirmations on the MERGED result
All of BL-601's money traps hold on merged main, so the gate removal is safe:
* **BatchSubmitSection renders for a normal (non-test) clipper.** `page.tsx` renders `<BatchSubmitSection>` at line 309 inside a plain `<div>` with **no `realIsTestUser` / `isTestUser` condition** (`realIsTestUser` is fully removed, 0 occurrences). The `isTestUser` const still present in the page is the unrelated legacy always-on premium-layout rollout gate, not the batch gate.
* **The batch endpoint takes a normal authenticated clipper, still scoped to their own accounts and campaigns.** `POST /api/clips/batch` has **0** `isTestUser` gates; `canActAsClipperFromSession` is the only gate. The shared clipper core still resolves each row's account by ownership + campaign membership, so opening the gate does not widen what any clipper can reach.
* **TRAP 1 (per-row `clipAccountId`):** `row.clipAccountId` referenced 5 times; each row's own account is passed to the core (never a batch-wide account).
* **TRAP 2 (clipper core only):** `processOwnerSubmitLink` referenced **0 times** in both `batch/route.ts` and `batch/draft/route.ts`; `processClipperSubmitLink` is the only submit path (no force-approve, no earnings write, no placeholder minting).
* **Case-insensitive platform, server-side limit + platform constraint + dedup before any write:** the `campaignAllowedPlatforms` guard (2), the `todayCount` per-campaign limit pre-check (2), and the in-batch duplicate rejection (1) are all present and run before the sequential core commit; the clipper core (byte-identical) carries RULE 1/1b case-insensitively.
* **Single-submit unchanged:** `SubmitClipPremium` is not in the diff.

## Money-file safety
**6 money files + `tracking.ts` + `campaign-era.ts` + the clipper core + `clips/route.ts` BYTE-IDENTICAL by blob OID** on both refs and on the merged HEAD (`git rev-parse HEAD:$f` == `git rev-parse origin/main:$f`). None appear in the diff. No clip's earnings or status can change. No `prisma migrate`.

## Build (real exit codes, from logs, not piped)
`npm ci` exit 0 (wiped the generated Prisma client) → `npx prisma generate` exit 0 → `npx tsc --noEmit` exit 0 → `npm run build` **BUILD_EXIT=0**. Prebuild gates: check:prisma-bypass 0 violations; check:removed-fields OK; hooks gate `eslint --max-warnings 11` = **11 problems (0 errors, 11 warnings)** with eslint present (gate real). `/api/clips/batch` and `/api/clips/batch/draft` both in the route table. tsc and `next build` both ran.

## Ship
Verified push per BL-288: `git push origin bl619-merge:main` (`c784ec33..adfef927`), then fetch + assert `origin/main == adfef927`. Tags `pre-merge-bl619` / `post-merge-bl619` pushed.

**Rollback:** `git reset --hard pre-merge-bl619` (`c784ec33`) then force-push, or `git revert adfef927`.
