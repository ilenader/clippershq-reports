# BL-1264 — where the two killed rounds stopped

**Read-only forensic round.** No product code was written. Every number below was produced by
`.venv\Scripts\python.exe` (3.11.9) by full path; `python` on PATH is 3.13 and lacks the
project's packages, so it was never used. Nothing was re-run to "check" a killed round's
result — that would overwrite the numbers a resuming round needs.

His PC killed every terminal mid-run. Four rounds were in flight; **BL-1261** and **BL-1263**
published, **BL-1260** (TikTok funnel) and **BL-1262** (the judge's three disagreements) did
not. This report establishes exactly where those two stopped.

**Headline: nothing is broken, nothing of his is lost, and both rounds are resumable rather
than restartable.** The one real loss is a *cost figure*, not work — and it is recoverable
with a single free call. The real hazard was **300 lines of uncommitted work belonging to the
two departed rounds, sitting outside every path they declared and owned by nobody** — it has
been landed under their own names, not mine.

---

## 1. The shared index is empty, and that matters most

```
git diff --cached --name-only   ->   0 paths
git stash list                  ->   empty
```

The previous incident this brief warns about — a departed round's staged files being swept up
by the next commit under the wrong name, 1,034 unreviewed lines — **did not recur.** Neither
killed round left anything staged. The next round to commit cannot inherit their work by
accident.

Both killed rounds also committed **nothing**: `git log --all --grep` for BL-1260 and BL-1262
returns only BL-1261's commit (which merely mentions them in passing). Their entire output is
uncommitted working-tree state.

---

## 2. BL-1260 — the TikTok funnel

### Its claim

| | |
|---|---|
| filed | `2026-08-14T18:39:02` (`started_epoch` 1786725542.42) |
| still open | **yes**, 80 min at time of writing |
| declared | `scratch/bl1260_*`, `output/bl1260_tiktok_run/*`, `reports/BL-1260.md`, `spend.json`, `tiktok_pages_seen.json`, `suppress_mx.json`, `config.json` |

The claim file carries `"forced": true` with the reason *"re-filing my own claim with the
COMPLETE path list; the previous start REPLACED will_write rather than appending and left 5
paths uncovered."* **So 18:39:02 is a re-file, not the round's start** — BL-1260 is older than
its claim file says, which is consistent with it having been claimed for the TikTok funnel
shortly before BL-1261 filed.

### What it wrote

Eleven files under its scratch prefix, all untracked:

| file | bytes | last written |
|---|---:|---|
| `scratch/bl1260_suites_baseline.txt` | 27,417 | 18:53:20 |
| `scratch/bl1260_brief_addendum.md` | 6,869 | 18:58:06 |
| `scratch/bl1260_brief_tt.md` | 28,492 | 18:58:37 |
| `scratch/bl1260_judge.py` | 15,534 | 19:00:09 |
| `scratch/bl1260_seen_before.json` | 2,419 | 19:03:12 |
| `scratch/bl1260_rates.py` | 9,650 | 19:04:48 |
| `scratch/bl1260_sheet.py` | 9,151 | 19:05:47 |
| `scratch/bl1260_smoke.json` | 126 | 19:06:12 |
| `scratch/bl1260_smoke_out.json` | 2,189 | 19:06:34 |
| `scratch/bl1260_partition.py` | 4,109 | **19:11:30** |
| `scratch/bl1260_budget.json` | 1,175 | 18:40:35 |

`reports/BL-1260.md` **does not exist.** `output/bl1260_tiktok_run/` exists, was created at
18:40:35, and is **empty**.

### And it shipped product code — none of it under any path it declared

This is the round's largest deliverable and it is invisible to every attribution mechanism in
the repository. All three were dirty and uncommitted; `round_of_path()` returns `None` for all
three and no live claim held any of them:

| file | change | last written |
|---|---|---|
| `clippershq/tiktok_finder.py` | **+260 / −3** | 19:03:06 |
| `clippershq/run.py` | **+23 / −1** | 18:54:23 |
| `tests/test_bl1260_tiktok_run.py` | **new, 11,909 B** | 19:01:47 |

Every line is self-attributed to BL-1260 in its own comments. What it fixes:

1. **The funnel ended in the air.** BL-1259 connected the dashboard option to the module; the
   chain still stopped one step later, because `discover()` returned rows, `run_funnel` printed
   them, and nothing offered them to `master_leads.csv`. BL-1260 added the missing call site.
2. **The contact sheet was promoted out of `scratch/`** into the shipped module — rebuilt from
   `video.cover`, riding on a handle-search the funnel already pays for, because Playwright
   cannot reach a TikTok profile at all.
3. **`CAMPAIGNS["tiktok"]` said `TIKTOK` while `tiktok_finder.CAMPAIGN` says `TIKTOK_FINDER`.**
   `_spend_now` filters the ledger on an exact campaign match, so the status card reported
   **$0.0000 for a run that really spent.** It also adds the absent `meme_pages` key — absent
   is worse than wrong there, because `CAMPAIGNS.get()` returns `None` and `_spend_now` then
   falls back to the *whole ledger* and reports another round's spend as this run's.

`tiktok_finder.py` was last written at **19:03:06, thirty-nine seconds before the second run
started at 19:03:45** — so the 34 sheets were captured *by this code*, and it held up for
twenty minutes. **`tests/test_bl1260_tiktok_run.py` passes: 19 checks** (§8).

The master write nevertheless never happened: `master_leads.csv` is unchanged since
2026-08-12 21:02:40 and `output/TIKTOK_PAGES_BOT_READY.csv` since 16:43:45. The run was killed
inside the capture loop, before it reached the write.

### The last thing it did — and it is later than its own scratch prefix

The round's real product is **not** under any path it declared. The shipped capture writes to
`output/tiktok_sheets/<stamp>/`, so BL-1260's run landed in
`output/tiktok_sheets/20260814_190349/`:

- **34 profile sheets**, 16.7 MB, all 465×992
- **all 34 decode cleanly** (PIL `.load()` on every one; zero malformed, zero truncated)
- first written **19:05:14**, last written **19:24:22**

**19:24:22 is the last thing either killed round did** — three minutes after BL-1263's final
commit (`be2d13a`, 19:21:09). The run was still capturing when the terminal died.

Two consequences:

1. The surviving product sits outside every path BL-1260's claim protects, and outside
   `round_of_path()`'s reach — that function reads the **basename** only, and
   `20260814_190349` carries no round id. It is owned by nobody.
2. Both run logs — `logs/runs/20260814-185553-tiktok-27012.log` and
   `logs/runs/20260814-190345-tiktok-27012.log` — are **0 bytes**. The run's own log says
   nothing about where it stopped; the sheet mtimes are the only record.

### Was the capture wasted? No.

`scratch/bl1260_seen_before.json` holds the 125 handles the round had already seen. Of the 34
captured, **0 were already seen — all 34 are genuinely fresh supply.**

### Dirty declared paths

`claim.py`'s own `dirty_declared("BL-1260")` returns **12**:

- `suppress_mx.json` — **tracked, modified**, 18:39:29 (27 s after the claim; MX probing is
  free, so this is the round's first action)
- the 11 untracked `scratch/bl1260_*` files

`suppress_mx.json` is the one that blocks a *different* round on that path.
`tiktok_pages_seen.json` and `config.json` were declared but are **clean** — the seen-file has
not moved since 2026-08-08 19:31:19.

Note what `dirty_declared` **cannot** see: the 283 lines of product code and the new test
above. A round's dirty check reads its own declaration, so work outside it is invisible to the
round that wrote it as well as to everyone else.

---

## 3. BL-1262 — the judge's three disagreements

### Its claim

| | |
|---|---|
| filed | `2026-08-14T18:41:34` (`started_epoch` 1786725694.27) |
| still open | **yes**, 77 min at time of writing |
| declared | `reports/BL-1262.md`, `scratch/bl1262_*`, `output/bl1262_rejudge/*`, `spend.json` |

Its intent, verbatim from the claim: fix the three disagreements *"in the DECIDING layer, not
the seeing layer"*, remove the dialogue-overrides-capability precedence, add the still-picture
disqualifier, **"then re-score all 100 blind with before/after agreement and prove no page
moved away from his mark."**

### What it wrote

Fourteen files, all untracked. `reports/BL-1262.md` does not exist.
`output/bl1262_rejudge/` **was never created.** Last write: `scratch/bl1262_suites.txt` at
**19:17:03**.

### What it actually established — this is the part worth keeping

It ran a 16-page regression (`scratch/bl1262_v7_regression.json`, 87 KB, 19:02:01) plus a
3-page baseline and a 3-page miss re-check. Against his marks:

| | before (v4 brief) | after (v7 brief) |
|---|---:|---:|
| agreeing with his mark | **16 of 19** | **12 of 19** |

**The three target disagreements were all fixed** — `acog.hitbox`, `maestremoros` and
`thedunkcentralhq` each moved onto his mark. **And seven pages that were already right broke:**
`aboutfacts_786`, `cultr.ent`, `filmoviment`, `merneculture.in`, `midgedotcon`,
`shadow_movies_05`, `songss`.

The cause is recorded page-by-page in `scratch/bl1262_broke_reasons.txt`. Five of the seven
died on the **new still-picture disqualifier** or on a **zero qualifying-tile count** — i.e.
the fix for the three overshot. This is the same shape BL-1261 published independently: *his
still-picture rule kills a page he approved.*

**The round then wrote the corrections** — `scratch/bl1262_v7_corrections.md` (12,963 B,
19:07:09) and a revised `scratch/bl1262_brief_v7.md` (34,848 B, 19:07:23) — **and never
re-scored anything against them.** Its last spend row is 19:02:41, five minutes before the
corrections existed. The corrected brief has **zero paid validation.**

### Dirty declared paths

`dirty_declared("BL-1262")` returns **14**, all untracked `scratch/bl1262_*`. Nothing tracked
is dirty under its own declaration — but see §6.

---

## 4. Paid work: one loss, and it is a number, not a result

### BL-1262 — every dollar reached disk

| spend row | outputs on disk | match |
|---|---|:--:|
| `bl1262_diagnosis` 18:55:35 **$0.449548** (6 calls) | `bl1262_baseline_v4.json` $0.280007 (3 pages) + `bl1262_v7_misses.json` $0.169541 (3 pages) = **$0.449548** | exact |
| `bl1262_regression16` 19:02:41 **$0.706628** (17 calls) | `bl1262_v7_regression.json` $0.706628 (16 pages + 1 warm) | exact |

**$1.156176 spent, $1.156176 accounted for. Nothing lost.**

One provenance defect: all three result files stamp `"round": "BL-1258"`, because they were
produced by `scratch/bl1258_judge_api.py`, which stamps its own name. **BL-1262's paid evidence
carries BL-1258's round id** — a resuming round must not read those files as BL-1258's.

### BL-1260 — $0.0432 discarded, and a capture bill that was never written down

| spend row | outputs on disk |
|---|---|
| `bl1260_run_killed_for_persistence_fix` 19:03:34 **$0.043200** (72 calls) | **none** — `output/bl1260_tiktok_run/` empty, no sheet predates 19:05:14, seen-file untouched since 08-08 |
| `bl1260_tiktok_judge` 19:06:36 **$0.116699** (1 vision call) | `bl1260_smoke_out.json` 19:06:34 — reconciles |

The first row is spend with no surviving output, but the label says so itself: the run was
killed deliberately for a persistence fix. **$0.0432 knowingly discarded.**

**The real gap runs the other way.** The 34 sheets were captured 19:05:14 → 19:24:22, and:

```
spend.json last write                              19:06:36
tiktok_spent_usd                                   0.641600
BL-1260's own recorded baseline (bl1260_budget)    0.598400
delta                                              0.043200   <- exactly the 72-call row
LamaTok spend recorded after 19:03:34              0.000000
```

**Roughly twenty minutes of capture produced 34 sheets and cost the vendor something that was
never written to the ledger.** The amount is not recoverable *from the ledger* — but it is
recoverable. BL-1260 wrote its vendor receipt baseline down before it spent anything
(`scratch/bl1260_budget.json`: **446,717 requests remaining / $268.0455, read 18:41**), and a
LamaTok `/sys/balance` read is free. **One free call closes this.**

**The mechanism is structural, not an accident.** `clippershq/tiktok_finder.py` has exactly one
ledger write, at line 1374, and it sits **after `discover()` returns** — and `discover()` *is*
the twenty-minute capture loop. Spend is metered once, at the end, from `used["calls"]`:

```python
res = discover(cli, hashtags=tags, ... sheet_dir=sheet_dir)   # <- killed in here
...
_main.record_aux_spend(..., campaign=CAMPAIGN, tt_calls=used["calls"], ...)   # never reached
```

**A killed run records nothing, however long it ran and however much it bought.** Confirmed
against the ledger: `TIKTOK_FINDER` has **0 rows, ever** — not one in 2,801. The code comment
beside that call records the same failure from BL-1191, which measured $0.0804 by quota delta
and wrote no row. It is worth fixing by checkpointing spend as calls are reserved rather than
at the end; that is a change to shipped code and belongs to BL-1260, not to this round.

Records that were started and never appended: `tiktok_pages_seen.json` (34 handles never
persisted), `output/bl1260_tiktok_run/` (created, never written), `dashboard/static/runs.json`
(no record of either TikTok run), and both 0-byte run logs.

### A false alarm, checked and cleared

`config.backups/spend.json.20260814_19{08,09,14,16,17}*.bak` appear to postdate spend.json's
own 19:06:36 mtime, which would read as ledger writes that never landed. They carry `runs=1..6`
and totals of $0.002–$2.504: **they are test-fixture ledgers backed up by the suite run**, not
the live ledger. Not lost work.

---

## 5. His marks are intact

### The 100 from sheet BL-1257-FRESH100

| | |
|---|---|
| rows / distinct handles | **100 / 100** |
| sha256 | `151d5d7d5aa392d16ddf0fef3e5ffd28e984a01f47a0f400a12af389780aa587` |
| bytes | 13,594 |
| mtime | 2026-08-14 **18:26:20** — *before* BL-1260 (18:39:02) and BL-1262 (18:41:34) filed |

| his mark | judge said | count | expected |
|---|---|---:|---:|
| GOOD | REJECT | **79** | 79 |
| GOOD | WANT | **18** | 18 |
| BAD | WANT | **2** | 2 |
| BAD | REJECT | **1** | 1 |

The three BAD are exactly `acog.hitbox` (WANT), `thedunkcentralhq` (WANT), `maestremoros`
(REJECT). **Matches the expected breakdown exactly.** Neither killed round touched the file.

Two copies exist and are **byte-identical** (same sha256): `~/Downloads/` and
`output/BL-1257-FRESH100_marks.jsonl`.

> **Durability risk, flagged not fixed:** `output/` is gitignored (`.gitignore:98`) and
> Downloads is not a repository. **The only human ground truth for the 100 has no committed
> copy.** Committing it needs a decision about the leak rule, so this round reports it rather
> than acting.

### The earlier 623

`ground_truth/review_marks.jsonl`, tracked and **modified**:

| | |
|---|---|
| sha256 (working tree) | `3b06a611a55c2a9240a66b5052dc0a9953827f90501344c259c044084535ddd2` |
| bytes / rows | 179,917 / **669** |
| **distinct handles / distinct keys** | **623 / 623** — the 623 is 669 rows with 46 re-marks |
| by sheet | BL-1196-500 463, bl1186_pages 103, BL-1207-FRESH50 52, bl1190_pages 51 |
| marks | GOOD 463, BAD 206 |

Against `HEAD`: **515 insertions, 0 deletions.** Set-compared line by line: **0 lines present
in HEAD and missing from disk**, 509 added. The dirt is purely additive and its mtime is
**2026-08-11 14:24:10** — three days before either round filed. **Nothing was lost; the dirt is
not theirs.**

---

## 6. Cleanup: one claim decision, one orphan landed

### Neither claim was released, and that is the correct outcome

The 480-minute rule **does not apply and would not help if it did**:

- Both claims are ~80 and ~77 minutes old, far under `STALE_TAKEOVER_MINUTES = 480`.
- The rule escalates a **path's protection**; it never ends a claim.
- `claim.py end` refuses while a round's declared paths are dirty — and `dirty_declared`
  returns **12 for BL-1260** and **14 for BL-1262**.

The order is always land the work, then release. Releasing now would strip protection from
exactly the work the next two rounds must resume. **Both claims stay open. Recorded here as
the decision, not an oversight.**

### Orphan 1 — BL-1260's product code, 283 lines and a test

The three files in §2 were **dirty, uncommitted, owned by nobody, and outside BL-1260's own
declaration**, so neither its claim nor its dirty check could see them. That is the 1,034-line
incident's exact shape, at three times the size: one careless `git add -A` and 283 lines of
TikTok funnel work lands under somebody else's name.

Landed as **`36b86b7`** under **BL-1260's** attribution — `clippershq/run.py`,
`clippershq/tiktok_finder.py`, `tests/test_bl1260_tiktok_run.py`, 536 insertions, 4 deletions.
`dashboard/static/runs.json` is also dirty and was **deliberately left alone**: it carries a
99,948-line diff that is not BL-1260's.

The write-point guard reported 3 email-shaped literals and 0 in a gitignored lead store; all
three are the same one-character-user synthetic placeholder inside the test's fixtures, and the
publish scanner's join against the 6,954 indexed lead addresses returns **0**. **No lead data
was committed.**

### Orphan 2 — BL-1262's judge harness

`scratch/bl1258_judge_api.py` was **tracked, dirty, +17/−2, modified 18:54:12** — inside
BL-1262's window. It is BL-1262's judge harness, and the diff **self-attributes to BL-1262 in
three of its own comments**. BL-1262 never declared it, so:

```
round_of_path("scratch/bl1258_judge_api.py")  ->  BL-1258     (basename only)
rounds_owning(...)                            ->  {}          (no live claim)
dirty_declared("BL-1262")                     ->  does not include it
```

**Owned by nobody, invisible to its own round's dirty check, and one careless `git add -A` away
from landing under someone else's name.** That is the 1,034-line incident's exact shape.

Landed as **`137e379`**, under **BL-1262's** attribution rather than mine, with
`--foreign BL-1258` writing a Claim-Override trailer so the record says the *name* is
BL-1258's and the *change* is BL-1262's. The shared index was empty before and after.

The change itself: `max_tokens` was bound as a **default at import**, so `--max-tokens` changed
nothing and the 1500 ceiling truncated `thedunkcentralhq`'s verdict into unparseable JSON.
Note that BL-1263 published the same class of defect at 19:20 — **two rounds hit the invisible
token-cap bug independently within the same hour.**

---

## 7. Per round: what is done, what remains

### BL-1260 — TikTok funnel

**Done (do not redo):**

- Suite baseline captured (`bl1260_suites_baseline.txt`, 3 red of 315 at 18:53).
- Budget declared before any spend, **with the vendor receipt baseline** — this is what makes
  the missing cost recoverable.
- The TikTok brief is **complete and composed**: `bl1260_brief_tt.md` (28,492 B) already
  contains the operator addendum as C13/C14.
- **The product code is written, green and now committed** (`36b86b7`): the master-write call
  site, the contact sheet promoted out of `scratch/`, the campaign-table fix, and a 19-check
  test that passes. This was the round's biggest deliverable and it was sitting unowned.
- The judge is **written and proven end to end** on one page: `bl1260_judge.py` →
  `bl1260_smoke_out.json`, verdict WANT, $0.116699, 0 truncations, 0 parse errors.
- `bl1260_rates.py`, `bl1260_sheet.py`, `bl1260_partition.py` are **written** — none has ever
  been run to an output.
- **34 fresh profile sheets are captured, intact, and paid for.** Zero overlap with the
  125-handle seen-list.

**Outstanding:**

- **33 of the 34 sheets are unjudged.** They can be judged straight from disk — no re-capture,
  no re-spend. `bl1260_smoke.json` shows the exact input shape.
- **The persistence fix is written and unit-tested but never verified end to end.** The second
  run ran *with* it for twenty minutes and still reached no store: `master_leads.csv` unchanged
  since 08-12, `tiktok_pages_seen.json` since 08-08, `output/bl1260_tiktok_run/` empty. The run
  died inside the capture loop, before the write. **A green unit test is not the proof here;
  one completed run is.**
- Spend is metered once, after `discover()` returns, so a killed run banks nothing. Worth
  checkpointing as calls are reserved — shipped-code change, BL-1260's to make.
- Email base rates on fresh pages: not measured (`bl1260_rates.py` never run).
- Operator sheet: not built. Three-way partition: not run. `reports/BL-1260.md`: absent.
- Its capture bill is unrecorded — one free `/sys/balance` read against the 18:41 baseline.

**One line:** *the code is written, tested and now committed and the supply is captured and
paid for — 34 fresh sheets, a finished brief and a proven judge sit on disk; resume by judging
the 33 and proving one run reaches the master, not by re-crawling.*

### BL-1262 — the judge's three disagreements

**Done (do not redo):**

- The diagnosis: the three misses are **decision-layer, not seeing-layer** failures — on each,
  the judge's own written reason already contained the facts and then decided the other way.
- The v7 brief, and a 16-page paid regression proving what it does. **$1.156176 of evidence,
  all on disk.**
- **The three target disagreements are fixed.** Confirmed against his marks.
- The overshoot is measured and explained per page: **7 previously-correct pages broke**,
  16/19 → 12/19. `bl1262_broke_reasons.txt` names the cause for each.
- Corrections written: `bl1262_v7_corrections.md` + revised `bl1262_brief_v7.md`.
- Its `max_tokens` fix is now committed (`137e379`).

**Outstanding:**

- **The corrections have never been scored.** They were written 19:07, five minutes after the
  last paid call. Zero validation.
- **The claim's headline was never attempted**: "re-score all 100 blind." The largest run was
  **16 pages**, hand-picked as at-risk. `output/bl1262_rejudge/` was never created.
- The seven regressions are unresolved — the still-picture disqualifier and the
  qualifying-tile gate over-fire as written.
- `reports/BL-1262.md`: absent.

**One line:** *the three are fixed and the fix's collateral damage is already measured and
explained — resume by scoring the corrected v7 brief, first on the 7 it broke, then on all
100.*

### A collision the resuming rounds must settle between them

Both rounds independently withdrew the dialogue-overrides-capability rule within the same hour,
**and set different gates under the same section numbers**:

| | BL-1260 `bl1260_brief_addendum.md` C13 | BL-1262 `bl1262_v7_corrections.md` C14 |
|---|---|---|
| gate | **≥ 1** qualifying clip → WANT | **≥ 2** → WANT; exactly 1 → judge and say so |
| C14 means | "a page whose own evidence names a disqualifier must not pass" | the qualifying-tile definition itself |

Two live briefs, both with a "C13" and a "C14", meaning different things. Whichever round
resumes second must reconcile these rather than inherit whichever file it opens first.

### One thing to check before trusting the 34 sheets

They are **465 px** wide. BL-1257 measured the capture floor at **467×467** (n=14; agreement
breaks at 374, and illegible-tile count is the leading indicator). That floor was measured on
square Instagram grids, not 465×992 TikTok sheets, and the one sheet actually judged reported
`tiles_text_present_but_ILLEGIBLE: 0`. **Worth one control before judging all 33 — not a
blocker.**

---

## 8. Verification

| check | result |
|---|---|
| interpreter for every number | `.venv\Scripts\python.exe` 3.11.9, by full path |
| preconditions | `tools/claims_read.py` (not `claim.py`) + `git status --porcelain` |
| product code written | **none** — this round reports state |
| staged in shared index | **0**, before and after |
| campaigns fingerprint | default `8e02f8d6f6307ae8`, compact `7a029ee5447cddd8` — **unchanged**, and `tests/test_governance_rules.py` passes |
| config.json | parses, 164 top-level keys, 5 campaigns |
| his 100 | 100 rows, 79/18/2/1, sha256 `151d5d7d…` |
| his 623 | 623 distinct keys of 669 rows, sha256 `3b06a611…`, **0 rows lost vs HEAD** |
| claims released | **none** — both fail the dirty-path test and neither is near 480 min |
| orphans landed | `36b86b7` BL-1260's name; `137e379` BL-1262's name with `--foreign BL-1258` |
| lead data in this report | none — no address quoted; the 34 fresh handles given by count and path only |

### Suites

`tests/run_all.py --jobs 4`, 3 rounds in flight at 20:07: **4 red of 316 (458.9 s)**. Set
against the two killed rounds' own runs:

| suite | BL-1260 18:53 | BL-1262 19:17 | BL-1264 20:07 |
|---|:--:|:--:|:--:|
| `memebot/meme/tests/test_render.py` | red | red | red |
| `memebot/meme/tests/test_ocr.py` | red | red | red |
| `tests/test_doc_citations.py` | red | red | **green** |
| `tests/test_estimated_flag.py` | green | red | red |
| `tests/test_repost_export.py` | green | red | red |
| **total** | 3 red / 315 | 5 red / 316 | **4 red / 316** |
| `tests/test_bl1260_tiktok_run.py` | — | — | **PASS, 19 checks** |

**All four reds predate this round**: two are red in every run going back to 18:53, and the
other two were already red in BL-1262's 19:17 run — before either of my commits. The 315 → 316
jump is BL-1260's new test file appearing. `tests/test_governance_rules.py` passes, which is
what pins the campaigns fingerprint. A suite count is a moment, not a property.

Evidence: `scratch/bl1264_state.json`, `scratch/bl1264_marks.json`, `scratch/bl1264_spend.json`,
`scratch/bl1264_suites.txt`.

No lead data appears in this report: no address is quoted, and the 34 freshly discovered
handles are referenced by count and path only.
