# BL-922: the owner selects which campaign states his clips list shows, the scope goes into the query, and every count says what it counts

**2026-09-21 20:19:09 UTC (first DB read) to 2026-09-22 09:5x UTC · DB `now()` cast `::text` on every read · BUILD, merged.**
Base `origin/main` @ `98f87993` (BL-920, the sha production reported at `/api/version`). Branch `checkpoint/BL-922` @ `c32ec1af`, merge `d19025ec`, tags `pre-BL-922` / `post-BL-922` / `pre-merge-BL-922` / `post-merge-BL-922`, all pushed and verified (`safe-push`: origin == local both times). Merge tree OID `c5cbb28a` == branch tree OID, so build #2 on the branch IS the merge build. `checkpoint/BL-723` confirmed not an ancestor. Worktrees `C:/w922` (branch) and `C:/w922m` (main baseline) removed and verified gone. **Requires a Railway REDEPLOY.**

**NOTHING UNREMOVABLE. NOTHING WRITTEN.** The round created no row: the `bl922sbx-` prefix named 0 rows before and 0 after; every money and status fingerprint identical at open and close; one DB connection held serially, subagents opened none; no vendor call, no Apify actor.

**Model split.** Opus judged, designed, wrote every query and page change, the guard and this report. Haiku (Explore) did the call-site census: its totals were READ and re-derived, not inherited: it reported 84 Family 1 call sites, my own `grep -c` says **82 in 55 files** (VERIFIED), and its "12 Family 2" matched (VERIFIED). Its claim that `marketplace-v2-poster-dashboard.ts` is a poster-facing list was VERIFIED by reading (three `clip.findMany`, `take: 5000` and `take: 200`, no campaign-state predicate). The accessibility lead's review (aria, keyboard, live-region, contrast specialists) produced six MUST-FIX items; every one was VERIFIED against the file before being applied, and one of its own findings it corrected itself (`reqIdRef` exists at page.tsx:481).

## PART 1 — what was hidden, and where the hiding happened

**The real states, verbatim from `prisma/schema.prisma:668`:** `enum CampaignStatus { ACTIVE PAUSED COMPLETED DRAFT PAST }`, plus `pauseSource PauseSource?` (`enum PauseSource { AUTO MANUAL }`, null = never paused) and `isArchived Boolean @default(false)`, which is orthogonal to status. **The owner's words, mapped:** running = `ACTIVE`; paused = `PAUSED` with `pauseSource` MANUAL or null (the card badge "PAUSED · MANUAL"); frozen = `PAUSED` with `pauseSource` AUTO (badge "PAUSED · BUDGET", the button that sets it is "Pause & Freeze"); completed = `COMPLETED`; finished = `PAST` (the schema calls it "archival display state", the showcase at `/admin/past-campaigns`); archived = `isArchived = true`, any status. The first five all require `isArchived = false`. Whether he means COMPLETED or PAST by "finished" does not change the outcome: both are in the wider view. **Era (`src/lib/campaign-era.ts`) is a money concept**: the most recent AUTO-pause-then-resume closes an earning era for older clips. It is read by 14 money and mutation files and by **zero** list paths (`grep -c` on the route and the page: 0 and 0). It excludes nothing.

**The populations (2026-09-21 20:24:00 UTC), live clips only:** running 29 (2 campaigns), paused 1,179 (1), frozen 939 (1), completed 162 (2), finished 6,835 (10, six of them holding clips), archived 1,088 (20), draft 0 (2 archived fixtures). Total 10,232; non-archived 9,144; non-archived and not running 9,115. **The owner's platform has moved:** the only running campaign with clips is ANGIE BROWN (marketplace, 29 clips); 66.8 percent of every clip he has ever had sits on finished campaigns.

**Where the owner's list filters, stated plainly: IN THE QUERY, and by campaign state it did not filter at all.** `src/app/api/clips/route.ts` (the block at :461-466, BL-850) lifts the archived exclusion for `role === "OWNER"`, so the unscoped owner list was already `{ isDeleted: false }` plus his own filters: **10,232 rows, the whole platform, archived included.** The status chips and the campaign picker are sent to the server (BL-837, BL-269); the client refine at page.tsx:1242 is a no-op over an already-scoped set. There is no client-side campaign filter to name. **What hid the old campaigns was two other things:** (1) **rank**: newest first, page of 30, and the running campaign receives every new clip, so on 2026-09-21 page one held 20 ANGIE BROWN and 10 Zhus Edit (paused, still receiving clips) and the first finished campaign's clip sat at **row 289**; (2) **the campaign picker could not name a finished campaign.** BL-850 wrote "the campaign filter can name every campaign now" and it was **not what shipped**: `scope=manage` keeps `status != PAST` (campaigns/route.ts:125) and the second request `archived=true&includePast=true` returns only `isArchived = true` rows (:107), so a campaign that is finished but not archived was in neither list. Measured on the real API before any change: the picker offered **26 of 36** campaigns, 0 of the 10 PAST ones, which hold 6,835 clips.

**Every clips-list surface, counted with `grep -c` (Family 1 `clip.findMany`: 82 call sites in 55 files under src/app + src/lib; Family 2 `marketplaceV2Clip.findMany`: 12; a third family, raw SQL selecting from clips: 15 files, all aggregates):**
| surface | who | campaign-state predicate | where applied | take | orderBy |
|---|---|---|---|---|---|
| `/api/clips` GET → `/admin/clips` | OWNER (ADMIN, REVIEWER scoped) | OWNER none; non-owner `campaign.isArchived=false` | query (route.ts:461-466) | `limit` ≤ 1000, page 30; legacy array 500 | `createdAt desc` (now + `id desc`) |
| `/api/clips/mine` → `/clips` | CLIPPER | `campaign: { isArchived: false }` (:106) | query | 5000 / `limit` | `createdAt desc` |
| `/api/admin/users/[id]/clips` | OWNER | none | query | 10 | `createdAt desc` |
| `/api/admin/watch-clips` | OWNER, watch reviewer | non-owner `isTestCampaign=false` | query | 30 to 60 | `createdAt desc` |
| `creator-scan/our-clips.ts` | OWNER | none | query | 40 | `createdAt desc` |
| `/api/client/campaigns`, `[id]` | CLIENT (not built), OWNER | none; `.filter(status)` in JS over `take: 5000` | **after load** | 5000 | none / `createdAt desc` |
| `marketplace-v2-poster-dashboard.ts` | POSTER | none | query | 5000, 200 | `createdAt desc` |
| `marketplace-v2-owner-overview.ts`, queue | OWNER | none on clips; picker `ACTIVE, PAUSED` ∪ campaigns holding clips | query | 20 / 50 | `createdAt desc` |
The CLIENT routes are the one place a status filter runs over loaded rows; the role is not built and no user holds it; logged, not changed.

**Counts and money derived from loaded rows on the owner's page:** one count, `activeClipsByCampaign` (page.tsx, the Track modal's "N clips · status"), is computed from the rows loaded, at most one page; it was wrong before this round and is unchanged by it (BACKLOG). **No money total exists on the page.** Per-row figures read the stored `Clip.earnings` / `baseEarnings` / `bonusAmount` written by `writeClipEarnings` and the stored `agency_earnings.amount`, with BL-756's fallback recompute for CPM_SPLIT rows lacking an agency row (pre-existing, untouched). The page's `totalCount` comes from `db.clip.count({ where })` over the same where as the rows (route.ts).

**The clippers' own pages carry ONE restriction and it is different:** `/api/clips/mine:106` hides clips of ARCHIVED campaigns from every clipper and hides nothing by status. The poster's v2 dashboard hides nothing. **Not changed** (1,771 people). His call.

## PART 2 — the control, in the query

* **`src/lib/campaign-state-scope.ts`**: the six words, their labels, `parseCampaignStates` (unknown dropped, case folded, de-duplicated, canonical order), `campaignStateBranch` (paused spelled as `OR [MANUAL, null]` because `{ not: "AUTO" }` compiles to `<> 'AUTO'` and drops nulls), `campaignStateRelation` (an OR of branches, null for empty) and `describeCampaignStates` (the words for the sentence).
* **`src/app/api/clips/route.ts`**: after the BL-850 block, `campaignStates` is parsed for OWNER only and merged into `where.campaign` (**:488-493**). Empty or unknown = no clause. The paged sort is now `orderBy: [{ createdAt: "desc" }, { id: "desc" }]` (**:859**; 19 tie groups measured). When a scope applied, the rich response adds `campaignStates` (echo) and `totalCountAllStates` (the same where with only the state clause lifted, counted in the same request, **:1064-1072**); `totalCount` is unchanged and still the count of the same filtered set. Non-owners: the parameter is ignored, the archived exclusion stays.
* **`src/app/(app)/admin/clips/page.tsx`**: a "Campaign state" `MultiDropdown` beside Status (OWNER only, `menuAlign="left"`), `params.set("campaignStates", …)` in `fetchPage` beside the campaign and status params, `filterCampaignStates` in the fetch deps; the sentence **"Showing 30 of 6,835 clips from finished campaigns. 3,397 more clips sit on campaigns outside this selection."** (default: "Showing 30 of 10,232 clips from campaigns in every state, archived included."), a plain `<p>` per the review; the selection in `sessionStorage` (`adminClips.campaignStates`, dies with the tab), restored once on mount before the first fetch; a change the owner causes is spoken once through the existing polite region (cause flag consumed after the stale-request guard, pending append timer cleared first), never by the restore or the 30 s poll. The live picker request sends `includePast=true` for the OWNER: **36 of 36 campaigns nameable, was 26.** Take 30 a page, `Load more` and the sentinel append the same scope with `offset`; past the `limit` cap of 1,000 the route still pages by offset, and the sentence always states shown of total, so a truncated set cannot pose as everything.
* **Design:** no new anchor; the control is the house `MultiDropdown` (BL-401 keyboard and ARIA), CSS vars only, lucide icons only, no dashes, no emojis; every string names real information (frontend-design skill read; content discipline applied). **Not done, on purpose:** no redesign, no change to the default population (see the decision below), no change to the shared component (six a11y SHOULD-FIX items logged).

## PART 3 — proofs, each with what made it pass

**`scripts/sandbox/bl922-prove.ts`, 40 of 40**, two PRODUCTION builds side by side (main @ 98f87993 on 3921 from `C:/w922m`, the branch on 3922), `DEV_AUTH_BYPASS=false`, minted read-only JWTs for existing rows (owner, a second non-test OWNER, a REVIEWER, a CLIPPER, and nobody), 52 GETs, 2 refused by design, **0 of 429**.
* **A1 to A6, the default is identical:** page 1 the SAME 30 ids in the SAME order on both builds; `totalCount` 10,232 on both = DB 10,232; response keys identical (`clips,hasMore,limit,offset,totalCount`, no new key); money per clip identical field by field (earnings, base, bonus, agency, marketplace legs, both CPM stamps); pages 2, 3, 4 row for row identical (the id tie-break moved nothing today); the non-test OWNER row gets the same 10,232, so the answer follows the role, not `isTestUser`.
* **B1 to B5, the widened view is the real number, on a population that does not fit a page:** each word equals the DB count read immediately before and after (29 / 1,179 / 939 / 162 / **6,835** / 1,088, all echoed back), the six sum to 10,232; finished walked to the end at the route's 1,000 cap: 7 pages, 6,835 distinct ids, 0 duplicates, union = totalCount = DB, every id confirmed PAST and non-archived by the database; **offset 6,000 served 30 rows with totalCount 6,835**, which no filter over 30 loaded rows could produce. What made it pass: 30 shown of 6,835 on page one, not a set that fits on one page.
* **C1 to C4, archived stays out:** the five non-archived words walked to the end, 9,144 ids over 10 pages, **0 archived**; adding `archived` gives 10,232 = 9,144 + 1,088; `archived` alone returns only archived rows; paused (1,179 over 2 pages) and frozen (939) told apart by `pauseSource` on every id, both beyond the page.
* **D1 to D7, failure paths, each its own user:** nobody 401; a CLIPPER 403; the REVIEWER's answer identical with and without the parameter (5,281 both, no echo, archived still excluded); `bogus,also-bogus` = the unscoped 10,232 with no echo; `FINISHED,bogus,finished` = finished once; the scope composes with `statuses=APPROVED` (6,152 = DB) and with `views_desc` (the raw LATERAL path, 6,835, 30 rows).
* **E1, E2, the picker:** the old live request named 0 PAST campaigns, the new one names all 10; merged, 36 of 36 (was 26 of 36).
* **F1 to F6, G1, H1 to H3, money, the full population:** 10,232 live clips, 0 invariant breaches, 0 negative; 29 v2 editor rows, 0 breaches; no double pay across agency, maker, platform and posts; 21 budgeted campaigns, 0 over budget counting both v2 aggregates and the owner's cut; paid is final for the MAKER and the V2 POSTER separately (0, 0); **ANGIE BROWN found by name, exactly one row, budget $2,700.00, ACTIVE, spent $6.03**; both reconciliation forms 0 rows; every fingerprint (clip earnings, clip status, campaign state, agency, v2 editor, v2 platform, payouts) identical at open and close; live clips 10,324 to 10,324 across the run.
* **The filter reached the server, observed on the wire:** the render pass captured the page's own requests: `/api/clips?_t=…&sortBy=newest&limit=30&offset=0&campaignStates=finished` on select, and `…&offset=30&campaignStates=finished` on Load more, at every width; the first request of a fresh open carried **no** `campaignStates`.

**Six money files, `tracking.ts`, `campaign-era.ts`, `prisma/schema.prisma`: byte-identical by `git rev-parse pre-BL-922:<path>` == `HEAD:<path>` == `git hash-object` of the tree** (`5b40d49e`, `00410634`, `67c30c89`, `672d2ab3`, `61cef393`, `ef5cdae7`, `106e16ad`, `77c3a531`).

**Guards.** Every prebuild guard passes (`npm run prebuild` exit 0 on the branch, twice); `check:cron-wiring` passes; hooks gate **0 errors, 10 warnings against the cap of 11** (the brief said 11; measured 10 on main and on the branch); eslint present at `node_modules/.bin/eslint`. Baseline `tsc` on main's tree: 0 errors; on the branch: 0. Build #1 (pre-commit) exit 0, build #2 (post-commit) exit 0, both from a log with the exit code echoed. **A guard against this family was shipped:** `scripts/check-clips-list-scope.js` (`check:clips-list-scope`, wired into prebuild), S1 the page sends `campaignStates` exactly once, S2 no `.filter(` on the page names the scope within 400 characters (a multi-line lambda cannot hide), S3 the route builds the relation once and merges it into `where.campaign` once, S4 the paged list carries the id tie-break and no bare `createdAt` clause before the take, S5 `totalCount` is `db.clip.count({ where })` once and never an array length, S6 the page reads `totalCount` and the outside figure from the response. Comments are stripped before reading; anchors are single-line (CRLF). **`scripts/bl922-guard-demo.mjs`, 10 of 10:** each check broken one at a time (drop the param, re-implement the scope over loaded rows across a multi-line lambda, stop merging into the where, revert to bare `createdAt`, `totalCount = clips.length`, outside figure from `clips.length`, a `campaignStatesV2` rename), the guard failing on exactly that check and naming it, the tree restored with sha256 asserted equal, the guard green again.

**Renders, `scripts/sandbox/bl922-render.mjs`, 21 of 21** against the branch's production build as the owner, at **320, 375, 414, 1280, 1440**, `window.innerWidth` and the URL read back on every shot, pan measured by scrolling (**0 px everywhere**), `--bg-page` references in the page **0** (repo-wide: 0 definitions, 6 references in 4 files, 0 in the changed files). Default: "Showing 30 of 10,236 clips from campaigns in every state, archived included", control "All states", no scope on the wire. Widened: "Showing 30 of 6,835 clips from finished campaigns. 3,401 more clips sit on campaigns outside this selection.", control "Finished", `campaignStates=finished` on the wire, the polite region spoke "6,835 clips from finished campaigns. 3,401 outside this selection." once, sessionStorage "finished". Overflow: after Load more, "Showing 90 of 6,835" (the scroll sentinel fetched a third page too), 90 distinct rows on screen, the append on the wire with `offset=30&campaignStates=finished`. Persist: navigating to `/admin` and back restored Finished, the FIRST request carried it, nothing spoke. **Keyboard at 1280:** Enter opens a `listbox` with `aria-multiselectable="true"` and 6 `option`s, ArrowDown ×5 reaches Finished, Space sets `aria-selected="true"`, Escape closes (`aria-expanded="false"`) and returns focus to the trigger, whose name is its visible text "Campaign state: Finished". (10,236 not 10,232: four real clips arrived overnight; the proof and the renders each read the database at their own moment.)

**Measurements of my own that were wrong, disclosed.** (1) The first render pass reported 401 on every page and I first blamed Chromium refusing a Secure cookie over http; the cause was my own one-hour token expiring across the session's pause. Re-minted at six hours; the cookie recipe is BL-850's, unchanged. (2) The harness first asserted "Showing 60" after one Load more; the page honestly showed 90 because the scroll sentinel fired as well; the assertion was wrong, the page right. (3) The harness compared the trigger's text with a space after the colon; the DOM has none (flex gap). Normalised. (4) The "Install Clippers HQ" sheet intercepted the first tap at phone widths; dismissed through its own "Maybe later" before interacting.

## THE DECISION IN FRONT OF THE OWNER

**The default view still includes archived campaigns' clips, 1,088 of them, deep in the list.** That is BL-850, on your instruction then ("the list matches the dashboard"), and the brief for this round says the default must be exactly today's view, proven by count, which it is. Your words today also say archived is excluded from the normal view. Both cannot hold at once. If you want the normal view to exclude archived, that is one line (`fetchPage` sends the five non-archived words when nothing is selected) and the unscoped total becomes 9,144. Not done without your word.

**ONE LINE:** before, the owner could reach every one of his 10,232 clips only by pressing Load more up to 341 times or by naming a campaign the picker did not offer; now, selecting the wider view puts all **9,115** clips from paused, frozen, completed and finished campaigns (6,835 finished alone) on page one of a scoped, server-counted list, archived (1,088) only when he asks, and **the default is unchanged: the same 30 ids, the same order, the same 10,232.**
