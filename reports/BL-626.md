# BL-626 — Merge BL-625: the campaign money calculator

**Status: SHIPPED.** Merge commit `a9eb22de` is on `origin/main` (verified `origin/main == local HEAD == a9eb22de`). Merge only; no new code authored.

## STEP 0 — TRUTH (with SHA)
* `origin/checkpoint/BL-625` tip = `20d421f1c1be0aa3911a106d3b7e68d5e7e864c2` (matches the stated `20d421f1`).
* **Genuinely unmerged:** `git merge-base --is-ancestor origin/checkpoint/BL-625 origin/main` returned 1 (not an ancestor). Exactly one commit was on the branch and not on main: `20d421f1` itself.
* **The merge-base WAS origin/main HEAD** (`bdc8c1d4`), so BL-625 = main + 1 commit with zero divergence. Local `main` was aligned to `bdc8c1d4` before merging. The `--no-ff` merge had **zero conflicts**.
* Merge commit `a9eb22de` parents: `bdc8c1d4` (main) + `20d421f1` (BL-625). Correct.
* **Nothing a live round holds was merged:** the merge diff is exactly BL-625's 5 paths (below); the working tree held only untracked `.claude/` + two stray docs, never staged.

## Conflicts
**None.** merge-base == origin/main HEAD, so git did a clean three-way merge with no manual edits. A grep for real conflict markers (`^<<<<<<<` / `^>>>>>>>`) across tracked files returned **nothing**. The `=======` hits in the scan were decorative separators in the pre-existing `FULL-REPORT.txt`, which is **not in the merge diff**. BACKLOG.md took BL-625's single appended entry cleanly (`^## BL-625` occurs **1** time, no `$2` capture-group corruption, since nothing had to be resolved).

## Merge diff (bdc8c1d4..a9eb22de) — exactly the 5 BL-625 paths
```
BACKLOG.md
scripts/bl625-prove.ts
src/app/(app)/admin/campaigns/page.tsx
src/components/campaigns/CampaignMoneyCalculator.tsx
src/lib/campaign-money-calc.ts
```

## Confirmations on the MERGED result
* **The calculator renders in the campaign form.** `page.tsx` imports `CampaignMoneyCalculator` (line 29) and renders it inside the create modal (line 1299), owner-gated, above the Platform CPM rates section.
* **Derived ownerCpm/clipperCpm ratio equals the locked share s/(1-s) exactly by construction.** `ownerCpm = round(clipperCpm * s/(1-s), 4)`; the create route derives the stored share ITSELF as `ownerCpm/(ownerCpm+clipperCpm)` (schema:582), so the two CPMs are the single source of the ratio. The owner's fraction of any clip's total is `ownerCpm/(ownerCpm+clipperCpm)` exactly at every view count (the views/1000 factor cancels; a multiplication identity, no division), asserted as `cpmRatioMatchesShareExactly`.
* **Money uses the decimal type, no floating point.** `campaign-money-calc.ts` runs every arithmetic op on `Prisma.Decimal` (decimal.js, 7 references), imported via the browser-safe `@/generated/prisma/browser` entry. The only `Number(...)` is `Number(d.toFixed(2))` inside the DISPLAY-only `fmtMoney` helper (converting an already-exact decimal string for `toLocaleString` currency grouping); no money calculation touches float.
* **Creating a campaign stores exactly what the calculator displayed.** Apply writes only `budget` + the derived `clipperCpm`/`ownerCpm` into the existing form fields via `setForm`; there is no fetch/API in the render block, so the existing create route stores precisely the displayed values.
* **No existing campaign's stored values changed.** No API route, schema, or migration is in the diff; the calculator only fills form fields for a NEW submit and, on an existing-campaign edit, requires an explicit in-DOM overwrite confirmation before touching anything.
* **Proof re-run on the merged tree:** `scripts/bl625-prove.ts` = **16 passed, 0 failed**, including the flagship $5,000 / 11% / 33% / 9% / $1.00 CPM giving owner **$1,468.50** and pool **$2,981.50**.

## Money-file + path safety
**6 money files + `tracking.ts` + `campaign-era.ts` BYTE-IDENTICAL by blob OID** on both refs and on the merged HEAD (`git rev-parse HEAD:$f` == `git rev-parse origin/main:$f`), so no STOP was triggered: `clip-earnings-writer`, `earnings-calc`, `balance`, `tracking`, `clip-earnings-invariant-middleware`, `money-decimal`, `campaign-era`. The verify, tracking and submit paths are not in the diff (untouched). No clip's earnings or status can change. No `prisma migrate`.

## Build (real exit codes, from a log, not piped)
`npm ci` **NPM_CI_EXIT=0** (wiped the generated Prisma client) → `npx prisma generate` **PRISMA_EXIT=0** → `npx tsc --noEmit` **TSC_EXIT=0** → `npm run build` **BUILD_EXIT=0**. Prebuild gates: check:prisma-bypass 0 violations; check:removed-fields OK; hooks gate `eslint --max-warnings 11` emitted **11 problems (0 errors, 11 warnings)** (eslint genuinely present, so the gate is real). "Compiled successfully".

## Ship
Verified push per BL-288: `node scripts/safe-push.mjs main` (`bdc8c1d4..a9eb22de`), then fetch + assert `origin/main == a9eb22de`. Tags `pre-merge-bl626` (`bdc8c1d4`) / `post-merge-bl626` (`a9eb22de`) pushed.

**Rollback:** `git reset --hard pre-merge-bl626` (`bdc8c1d4`) then force-push, or `git revert a9eb22de`.
