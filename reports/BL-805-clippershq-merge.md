# BL-805 — BL-804 merged: the chat is gone, the report button is all that is left

**Merged and verified pushed. `origin/main` == local at `15fc29b1`.** One branch, `--no-ff`, no conflicts, nothing else touched.

**THE OWNER MUST REDEPLOY ON RAILWAY BEFORE ANY OF THIS IS LIVE.** BL-804 said so explicitly and it is still true: main now carries the removal, but production is still running the old code. **The chat is still live and reachable for users right now**, and the section at the end of this report shows that somebody used it 18 minutes before my final check.

## STEP 0 — the truth before merging

| question | answer |
| --- | --- |
| `checkpoint/BL-804` on origin | **YES**, `9d62049a588cba1e02597570920bc04ab8fab627`, identical local and remote |
| genuinely not an ancestor of main | **NO, it was not merged** — confirmed by `git merge-base --is-ancestor` returning false |
| merge base | `d004b396`, exactly main's tip, so the branch forked from current main |
| diff non-empty | **YES — 23 files, 1,151 insertions, 3,217 deletions**, ten chat files deleted |
| BL-723 | **NOT merged**, confirmed after the push: still not an ancestor of main |
| dirty or stale worktree | **none.** One worktree only, on `main`, **0 tracked changes**. Merged in place; **no worktree was created, so none had to be removed** |

**Clean tsc baseline recorded BEFORE merging: exit 0, `grep -c "error TS"` = 0.** The first baseline attempt reported 1 error and I did not accept it: it was in `.next/dev/types/…`, a stale artifact left by a dev server from the previous round, not source. I cleared that generated cache (gitignored, 0 tracked files) and re-recorded the baseline clean. Nothing was attributed to BL-804 on a dirty baseline.

## The merge

`15fc29b1` merges `9d62049a` into `d004b396`. **Zero conflicts** — the branch forked from main's tip, so no union resolution was needed anywhere. **0 conflict markers** across ts, tsx, md, css and json.

**BACKLOG: every entry kept.** Counted with `grep -c`, never piped to `head`: **613 before → 614 after**, 557 unique BL ids. Union verified in both directions: **zero ids from old main missing, zero ids from BL-804 missing.**

## The gates, honestly

`npm ci` **failed the first time** and I am reporting it rather than hiding it: exit **127**, `EPERM` unlinking `next-swc.win32-x64-msvc.node`, and it had already partly emptied `node_modules`. The cause was three orphaned `next dev --webpack -p 3800` processes left over from the previous round's proof server, still holding the native binary. I confirmed their command lines were mine, stopped them, and re-ran.

| gate | result |
| --- | --- |
| `npm ci` (second run) | **exit 0**, 561 top-level packages |
| `npx prisma generate` (after `npm ci`, before tsc) | **exit 0**, client generated in 1.81s |
| `npx tsc --noEmit` | **exit 0, 0 errors — identical to the baseline** |
| `npm run build` | **`REAL_BUILD_EXIT=0`**, echoed from the log, never piped through `tail`. Compiled in 46s, 61/61 static pages |
| prisma-bypass | 0 violations |
| removed-fields | OK |
| **hooks gate** | **0 errors, 11 warnings**, with **eslint v9.39.4 confirmed present** so the gate did not silently no-op |

One honest detail on tsc: an intermediate run showed 7 errors, all of them in `.next/types/validator.ts` still importing the seven deleted chat routes. That file is generated by the previous build, not source. Clearing `.next` and running the real build regenerated it, after which tsc returned to **0**. Those 7 errors were themselves confirmation that the routes are genuinely gone.

## STEP 6 — confirmed on the merged result

**No chat surface and no chat route is reachable.** `src/app/api/chat` contains **0 files** and **0 git-tracked paths**; only empty directory husks remain, which Next.js cannot route to. `src/components/chat/` is gone, as are `chat-access.ts` and `sse-broadcast.ts`. **The production build emitted 0 `api/chat` routes.** This is deletion, not hiding: there is no code left to reach by direct URL. The only four remaining textual mentions in `src/` are comments explaining the removal.

**The report entry is not width-gated.** The launcher `<button>` in `ReportProblemWidget.tsx` renders unconditionally with no `hidden` class and no `md:` visibility gate; its responsive classes change position only (`bottom-[calc(env(safe-area-inset-bottom,0px)+104px)] md:bottom-4 lg:bottom-6`). The only two matches for a width gate in the whole file are **comment lines** describing the old launcher's `hidden md:flex` bug, which is exactly what BL-803 found made it **unreachable on a phone entirely**. It is present at 320px.

**The archive is owner-only.** `/admin/chat-archive` is a server component that calls `getSession()` and then `if (role !== "OWNER") notFound()`, on top of the admin layout's own restriction. A non-owner gets a 404, not a redirect, so the route's existence is not disclosed. There is no composer behind it, so no clipper can reach another clipper's messages (BL-531 holds).

**No schema change.** `git diff --name-only pre-BL-805 HEAD -- prisma/` returns **0 files**, and `prisma/schema.prisma` has the **identical blob OID `59c40a6d58cd`** on both refs. The `Conversation`, `ConversationParticipant` and `Message` models are all still present. **No `prisma migrate` was run.**

BL-804 ships `scripts/migrations/BL-804-remove-proof-rows.sql`, a data cleanup that deletes only its own render-proof rows from `problem_reports`. **I did not run it.** The table currently holds **0 rows** in total, so there is nothing pending.

**The preserved surfaces all still work.**

- **Owner's report list**: `src/app/api/admin/problem-reports/route.ts` and `ProblemReportsClient.tsx` both present; the client changed by 8 lines only.
- **BL-776's evidence panel**: `admin/clips/page.tsx` is **not in the diff at all** (0 changes), and `<ReviewEvidencePanelMount clipId={clip.id} />` still renders at the expected place.
- **BL-788's reviewer surfaces**: undisturbed. The only reviewer-adjacent file touched is `sidebar.tsx`, and its diff is **purely additive** — one lucide icon import plus one `Chat Archive` entry inside `ownerNav`. No reviewer nav logic was altered.

## Money and data: nothing moved because of this round

| check | value |
| --- | --- |
| Earnings invariant violations | **0** |
| Clip decisions during this round | **0** |
| Audit events during this round | **0** |
| Payouts touched by this round | **0** |
| Money files, byte-identical by blob OID on **both** refs | all 7 **IDENTICAL**: `clip-earnings-writer ac5be7de`, `earnings-calc 797e2098`, `balance e887f80a`, `tracking 83ce4bab`, `invariant-middleware 61cef393`, `money-decimal ef5cdae7`, `campaign-era 106e16ad` |
| BL-678 Apify guards | all guard files **IDENTICAL**, `APIFY_HARD_OFF: true` intact, **no Apify actor run** |

**The live database has moved since BL-804 measured it, and none of it was this round.** Reported plainly rather than smoothed over. Payouts went **167 → 168**; that row was created at **`2026-08-13 12:34:46.065`**, status `REQUESTED`, which is **two and a half hours before my round's first write** and is the same row BL-804 already disclosed. Conversations went 54 → **55**, messages 108 → **109**, people 50 → **51**, because a clipper opened a new chat at **`2026-08-13 15:05:31.300`**. Clips grew to 5,681 through ordinary submissions. **Nothing was lost: every count only increased, no destructive operation was run, and there were 0 audit events inside my round.** All 54 original conversations, 13 escalations, 108 messages and 50 people are intact and readable at the new archive.

## The people who were never answered

BL-804's definition, reproduced exactly: a conversation is unanswered when **the last word in it was the clipper's**. That yields **18**, matching BL-804 precisely, **plus one new one opened today**, for 19 now. **7 had explicitly asked for a person**, also matching. Short id is the first 8 characters of the user id, which maps directly in the database and is not a handle. No wallet address appears anywhere.

| # | short id | last wrote (::text) | days | asked for a person |
| --- | --- | --- | --- | --- |
| 1 | `cms4qr2d` | 2026-08-13 15:06:04.572 | 0 | no |
| 2 | `cmpu64wk` | 2026-08-05 22:23:08.154 | 7 | no |
| 3 | `cmpqwj5m` | 2026-07-13 14:14:26.380 | 31 | no |
| 4 | `cmp0zwli` | 2026-07-11 23:40:17.732 | 32 | **YES** |
| 5 | `cmpqwj5m` | 2026-07-11 08:14:44.983 | 33 | no |
| 6 | `cmqpkfgv` | 2026-07-08 21:15:54.514 | 35 | no |
| 7 | `cmqtv3b3` | 2026-07-01 16:20:16.589 | 42 | no |
| 8 | `cmqic2kl` | 2026-06-28 04:35:17.161 | 46 | no |
| 9 | `cmpz98n7` | 2026-06-11 13:29:59.797 | 63 | no |
| 10 | `cmq5g5cb` | 2026-06-10 15:52:14.794 | 63 | no |
| 11 | `cmpx17dw` | 2026-06-03 10:16:54.217 | 71 | no |
| 12 | `cmn4nlfg` | 2026-05-31 20:33:35.161 | 73 | no |
| 13 | `cmoyq9m9` | 2026-05-30 07:54:50.735 | 75 | **YES** |
| 14 | `cmpiiy8o` | 2026-05-27 13:56:09.780 | 78 | **YES** |
| 15 | `cmpbl72e` | 2026-05-22 23:50:37.313 | 82 | **YES** |
| 16 | `cmpcip3n` | 2026-05-21 04:46:17.960 | 84 | **YES** |
| 17 | `cmofpudr` | 2026-04-26 17:19:43.723 | 108 | **YES** |
| 18 | `cmoaejuc` | 2026-04-23 22:17:39.604 | 111 | **YES** |
| 19 | `cmn4nlfg` | 2026-03-31 14:24:25.689 | 135 | no |

Two people appear twice (`cmpqwj5m`, `cmn4nlfg`), so these 19 conversations are 17 distinct people. All are `CLIPPER`. Every one is preserved and readable by the owner at `/admin/chat-archive` once the redeploy lands.

**One thing has changed since BL-804 and the owner should know it.** BL-804 correctly reported that **nobody was mid-conversation** and the newest unanswered message was 8 days old. That is **no longer true.** Row 1 above is a clipper who wrote at **`2026-08-13 15:06:04.572`**, roughly 18 minutes before my final database read, through the chat that is still live in production. **That person is waiting for a reply that will never come once this deploys.** The owner may want to answer them outside the platform before or right after redeploying.

## What the owner does now

1. **REDEPLOY ON RAILWAY.** Nothing above is live until then. Main carries the removal; production still runs the chat.
2. **Consider replying to the 7 who asked for a person**, and to row 1 who wrote today, outside the platform, since the channel they used is about to disappear.
3. Optional, not done here: run `scripts/migrations/BL-804-remove-proof-rows.sql` if any render-proof rows ever reappear. Right now `problem_reports` holds 0 rows, so it is a no-op.
