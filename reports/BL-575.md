# BL-575 — merge BL-562 + BL-563 (and the two docs-only STOPs) to main

**2026-07-17 · MERGE ONLY. No data or money write; BL-562's write script was NOT re-run. Base: origin/main
ae148f3d → now `1d69a62c`.** Verified push (origin/main == local). The code that PROTECTS STRAENGE's
$173.41 is now on main. The somesome $933.94 exposure is a DIFFERENT site and deliberately survives (below).

## STEP 0 — truth per branch (SHAs, checked against origin/main @ ae148f3d)
| Branch | SHA | State before this round | Merged? |
|---|---|---|---|
| checkpoint/BL-562 | f4db0480 | UNMERGED, 1 ahead (`merge-base --is-ancestor` false) | YES (1st) |
| checkpoint/BL-563 | 55b88301 | UNMERGED, 1 ahead | YES (2nd) |
| checkpoint/BL-571 | 5a79d06a | UNMERGED, 1 ahead, docs-only (confirmed) | YES (3rd) |
| checkpoint/BL-572 | 11d63554 | UNMERGED, 1 ahead, docs-only (confirmed) | YES (4th) |
| checkpoint/BL-573, BL-574 | n/a | NOT on origin | left alone |

Merged --no-ff, ONE at a time, verifying between each. Order BL-562 → BL-563 (BL-563 builds on it) →
BL-571 → BL-572. Worked in isolated worktree C:/b575 on main (short path, real `npm ci`, no node_modules
junction) because the primary tree is held by the live BL-569 round.

**BL-562: its already-live data write vs what its branch contains.** BL-562's DATA WRITE (repaired 93
STRAENGE owner rows to gross, then credited kujtaa._24643's 3 override clips +$173.41 → owner $999.72) is
ALREADY in production and was NOT touched here. Its BRANCH holds only `docs/STRAENGE-OWNER-PAYMENT-BL562.md`,
`scripts/bl-562-write-straenge.ts` (the write script) and a BACKLOG entry. **The write script was NOT run**
(re-running would double-credit). No source or money file changed.

## What landed (full file list, ae148f3d..1d69a62c)
`M BACKLOG.md`; docs (BL-562/563/571/572, 4 added); scripts `bl-562-write-straenge.ts`,
`bl-563-prove.ts` (added, NOT run); and BL-563's code: `M force-recalc-earnings/route.ts`,
`M agency-monitor.ts`, `M gamification.ts`, `A owner-share-guard.ts`. **None of the 6 money files is in
the diff.**

## Diff scrutiny (BL-563, the one that matters) — all confirmed on the MERGED result
- **The guard exists in ONE place, reused not duplicated.** `decideOwnerGross` is defined only in
  `src/lib/owner-share-guard.ts:57`; imported and called by exactly three callers —
  `agency-monitor.ts:171`, `gamification.ts:884`, `force-recalc/route.ts:369`. No second implementation.
- **gamification.ts:871 no longer writes base-only.** It now (883-896) computes `decideOwnerGross`; `gross`
  → `decision.amount` (BL-162 gross); `ambiguous`/`not_guaranteed` → the EXACT pre-BL-563 base-only value;
  non-split/!ownerShareApplies → 0 (unchanged). So an ambiguous clip is **skipped (kept at its prior
  base-only value), never rewritten to a wrong-era gross and never zeroed**; a stamp-agree clip gets gross.
- **agency-monitor is byte-identical to its pre-refactor behaviour.** The BL-541 inline block was extracted
  verbatim; ambiguous → `totalSkippedAmbiguous++; continue` (skipped, --fix never touches it), gross →
  `decision.amount`, else → legacy `calculateOwnerEarnings`. Same `s/(1-s)` implied ratio, same raw
  `oCpm/cCpm` stamped ratio, same `>0.01` test, same round2. (BL-563 measured checked=3146 violations=191,
  STRAENGE 0 / somesome 186, identical before and after.)
- **No earnings maths changed:** `earnings-calc.ts` and the other 5 money files are byte-identical.

## Safety — 6 money files byte-identical by blob OID (git rev-parse on BOTH refs, ae148f3d and HEAD)
```
7aa6be48 clip-earnings-writer   797e2098 earnings-calc   f8499e18 balance
c2db8615 tracking               61cef393 invariant-middleware   ef5cdae7 money-decimal
```
`tracking.ts` is NOT in the diff. No data/money write; no clipper earnings changed (this round runs zero
write statements); somesome and STRAENGE not unfrozen; agency-monitor --fix not run; BL-538 repair SQL not
run; campaign-era.ts and the scrapers untouched. BACKLOG unioned by **line-number marker deletes only** (no
regex, no String.replace → no `$2` capture-group corruption): 66 → **70** BL headings (exactly +4), 0
conflict markers repo-wide, only the known BL-536/BL-541 duplicates, `$2,000` intact (24 occurrences).

## Gates (honest; every trap guarded)
`npm ci` exit **0** (run first). `npx prisma generate` exit **0**, run **before** tsc (ci wipes
`src/generated/prisma`). `tsc --noEmit` exit **0, 0 errors** — not trusted alone. `npm run build` exit **0**
read directly from a log (never piped to tail): **"✓ Compiled successfully", Finished TypeScript, 61 static
pages generated, no OOM**. eslint **confirmed present (v9.39.4)**; hooks gate exit 0, **0 errors / 11
warnings** (genuinely ran, not a silent no-op). No dashes.

## THE ONE THING THE OWNER NEEDS (post-merge, quoted from merged main)
**1. STRAENGE's $173.41 IS now protected in code for its move to PAST.** `gamification.ts:871` is the last
automatic owner-amount writer that reaches a frozen guaranteeOwnerSplit campaign; it now writes gross
behind the shared guard, so a clipper opening their progress page reproduces STRAENGE's gross owner row
instead of eroding it to base-only. BL-563 proved the $173.41 survives across all four paths (cron skips;
gamification writes gross=stored; agency-monitor 0 violations; force-recalc writes gross=stored).

**2. The somesome $933.94 exposure SURVIVES this merge — by design, not fixed here.** `tracking.ts` is
byte-identical and does NOT import the guard, so the **BL-163 owner-lock at tracking.ts:2557-2582 is still
unguarded**:
```
2577  const newOwnerLocked = Math.round(newEarnings * (s / (1 - s)) * 100) / 100;
2581  newOwnerAmt = Math.max(0, newOwnerLocked);
```
BL-572 proved a guard at 2145 is COSMETIC because this owner-lock re-derives the owner row to gross-via-s
UNCONDITIONALLY (`ownerShareApplies()`≡true) and overwrites whatever 2145 wrote. So on an eraExempt resume
somesome's 108 ambiguous rows still collapse $1,928.71→$994.77 (−$933.94). **This merge does not change
that and did not try to** — it is a money file and its own reviewed round (guard the owner-lock, pin
ambiguous to the stored value). Meanwhile a plain budget top-up stays safe (BL-570): the boundary gates the
ambiguous clips out at :1935 so 2557-2582 never reaches them. The discipline remains: do NOT tick
keep-existing-earning until the owner-lock round lands.

## Rollback
`git reset --hard ae148f3d && node scripts/safe-push.mjs main --force`. Code only; nothing written to the
database by this round (BL-562's production data write is separate and stays).
