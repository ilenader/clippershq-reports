# BL-637 — Merge BL-632: daily submission counter counts UP as used

Status: SHIPPED to main. Merge only (wording + presentation). No logic changed.

## STEP 0 — TRUTH (with SHAs)
- checkpoint/BL-632 on origin @ **5c481efa** (5c481efac3ed6dca576acaa3ea5ade24dd78ba36). Confirmed present on origin.
- Genuinely NOT on main: `git merge-base --is-ancestor origin/checkpoint/BL-632 main` returned exit 1 (not an ancestor).
- Unmerged commits (`git log main..origin/checkpoint/BL-632`):
  - 0952c6e2 — daily submission counter counts UP (used) not DOWN (left), wording only
  - 5c481efa — BACKLOG entry
- Merge base = **6b0343a7** = the current main HEAD. The checkpoint branches directly off current main, so nothing a live round holds sits between them; no live-round work was pulled in.

## THE MERGE
- Pre main HEAD: 6b0343a7. Post-merge main HEAD: **9f5de583** (9f5de583b1bfd2ef5bf842b87350809185b39170).
- `git merge --no-ff origin/checkpoint/BL-632` — "ort" strategy, clean, ZERO conflicts (merge base was main HEAD, so nothing to reconcile). No manual union resolution was needed; no capture-group corruption risk since no conflict was resolved.
- Conflict-marker scan (`git grep -nE '^(<<<<<<<|>>>>>>>|=======)$'`): none.
- Files changed by the merge (4): BACKLOG.md, src/app/(app)/clips/page.tsx, src/components/clips/BatchSubmitSection.tsx, src/components/clips/SubmitClipPremium.tsx. 21 insertions, 6 deletions.

## THE FOUR ALLOWANCE SURFACES — all count UP as used
Each display now shows `used = limit - remaining`, so the number on screen always matches what the server enforces (the underlying remaining/limit values are unchanged).

1. Single-submit chip (SubmitClipPremium.tsx ~544): normal `${limit - remaining} of ${limit} used today`; at-cap `${limit} of ${limit} used today`.
2. Modal line (SubmitClipPremium.tsx ~735): normal `${limit - remaining} of ${limit} submissions used today for this campaign.`; at-cap `${limit} of ${limit} used today, come back tomorrow.`
3. Batch live role=status line (BatchSubmitSection.tsx ~295): `${remainingInfo.limit - remaining} of ${remainingInfo.limit} used today for this campaign.`
4. Clips page (clips/page.tsx ~531): normal `${limit - remaining} of ${limit} submissions used today for this campaign.`; at-cap `${limit} of ${limit} used today, come back tomorrow.`

At-cap wording reads as used-up with a come-back-tomorrow note, not a bare zero-left. No dashes introduced in any copy.

## BYTE-IDENTICAL PROOF (blob OID via git show on BOTH refs)
getDailyRemaining is defined inside clips/page.tsx at line 184; the merge only touched line ~531 (display strings), so its useCallback logic is unchanged. The limit logic and every load-bearing file are byte-identical by blob OID — verified merged HEAD (9f5de58) vs pre-merge main (6b0343a7), all OK:

| File | Blob OID (unchanged) |
| --- | --- |
| clip-earnings-writer.ts | 7aa6be48345dfad1b785ccb9ad30cd41f128f753 |
| earnings-calc.ts | 797e20985ad57475ef321afcf3cb1ea7b0d6ab84 |
| balance.ts | e887f80acfc70fee438e719a32a60025eda22749 |
| tracking.ts | 847dcf70d252c64b7ee68efffa0e9d4fc3c082b3 |
| clip-earnings-invariant-middleware.ts | 61cef39395363c31f0c902dd4c64e8c06b3e6449 |
| money-decimal.ts | ef5cdae757b9ad3c23380ee8b63e279f98d0b6ac |
| campaign-era.ts | 106e16ad75125c3b10b6949a2981d33614c69ab9 |
| clipper-submit-core.ts (clipper core) | ac7a4dc179e13f72c26a89fe94f38a19b2afeeac |
| clipper-access.ts (clipper core) | 32996bd27e13cf8072cfa20200ede6f66a3a6771 |
| api/clips/route.ts (clip route) | 7a8987090bbed37f5da1ed8a29ee10cc3e125cdb |
| api/clips/batch/route.ts (clip route) | 5cee63770c6184b85d36b17e324dbfbed12a4b58 |

- getDailyRemaining and both clip routes byte-identical: limit logic unchanged; displayed count matches server-enforced logic. No STOP triggered.
- No clip's earnings or status changed (no money/tracking/era file touched).
- Marketplace "slots" surfaces untouched: `git diff --name-only 6b0343a7 HEAD` shows no marketplace/slot path.
- BACKLOG unioned exact (clean merge, 15-line entry added, no capture-group corruption).

## BUILD (honest exit codes, from logs, not piped through tail)
- npm ci: NPM_CI_EXIT=0 (wipes node_modules; prisma client regenerated after).
- npx prisma generate: PRISMA_EXIT=0 (Prisma Client 7.8.0) — run before tsc.
- npx tsc --noEmit: TSC_EXIT=0 (0 TS errors).
- npm run build: BUILD_EXIT=0. Prebuild gates passed (check:prisma-bypass, check:removed-fields, lint:hooks). Hooks gate: eslint PRESENT (node_modules/.bin/eslint), `--max-warnings 11` → 11 problems (0 errors, 11 warnings) = pass. Next build compiled successfully in 14.1s, 61/61 static pages generated.

## PUSH (verified per BL-288)
- `node scripts/safe-push.mjs main --tags=pre-merge-BL-637,post-merge-BL-637` → "VERIFIED PUSHED ✓ origin/main == local HEAD (9f5de58)". Branch + both tags pushed on attempt 1.
- Tags: pre-merge-BL-637 (@ 6b0343a7), post-merge-BL-637 (@ 9f5de583). Rollback: `git reset --hard pre-merge-BL-637` or `git revert -m 1 9f5de58`.

## VERIFY CHECKLIST
- [x] STEP 0 stated with SHAs; checkpoint genuinely unmerged; nothing a live round held merged (merge base == main HEAD).
- [x] All four allowance surfaces count UP as used; at-cap reads used-up + come-back-tomorrow; displayed count matches server enforcement.
- [x] getDailyRemaining, both clip routes, 6 money files, tracking.ts, campaign-era.ts, clipper core byte-identical by blob OID (no STOP).
- [x] Marketplace slots surfaces untouched.
- [x] BACKLOG unioned exact, no capture-group corruption, no conflict markers.
- [x] npm ci clean; prisma generate before tsc; tsc 0 errors; next build actually run, real exit 0 from log; hooks gate with eslint present.
- [x] main pushed, origin == local (9f5de58). No dashes.
