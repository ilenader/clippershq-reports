# BL-669 — MERGE of BL-668 (caption / sound / hashtags reach the reviewer note behind an opt-in flag)

**Merge commit:** `c8321777` on `main` (origin == local, verified).
**Merged branch:** `checkpoint/BL-668` @ `02dfb031`.
**Tags:** `pre-merge-BL-669` (91bf3759), `post-merge-BL-669` (c8321777), both pushed.
**Rollback:** `git revert -m 1 c8321777`, or `git reset --hard pre-merge-BL-669`.

## STEP 0 — was BL-668 genuinely unmerged?

Yes, with a real code diff.

* `git merge-base --is-ancestor 02dfb031 origin/main` → **NO** (not already on main).
* `git log origin/main..checkpoint/BL-668` → exactly one commit, `02dfb031`.
* `git merge-base origin/main checkpoint/BL-668` → `91bf3759`, i.e. BL-668 branched off the *current* tip of main, so the merge was structurally conflict free.
* Non empty `.ts` diff: 4 TypeScript files, **+358 / −7** (`src/lib/apify.ts`, `src/lib/campaign-rules.ts`, `src/app/api/clips/route.ts`, `scripts/bl668-live-proof.ts`), plus `BACKLOG.md` and `prisma/schema.prisma`.

Nothing held by a live round was merged: only `checkpoint/BL-668` was merged, and that branch's worktree (`C:/b668`) was already parked at `02dfb031` with the round finished and pushed to origin.

## The dirty main worktree, handled without touching it

`C:/b575` holds the local `main` branch and was **dirty**: 77 tracked entries (staged deletions of docs/scripts/splash assets, modified `BACKLOG.md` and `prisma/schema.prisma`), and its `main` ref sat at `91b84410`, **stale** versus `origin/main` `91bf3759`. It was left completely untouched: not checked out, not reset, not staged, not cleaned.

The merge was done in a **fresh detached worktree at a short path**, `C:/b669`, created directly from `origin/main`. `.env` and `.env.local` were copied in; **no `node_modules` junction was created** (a real `npm ci` was run). The result was pushed with `git push origin HEAD:main`, so the stale local `main` ref in `C:/b575` was never written to.

Verified push (BL-288 semantics): push succeeded on attempt 1, then `git fetch origin` and `origin/main == local HEAD == c8321777` was asserted.

## Merge mechanics

* Zero conflicts (the merge base was main's own tip), so no union resolution was needed.
* Conflict marker grep over `*.ts` / `*.tsx` / `*.md` / `*.prisma` / `*.json` → **none**.
* **BACKLOG:** 97 entries on main, 98 on BL-668, **98 on the merged result**. `git diff checkpoint/BL-668 HEAD -- BACKLOG.md` is **empty**, so the union is exact and every pre existing entry survived. Grep for `$2` found no capture group corruption.

> **Correction, added during BL-672.** This line originally read "Grep for `$2` found 5 hits". That number was wrong: the grep had been piped through `head -5`, so 5 was the truncation limit, not the count. The real figure is **112 occurrences of `$2`**, and the count is **identical on the merge base, on main, on `checkpoint/BL-668` and on the merged result**, so none was introduced or lost. Every one is a genuine dollar amount (`$2/account`, `$2 via campaignBudgetLiability`, `$2.3K`, `$2,071.84`, `$2,000.00` and so on). The conclusion of no capture group corruption stands; only the evidence figure was misstated.

## Schema — already on prod, `generate` only

The 4 additive columns BL-668 adds to `RuleShadowDecision` were **already applied to prod** by the BL-668 round. Confirmed read only against the live DB via `information_schema.columns` on `rule_shadow_decisions`:

| column | type | nullable |
| --- | --- | --- |
| `hashtags` | ARRAY (text[]) | NO (defaults empty) |
| `postedAt` | timestamp | YES |
| `durationSec` | integer | YES |
| `source` | text | YES |

No re run, and **no `prisma migrate` of any kind**. Only `npx prisma generate` (exit 0, Prisma Client 7.8.0), run **after** `npm ci` and **before** `tsc`, because `npm ci` wipes the generated client.

## Gates — real exit codes, read from logs

| gate | result |
| --- | --- |
| `npm ci` | **exit 0** |
| `npx prisma generate` | **exit 0** (before tsc) |
| `npx tsc --noEmit` | **exit 0**, log 0 lines |
| `npm run build` | **BUILD_EXIT=0**, echoed from `$?` directly, not piped through `tail` |
| `check:prisma-bypass` | ran (prebuild) |
| `check:removed-fields` | ran (prebuild) |
| hooks gate `lint:hooks` | **eslint present and actually executed** (`node_modules/.bin/eslint`, eslint 9.39.4); `eslint --config eslint.hooks.mjs --max-warnings 11` → **11 problems: 0 errors, 11 warnings**, i.e. at the cap, passing |

The two pre existing static gates that assert the submit path's call shape (`scripts/test-bl-137-preflip-hardening.ts`, `scripts/test-bl-138-agegate-sweep.ts`) still match the merged `apify.ts` (both regexes evaluated `true`), so adding `includeRawMeta` to that call did not silently break them.

## Money files — blob OID on BOTH refs

`git rev-parse <ref>:<path>` on `origin/main` and on the merged `HEAD`:

| file | verdict |
| --- | --- |
| `src/lib/clip-earnings-writer.ts` | IDENTICAL `7aa6be48` |
| `src/lib/earnings-calc.ts` | IDENTICAL `797e2098` |
| `src/lib/balance.ts` | IDENTICAL `e887f80a` |
| `src/lib/tracking.ts` | IDENTICAL `847dcf70` |
| `src/lib/clip-earnings-invariant-middleware.ts` | IDENTICAL `61cef393` |
| `src/lib/money-decimal.ts` | IDENTICAL `ef5cdae7` |
| `src/lib/campaign-era.ts` | IDENTICAL `106e16ad` |
| `src/lib/campaign-rules.ts` | **DIFFERS** `f04a3bae` → `fc91216f` |

**Correction to the round brief, stated plainly:** the brief listed `campaign-rules.ts` among the files that must be byte identical. It cannot be. `campaign-rules.ts` **is** the evaluator BL-668 exists to change: it adds `extractClipExtras` (postedAt + durationSec), appends the Instagram sound keys, and adds a YouTube specific wording. That change is intended and reviewed below. Every other file on the list is byte identical.

What the `campaign-rules.ts` change does and does not do:
* The new Instagram sound keys are **appended** to a `firstString(...)` list, so any payload that already resolved a sound id resolves to the **same** id. It can only turn a null into a value.
* `extractClipExtras` is a **separate exported function**, deliberately not widening `ClipMetadata`, so `evaluateRuleShadow` and `composeReviewerNote` are handed exactly what they were handed before.
* The YouTube branch changes **wording only**. The verdict stays `cannot_evaluate`, fail open, never a reject.

## Confirmed on the MERGED result (live, read only, nothing written)

Run from the merged worktree against real prod data. Captions are clipper content and appear nowhere in this report: only lengths and booleans were printed.

**1. The tracking cron and thumbnail paths do not pass `includeRawMeta`.** Grepped on the merged tree: `includeRawMeta` has exactly **one production call site**, `src/lib/apify.ts:2497` inside `fetchClipFreshnessWithRetry`, whose only production caller is `src/lib/clipper-submit-core.ts:138` (the submit path). The money and image paths pass it nowhere:

| caller | call shape |
| --- | --- |
| `src/lib/tracking.ts:1426` | `fetchClipStats(clip.clipUrl)` — no options at all |
| `src/app/api/cron/tracking/route.ts:520` | `fetchClipStats(url, { skipHikerOverlay: true })` |
| `src/lib/clip-thumbnail.ts:289` | `fetchClipStats(clip.clipUrl, { skipHikerOverlay: true })` |

**2. They behave identically, proven by a control call.** On 2 real PENDING TikTok clips, the exact call shape the cron and thumbnail make was executed against the merged code and compared to the opt in submit call:

| clip | control views | control `raw` | submit views | submit `raw` | views equal |
| --- | --- | --- | --- | --- | --- |
| `cms5t8xd1…62quu` | 57 | **absent** | 57 | present | **true** |
| `cms5rfzpa…lhaj9` | 437 | **absent** | 437 | present | **true** |

Provider `lamatok-tiktok` in both cases, scraper mode `lamatok`. The control object carries no `raw` at all, so nothing on the money path can even see the new field.

**3. A real pending TikTok clip's note now carries actual caption and sound findings.** Composed live through the merged `composeReviewerNote` for the same two PENDING clips:

| clip | captionPresent | captionLen | soundIdPresent | hashtags | postedAt | durationSec | confidence | contains the pipeline gap message |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `cms5t8xd1…62quu` | true | 117 | true | 2 | true | 12 | high | **false** |
| `cms5rfzpa…lhaj9` | true | 98 | true | 5 | true | 19 | high | **false** |

Both notes returned 2 findings, 0 blind spots, suggestion `nothing_flagged`. Before this merge both would have carried "No clip metadata reached the evaluator at all … It is a pipeline gap on our side".

**4. `rule_shadow_decisions` records `captionPresent` true on new rows — partly proven, one honest gap.** The 2 rows written through this exact code path by the BL-668 round (`source='bl668-proof'`, TikTok) persist `captionPresent=true`, `soundIdPresent=true`, non empty `hashtags`, `postedAt` set and `durationSec` set. The persistence mapping on the live submit path is `captionPresent: meta.caption != null` at `src/app/api/clips/route.ts`, fed by the same `meta` the proof above produced. **Gap:** all 198 organic rows still read `captionPresent=false`, because they were all written before this merge deployed. The first organic `captionPresent=true` row can only appear when a real clipper submits a TikTok clip on the deployed build. That has not happened yet and is not claimed.

**5. Auto reject is still OFF and `RULES_AUTO_REJECT_LIVE` gates nothing.** `isAutoRejectLive()` returns `process.env.RULES_AUTO_REJECT_LIVE === "true"` and has exactly **one** call site, `src/app/api/clips/route.ts:977`, where it only stamps `autoRejectLive` onto the shadow row. No reject path consumes it. All 200 rows in `rule_shadow_decisions` have `autoRejectLive = false`.

**6. No clipper facing route reaches any note — re grepped on the merged tree, not inherited.** The only reader of `noteJson` / `noteText` / `composeReviewerNote` outside the write site is `src/app/api/admin/reviewer-note/[clipId]/route.ts`, gated by `requireOwnerOrCapability("CLIP_VIEW")` and living under `/api/admin/*`, which CLIPPERs cannot access at all. The submit route composes the note but returns `NextResponse.json(clip)`, so the note never enters a clipper response. No page, component or clipper route references it.

## Money path unaffected

A real sample of 12 APPROVED, non `videoUnavailable` clips (mixed TikTok and Instagram) was snapshotted before the push and re read twice after.

| read | time (UTC) | result on the 12 sampled clips |
| --- | --- | --- |
| BEFORE push | 09:47 | baseline: views 115 / 453 / 6904 / 468 / 9074 / 956 / 1143 / 542 / 5066 / 1099 / 507 / 2008, all `checkedAt` 09:11 |
| AFTER push | 09:59 | **identical** views, identical `checkedAt`, identical `earnings` and `baseEarnings` |
| AFTER the 10:00 batch (partway) | 10:13 | **identical** again, every field |
| AFTER the 10:00 batch finished | 10:31 | **identical** again, every field |

The tracking poll itself kept running normally across the push. The 10:00 UTC batch ran from 10:01 to 10:21 and wrote **90 rows** in total: 0 null views, 0 zero views, **0 clips whose views went down**.

**Deploy timing, stated honestly:** the push landed at about 09:58 UTC and the first rows of the 10:00 batch were written at 10:01 UTC, so the earliest rows in that batch may still have run on the pre merge build. `/api/health` returns only `{ok, ts}` and carries no commit sha, so the exact deploy moment cannot be pinned from here. The **29 rows written from 10:10 onward**, 12 to 23 minutes after the push and therefore comfortably past a Railway rebuild, are the unambiguously post deploy slice: **0 null views, 0 zero views, 0 went down, 19 up, 10 flat**. The 12 sampled clips are not in that slice (the auto ladder does not check every clip hourly), which is exactly why their stored views are byte for byte the same across all four reads.

**No organic submit has landed since the deploy** (`rule_shadow_decisions` has zero new rows after 09:58 UTC), so the first organic `captionPresent=true` row still cannot be shown. That is the same gap stated in point 4 above, not a new one.

Independently of the sample, the control call in section 2 is the stronger proof: the merged code, called exactly the way the cron calls it, returns the identical view number and no `raw` field. `tracking.ts` is byte identical by blob OID, and it reads only `stats.views/likes/comments/shares`.

## What BL-668's own report admitted, carried forward

* The **Instagram sound key** could not be re probed live (Apify actor runs are forbidden and HikerAPI's raw is useless for sound). The added IG keys are appended only, so the worst case is that they stay unused.
* **TikTok short links** are roughly 70 percent of submissions, not the 6 percent an earlier round claimed. LamaTok resolves all three link forms, so nothing was built for it.

## Notes

* A read only verification script was written for this round at `scripts/bl669-merged-proof.ts` in the merge worktree and **deliberately left uncommitted**, because this was a merge only round. It writes nothing.
* Worktree `C:/b669` and the reports clone `C:/chqr` are the only new paths created.
