# BL-646 — merge BL-643. It is a DOCUMENT, not a code change. The 110.8 KB saving does not land.

**2026-07-23** · `d6a5ca46` → merged `b213ed44` · Tags `pre-merge-BL-646` / `post-merge-BL-646`

**The headline first, because the brief said otherwise.** `checkpoint/BL-643` does **not** defer the recharts chunk. It contains **no code at all**. It adds exactly one markdown file. The merge is correct and is done, but the `/earnings` bundle is **268.0 KB gzip before and 268.0 KB gzip after**, measured both times. The 158 KB figure is BL-643's *projection of a fix that was never written*.

---

## STEP 0 — TRUTH, WITH SHAs

| Ref | SHA |
|---|---|
| `origin/checkpoint/BL-643` | `789dc2d66037b85ed2e6a3c7e12b65acdd783bba` |
| `main` before | `d6a5ca46bc2c4c80279043421ee045518608a827` |
| `main` after (this merge) | `b213ed442e8d780c74630b8426156608abaf93a4` |

```
$ git log --oneline main..origin/checkpoint/BL-643
789dc2d6 BL-643: /earnings page weight investigation (READ ONLY, nothing built)...

$ git merge-base --is-ancestor origin/checkpoint/BL-643 main
NOT MERGED (correct, work to do)
```

**BL-643 was genuinely unmerged.** It is now merged. **Nothing a live round holds was merged:** `origin/checkpoint/BL-642` was checked individually and remains **NOT on main**. `checkpoint/BL-644` does not exist on origin.

---

## WHAT THE BRANCH ACTUALLY CONTAINS

```
$ git diff --name-status main...origin/checkpoint/BL-643
A       docs/EARNINGS-PAGE-WEIGHT.md
```

**One file. 210 lines. Added. Zero `.ts` or `.tsx` files touched** (`git diff --name-only ... | grep -cE '\.(ts|tsx)$'` → **0**).

`src/components/earnings/EarningsChart.tsx` is **byte-identical on both refs**, blob `d882215fa2ba4fe726f6b65f6899d2b73fc7671a`. So is `src/components/ui/area-gradient-chart.tsx` (`3b1a94c1`), which is the file that actually imports recharts — `EarningsChart.tsx` never imported recharts directly; it imports `AreaGradientChart`.

BL-643's own commit message says it: *"READ ONLY, nothing built ... **Recommends** deferring the recharts leaf inside EarningsChart.tsx"*. It is an investigation with a recommendation, and the recommendation was never implemented.

**Two further corrections to the brief.** (1) BL-643 does **not** propose replacing the existing pulsing skeleton at `EarningsChart.tsx:54-72`. The doc states the opposite in as many words: the fix *"leaves that existing skeleton completely untouched and static"* and adds a plain 220px box for the **separate** window while the chart **code** downloads. Those are two different windows. (2) **No skeleton was introduced by this merge**, trivially, because no component changed.

---

## STEPS 4, 5 and 6 — the real measurement

**Method** (BL-643's, reproduced independently): resolve each route's `page_client-reference-manifest.js` to its emitted chunks and gzip the actual files on disk at level 9. Gzip is what the phone pays. Measured on a clean build of `d6a5ca46` **before** the merge, and again on a clean build of `b213ed44` **after**.

| Route | Chunks | Raw | **Gzip BEFORE** | **Gzip AFTER** | Moved? |
|---|---|---|---|---|---|
| **`/earnings`** | 10 | 1067.4 KB | **268.0 KB** | **268.0 KB** | **NO** |
| `/progress` | 9 | 691.3 KB | 158.4 KB | 158.4 KB | NO |
| `/referrals` | 10 | 1071.9 KB | 269.1 KB | 269.1 KB | NO |
| `/clips` | 10 | 752.0 KB | 175.4 KB | 175.4 KB | NO |

The recharts chunk `static/chunks/0mzfj-ozdd0fb.js` is **380.9 KB raw / 110.6 KB gzip** and is **still present in `/earnings`'s initial bundle after the merge**, exactly as before. It is also still on `/referrals`, byte-identical.

**This independently confirms BL-643's investigation is accurate**, to within rounding: it reported `/earnings` at 269 KB (measured 268.0), the recharts chunk at 110.8 KB (measured 110.6), the page-without-chart at 158 KB (268.0 − 110.6 = **157.4**), and `/progress` at 159 KB (measured 158.4) as the sanity check that nothing else heavy hides in the page. **The analysis is sound. Only the implementation is absent.**

**Step 6 cannot be confirmed as asked, and it would be dishonest to claim otherwise:**

| Asked | Actual |
|---|---|
| "`/earnings` is ~158KB not ~269KB" | **NO. It is 268.0 KB.** Unchanged by this merge |
| "recharts chunk no longer in the initial bundle" | **NO. It is still there**, 110.6 KB gzip |
| "the chart still renders when scrolled to" | Yes, and it renders immediately as it always has, because nothing was deferred |
| "220px reserved height intact" | Yes. `height = 220` default (`:44`), applied in **both** of `EarningsChart`'s return paths: `style={{ height }}` on the loading state (`:63`) and `height={height}` passed to the chart on the main path (`:85`). Unchanged |

**Step 7, nothing else moved:** no other route's bundle changed by a single byte, per the table above. No page imports the chunk in a way this could break, because no import changed anywhere.

---

## Safety, verified rather than inherited

Every earnings number, balance and withdrawal figure is **structurally independent of the chart**, and this holds now as it did before:

* **Zero fetches in either chart component.** `grep -c "fetch(\|useSWR\|axios"` → **0** in `EarningsChart.tsx` and **0** in `area-gradient-chart.tsx`. Data timing cannot change.
* **`gainedDate` feeds only the chart.** In `earnings/page.tsx` the state appears at exactly three sites: its declaration (`:51`) and the two `<EarningsChart gainedDate={...}>` props (`:189`, `:345`). **No money figure reads it**; every figure reads `summary`.
* **The page returns a spinner from the server** (`if (loading && !earnings)` at `:164`), so the chart never server-renders or hydrates.
* **If the chart chunk failed to load, the page would still show every number**, because the numbers are rendered by sibling blocks that make their own fetches and share no state with the chart. That is true today and is unchanged.

**No clip's earnings or status changed** — this merge contains a single markdown file and touched no database, no API and no component.

**6 money files + `tracking.ts` + `campaign-era.ts` BYTE-IDENTICAL by blob OID**, compared on both refs (`pre-merge-BL-646:<f>` vs `main:<f>`): writer `7aa6be48`, earnings-calc `797e2098`, balance `e887f80a`, tracking `847dcf70`, middleware `61cef393`, money-decimal `ef5cdae7`, campaign-era `106e16ad`. Also unchanged: `EarningsChart.tsx` `d882215f`, `area-gradient-chart.tsx` `3b1a94c1`.

**Conflicts and BACKLOG:** the merge applied cleanly with the `ort` strategy, one file, **no conflicts to union**. `grep -rnE "^(<<<<<<<|>>>>>>>|=======$)"` across `src/`, `scripts/`, `docs/` and `BACKLOG.md` → **0 hits**. **BACKLOG.md is not in the merge diff at all** (BL-643 never touched it), so the union count is trivially exact: **89** `## BL-` entries, unchanged. The two bare `$2` sites in BACKLOG.md are the same pre-existing prose noted in BL-645 (`"owner cap $2/account/10d"`, `"leaked $2 via campaignBudgetLiability"`), **not** capture-group corruption, and neither is in this diff.

---

## Gates, stated honestly

Run in an isolated worktree at the short path `C:/wt/bl641`, detached at `b213ed44`, with its own `node_modules`. **Never junctioned.** Each exit code read from a log with a real `echo $?`. **Never piped through tail** — the brief was right to warn: BL-643's own first build exited 1 on a missing `.env` while compiling fine.

| Gate | Result |
|---|---|
| `npm ci` | **exit 0** |
| `npx prisma generate` | **exit 0** (run after `npm ci`, which wipes the generated client). No `migrate`, ever |
| `npx tsc --noEmit` | **exit 0**, **0 lines of output** |
| `npm run check:prisma-bypass` | **exit 0**, 0 violations |
| `npm run check:removed-fields` | **exit 0** |
| `npm run lint:hooks` | **0 errors, 11 warnings** (at the ≤11 cap) |
| `npm run build` | **exit 0**, compiled in 17.9s, **61/61** static pages |

**eslint is present and genuinely executing: v9.39.4.** It reported 11 real warnings, which a silent no-op could not produce.

**Note on the build output:** this project's `next build` prints the route table **without size columns**, so bundle sizes cannot be read from it. That is why the measurement above resolves the client-reference manifests and gzips the chunks directly, which is also the method BL-643 used.

`origin/main == local main == b213ed44`, verified by `scripts/safe-push.mjs` (BL-288): *"VERIFIED PUSHED ✓"*. NO dashes as bullets.

---

## What the owner should do next

The 110.8 KB saving is real and well-evidenced, but it needs a round that **writes the code**. That round is one `next/dynamic` import with `ssr:false` and a fixed 220px placeholder in `EarningsChart.tsx`, plus a decision on whether to do `/referrals` at the same time (it carries the identical chunk and would need its own one-line change). BL-643 recommends doing `/earnings` alone first and proving it.

**Rollback:** `git revert -m 1 b213ed44`, or `reset --hard pre-merge-BL-646`. Doc only; nothing to undo anywhere else.
