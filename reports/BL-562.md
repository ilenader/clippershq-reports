# BL-562 — STRAENGE owner share paid: repair to gross, then credit $173.41

**2026-07-17 · REAL MONEY WRITTEN TO THE OWNER. agency_earnings only. No clipper earnings changed.**
**Base:** origin/main @ 9e13650a · **Branch:** checkpoint/BL-562

---

## THE RESULT

STRAENGE's owner accrual is now **$999.72** (was $874.25), total spend **$2,999.72** of a $3,000 budget. The write repaired 93 stale owner rows to BL-162 gross (reducing owner revenue by $47.94, as the owner signed off) and then created 3 credit rows for kujtaa._24643's override clips totalling $173.41. Order was not optional: crediting first would have breached the $1,000 owner reserve.

Every clipper's earnings are unchanged to the cent across the full 3,909-clip population. somesome was not touched. A second run writes nothing.

---

## PART 0 — every premise verified against live data before a cent was written

| # | Premise | Verdict |
|---|---|---|
| 1 | BL-541 fix live; the 4 agency-row deletion paths neutralised | **TRUE.** tracking.ts:2752 delete-branch removed; gamification.ts:942 delete removed (now upsert); force-recalc:368 deletes only when ownerAmt≤0; agency-monitor guarded. kujtaa opening his page can no longer erase the credit. |
| 2 | Six easy campaigns self-accrued ~$25.89 via the cron | **TRUE, and it is the proof.** BL-541 measured $25.86 owed; today only **$1.92** remains uncredited, so ~$23.94 accrued automatically since BL-541 deployed. The fix works. |
| 3 | STRAENGE still AUTO-paused, pool $2,000.00 of $2,000.00 | **TRUE.** status PAUSED, lastBudgetPauseAt set, clipper pool exactly $2,000.00. |
| 4 | Stored owner $873.68 re-deriving to ~$826.31 | **stored is now $874.25** (drift +$0.57: gamification self-healed 2 rows on 2026-07-17). Re-derives to **$826.31 exactly** (using the real `s/(1-s)=0.4999999925`, not ×0.5). Repair delta is therefore **−$47.94**, not −$47.10; the end state $826.31 is identical. |
| 5 | $173.41 across 3 clips still outstanding | **TRUE.** kujtaa._24643's 3 override clips (145.69, 135.86, 65.29 earnings) held no agency row. Gross = 72.84 + 67.93 + 32.64 = **$173.41 exactly**. |

No premise was false, so the write proceeded.

## PART 1 + PART 2 — the write, in order

Repair FIRST (93 rows to gross, owner $874.25 → $826.31), then credit (+$173.41). The fit:

```
clipper pool   $2,000.00   (UNCHANGED)
owner repaired   $826.31
owner credit     $173.41
owner AFTER      $999.72    <= reserve cap $1,000.00  (s x budget)
total AFTER    $2,999.72    <= budget $3,000.00
```

Crediting before repairing would have put the owner at $874.25 + $173.41 = $1,047.66, breaching the $1,000 reserve by $47.66. Order matters.

**STRAENGE is the mechanical case** (BL-539): 0 stamp disagreements. Its stamped rows read 0.25/0.50 = 0.5000 = exactly `s/(1-s)`, so gross is a genuine repair, not a repricing. **somesome was excluded**: 255 of its 256 stamped rows disagree with the locked share (its ownerCpm moved 1.00 to 0.49 mid-campaign), so a re-derive there would move real money on the era question, which is the owner's call. Its $133.51 remains pending.

## PART 3 — proof the credit survives

| Path | Trigger | Survives? | Evidence |
|---|---|---|---|
| tracking cron | every 10 min | **YES** | `campaignStatusBlocks` (tracking.ts:1929) includes `isAutoPausedCampaign`; the accrual block runs only `if (!campaignStatusBlocks)`, so STRAENGE is skipped. |
| agency-monitor `--fix` | owner-invoked | **YES** | Ran it READ-ONLY: **STRAENGE 0 violations across 156 clips** — the monitor already agrees my repaired values are correct gross, so `--fix` would leave them unchanged. |
| second money write | idempotency | **YES** | Re-ran `--write`: **0 rows moved, 0 credited**. Every value is the deterministic gross of the clip's current earnings. |
| force-recalc | admin-manual | **conditional** | Uses base-only (`calculateOwnerEarnings(..., newBase, ...)`, route.ts:359). It is manual, not automatic, so it does not fire on its own, but an admin running it on STRAENGE would rewrite owner rows to base-only. See the honest gap below. |

**The ambiguity guard protecting somesome is live** (agency-monitor.ts:174-177): when a clip's stamped `oCpm/cCpm` disagrees with `s/(1-s)` by >0.01, the row is `skippedAmbiguous` and the loop `continue`s. somesome's 255 stamp-disagreeing rows are protected from a `--fix` rewrite. (Honest nuance: somesome also has null-stamp rows the monitor still flags; nobody should run `--fix` on somesome until the owner settles its era, which the brief already knows.)

### The honest durability gap, reported not hidden

**gamification.ts:871 still computes owner as base-only** (`calculateOwnerEarnings(stat.views, oCpm, result.baseEarnings, cCpm)`), and `recalculateUnpaidEarnings` has no campaign-status filter, so it reaches STRAENGE. BL-541 removed its *deletion*, not its *formula*. It only writes the owner row when a clip's clipper earnings change (gamification.ts:936). STRAENGE is frozen at its budget cap, so clipper earnings are stable and the guard holds — which is why the 2 rows that gamification did upsert on 2026-07-17 are anomalous zero-earnings rows, not base-only rewrites, and why agency-monitor now reports 0 violations.

But **28 STRAENGE clips have a post-BL-541 `updatedAt`** (most recent today), so the campaign is not perfectly inert. If any one clip's earnings shift and gamification recomputes it, that single row would revert to base-only until the next agency-monitor `--fix` restores it to gross. The money cannot be deleted, and agency-monitor re-repairs it, but the airtight fix is to **teach gamification.ts:871 the gross formula behind the same ambiguity guard BL-541 gave agency-monitor**. Recommended as a follow-up; it is the last base-only writer that reaches a frozen split campaign.

## PART 4 — snapshot and exact rollback (printed BEFORE the write)

The write script printed the full snapshot and exact rollback before writing a cent: 93 `UPDATE agency_earnings SET amount=<prior> WHERE id=...` statements restoring each pre-repair amount, and 3 `DELETE FROM agency_earnings WHERE "clipId"=...` for the created credit rows. Six invariant checks all passed before the write (total ≤ budget, owner ≤ reserve, credit ≥ 0, credit = $173.41, repaired = $826.31, clipper pool = $2,000.00). The rollback is saved and reproducible by re-running the script's dry-run.

**Exact rollback summary:**
```
-- undo the credit:
DELETE FROM agency_earnings WHERE "clipId"='cmr7ui66t00ci0pnydg40ev1u';  -- +$72.84
DELETE FROM agency_earnings WHERE "clipId"='cmr7ui56500ca0pny503k44l3';  -- +$67.93
DELETE FROM agency_earnings WHERE "clipId"='cmr7ui5gz00ce0pnyi5ofkebg';  -- +$32.64
-- undo the repair: 93 UPDATE statements restoring each prior amount (full list in the write-script snapshot).
```

## PART 5 — evidence

* **STRAENGE owner $999.72**, total spend **$2,999.72** of $3,000 (live SELECT after write).
* **Clipper earnings UNCHANGED across the full population.** Before: 3,909 clips, $9,340.68, hash `65870461fa3a1625aa9c07eb5c370d0e`. After: identical, same hash. This script never writes `Clip.earnings`.
* **Idempotent:** second `--write` moved 0 rows, credited 0 clips.
* **Audit trail:** the 3 credit rows are identifiable — clip `cmr7ui66t…` $72.84, `cmr7ui56…` $67.93, `cmr7ui5g…` $32.64, all `userId` = kujtaa._24643, `createdAt` 2026-07-17 16:29.
* **somesome untouched:** $3,427.03 across 483 rows, identical before and after.
* **Six easy campaigns' automatic accrual:** only $1.92 uncredited remains (vs $25.86 at BL-541), confirming the cron self-accrued the rest.

## Safety

Only agency_earnings for STRAENGE was written. The 6 money files are byte-identical by blob OID (`git rev-parse` on origin/main vs HEAD; no `src/` file changed). No `Clip.earnings` touched, no clipper earnings changed, total never exceeded budget, no credit negative, BL-538's optional repair SQL not run, campaign-era.ts and the scraper paths untouched. `s/(1-s)` used exactly, matching production `calculateOwnerEarningsGuaranteed`.

**Gates:** eslint present (v9.39.4); `tsc --noEmit` exit 0, 0 errors; `npm run build` run (result stated in the round summary). This was a data write plus one standalone script, so no production code changed.

## Rollback

Re-run the write-script dry-run to regenerate the exact 96-statement rollback, or apply the summary above. Data-only; nothing to revert in code.
