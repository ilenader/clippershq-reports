# BL-638 — somesome forensic: did money move, or is the screen showing a different number?

**2026-07-23 · AUDIT ONLY. READ ONLY.** No code, data, schema or money change. `agency-monitor --fix` NOT run. No owner rows re-derived (platform-wide or somesome). somesome NOT unfrozen. BL-538's repair SQL NOT run. Read-only `run-select.js` only, every timestamp cast `::text` against DB `now()`.
**Base:** origin/main `9f5de583` · **Branch:** checkpoint/BL-638 · **DB now() = 2026-07-23 19:25 to 19:37 UTC**
**Campaign:** somesome `cmoaa70j2000z0ppfyaco13mj`, budget $9,750, PAUSED/AUTO, CPM_SPLIT, guaranteeOwnerSplit s=0.32885906, 953 clips, 84 clippers with clips.

---

## THE ANSWER (first line)

**NO money was lost. Not one cent moved.** The clipper earnings total is **$5,452.06** and the owner accrual is **$3,427.03**, both **identical to BL-570's 2026-07-17 measurement to the cent**, summing to the same **$8,879.09**. The screen is showing a **different population**, not a smaller pile of money.

**But the owner's premise is wrong in one detail, and the real reason matters more.** The screen is **NOT** "clipper-pool-only". It shows clipper earnings **PLUS the full owner accrual**, and then **excludes every retired (video-unavailable) clip**. On 2026-07-18 at 19:09:41 UTC, **710 clips across 6 campaigns were marked video-unavailable inside a 30-second window**, 419 of them somesome's, carrying **$3,412.18**. That is the entire drop:

```
2026-07-17 (BL-570): 5,452.06 clipper + 3,427.03 owner = 8,879.09   <- what the card showed
2026-07-18 19:09:41: 419 somesome clips stamped videoUnavailable    <- -3,412.18 (+21.11 later)
2026-07-23 (today):  2,018.77 clipper + 3,427.03 owner = 5,445.80   <- what the card shows now
                                              8,879.09 - 3,433.29 = 5,445.80  EXACT
```

**The money is all still there. What changed is which clips the screen is willing to count.** One genuinely new finding does need the owner's attention (PART 2C): 20 clippers can see a balance they cannot withdraw.

---

## PART 1 — what "$5,445.80 spent" actually means

### The exact code, file:line

| Step | Location | What it does |
|---|---|---|
| Render | `src/app/(app)/admin/campaigns/page.tsx:1095-1108` | `{formatCurrency(spent)} spent of {formatCurrency(c.budget)}` |
| State | `page.tsx:96` | `spendByCampaign` |
| Fetch | `page.tsx:123-136` | `fetch("/api/campaigns/spend")` |
| Clip side | `src/app/api/campaigns/spend/route.ts:63-83` | `db.clip.groupBy` where `status:"APPROVED", isDeleted:false, videoUnavailable:false`, `_sum: earnings` |
| Owner side | `src/app/api/campaigns/spend/route.ts:92-105` | `db.agencyEarning.groupBy` **with no `where` clause at all**, added unconditionally |
| Denominator | `page.tsx:1097,1101` | `c.budget`, the raw marketed budget |

**Which population does it sum?** Clipper clip earnings **on live clips only** (`videoUnavailable:false`) **PLUS the owner's entire agency accrual, unfiltered**. It is neither clipper-only nor the true total. It is a third definition.

The clipper-facing reshaping (`route.ts:128-176`, `resolveClipperFacingSpent`) never runs for OWNER/ADMIN, per the comment at `:126-127` ("OWNER / ADMIN / REVIEWER keep the real total spend"). The OWNER path also omits marketplace earnings and the BL-630 ghost fee.

Note this diverges from `balance.ts`'s `getCampaignBudgetStatus` (`src/lib/balance.ts:271-441`), which adds the owner aggregate **only** when `pricingModel === "CPM_SPLIT"` (`:366-378`), includes marketplace and the ghost fee. somesome IS CPM_SPLIT so the owner side is added either way, but the two numbers are not the same function.

### Both figures computed live, right now

| # | Figure | Amount |
|---|---|---|
| (a) | Clipper earnings, **every** clip (953, all APPROVED) | **$5,452.06** |
| (b) | Owner agency accrual (483 rows, unfiltered) | **$3,427.03** |
| (c) | (a) + (b) = true total spend | **$8,879.09** |
| (d) | Clipper earnings on **live** clips only (the card's clip side) | **$2,018.77** |
| (e) | **(d) + (b) = what the screen shows** | **$5,445.80** |

**$5,445.80 equals (e), not (a) and not (c).** The reconciliation is exact:
```
8,879.09 (true total) - 3,433.29 (earnings on 433 retired clips) = 5,445.80
```
Every one of these matches BL-570 to the cent: (a) $5,452.06 = BL-570's $5,452.06; (b) $3,427.03 = BL-570's $3,427.03; (c) $8,879.09 = BL-570's $8,879.09.

**Plainly: nothing was lost. BL-570's $8,879.09 counted every clip ever approved. The screen counts only clips whose video is still up, then adds the owner's full accrual on top. The $3,433.29 difference is earnings sitting on clips that went unavailable, frozen and not zeroed.**

---

## PART 2 — did any clipper lose a cent?

### 2A. Stored earnings: NO. Zero clippers down, zero up, net $0.00

| Evidence | Measurement |
|---|---|
| Clipper earnings total | **$5,452.06**, identical to BL-570 to the cent |
| Owner accrual total | **$3,427.03**, identical to BL-570 to the cent |
| Net change across all clippers since 2026-07-17 | **$0.00** |
| Clippers whose stored earnings went DOWN | **0** |
| Clippers whose stored earnings went UP | **0** (campaign frozen, no growth) |
| agency_earnings rows written since 2026-07-17 | **0** (last write **2026-06-22 17:01:43**, a month ago) |
| The 419 retired clips rewritten since the event | **0** |
| Clips where `savedEarnings > earnings` (a write-down signature) | **0**, drop $0.00 |
| PRR clips | **328**, exactly BL-570's 328 |

**Honest limit on this evidence.** There is **no per-clip earnings history table** in this database. `clip_stats` holds views only (id, clipId, views, likes, comments, shares, checkedAt, isManual) and `clip_snapshot_changes` holds pricing-model fields only. So a per-clipper 2026-07-17 versus today diff cannot be reconstructed row by row from history. What CAN be proven, and is proven above, is that the **aggregate is identical to the cent**, **no earnings-writing path ran** (0 agency writes in a month, 0 rewrites of the retired set), and **no write-down signature exists**. For an offsetting pair of moves to hide inside an unchanged total it would have to net to exactly $0.00 while the only code path that writes owner rows was provably gated out. BL-570 and BL-572 both established that old-window clips are gated out at `tracking.ts:1935` and never reach the owner-amount sites at `:2145` or `:2557-2582`.

**No clipper's earnings decreased. The BL-538 never-decrease guard was not bypassed, because no decrease occurred.**

### 2B. The specific balances the owner named

Computed both ways: `withdrawable` uses the withdrawal gate's basis (`/api/payouts/route.ts:411`, `videoUnavailable:false`), `displayed` uses the earnings page's basis (`/api/earnings/route.ts:64`, no such filter). Handles redacted to a hash.

| Clipper | Lifetime earned (live basis) | Paid out | Locked | **Withdrawable** | **Displayed** |
|---|---|---|---|---|---|
| `3159ac` | $0.00 | $0 | $0 | **$0.00** | **$147.61** |
| `cc40d7` | $154.38 | $0.00 | $0.00 | **$154.38** | $154.38 |
| `fa9808` | $51.57 | $0.00 | $0.00 | **$51.57** | $51.57 |
| `003014` | $51.37 | $0.00 | $0.00 | **$51.37** | $51.37 |
| `62cdaa` | $98.79 | $53.06 | $0.00 | **$45.73** | $45.73 |

**Best match to the owner's "roughly $140, $57, $57 and $48": `3159ac` $147.61, the near-identical pair `fa9808` $51.57 and `003014` $51.37, and `62cdaa` $45.73.** Every one of these is **exactly what that clipper earned minus what they were already paid minus what is locked in flight**, so each is owed precisely what they hold. Arithmetic verified per row above.

**One caveat stated plainly:** I could not determine which screen the owner read these off, so the mapping of "$57, $57" onto $51.57 and $51.37 is inference from the near-identical pair, not proof. The verification that each balance equals earned minus paid minus locked holds regardless of which rows he meant.

**`3159ac` is the case that matters:** they display **$147.61** but can withdraw **$0.00**, because every dollar they earned on somesome sits on clips retired in the 2026-07-18 sweep.

### 2C. NEW FINDING — 20 clippers can see money they cannot withdraw

The two code paths disagree:

| Path | file:line | Filters retired clips? |
|---|---|---|
| Withdrawal gate | `src/app/api/payouts/route.ts:411` (also `:119`, `:162`) | **YES**, `videoUnavailable: false` |
| Displayed balance | `src/app/api/earnings/route.ts:64` `clipWhere` | **NO** |

`clipWhere` is `{ userId, isDeleted: false, campaign: { isTestCampaign: false } }`. It feeds `computeBalance` at `:181`, whose `approvedEarnings` (`balance.ts:167-169`) sums every clip with `status === "APPROVED"`, and retired clips remain APPROVED with earnings intact. So retired earnings **are** counted into the displayed available balance and **are not** counted by the gate that actually authorizes a withdrawal.

Measured across somesome's clippers:

| Metric | Amount |
|---|---|
| Clippers whose displayed balance exceeds their withdrawable balance | **20** |
| Total shown but not withdrawable | **$366.82** |
| Total actually withdrawable | $591.98 |
| Total displayed | $958.80 |

**This corrects a prior audit.** BL-617 line 51 states that the withdrawal gate "and `/api/earnings` both fetch clips with `videoUnavailable:false`". That is **correct for the gate and incorrect for `/api/earnings`**. The only `videoUnavailable` filters in that file (`:81`, `:98`) belong to the marketplace creator and poster sub-queries, not to the main `clips` array that produces the balance. The file is unchanged since BL-521, well before BL-617, so this is a misreading in BL-617 rather than a regression.

This is **not** money lost and **not** caused by any round this week. It is a pre-existing display-versus-enforcement divergence that only became visible at scale when 419 clips retired at once. Flagged, not fixed: this round is read only, and `/api/earnings` is a money-adjacent path that deserves its own scoped round.

---

## PART 3 — did the owner's rows move? (the $933.94 question)

**NO. Re-measured with BL-570's own query. Every figure is unchanged to the cent.**

| Class | BL-570 (2026-07-17) | **Now (2026-07-23)** | Moved? |
|---|---|---|---|
| AMBIGUOUS rows | 108 | **108** | no |
| AMBIGUOUS stored | $1,928.71 | **$1,928.71** | **no** |
| AGREE rows | 375 | **375** | no |
| AGREE stored | $1,498.32 | **$1,498.32** | **no** |
| Total accrual | $3,427.03 | **$3,427.03** | **no** |

**The ambiguous total is $1,928.71. It did NOT collapse toward $994.77. The eraExempt path did NOT fire. The owner did NOT lose $933.94.**

Read-only re-derive cost, recomputed (a SELECT, nothing written):

| Class | Stored | Gross re-derive | Delta if re-derived |
|---|---|---|---|
| AMBIGUOUS | $1,928.71 | $994.77 | **−$933.94** (would LOSE) |
| AGREE | $1,498.32 | $1,543.27 | **+$44.95** (owed) |

Both match BL-570 exactly.

**eraExempt still untripped and no unexpected boundary:**

| Check | Result |
|---|---|
| `eraExempt = true` rows, platform-wide | **0 of 33** (BL-617 measured 0 of 32; one new row since, still 0 exempt) |
| Era boundaries for somesome (`toStatus=ACTIVE, fromPauseSource=AUTO`) | **4**, unchanged |
| Newest somesome boundary | **2026-06-22 03:07:29.621**, unchanged from BL-570 |
| somesome's most recent status change of any kind | **2026-06-22 17:59:21.913** (PAUSED, MANUAL_OWNER) |
| New boundary minted since BL-570 | **none** |

somesome's status machinery has not moved in a month.

---

## PART 4 — the full timeline, verified not inherited

### somesome's money history

| Timestamp (`::text`, UTC) | Event |
|---|---|
| 2026-04-22 16:42:51.374 | Campaign created |
| 2026-05-13 (x2), 2026-05-27 18:36:00.378, 2026-06-22 03:07:29.621 | The 4 era boundaries (all `eraExempt=false`) |
| 2026-05-15 | 6 clips retired, **$0.00** earnings |
| 2026-06-22 17:01:43.068 | **Last owner-earnings write.** No agency row touched since |
| 2026-06-22 17:59:21.913 | Last status change: PAUSED, MANUAL_OWNER. `lastBudgetPauseAt` 2026-06-22 17:59:21.894 |
| 2026-07-17 | BL-570 measures: clipper $5,452.06, owner $3,427.03, total $8,879.09 |
| **2026-07-18 19:09:41.778 to 19:10:11.545** | **710 clips stamped `videoUnavailable` in 30 seconds across 6 campaigns and 70 clippers. 419 were somesome's, carrying $3,412.18** |
| 2026-07-19 / 07-20 / 07-23 | 6 / 1 / 1 further somesome clips retired ($18.15 / $1.24 / $1.72) |
| 2026-07-20 11:28:37.103 | somesome's campaign row `updatedAt` moved. Budget still $9,750, fee still NULL, status still PAUSED. **What field changed cannot be determined** (no campaign field history, no audit row) |
| 2026-07-23 19:25 to 19:37 | This audit |

### The 2026-07-18 event, characterized

| Property | Measurement |
|---|---|
| Window | 19:09:41.778551 to 19:10:11.545056 (**~30 seconds**) |
| Scope | **710 clips, 6 campaigns, 70 clippers** |
| somesome's share | **419 clips, $3,412.18** |
| Other campaigns | GainzAlgo REPOST 187 ($71.70), WinGram 53 ($19.07), STRAENGE 23 ($4.15), bees.n.honey 17 ($0.00), Panic Baby 11 ($0.61) |
| Platform mix | **419 of 419 Instagram**, 0 TikTok, 0 YouTube |
| Normal daily retirement rate | 1 to 15 clips |
| Retired platform-wide **before** 2026-07-18 | **6** |
| Retired platform-wide now | 745 |
| `updatedAt` on the 419 | still **2026-06-24 / early July**, i.e. **NOT bumped by this write** |
| Audit log rows for it | **0** (audit logging is live: 590 rows since 07-17, newest 2026-07-23 19:36:18) |

**`Clip.updatedAt` is `@updatedAt` in `prisma/schema.prisma:144`**, so any Prisma write bumps it. It was not bumped. **Therefore this write did not go through Prisma. It was raw SQL**, and it left no audit row.

**It was NOT the `retire-dead-clips` cron.** `src/lib/retire-dead-clips.ts` cannot produce this shape: it writes via `db.clip.update` (`:122-125`, would bump `updatedAt`), is capped at `maxPerRun <= 500` (`:59`), paces every probe by `REPROBE_DELAY_MS = 1200` (`:48`, so 710 clips needs 14+ minutes, not 30 seconds), and requires a fresh live HikerAPI 404 per clip (`:111`). BL-617 independently observed that cron firing at its normal small volume (2 on 07-20, 1 on 07-21, 4 on 07-22), which matches my platform-wide daily counts exactly and is a different population from the 710.

**Was the retirement correct?** **Cannot be determined from the database, and I will not guess.** Two facts point in opposite directions and neither is conclusive:
* Against: **381 of the 419 carry `consecutiveGone = 0`** (max 1 across the whole set), so the gone-counter never indicated these videos were dead.
* For: `retire-dead-clips.ts:64-68` documents that BL-588 proved this counter "structurally under-fires", so a 0 does **not** prove a clip is alive.

Settling it requires probing the 419 Instagram URLs live, which is outside a read-only DB audit. **This is the single highest-value follow-up**, because $3,433.29 of clipper earnings and the $366.82 in PART 2C hang on whether those videos are actually gone.

**A related fact the owner should know: the documented auto-restore cannot fire for these clips.** `tracking.ts:1726-1741` restores `videoUnavailable=false` when a poll returns `views > 0`, but all 419 tracking jobs sit at `nextCheckAt = 2026-07-18 19:00:00`, **5 days overdue**, with `lastCheckedAt = 2026-07-18 18:11:38` and **zero `clip_stats` rows since the event**. They are active (419 active, 0 inactive) yet never polled. So the retirement is effectively permanent until reversed by hand. I could not isolate whether they are skipped because they are `videoUnavailable` or because somesome is PAUSED; both would produce this same observation.

### Did BL-562, BL-563, BL-572, BL-630, BL-631 touch somesome? Verified, not inherited

| Round | Claim | Verified verdict |
|---|---|---|
| BL-572 | STOPPED, shipped nothing | **TRUE.** No somesome data write; its own report documents the stop, and no owner row has moved since 2026-06-22 |
| BL-630 / BL-631 | Added `platformFeePctDecimal`, all rows NULL | **TRUE, measured: 30 campaigns, 0 with a non-null fee.** somesome's fee is NULL, so `realBudgetFromFee` returns `realBudget == marketed` and every cap is byte-identical. Consistent with BL-635 PART 4 |
| BL-562 / BL-563 | Owner-share guard work | **No somesome data effect.** Owner rows unchanged since 2026-06-22 17:01:43, ambiguous and agree totals identical to BL-570 |

**Did any round this week write to somesome? Answer plainly: no ROUND did, but something DID.**
* No audited round changed somesome's money: 0 agency writes in a month, 0 earnings changes, 0 status changes since 2026-06-22, budget still $9,750, fee still NULL.
* **But on 2026-07-18 19:09:41 a raw SQL bulk update stamped 419 somesome clips `videoUnavailable`, outside Prisma and outside the audit log.** It changed **no** earnings value (proven: totals identical to the cent), yet it is what moved the number on the owner's screen and what removed $3,433.29 from the withdrawable basis. It is not attributable to any BL round from the reports read here, and the database preserves no actor for it.

---

## PART 5 — the verdict

### One line

**NO. somesome did not lose money. Not one cent moved.** Clipper earnings $5,452.06 and owner accrual $3,427.03 are identical to BL-570's 2026-07-17 figures to the cent.

### The two numbers, explained for a glance

> **Your screen shows $5,445.80. The real total is $8,879.09.**
>
> Both numbers include your owner earnings. The difference is **which clips get counted**.
>
> **$8,879.09** counts every clip anyone was ever paid for on somesome.
> **$5,445.80** counts only clips whose video is still up on Instagram, plus all your owner earnings.
>
> On **2026-07-18** 419 somesome clips were marked "video unavailable" in half a minute. Those clips still hold **$3,433.29** of clipper earnings. The money is still on the rows, frozen, not deleted. The screen simply stopped counting it.
>
> $8,879.09 − $3,433.29 = **$5,445.80**. That is the whole story.

Nothing needs recovering, because nothing was lost. The one thing worth checking is whether those 419 videos are genuinely gone (PART 4), because if they are not, 20 clippers are wrongly blocked from $366.82 they earned.

### If the owner closes somesome and starts a fresh campaign for this client

| Item | What happens on close | Stranded? |
|---|---|---|
| **419 frozen clips ($3,433.29)** | Stay exactly as they are. Earnings are frozen, **never zeroed**, and the freeze is reversible by clearing `videoUnavailable` + `videoUnavailableSince`. Closing does not touch them | **No**, but they stay non-withdrawable while retired |
| **$133.51 blocked override credit** | Verified live: **7 override clips with no agency row, $133.51 exactly** (BL-570's figure). Closing changes nothing. It stays neither payable nor lost, exactly as BL-539/562/570 found | **No**, unchanged |
| **$44.95 the owner is owed** | Sits on the 375 agree rows. Closing does not pay it. It is only realizable via a re-derive, and a **blind** re-derive also hits the 108 ambiguous rows for **−$933.94**, a net **−$888.99**. **Do not re-derive to chase it** | **No**, still owed, still not worth chasing blind |
| **Clippers' current balances ($591.98 withdrawable)** | The payout route has **no campaign-status gate** (checked `src/app/api/payouts/route.ts`), so PAST/COMPLETED does not by itself block withdrawal | **No**, on this evidence |
| **A fresh campaign for the client** | Clean. New clips stamp at the current ownerCpm/clipperCpm 0.49/1.00, whose ratio equals the locked share exactly, so they are born non-ambiguous (BL-570 PART 3). A new campaign carries no fee (all 30 are NULL) | n/a |

**One risk to confirm before closing, which I could not measure.** `balance.ts:110-115` documents that a past **campaign archive** bulk-VOIDED payout rows ("all 29 VOIDED rows also paidAt=NULL (test-data cleanup or campaign archive)"). If the close/archive action still voids in-flight payout requests, any clipper with a REQUESTED or UNDER_REVIEW payout on somesome could have it voided. **Check for in-flight payouts on somesome immediately before closing.** I did not test the archive action because it is a write.

---

## What could NOT be measured

* **Whether the 419 retired Instagram videos are actually gone.** Requires live URL probes, outside a read-only DB audit. The gone-counter reads 0 for 381 of them, but BL-588 proved that counter under-fires, so it settles nothing either way. **The highest-value follow-up.**
* **Who or what ran the 2026-07-18 19:09:41 raw SQL update.** It bypassed Prisma's `@updatedAt` and wrote no audit row, and the database preserves no actor. I can prove what it did and when, not who.
* **Which field changed on somesome's campaign row at 2026-07-20 11:28:37.** No campaign field history and no audit row exists. Budget, fee, status, pause stamp and locked share are all verified unchanged, so whatever moved was none of those.
* **A true per-clipper earnings diff versus 2026-07-17.** No per-clip earnings history table exists (PART 2A). The aggregate-identity plus zero-write evidence is the strongest proof available.
* **Which exact screen the owner read the "$140 / $57 / $57 / $48" figures from** (PART 2B), so that mapping is a best match, not a certainty.
* **BL-535 could not be read: `reports/BL-535.md` returns HTTP 404 in `ilenader/clippershq-reports`.** Its claim about clipper-facing display was therefore not verified from source. It does not affect any finding here, because PART 1 is proven from the code and live data directly.

## Safety

READ ONLY, one markdown document, no code/data/schema/money change. `agency-monitor --fix` never run. No owner rows re-derived for somesome or platform-wide (the re-derive figures in PART 3 are SELECT-only arithmetic, nothing written). somesome not unfrozen. BL-538's repair SQL never run. All timestamps `::text` against DB `now()`. Clipper handles redacted to hashes. The verify and tracking paths untouched. Isolated worktree `C:/b638`, no `node_modules` junction. **Markdown-only diff: no build was run and none is claimed** (a doc change cannot alter tsc or the build).
