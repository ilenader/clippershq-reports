# BL-1474 — THE GATE IS SHUT AGAIN. Three of my six checks are OTHER ROUNDS' OPEN WORK.

**No batches were run. No sheets were built. Zero vendor calls. $0.00 of the $3.00 cap.**

My brief opens with a blocking precondition:

> *"⚠️ DO NOT START UNTIL PROMPTS 2, 3 AND 4 HAVE ALL LANDED. Re-verify each yourself with a
> runtime spy before the first page — a report saying a thing shipped is not the thing shipping.
> If any is missing, SAY WHICH AND STOP. DO NOT SPEND."*

**None of the three has landed. All three are in flight right now**, and — this is the part worth
saying plainly — **each one is precisely one of the six checks I was told to run before spending.**
I did not have to guess at the mapping; the claims name it themselves.

---

## 1. WHICH IS MISSING — named, with the evidence

| my check | the round that owns it | filed | state |
|---|---|---|---|
| **(e) a model with cut authority is alive** | **BL-1468** — *"NOTHING RUNS UNTIL THE JUDGE IS PROVEN ALIVE"* | 21:02 | **IN FLIGHT, 120 min** |
| **(d) exemplars are the right platform's** | **BL-1471** — *"FIX THE INSTAGRAM EXEMPLAR PACK — it is 8 TikTok pages out of 8"* | 22:48 | **IN FLIGHT, 14 min** |
| **(b) the hero strip reaches the model whole** | **BL-1473** — *"CLOSE THE ENCODER QUESTION… 83.4% of judging rows arrive as 155×275"* | 22:58 | **IN FLIGHT, 3 min** |

Two more are live alongside them: **BL-1469** (search paging, 32 min) and **BL-1472** (read-only
history reconstruction). With the two stale claims, **seven rounds are open.** The brief warned six
had run at once; tonight it is seven.

**I did not take any report's word for it.** I read the working tree.

### The judge is being rewritten as I write this

`git status` on BL-1468's three declared files, with mtimes against a 23:02 clock:

| file | git | last modified |
|---|---|---|
| `clippershq/free_judge.py` | ` M` uncommitted | **24.4 min ago** |
| `clippershq/preflight.py` | ` M` uncommitted | **23.3 min ago** |
| `clippershq/run.py` | ` M` uncommitted | **18.2 min ago** |
| `clippershq/tiktok_finder.py` | `M ` staged | **14.7 min ago** |

All five funnel modules **compile** — I checked by compiling the source in memory rather than
importing it, so nothing was executed and no `.pyc` was written under `clippershq/`. The detector
was proved first on a deliberately broken source, which it correctly rejected. So the tree is not
*corrupt* — it is simply **mid-edit and uncommitted**, which is the same reason not to measure
through it. Running four batches through a judge a peer is actively rewriting measures a moving
target and bills for the privilege.

### And a peer is spending right now

`spend.json` was last written at **23:04:15**, seconds before this check. That is not me — **this
round made zero vendor calls.** It is one of the live rounds billing into the shared ledger, which
is exactly why the brief says to use each run's own counter and never a ledger delta.

---

## 2. TWO CHECKS I COULD SETTLE ON THE CURRENT BYTES — BOTH STILL FAIL

I could not run a full runtime spy against a half-written judge without importing it, and
importing a module a peer is rewriting is neither safe nor honest measurement. But **(b) and (d)
are decidable from the shipped bytes**, and both are unchanged since BL-1470 found them broken.

### (d) exemplars — STILL 8/8 the wrong platform

`clippershq/meme_finder.py:4359` still holds **one flat `PINNED_EXEMPLARS` list of 8 entries** with
**no platform split**, and `_exemplar_pack()` hands that same pack to every brain:

- entries: **8**, all from the TikTok marks file (`ground_truth/score_marks_tiktok.jsonl`)
- platform-split keys in the block: **False**
- Instagram-specific exemplar suppliers defined anywhere in the module: **0**

Positive control: the same regex found `def _exemplar_pack` (expected present) → the detector
works, so the zero is a real zero and not a broken scanner. **The Instagram brain would still be
shown eight TikTok pages as worked examples.** BL-1471 was opened 14 minutes ago to fix exactly
this and has not finished.

*(Handles redacted here — this is a public report.)*

### (b) the encoder — the sliver path is still the shipped path

`clippershq/free_judge.py` still carries, at **two** sites:

```
917:  enc = tile_b64 if single_video else grid_b64
1894: enc = tile_b64 if single_video else grid_b64
```

Control: `grid_b64` appears 7 times in the file, so the scanner can see the alternative and the
match is not an artefact. This is the line BL-1473's claim quotes verbatim as the open defect —
`tile_b64` crops to the top-left cell and caps the long edge at 760 without upscaling, so a sheet
that could deliver 427×760 arrives at **155×275**. BL-1473 also records *"HANDOVER REQUESTED, NOT
TAKEN: clippershq/free_judge.py"* — it is contending for the same file BL-1468 is holding.

**Measuring accuracy through an encoder that is about to change, using exemplars that are about to
change, judged by a model chain that is being rewritten, would produce four sheets whose numbers
describe a configuration that will not exist by morning.**

---

## 3. WHAT I DID NOT DO

- **No batches.** Not one page discovered, captured, judged, paid for or delivered.
- **No sheets built**, so there are no launcher paths to give. Publishing four `.bat` files against
  a judge mid-rewrite would hand over grading work whose marks could not be reused.
- **Zero vendor calls, $0.00 spent** of the $3.00 cap. I did not verify the cap-binding behaviour
  (`effective_run_cap`'s falsy-zero fallthrough) because verifying it is only worth doing
  immediately before spending, and nothing will be spent.
- **No writes under `clippershq/` or `dashboard/`.** The only repo file this round creates is this
  report.
- **No process killed.** The four servers were confirmed listening with
  `Get-NetTCPConnection -State Listen` — never a command-line process filter, which the brief
  records once matching itself and reporting two live where there were none. All four are on their
  original pids.
- **No seen store touched**, because nothing ran. No backups were needed and none were taken;
  taking timestamped copies of six stores while seven peers write them would have created exactly
  the stale-snapshot problem BL-1470 had to disprove.

---

## 4. THE ONE NUMBER I CAN STILL GIVE HONESTLY

The brief asks for 95% accuracy. That answer does not depend on tonight's batches, and it has not
changed:

**His own marks agree with themselves 75.8% [68.4, 82.0].** Nothing scored against them can exceed
that, and reporting a higher figure would be flattery rather than measurement. The number that
*can* reach 95% is **kills of pages he wants** — a different quantity with a much better ceiling,
because it only has to be right about the confident cases. BL-1468's own table puts the best
candidate at 1 kill in 76 wanted pages: a **1.3% [0.2, 7.1]** kill rate, i.e. keeps **≥ 92.9%** at
the lower bound.

**That lower bound is the only half that counts, and it is not yet above 95%.** Reaching a provable
95% floor needs **110 marked wanted pages** at the current single observed kill; the graded set
holds **78**. **Thirty-two more marked wanted pages** turns "this gate keeps at least 95% of the
pages you want" from a hope into a measurement. Four sheets of fifty would have supplied them —
which is why this run is worth doing, once the three rounds beneath it land.

---

## 5. WHAT HAS TO HAPPEN BEFORE THIS RUN IS WORTH SPENDING ON

1. **BL-1468 lands** and names a live model that may cut, with its `_bar = MAY_REJECT.get(model)`
   property intact so an unscored model may answer but never reject.
2. **BL-1471 lands** an Instagram exemplar pack that is actually Instagram — verified by opening the
   images, not counting them.
3. **BL-1473 lands** the encoder fix, so the strip reaches the model whole rather than as 84.8% of
   its area removed.
4. Then re-run all six checks **from scratch** — not these three — because (a), (c), (e) and (f)
   were verified against bytes that will have changed.

Only then do the four batches measure anything. **I would rather hand back an empty round than four
sheets of numbers about a configuration that no longer exists.**

---

## 6. INSTRUMENTS, AND WHERE I DISTRUSTED MY OWN

- **Compiled in memory, never imported.** Importing `free_judge` mid-rewrite would execute
  module-level code in a half-edited state, and would write a `.pyc` under `clippershq/` — a
  directory this brief forbids writing to while runs are live.
- **Every zero carries a control.** The "0 Instagram exemplar suppliers" zero is only reportable
  because the same scanner found `def _exemplar_pack` in the same file. The "no syntax errors"
  result is only reportable because a deliberately broken source was correctly rejected first.
- **Process detection by name, never by command line.** The brief records a command-line filter
  matching its own query and reporting two live processes where there were none; I enumerated by
  process name and cross-checked against `Get-NetTCPConnection -State Listen`.
- **Claims registry AND live processes**, as instructed — the registry showed seven open rounds,
  and the process table independently showed python processes started at 22:46, 22:48, 22:54,
  22:58, 23:00 and 23:02, several holding 80–390 MB. The two agree, which is why I trust either.
- **I did not re-derive BL-1470's four passing checks.** They were measured against files that have
  since been modified, so quoting them tonight would be quoting a stale instrument. That is the
  same error class as the round that published "delta 0" — true when checked, stale when written.

---

## 7. THE HONEST SUMMARY

| asked | delivered |
|---|---|
| six checks passed before spending | **3 of 6 are other rounds' open work; 2 verified STILL FAILING on current bytes** |
| four batches of fifty | **none — the gate is shut** |
| four sheets with accepts and rejects | **none built** |
| launcher paths | **none — there is nothing to launch** |
| cost and clock table per brain | **none — no page was bought** |
| kills of his 8–10s with Wilson bounds | **unchanged from BL-1468: ≥ 92.9% lower bound, below the 95% bar, 32 marks short** |
| spend | **$0.00 of $3.00** |

**The brief told me that if any prerequisite was missing I should say which and stop without
spending. Three are missing, all three are actively being worked on by other rounds, and two of
them I confirmed still broken in the shipped bytes. So I stopped.**

---

## 8. RECEIPTS

- Claim `BL-1474` filed and released; declared `reports/BL-1474.md` and wrote nothing else.
- Peer rounds in flight at check time: **BL-1377, BL-1438** (both stale), **BL-1468, BL-1469,
  BL-1471, BL-1472, BL-1473**.
- Working tree at check time: `free_judge.py` `M` (24.4 min), `preflight.py` `M` (23.3 min),
  `run.py` `M` (18.2 min), `tiktok_finder.py` staged (14.7 min).
- `meme_finder.py:4359` `PINNED_EXEMPLARS` — 8 entries, one flat list, platform split **absent**.
- `free_judge.py:917` and `:1894` — `enc = tile_b64 if single_video else grid_b64`, unchanged.
- Four servers confirmed listening on their original pids; **no python process killed**.
- Campaigns SHA `8e02f8d6f6307ae8` — not read as changed; nothing this round could have changed it.
- **Vendor spend this round: $0.00. Vendor calls this round: 0.**
