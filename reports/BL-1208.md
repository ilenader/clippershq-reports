# BL-1208 — REPORT AND STOP: the fresh-50 sheet and all four rules sit inside live round BL-1207

**Status:** STOPPED before the first write to shared code. Nothing shipped.
**Interpreter for every number below:** `.venv\Scripts\python.exe` (3.11). The system `python` (3.13) was not used.
**Vendor spend:** $0.00 of the $1.50 cap. No vendor call was made — every number here is read from disk.

---

## 1. Why this round stopped

The brief said `RUN ALONE`. It is not alone. Round **BL-1207** started at
`2026-08-10T14:35:29`, fourteen minutes before this round's first command, and it owns
exactly the territory this brief targets.

`.claims/BL-1207.json` declares, among 13 paths:

```
scratch/bl1207/build_sheet.py
scratch/bl1207/uiverify.py
tools/serve_fresh50.py
OPEN_FRESH_50.bat
```

`tools/claim.py brief` at 14:49 rates the other four in-flight claims **POSSIBLY STALE**
(1,536–7,349 minutes untouched). BL-1207 is not one of them: 14 minutes old, with files
written at 14:41–14:47 and a suite run in progress.

### The collision is not partial — it covers every task in the brief

**Task 1 (offline sheet playback).** The file the operator opens,
`output/fresh50/REVIEW_50_FRESH_offline.html`, is not hand-written. It is emitted by
`scratch/bl1207/build_sheet.py` — a file BL-1207 holds:

```
scratch/bl1207/build_sheet.py:46   OUT_HTML = os.path.join(OUTDIR, "REVIEW_50_FRESH.html")
scratch/bl1207/build_sheet.py:194  thumb_dir=THUMBS, offline=True, sheet=SHEET)
scratch/bl1207/build_sheet.py:207  print("OFFLINE %s  (%.1f KB)" % ...)
```

Editing the HTML directly is not a fix; the next build overwrites it. The real fix belongs
in the held builder.

**Tasks 2–5 (person-vs-page, promo, repeated-meme, the positive description).** All four
rules live in `clippershq/meme_finder.py` and `clippershq/review_sheet.py`. Those two
modules are the direct imports of BL-1207's live runner and its server:

```
scratch/bl1207/build_sheet.py:36   import meme_finder as mf
scratch/bl1207/build_sheet.py:37   import review_sheet as RS
tools/serve_fresh50.py:39          import review_sheet as RS
```

Both modules are git-clean and both read `FREE`. Editing them anyway would change the
judge under a round that is at this moment building and serving a sheet the operator is
marking on — the precise defect this repo has already recorded twice (*a rule can be
correct and the runner still have the old one*; *a fix does not reach a running server*).

### The registry lied exactly where the brief warned it would

`tools/claims_read.py --holders` reports:

| path | registry says | reality |
|---|---|---|
| `output/fresh50/REVIEW_50_FRESH_offline.html` | FREE | build artifact of BL-1207's held builder |
| `clippershq/meme_finder.py` | FREE | imported by BL-1207's live runner |
| `clippershq/review_sheet.py` | FREE | imported by BL-1207's runner **and** its server |
| `ground_truth/review_marks.jsonl` | FREE | ` M` in git — 463 uncommitted insertions |

Four of my targets read FREE. None of the four is safe. `git status` was the honest
source, as instructed.

### The suite could not be run

BL-1207's own `scratch/bl1207/suites.log` last advanced at 14:47:04 and was still at 105
lines (stalled at `test_dashboard_panels.py`) when re-checked at 14:51:50. A concurrent
`tests/run_all.py --jobs 4` would have corrupted both runs. **No suite was run by this
round.** Rounds in flight: 5 (BL-1138, BL-1159, BL-1162, BL-1195 all possibly stale;
BL-1207 live).

---

## 2. His marks are intact, and here is the baseline to prove it

Read-only. Nothing was written to any marks file.

| file | rows | distinct handles | sha256 (first 16) |
|---|---|---|---|
| `ground_truth/review_marks.jsonl` | 617 | 573 | `3de8c947610bf0f4` |
| `output/review_marks_BL-1196-500.jsonl` | 441 | 424 | `39d7ef0ca7a36075` |
| `review_marks.jsonl` (repo root) | 51 | 49 | `cda0d75f8d25f641` |

**Union of all three = 573 distinct handles**, identical to the union of the two named
files. The brief's 573 is confirmed. The 441-row file's 424 handles and the root file's
49 are both already subsets of the 617-row file — BL-1206's merge already absorbed them.

One thing the next round must not miss: `ground_truth/review_marks.jsonl` carries **463
insertions that are not committed** (HEAD has 154 rows, the worktree has 617), last
touched 12:19 by the departed BL-1206. Scoring against it today bakes in a state no
commit records.

---

## 3. The finding that changes the brief: @centregoals was never rejected by a person rule

This is the part worth reading. The brief asks to rebuild the person rule on
*posting-themselves* instead of *verified status*, so that @centregoals passes. **That
change would not rescue @centregoals**, because no person rule and no verified rule
rejected it.

Its stored verdict in `scratch/bl1207/fresh50_pages.jsonl`:

```json
{"is_meme_page": false, "meme_share": 0.3, "hooks": 3, "synopsis": 3,
 "promotional": 0, "news": 4, "news_share": 0.4, "film_tv": 0, "dialogue": 0,
 "promo_bio": false, "judged": 10, "sampled": 10,
 "verdicts": ["NEWS","SYNOPSIS","SYNOPSIS","HOOK","NEWS","SYNOPSIS","HOOK","NEWS","NEWS","HOOK"]}
```

`promotional: 0`. `film_tv: 0`. `dialogue: 0`. `promo_bio: false`. Nothing about a person,
nothing about the blue tick. It failed on one number: **meme_share 0.3**, because RULE 4
(media/news) claimed 4 of the 10 sampled posts and the synopsis test claimed 3 more.

The page carries Instagram's self-declared `category: "Media/news company"` and the bio
*"The home of European football"* — and the sampled drawn lines it was rejected over are
the operator's own positive description almost word for word:

```
"Just wait for the last one"
"Imagine being so good that your teammates can only stand and admire"
"When Mesii finally found someone shorter than him"
"When a fan snuck into portugal's team photo"
"Bro had to make sure someone has filmed it"
```

Those were scored NEWS and SYNOPSIS. So the live lever for @centregoals is the **NEWS
classifier eating sport-meme hooks**, plus the meme_share threshold — not the person rule.
Rebuilding the person rule as instructed would have shipped a change that provably fails
the counter-example he supplied it for.

A second, smaller defect sits in the same record: the row's top-level `meme_share` is
`0.0` while `verdict.meme_share` is `0.3`. Two count levels disagreeing on one page.

---

## 4. BL-1207's fresh 50 is genuinely fresh — that part is sound

Cross-checking `scratch/bl1207/fresh50_pages.jsonl` against the 573 marked handles:
50 rows, 50 distinct, **0 overlap**. No page on that sheet has been marked before.

---

## 5. What the next round should pick up

1. Talk to BL-1207 before touching the sheet. The playback fix belongs in
   `scratch/bl1207/build_sheet.py`, under its owner — inline `<video>` created on click,
   one at a time, `preload="none"`, torn down on close, keeping the buffered-local marks
   path and the always-present Download.
2. Rebuild the rules only when `clippershq/meme_finder.py` and
   `clippershq/review_sheet.py` are not being imported by a live runner.
3. Aim the person/page fix at the **NEWS and SYNOPSIS classifiers on sport pages**, not at
   verified status — §3 shows that is where @centregoals actually dies.
4. Commit `ground_truth/review_marks.jsonl`'s 463 orphaned insertions, or record their
   hash, before any scoring run treats them as ground truth.

No lead data appears in this report. No address is quoted from any record read.
