# BL-609 — Merge BL-606 (multi-clip multi-platform batch submit for test clippers)

**Status: SHIPPED.** Merge commit `e2e3adea` is on `origin/main` (verified `origin/main == local HEAD`).
Merge only. No new feature code authored; BL-606 built on `checkpoint/BL-606`, this round merged it.

## STEP 0 — TRUTH (with SHAs)
* `checkpoint/BL-606` tip = `05df9b110f820a353f8a0c508df28f67b007cf7c` ("BL-606: multi-clip multi-platform batch submit for TEST CLIPPERS ONLY").
* Genuinely UNMERGED before this round: `git merge-base --is-ancestor origin/checkpoint/BL-606 origin/main` returned 1 (not an ancestor). Exactly ONE commit was on the checkpoint and not on main: the BL-606 commit itself.
* Merged AFTER the slideshow fixes: pre-merge `origin/main` was `286e386e` = "Merge BL-605: fix the two slideshow/carousel null-vs-0 bugs". BL-607 (`bl607-merge`) is already folded into that main line. Merge base of main and the checkpoint was `8ea418b0` (Merge BL-602), i.e. BL-606 branched BEFORE BL-605, so the slideshow fix had to be preserved by hand (see UNION below).
* Nothing a live round held was merged: the shared tree's staged `docs/SCRAPBADGER-TIKHUB-AUDIT.md` (BL-608 docs audit) was never touched. All work was done in an isolated worktree at `C:/b609` on a dedicated `bl609-merge` branch off `origin/main`; the shared tree's index was left alone.
* Merge commit `e2e3adea` parents: `286e386e` (slideshow merge) + `05df9b1` (BL-606). Correct.

## Conflicts (2), both resolved as UNIONS
1. **`src/app/api/clips/route.ts`** — the one real risk. HEAD (origin/main) held BL-605's null-safe first-ClipStat INLINE; BL-606 replaced that whole inline body with a call to the extracted core `processClipperSubmitLink(...)`. Resolved by keeping BL-606's delegate call (the inline body moved into the core), THEN relocating BL-605's fix into the core so BOTH survive.

   Resolved region (route side):
   ```
   const core = await processClipperSubmitLink({
     db, userId: session.user.id, campaignId: data.campaignId,
     clipAccountId: data.clipAccountId, clipUrl: data.clipUrl, note: data.note,
   });
   if (core.transient) refundRateLimit(RL_KEY);
   if (!core.ok) return NextResponse.json({ error: core.error, ...(core.extra ?? {}) }, { status: core.status });
   ```

   BL-605 null-safe fix, re-applied into `src/lib/clipper-submit-core.ts` (the core branched from pre-BL-605, so it carried a fabricated `views ?? 0`; corrected to SKIP on null):
   ```
   const resolvedFirstViews = fetchedStats?.views;
   if (resolvedFirstViews != null) {
     await tx.clipStat.create({ data: { clipId: newClip.id, views: resolvedFirstViews,
       likes: fetchedStats?.likes ?? 0, comments: fetchedStats?.comments ?? 0, shares: fetchedStats?.shares ?? 0 } });
   }
   ```
   Rationale: `8ea418b0..origin/main` touched `route.ts` ONLY via BL-605's block, so the sole origin/main-unique content in the conflict was that fix. Relocating it into the core is a faithful union.

2. **`BACKLOG.md`** — both the BL-605 and the BL-606 entries kept verbatim, in order, markers removed only. No capture-group corruption; boundary verified.

Global marker sweep across `*.ts`/`*.tsx`/`*.md`: NONE remaining.

## CRITICAL CHECK — normal single-submit produces IDENTICAL per-clip state
Traced. The refactored route delegates to `processClipperSubmitLink`, which creates, atomically: a PENDING `Clip` (status defaults to PENDING, no force-approve) + a first `ClipStat` (now null-safe: skips the snapshot when the provider resolve is null, exactly BL-605) + an active `TrackingJob` (same `:00` next-check + `getLockedIntervalFloor`) + a frozen CPM (`cpmAtSubmissionDecimal` / `ownerCpmAtSubmissionDecimal` via `enforceCpmStampInvariant`). NO earnings write and NO placeholder-account minting in the clipper path. BL-605's null-safe behaviour is preserved through the refactor (confirmed in the committed core, lines 197 to 202). The route's request-scoped shell (idempotency cache, burst rate limit + refund on transient, streak clear, growth/analytics `after()` hooks, RULES-SHADOW) is unchanged.

## Batch endpoint safety (`POST /api/clips/batch`)
* Test-only: `getSession()` + `checkBanStatus()`, then `isTestUser` gate returning **404** (not 403) BEFORE any work.
* Per-row `clipAccountId` resolved PER ROW inside the core (Trap 1 avoided) — no shared account across links.
* Owner force-approve core (`processOwnerSubmitLink`) referenced **0** times functionally (only two doc-comment mentions contrasting the cores; no import, no call). No `writeClipEarnings`, no clip force-APPROVE in the clipper path (the one `status: "APPROVED"` is an eligible-account WHERE clause).
* Platform comparison CASE-INSENSITIVE (`.toLowerCase()` on both sides in the core).
* Server-side per-campaign daily-cap pre-check BEFORE any write, plus the core's per-row cap on sequential commit; in-batch + DB dedup; per-row results (BL-213).
* `BatchSubmitSection.tsx` UI gated on the real `isTestUser`; every non-test user is byte-identical.

## Money-file safety
6 money files BYTE-IDENTICAL by blob OID on BOTH refs and in the merge result:
`clip-earnings-writer.ts` `7aa6be48`, `earnings-calc.ts` `797e2098`, `balance.ts` `f8499e18`,
`tracking.ts` `b311323c`, `clip-earnings-invariant-middleware.ts` `61cef393`, `money-decimal.ts` `ef5cdae7`.
`tracking.ts` absent from the merge diff. No existing clip affected. No prisma migrate.

## Build (real exit codes, from logs, not piped through tail)
* `npm ci` exit 0 (wiped generated Prisma client) → `npx prisma generate` exit 0 (Client 7.8.0) → `npx tsc --noEmit` exit 0.
* `npm run build` **BUILD_EXIT=0**. Prebuild gates: prisma-bypass 0 violations; removed-fields OK; hooks gate `eslint --max-warnings 11` = **11 problems (0 errors, 11 warnings)** (eslint present, gate real). `/api/clips/batch` and `/api/clips` both in the route table.

## Ship
Verified push per BL-288: `git push origin bl609-merge:main` (`286e386e..e2e3adea`), then fetch + assert `origin/main == e2e3adea`. Tags `pre-merge-bl609` / `post-merge-bl609` pushed. Branch `bl609-merge` pushed.

**Rollback:** `git reset --hard pre-merge-bl609` (`286e386e`) then force-push, or revert merge `e2e3adea`.
