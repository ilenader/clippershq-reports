# BL-1268 — TikTok runs end to end except the last link, and the bio is still the whole email surface

**Round:** BL-1268 · **Date:** 2026-08-14
**Interpreter: `.venv\Scripts\python.exe` (CPython 3.11.9) produced every number in this
report.** Nothing was run under the 3.13 on PATH.
**Budget: $2.00 hard cap, declared in `scratch/bl1268_budget.json` before the first billed
call.** Judging ran on the **free** path. **No lead address appears anywhere in this report.**

---

## The answer in one sentence

**He can type a hashtag, watch it run, and get judged pages with emails — but the last link
is not connected: nothing in the product builds the send file, and eight addresses the
funnel already paid for are sitting in `master_leads.csv` that `TIKTOK_PAGES_BOT_READY.csv`
has never seen.**

---

## 0. What this round did NOT do, and why

**BL-1260 holds an open claim on `clippershq/tiktok_finder.py` and ran its own funnel to
completion at 21:32:35.** Two of this prompt's instructions were already done, in that
round's uncommitted working copy, before this round started:

- **Move the ledger write.** Done. `reserve()` now flushes a **delta** to the ledger every
  `SPEND_CHECKPOINT_CALLS = 25` reserved calls, with a `finally` closing the tail.
- **Recover the missing capture bill.** Done, by one free `/sys/balance` read:
  `BL1260_CAPTURE_RECOVERED`, **309 calls, $0.1854, `estimated: true`**, stamped at
  19:24:22 — the moment the money was spent, not the moment it was noticed.

**Writing that file again would have destroyed a live round's uncommitted work.** So this
round **verified** both instead of duplicating them, and pointed its own run at
BL-1268-namespaced paths (`tiktok_seen_file`, `tiktok_sheet_dir`, `master_csv`) so nothing
of BL-1260's was raced. Confirmed: `tiktok_pages_seen.json` still carries its 21:32 mtime,
which predates this round's first call at 21:42.

### The fix is proven in production, not by reading it

| | |
|---|---:|
| `TIKTOK_FINDER` rows before the fix | **0 in 2,801, ever** |
| checkpoint rows now | 16 |
| calls booked by checkpoints | 400 |
| final-flush calls | 16 |
| **total** | **416 calls, $0.2496 at exactly $0.0006** |

**The double-count test.** If the flush passed the cumulative `used["calls"]` instead of the
delta, 16 checkpoints would have billed 25+50+…+400 = **3,400** calls. It billed **400**.
The delta accounting is correct.

**And it tracks the vendor.** Independent check against the receipt: LamaTok's request
counter moved **412** over the window; the ledger recorded **416**. The meter **over-records
by 4 of 416 = 0.96%** — `reserve()` counts attempts, and four reserved slots never became
billed requests. A meter that over-records is safe; the bug being fixed was the opposite.

This round's own free baseline, two consecutive reads byte-identical on both meters:
**445,924 requests / $267.8662 at 21:35:57.**

---

## 1. THE CHAIN, LINK BY LINK

Every link traced to `file:line` in the working copy, then the reachable ones exercised
live against the dashboard on `127.0.0.1:8787`.

| # | link | status |
|---|---|---|
| 1 | UI funnel picker | **WIRED** — derived from `run.FUNNELS`, not a hand-kept list |
| 2 | hashtag and search as **separate** fields | **WIRED** — `funnel_params.py:160-185`, six fields |
| 3 | estimate button | **WIRED, EXERCISED LIVE** |
| 4 | start → subprocess | **WIRED** — cap mandatory, 400 without it |
| 5 | `run.py` → `tiktok_finder.run_funnel` | **WIRED** — campaign constant matches the meter |
| 6 | funnel stages | **WIRED** — free cuts run before the paid profile call |
| 7 | email → `master_leads.csv` | **WIRED** — but writes none of the three columns the send file reads |
| 8 | master → **the send file** | **BROKEN — UNREACHABLE FROM THE UI** |
| 9 | path overrides | keys exist; **not settable from the UI** |

### 1.1 The estimate is real, and every number in it is sourced

Posted to `/api/start/estimate` exactly as the browser does, for this round's own five tags
at depth 8. It **spends nothing and writes nothing** — deliberately, so a field he is still
editing cannot become the setting every future run reads.

| stage | requests | $ |
|---|---:|---:|
| discovery (5 hashtags × depth 8) | 30 | 0.0181 |
| one search per author, for its OWN videos | 251 | 0.1507 |
| one profile per reachable author | 229 | 0.1376 |
| **TOTAL** | **511** | **$0.3065** |

→ 344 videos, 251 authors, **229 judgeable pages**, **25 emails [15, 41]**, **73m 08s**,
suggested cap **$0.34**.

**9 of 9 terms carry a source.** Scalars carry `value`; **proportions carry `pct/k/n/
interval_95` instead**, which is the correct schema for a rate — BL-1254's rule that a
number without its denominator is not a measurement. Both proportions reproduce from the
totals: 229/251 = 91.24% against the stated 91.3%, and 25/229 = 10.92% against 10.9%.

### 1.2 THE BROKEN LINK, and what it has already cost

**`tiktok_send_list.build()` and `write_csv()` are called by nothing in shipping code.** The
only entry point is `if __name__ == "__main__"` at `tiktok_send_list.py:259`. Every other
mention inside `clippershq/` is a comment (`tiktok_finder.py:1270`, `:1308`) or a read-only
viewer — `exporters.build_tiktok_pages`, which when the file is missing returns an empty
table with the note *"No TikTok send file yet - run clippershq/tiktok_send_list.py."*

**This is not theoretical. Measured today:**

| | |
|---|---:|
| send file on disk | **115 rows**, mtime **16:43** |
| what the builder would produce right now | **123 rows** |
| **addresses in master the send file has NEVER seen** | **8** |
| addresses in the send file no longer buildable | 0 |

**The builder is not broken — it is UNCALLED.** Invoked read-only it produces a correct
file: 62,092 master rows → 124 tiktok-tagged → 124 with an email → 1 excluded as already in
another send file → **123 kept**, 2 role inboxes kept and flagged, **MX 100% coverage, 0
days old**, independent re-check **CLEAN**. One command does it. There is no button.

**Three columns on that file are blank on every row.** `avg_views`, `videos_observed` and
`promo_in_bio` are blank on **123 of 123**, because `_append_to_master` never writes them
while `tiktok_send_list.py:162-164` reads exactly them. `activity` is not blank but
**constant** — `"UNKNOWN"` on 100% of rows, because it reads a key `send_suppress.classify`
never returns. Same finding BL-1251 recorded on 115 of 115.

**Two checks inside that manual step cannot fire.** `assert_clean`'s dead-MX branch tests
for the `mx_probe` verdict *names* (`NXDOMAIN`/`NULL_MX`/`NO_RECORDS`) while
`send_suppress.mx_status` returns `dead`/`no_route`/`ok`/`unknown`. **It certifies the file
CLEAN without looking.**

### 1.3 Two more things the UI does not tell him

- **`min_videos` < 3 kills the run.** The dashboard offers a "Videos needed before judging"
  box; typing 1 or 2 raises `ThinEvidence` on the first author and the run is recorded
  FAILED. The floor is correct — the UI simply does not say so.
- **A run with no hashtags and no searches returns silently** and is recorded **COMPLETED
  with 0 leads**.

---

## 2. CHASING EVERY LINK FOR AN EMAIL — each route with its own denominator

Measured **twice, on two populations**, and both are reported.

- **The route table below is the 135-page measurement** — every author whose profile was
  bought in BL-1260's volume capture, read **read-only**. **Fresh and unselected**: included
  whether or not it already had an address. That is a base-rate population, unlike the 115
  in the send file, and it is the larger n.
- **This round's own 63 WANT pages** were chased separately after the kill, and agree:
  **4 of 63 carry an address (6.3% [2.5, 15.2])**, all four from **bio text** (4 of 60 =
  6.7% [2.6, 15.9]). Every other route returned nothing on that population.

**Eight of the nine routes are free.** Only the Instagram resolution is billed.

| route | what it is | eligible | attempted | found | **yield** | 95% CI |
|---|---|---:|---:|---:|---:|---|
| **bio text** | the TikTok bio string | 117 (86.7%) | 117 | **13** | **11.1%** | [6.6, 18.1] |
| link slot IS an address | the bioLink field itself | 22 (16.3%) | 22 | 1 | 4.5% | [0.8, 21.8] |
| **aggregator (hop 2)** | Linktree/beacons/carrd behind the link | 6 (4.4%) | 6 | **1** | **16.7%** | [3.0, 56.4] |
| personal site | their own domain + /contact + /about | 11 (8.1%) | 11 | 0 | 0.0% | [0.0, 25.9] |
| **Instagram** (PAID) | the IG named in the bio, resolved | 7 (5.2%) | 7 | **0** | 0.0% | [0, 35.4] |
| X / Twitter | og:description on the linked X | 2 (1.5%) | 2 | 0 | 0.0% | [0, 65.8] |
| YouTube | the linked channel's /about | 8 (5.9%) | 8 | 0 | 0.0% | [0, 32.4] |
| linktr.ee **guess** | `linktr.ee/<handle>`, no link needed | 113 (83.7%) | 113 | **0** | 0.0% | [0, 3.3] |
| Reddit / LinkedIn | named in the bio | **0** | 0 | 0 | **no denominator** | — |

### What that table says

**The bio is the whole surface, and this reproduces the literature independently.** 13 of
117 = **11.1% [6.6, 18.1]**, against BL-1191's **10.9% [6.5, 17.8]** measured on a different
population. **Every other route combined added two addresses.**

**Following an aggregator DEEPER changed the answer from zero.** BL-1217 measured **0 of 6**.
Run to **hop 2** this round got **1 of 6**. n=6 and the interval is [3.0, 56.4] — that is a
hint worth keeping, not a rate worth planning on. It is also the one place where "probe
every variant before calling an endpoint dead" paid off.

**The linktr.ee guess is very nearly worthless, and mildly harmful.** 0 of 113 on the large
population — and it produced five platform boilerplate addresses that only the junk guard
stopped. On this round's own 63 it hit **1 of 55 (1.8% [0.3, 9.6])**, and that one is
`contactus@<brand>group.com` — a **media brand's corporate inbox**, not a creator's. So it is
not literally zero, but at 1.8% for a corporate address it is not worth building.

**Reddit and LinkedIn have no denominator.** Not one of 135 bios named either. There is
nothing to build a resolver against, which is a stronger answer than "it yields 0%".

**The Instagram cross-link is now run, across both populations: 13 of 13 resolved, ZERO
addresses.** The interval on 0/13 is **[0, 22.8]** — this still does **not** show the leg is
worthless, it shows nobody has run it at a size that could tell. Cost to find that out:
**$0.0078**. It has been "costed and unrun" since BL-1221; it is now costed and run, and the
answer is that it needs a few hundred resolutions before anyone can call it either way.

### The cross-link rate, both figures, labelled

| | k / n | rate | 95% CI | use it? |
|---|---|---:|---|---|
| **this round, fresh** | 7 / 135 | **5.2%** | [2.5, 10.3] | ✅ |
| **BL-1221, fresh** | 55 / 1,047 | **5.3%** | [4.1, 6.8] | ✅ **the base rate to plan with** |
| BL-1259, the send file | 32 / 115 | 27.8% | [20.5, 36.6] | ❌ **never as a base rate** |

The 115 are in the send file **because they already have an email**. People who publish an
address also cross-link, so that figure is conditioned on the outcome and overstates
discovery by a factor of five. This round's fresh 5.2% lands on top of BL-1221's 5.3%.

### The platform gap, restated because it keeps being mistaken for a filter fault

Instagram's **business-email field** gives 40 hits and its **contact button** gives 31
addresses — **51% of Instagram's entire yield**. Confirmed again from the TikTok parser:
there is **no** `business_email`, `public_email`, `contact_email` or contact-button key
anywhere in TikTok's payload. **The entire TikTok email surface is one string plus one
link.** TikTok reads 10.9% against Instagram's 16.9% on the same stage. **There is nothing
here to fix by tuning.**

### Three routes I reported at 100% before checking, and why that matters

The first pass of this measurement reported **personal_site 11/11, YouTube 8/8, X 2/2** —
three routes at 100% against recorded rates near 2%. All three were harness bugs:

- `enrich_links.site_contacts` and `extract_from_html` return a **dict** `{emails, socials,
  outbound}`. `list(...)` over a dict yields its **keys**, so the "addresses" were the
  literal strings `emails`, `socials`, `outbound`.
- `twitch_finder.x_bio_email` returns a **tuple** `(email, bio)`. Taking the tuple gave the
  og:description **prose** as an address.
- the linktr.ee guess returned SoundCloud's legal inboxes, one of them as
  `u003edsa-contact@…` — a JSON-escaped `>` scraped out of an inline script and never
  unescaped.

**A count is not a measurement until something has looked at what was counted.** Every
candidate now passes `bio_parser.valid_email`, the junk-domain list and the platform-hop
list before it is counted.

**And that exposed a real gap in the shipped guard — one that has already cost him.**
`bio_parser.PLATFORM_HOP_DOMAINS` holds 59 domains and **`soundcloud.com` is not one of
them**, so a platform's own legal inboxes passed every shipped check.

**This is not hypothetical: `master_leads.csv` already contains that exact SoundCloud
inbox**, plus a `distrokid.com` one — 2 junk rows among 6,993 addressed rows. The publish
guard found it for me by refusing to let me write the address into this round's own scratch
file, on the grounds that it *"lives in master_leads.csv"*. The scanner was right and I was
wrong about which of my examples was a real lead.

This round guarded **locally** and did not touch `clippershq/bio_parser.py` — that file is
shipped product code and not this round's to edit. **The fix belongs upstream in that
frozenset**, and it should sweep the 2 existing rows out of master while it is there.

### Refused, with the evidence, rather than silently skipped

**Sherlock** (refused previously with evidence), **name search** (0 for 6; six namesakes,
and one address belonged to a stranger at a transit authority), and **email-finder services**
(they permute a name against a *company domain*; these are gmail/outlook creators with no
company domain, so the method cannot work here by construction).

---

## 3. THE JUDGING — free, and his gate is worth six pages

**100 contact sheets judged on the FREE path** — Claude Code sub-agents across 6 batches,
one fresh context each, **$0.00 of vendor spend**. BL-1266 separated the two pockets and
BL-1267 measured the paid and free paths returning identical verdicts, so this is not a
cheaper approximation.

**The brief was written out in full rather than imported.** BL-1262 and BL-1265 set
**contradictory gates under the same section number of the same brief**, so any inherited
copy risks carrying the `>=2` gate he has already rejected.

| | |
|---|---:|
| judged | **100** |
| **WANT** | **63 (63.0%)** [53.2, 71.8] |
| REJECT | 37 |
| — `creator` | **21 = 56.8% of rejects** |
| — no disqualifier, just no hook drawn | 10 |
| — `still` | 4 |
| — `not_english` | 2 |

**Gate audit — both invariants hold:** **0** pages were WANT while carrying a disqualifier,
and **0** `creator` pages were rescued by their capability count. The creator rule was
independent and was never outvoted, on any of the 100.

**`creator` is again the dominant reject**, at 56.8% of rejects here against the 73.6%
(64 of 87) recorded on the previous batch. Same cause, same order of magnitude: the tail of
a hashtag walk is full of people filming themselves.

**The `>=1` gate is worth six pages.** Six of the 63 WANTs rest on **exactly one** qualifying
clip — `clippergetmeto10k`, `auratrolleveryone1`, `jonhyyporkyy01`, `quandalio_`,
`sleepingapples`, `theehausofmeme`. Under the `>=2` gate the killed rounds left behind, all
six flip to REJECT: **63 → 57, a 9.5% cut**, on his own stated rule.

**A second thing the `>=1` gate exposes: 10 of the 37 rejects fired NO disqualifier at all.**
They are pages that repost borrowed footage perfectly well and simply **never draw a hook
line on it** — bare Peppa Pig frames, clean green-screen assets, an untexted clip grid. They
are not creators and not stills; they just fail the capability test. That is a distinct
supply category worth knowing about, because no disqualifier tuning will ever recover them.

**On 63.0% against the recorded 13.3%** — that is not a contradiction and should not be read
as a base rate. Two causes: the five tags walked here (`memepage`, `funnymemes`,
`relatablememes`, `dankmemes`, `memes`) are meme-targeted, so supply is pre-enriched; and
`>=1` is genuinely permissive. **63.0% is the yield of THESE tags under HIS gate**, not a
platform rate.

**Capture held.** Playwright cannot reach a TikTok profile at all — headless returns
"Something went wrong", headed returns a slider CAPTCHA. Rebuilding the grid from the
vendor's own cover images over plain HTTP is the working method, it is free, and BL-1260
measured its real error rate at **0.1% failed cover fetches across 134 pages**. Blank tiles
are overwhelmingly accounts that simply have no more videos, not a capture failure.

---

## 4. MX AND THE PARTITION — in that order

**MX FIRST, because otherwise suppression is vacuous.** A domain absent from the table reads
as UNKNOWN and is **never cut**, so a suppression pass over an uncovered file reports a green
"0 suppressed" that means nothing. BL-1221 measured **12 of 23 domains uncovered (47.8%)**,
and `tools/refresh_mx.py` records worse: built once with 141 domains, by two days later the
export held 561, so **470 (83.8%) had never been probed**.

Refreshed at **21:34**, before this round's first billed call: **1,987 domains — 1,910
ok-MX, 45 DEAD, 29 noMX-Aonly, 3 null-MX, and ZERO unprobed.** Coverage for the live TikTok
send file: **100.0% of 25 domains, 0 days old, nothing uncovered.**

**The partition holds, measured on the files on disk rather than on the builder's own
bookkeeping:**

| | rows |
|---|---:|
| `ALL_BOT_READY.csv` | 3,978 |
| `MEME_PAGES_BOT_READY.csv` | 874 |
| `TIKTOK_PAGES_BOT_READY.csv` | 115 |
| TIKTOK ∩ ALL | **0** |
| TIKTOK ∩ MEME | **0** |
| ALL ∩ MEME | **0** |

**PARTITION HELD.** This round wrote none of those three files.

---

## 5. SPEND, AND AN ATTRIBUTION PROBLEM WORTH FIXING

| leg | campaign | $ |
|---|---|---:|
| the funnel run (killed at 22:32:07) | `TIKTOK_FINDER` | 0.2400 |
| profile recovery + the two-meter probe | **`BL1268_TIKTOK`** | 0.0402 |
| Instagram cross-link, both populations | `IG_AUX` | 0.0078 |
| cover fetches, MX, judging, aggregator walks, X, YouTube, linktr.ee guesses | — | **0.0000** |
| **TOTAL** | | **$0.2880** of the $2.00 cap (**14.4%**) |

**Neither paid leg carries this round's name.** The TikTok funnel books under
`tiktok_finder.CAMPAIGN`, a **module constant**, so this round's spend and BL-1260's are
indistinguishable by campaign — only by timestamp window. The Instagram leg booked under
`IG_AUX`/`aux`, its client's own default. **A round cannot attribute its own spend by
campaign label alone**, which is the same class of defect as the `$0.0000` status card
BL-1260 fixed by spelling. Worth a `campaign=` argument on both paths.

---

## 6. THE RUN — and the kill test nobody planned

**The run was killed at 22:32:07, from outside this session, before `discover()` returned.**
That is the exact failure BL-1260 built the checkpoint for, reproduced by accident on the
**new** code — which makes it a better test than any I would have written.

| | BL-1260, old code | **BL-1268, new code** |
|---|---:|---:|
| same failure | killed inside `discover()` | killed inside `discover()` |
| ledger rows written | **0** | **16** |
| calls banked | **0** | **400** |
| dollars banked | **$0.0000** | **$0.2400** |
| `run.json` written | no | no |
| sheets surviving on disk | 35 | **122** |

**The money is booked. The findings are not.** The checkpoint does what it claims; the
`run.json` export still sits *after* `discover()` returns and was never moved, so a kill
still loses every verdict, bio and bioLink the run collected. **That export deserves the
same treatment the ledger write just got** — it is the remaining half of the same bug.

### Survivors, as far as the run got

| stage | survivors |
|---|---:|
| hashtags walked | 5 at depth 8 |
| **billed requests booked** | **400** ($0.2400, 16 checkpoints) |
| contact sheets built | **122** |
| **pages judged** (free) | **100** |
| **WANT** | **63** |
| WANT pages with an address found | **4** |
| WANT pages **kept and flagged** with no address | **59** |

### The receipt gap, and why I will not attribute it

Two free reads bracket the window: **445,924 → 444,963 = 961 requests consumed**, against
**400 booked**. My first explanation was retries, because `api_client.py:306` exposes
`http_requests` — *"ACTUAL HTTP requests made (incl. retries) — the true billed count.
Callers meter THIS, not one-per-logical-call"* — and `tiktok_finder` meters one per logical
call.

**That explanation is wrong, and measuring it is what killed it.** Metering the same work
both ways: 63 profiles → **63 logical, 63 receipt delta, retry factor 1.0**. And every call
site is exactly 1:1 with the vendor:

| call site | logical | receipt delta |
|---|---:|---:|
| `videos_from_hashtag` | 2 | **2** |
| `videos_from_handle` | 1 | **1** |
| `profile_of` | 1 | **1** |

So the meter is **not** under-counting per call. The 561 are requests issued outside this
round's metered path — and **the LamaTok key is account-wide with three other rounds holding
open claims, one of them on `tiktok_finder` itself.** I cannot attribute them to this round
or to another, and I am not going to pretend otherwise.

**That rediscovers the rule the hard way: a quota delta cannot attribute a round.** BL-1260
wrote exactly that in its own budget file and BL-1264 recorded it; I reproduced it by trying
to use a delta as a referee under concurrency. **The campaign-tagged ledger row is the only
attributable record**, which is precisely why moving the write mattered more than any delta
ever could.

**`api_client.http_requests` still has zero readers anywhere in `clippershq/`.** It did not
bite here because nothing retried, but it exists for a 429/5xx storm and nothing is wired to
it — the same helper-with-no-caller shape as `parallel_judge`, `run_batch` and
`outcome_loop`.

**And I caught the same bug on myself.** The profile-recovery script calls `profile_of`
directly rather than through `run_funnel`, so the checkpoint never saw those 67 calls. They
are now on the ledger under **`BL1268_TIKTOK`** — this round's own campaign label, which also
fixes the attribution complaint in §5 for its own spend.

**Total: $0.2880 of the $2.00 cap, 14.4% used.** Judging, MX, cover fetches, aggregator
walks, X, YouTube and the linktr.ee guesses were all **$0.00**.

---

## 7. THE SHEET

**`C:\Users\Game Centar\OneDrive\Desktop\clipper finder\output\bl1268_tiktok_e2e\MARK_THESE_TIKTOK.html`**

A **subdirectory** of `output/`, never `output/` itself — the send guard scans top-level
`output/*.csv`, so a deliverable belongs one level down.

**63 rows — every WANT, not only the ones with an address.** His explicit instruction is to
keep good pages with no email, flagged, and `drknsstd` is the precedent: a WANT whose address
has since vanished and was correctly kept.

| | |
|---|---:|
| rows | **63** |
| with an address | 4 |
| **KEPT and flagged with no address** | **59** |
| with a linked Instagram | 6 |

**Five columns, checked by the shipped checker on the real header list.**
`exporters.check_operator_columns` is called **before any HTML is written** and raises on a
sixth column, a reorder, or a missing one — all three refusals verified. The three extra
columns sit **outside** the five and are not passed to it, exactly as BL-1257 rendered its
mark control:

- **INSTAGRAM** — the second handle he asked for. Blank on 57 of 63, and blank is an answer:
  5.3% is the honest cross-link rate.
- **FOUND VIA** — **which link the address came from**, in plain words ("bio text", "their
  own website", "aggregator page behind the link"). This is what makes the per-route yields
  auditable one row at a time.
- **NO EMAIL** — `KEPT - no address found`.

**Verified by re-reading the saved file**, because a sheet that was written correctly and a
sheet believed to have been written correctly are the same object until something opens it:
**14 of 14 checks pass** — 63 body rows, 63 clickable TikTok profile links (one per row), 6
clickable Instagram links, the first five headers matching the contract in order, the three
extras after them, and **no truncating CSS anywhere** in the document.

---

## 8. Suites

`.venv\Scripts\python.exe tests/run_all.py --jobs 4` — **314 of 317 green**, 3 rounds in
flight at 22:54.

**The 3 red are pre-existing and none is this round's.** `git status --porcelain clippershq/
tests/ docs/ config.json dashboard/ tools/` shows nothing from BL-1268 — this round wrote **no
product code at all**, only `scratch/bl1268_*`, `reports/BL-1268.md` and
`output/bl1268_tiktok_e2e/`.

| suite | why it is red | mine? |
|---|---|---|
| `memebot/meme/tests/test_render.py` | memebot is its own git repo and is dirty from another round | no |
| `memebot/meme/tests/test_ocr.py` | same | no |
| `tests/test_repost_export.py` | `test_both_files_exist_and_have_one_row_per_account`: **327 != 340** | no |

**The repost red, run standalone and diagnosed rather than waved through.** The test compares
a **saved export** against a **live recount**: `output/repost_accounts.csv` was written
**2026-08-12 20:31** — two days before this round — and holds 327 rows, while
`repost_export.build_rows()` now recomputes **340** accounts from the ledger. It is a stale
artefact against a moving source, not a regression, and this round touched neither. Re-running
the export would clear it; that belongs to whoever holds the repost path.

**A note on the count itself:** 317 discovered here against BL-1257's 314 — the suite has
grown by three. The runner says it plainly in its own footer: *"a suite count is a moment,
not a property."*

---

## 9. What I would fix next, in order

1. **Wire the send file to the funnel.** One call at the end of `run_funnel`, or one button.
   Eight addresses are stranded today and the number grows with every run.
2. **Move the `run.json` export the way the ledger write was moved.** The checkpoint saved
   the money from this round's kill; the findings still died with the process.
3. **Add `soundcloud.com` (and the aggregator hosts) to `bio_parser.PLATFORM_HOP_DOMAINS`**,
   and sweep the 2 platform inboxes already sitting in master.
4. **Give the funnel a `campaign=` argument.** Both paid legs currently book under a module
   constant or a client default, so no round can attribute its own spend by label.
5. **Wire `api_client.http_requests`** to the checkpoint. It did not bite this time, but it
   is the counter built for a retry storm and nothing reads it.
6. **Name the asset/template-supplier reject** in the brief. Four of 37 rejects were pages
   selling raw material to other editors, and only a judge reading what the text *said* —
   rather than counting that text existed — caught them.
