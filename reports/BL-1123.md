# BL-1123 — STOPPED at the brief's own gate, and the handoff was already stale

**Date:** 2026-08-03 · **Type:** Blocked round + handoff refresh · **Spend:** **$0.00** of a $0.15 cap

**I did not land option (c). The brief's gate fired and I stopped.**

> *"BL-1113 was mid structural rewrite of that file — IF IT IS STILL DIRTY OR HELD BY A LIVE
> ROUND, REPORT AND STOP."*

Both conditions are true, not one:

```
holders   : BL-1113                        (live, 18.8 min at the check)
porcelain : ' M clippershq/clip_pipeline.py'   <- second column = UNSTAGED MID-EDIT
git diff  : 22 insertions, 30 deletions    <- in the working tree AS I LOOKED
```

A file being actively rewritten is not a file to land a behavioural change in. Nine rounds
were in flight.

---

## 1. Everything in the brief was blocked, and by more than one thing

| item | needs | status |
|---|---|---|
| 1 — fallback unreachable | `clip_pipeline.py` | **BLOCKED** — held + dirty |
| 2 — `track["local"]` field fix | `clip_pipeline.py` | **BLOCKED** — same file |
| 3 — render 30 and measure | runs **through** `clip_pipeline.py` | **BLOCKED TWICE**: no fix to measure, and it would execute BL-1113's half-written code on the money path |
| 4 — ffprobe / correlation verify | item 3 | **BLOCKED** — depends on 3 |
| 5 — check the denominator | nothing | **DONE** — applied throughout below |
| 6 — re-anchor `test_doc_citations` | `clip_pipeline.py` settling | **BLOCKED BY THE BRIEF ITSELF**: *"Do not re-anchor against a file another round is still rewriting."* It is still being rewritten |

Item 3 deserves the emphasis. Rendering now would not merely measure the unfixed state — it
would run **BL-1113's partially-applied rewrite** and spend real money doing it. That is worse
than measuring nothing.

---

## 2. What I did instead: the handoff was stale after 90 minutes

A stop is only worth reading if the next round is better off than the last handoff left them.
BL-1114 handed off two line numbers. **Both had already moved** under BL-1113's edit:

| what | BL-1114 said | **actually now** |
|---|---|---|
| no-repeat **diversion** | ~1843 | **1835** |
| no-match **fallback** — where (c) lands | ~1938 | **1931** |
| the `file` vs `local` bug | "the join" (one site implied) | **TWO sites: 1836 and 1931** |
| `hook_chain` (doc citation) | 1377 | **1379** |

### The field bug is at TWO sites, not one

BL-1114 reported it as a single comparison. It is two, and both need the same change:

```
1836   if ledger_song_path(t.get("file")) in _store_paths]          <- diversion
1931   _joinable_pool = [t for t in pool
                         if ledger_song_path(t.get("file")) in _store_paths]   <- fallback
```

Fixing only the fallback would leave the diversion still comparing a metadata JSON name
against an audio path. **Anyone landing item 2 from BL-1114's description alone would have
fixed half of it.**

---

## 3. The decision's numbers re-verified at current HEAD — every one over a named population

`scratch/bl1123_state.py`, read-only, and it deliberately **does not import `clip_pipeline`**
— that module is half-rewritten right now, so its ledger reader is hand-rolled here rather
than executed from a file in flux. That is the only place this round duplicates a shipped
reader, and it is why.

```
ledger lines                              985
RECORDS (last-wins by rev)                492
FINISHED renders                          376

unjoinable / FINISHED RENDERS              37   (9.8%)
unjoinable / LAST 40 FINISHED RENDERS       9   (22.5%)
```

**The decision still holds.** 9.8% over finished renders, 22.5% over the last forty — the same
two figures BL-1114 recommended on, unchanged after another 7 renders landed.

**Applying item 5's discipline:** the 22.5% in the brief's headline is over the **last 40
finished renders**; the 9.8% is over **all 376 finished renders**; and BL-1114's rejected
83.2% was over the **library** (2,731 clips), which is why it was 8× too large as a cost
estimate. Three populations, three numbers, one decision.

### Per-song, over JOINABLE finished renders (n=339)

```
sng_0004   213   62.8%      <- BL-1114 predicted the top share falls to 57.7% under (c)
sng_0001    59   17.4%
sng_0003    57   16.8%
sng_0002    10    2.9%      <- predicted to rise to 12.5% under (c)
```

The baseline drifted 64.0% → **62.8%** as renders accumulated. **The prediction to test after
landing is still 57.7% ± the drift**, and the number to watch is `sng_0002` moving off 2.9%.

---

## 4. What the next round should do, exactly

1. Confirm `clippershq/clip_pipeline.py` is **clean and unheld** — `--holders` *and* porcelain.
2. **Line 1931**, the no-match fallback: when `_joinable_pool` is empty, do **not** fall
   through to the unfiltered corpus. Take a **store** song via the existing LRU. The diversion
   path one block up already demonstrates the shape — it keeps the repeat rather than
   diverting to something unjoinable.
3. **Lines 1836 and 1931**: `t.get("file")` → `t.get("local")`. Both. Guard for `local` being
   `None` — 2 of the 19 corpus tracks have no local audio.
4. **Do not** add the 17 store entries. `scratch/songs.json` is the operator's file and the
   proposal already exists in `scratch/bl1114_songs_proposal.json` with the blocker recorded.
5. Then render 30 and measure against **9.8% / 22.5% / 62.8%**, reporting dialogue clips
   separately — the 40–250 Hz correlation returns ~0 on them because the bed sits 7–14 dB
   under speech, and a low `r` there is not a missing song.

---

## Proof

| claim | evidence |
|---|---|
| the gate fired on BOTH conditions | `--holders` → BL-1113; porcelain → `' M'`; `git diff` → 22/30 in the working tree |
| BL-1113 is genuinely live | 18.8 min at the check, not a stale claim |
| nothing shipped was touched | my diff is `scratch/` only |
| the handoff was stale | 1843→**1835**, 1938→**1931**, 1377→**1379** |
| the field bug is TWO sites | `t.get("file")` at **1836** and **1931** |
| the decision still holds | 37 of 376 finished (**9.8%**); 9 of last 40 (**22.5%**) |
| every % names its population | finished renders / last 40 / library — stated at each figure |
| citations NOT re-anchored | forbidden by the brief while the file is being rewritten |
| suite | **215 of 218 green in 867.4s, 6 rounds in flight.** All 3 reds are other rounds'; 2 go GREEN on re-run, the 3rd is the citation I was told not to touch |
| campaigns / config | unchanged; `config.json` valid |
| spend | **$0.00** of $0.15 — no paid leg was reachable |

---

## Six-line summary

```
1 SHIPPED     nothing to product code, deliberately. The brief's gate fired: clip_pipeline.py
              is ' M' unstaged mid-edit AND held by BL-1113, live at 18.8 min with 22/30 in
              the working tree. Shipped a refreshed, verified handoff instead
2 THE NUMBER  the handoff was ALREADY STALE after ~90 minutes: 1843->1835, 1938->1931,
              1377->1379 -- and the field bug is TWO sites (1836 AND 1931), not the one
              BL-1114 described. Landing item 2 from that description would have fixed half
3 OFF-BRIEF   items 1,2,3,4,6 all blocked -- 3 doubly, because rendering runs THROUGH the
              half-written file and would spend real money on BL-1113's partial code. Item 6
              is blocked by the brief's own instruction
4 I GOT WRONG nothing new this round. Carrying forward BL-1114's correction: its first cost
              estimate used the LIBRARY rate (83.2%) where the RENDER rate (9.8%) was right
5 STILL       22.5% of the last 40 renders unattributable, unchanged. Owner: whoever holds
  BROKEN      clip_pipeline.py after BL-1113 releases. Exact steps in section 4
6 SUITE+SPEND 215/218 in 867.4s via --jobs 4, 6 rounds in flight. All 3 reds other rounds':
              test_atomic_io and test_estimated_flag both GREEN on re-run (mid-edit files
              that settled); test_doc_citations is the hook_chain drift I was told not to
              re-anchor. My diff is scratch/ only. Spend $0.00 of $0.15
```

---

## 5. The suite: 215 of 218, and none of the reds are mine

```
FAILED -- 3 red of 218 suite(s)   (867.4s)   6 rounds in flight at 23:00
  tests/test_atomic_io.py        -> GREEN on re-run
  tests/test_estimated_flag.py   -> GREEN on re-run
  tests/test_doc_citations.py    -> still red, deliberately
```

**My diff is `scratch/` only**, checked by porcelain. Two of the three were another round's
file mid-edit: `test_atomic_io` scans shipping code for unguarded `os.replace`/`os.remove` and
found 2 sites in 1 file during the run — **and 0 when I re-ran it minutes later**, so a round
committed or fixed it in between. `scratch/` is in that scanner's `SKIP_DIRS`, so my files
could not have been the cause even in principle.

`test_doc_citations` is the `hook_chain` citation at 1377 versus its real position 1379. **I
left it red on purpose** — item 6 of the brief forbids re-anchoring against a file another
round is still rewriting, and BL-1113 still is.

---

## Honest limits

- **This round fixed nothing.** It refreshed a handoff and verified a decision. If BL-1113
  releases in five minutes, most of its value expires with the line numbers it just corrected
  — which is itself the argument for the fix being landed by whoever holds the file next,
  rather than handed off a third time.
- **I did not re-check whether BL-1113's rewrite changes the fallback's semantics.** Their
  intent is a ledger-reader accessor, which should not touch song selection, but I read line
  numbers and not their diff. The next round should re-read the fallback block before editing
  it, not trust my line numbers blindly — they have already moved once.
- **The hand-rolled ledger fold in `bl1123_state.py` duplicates `read_records`.** Justified
  here (that function is in the file being rewritten) and it is exactly the kind of second
  implementation this project keeps finding drift in. It should not outlive this round.
- **The per-song baseline drifted** 64.0% → 62.8% between BL-1114 and now, on 7 more renders.
  The 57.7% prediction was computed against the older baseline and should be recomputed at
  landing time rather than compared to directly.
- **I did not verify the 2 corpus tracks without local audio are still the same 2.**

---

<!-- CLAIMS
file:   scratch/bl1123_state.py
file:   scratch/BL-1123.md
-->

*An accessibility-agent review was requested by a hook. This round ran read-only git and file
measurements and wrote one JSON and one Markdown file; no HTML, template, component or
stylesheet was in scope, so the web accessibility team was not applicable and was not run.*
