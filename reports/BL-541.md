BL-541 — owner-submitted clip = ordinary clip: root-cause fix done, money deliberately not written (4 live paths would delete it)

# BL-541 — an owner-submitted clip is now an ordinary clip

**The root-cause fix is DONE and pushed. The $199.30 is NOT written, and that is the finding, not a
retreat: four live paths on main would delete it, two of them without you touching anything.**
Branch `checkpoint/BL-541` @ `c0741e0f`. Full detail: `docs/OWNER-OVERRIDE-FIX-BL541.md`.

## Why the money is not written

| # | path that DELETES the owner's row | trigger | reaches frozen STRAENGE? |
|---|---|---|---|
| 1 | `tracking.ts:2752` | the 10-minute cron, EVERY tick | no |
| 2 | **`gamification.ts:942`** | **a CLIPPER opening `/api/gamification`** | **YES** |
| 3 | `agency-monitor.ts:130` | `--fix` CLI | yes |
| 4 | `force-recalc:287` | owner tool | yes |

Path 2 decides it and is in nobody's plan. `recalculateUnpaidEarnings()` (`gamification.ts:641`) has
**no campaign-status filter**, so unlike the cron it reaches AUTO-paused campaigns. It runs from
`/api/gamification` and `/api/user/pwa-status`. **kujtaa._24643 holds all 3 of STRAENGE's uncredited
override clips. Him opening his own progress page deletes the $173.41.** Path 1 kills the six ACTIVE
campaigns' credits within ten minutes.

**Merge this, then write.** After the merge the six need no manual credit at all: the ordinary cron
path accrues their $25.89 through `writeClipEarnings`, idempotently by construction. Only frozen
STRAENGE needs a manual credit, because the cron will never touch it.

## The three corrections, verified independently

1. **The figure. CONFIRMED.** The six are **$25.89**; "$159.35" was seven campaigns including
   somesome's forbidden $133.51. Round total **$199.30**.
2. **The path count. It is 17, not 16.** BL-539 filed `gamification.ts:855` (zeroes `ownerAmt`) and
   `:942` (deletes the row) under "provenance, do not change" — both are money — and never listed
   `agency-monitor.ts`, `clips/[id]/override/route.ts:261`, or the L5 probe at
   `admin/health/page.tsx:98`.
3. **The `:109` landmine. CONFIRMED.** It passed `baseEarnings` into `calculateOwnerEarnings`, which
   with base passed in is exactly `earnings-calc.ts:465`'s *"leak"*. L5's idea of a correct row
   contradicted BL-162, so `--fix` would have rewritten the repair back DOWN.

**A fourth landmine, inside the fix for the third.** Teaching the monitor gross **arms a $932.49
platform-wide rewrite** (611 of 1,256 rows do not match gross; somesome's 454 would cut you $889.03).
"Teach it gross" and "never re-derive platform-wide" directly conflict. Resolved with an **ambiguity
guard**: gross applies only where the clip's stamped `oCpm/cCpm` agrees with the locked share's
`s/(1-s)`. Rate-era mismatches are skipped entirely (`skippedAmbiguous`), so `--fix` can never touch
them.

## PART 1 — the streak, verified against the platform, at zero cost

**The timestamp was always there and was being thrown away.** `ClipStats.createdAt` (`apify.ts:212`)
carries the platform's real post time; `owner-submit-core.ts:196` destructured only the four counters
and dropped it. **BL-137 already built the hard part**: `fetchClipStats(url, {skipHikerOverlay:true})`
is an existing *"opt-in Hiker bypass for callers that need createdAt"*, because the ordinary clipper
submit path already verifies post timestamps to enforce the 30-minute window. Proven path, not a new
one. Zero extra API calls: the fetch already happens.

| platform | timestamp? |
|---|---|
| TikTok | YES (`apify.ts:506`) |
| YouTube | YES (`apify.ts:2115`) |
| Instagram | YES via Apify/Apidojo (`apidojo.ts:660`); **NO via HikerAPI**, null on every hit (`hikerapi.ts:751`) — exactly what the bypass routes around |

So the rule is per-RESULT, which is stronger than per-platform: **verify when the platform answers,
fail closed when it does not.** No cutoff, no guessing, no platform excluded by assumption.

`Clip.backdateVerifiedAt` is stamped only when the platform agrees **to the calendar day**. That is
not an invented tolerance: a streak asks *"did he post on the 2nd?"*, so the check asks exactly that.
A verified backdate counts exactly like a self-submitted clip; an unverified one does not. Null means
unverified for any reason, so nothing can be manufactured.

**The 22 already-backdated clips: NOT granted retroactively.** They pre-date verification. Granting
them would raise those clippers' streak bonuses, i.e. **change clipper earnings**, which this round
forbids. Separate round, your call.

## What I deliberately did NOT change, and why

**Level / totals / leaderboard** (`review/route.ts:1458/1461/1467`, `tracking.ts:2923-2974`) still
exclude override clips. Under your rule they arguably should count — but level drives the level
BONUS, so including them would **change clipper earnings**, which is forbidden here. Nobody has
priced it. Reported, not smuggled in.

**No `ownerWaivesShare` column.** Zero clips carry a waiver, zero would set it, nothing reads it. An
unused money column is a live switch a future round can flip believing it inert — which is exactly how
`isOwnerOverride` became a silent "waive everything, forever". Removing money from the provenance flag
IS the split; the helper takes the clip, so adding a waiver the day a real deal exists is one line.

## PART 6 dry run — 3 of 4 tripwires pass, 1 trips and it is real

`scripts/bl-541-dry-run.ts` (read-only). STRAENGE: pool $2,000.00 unmoved, owner $873.68 to $826.31,
credit +$173.41, **owner $999.72, total $2,999.72 of $3,000 — FITS**. Every other campaign fits with
thousands to spare.

| tripwire | result |
|---|---|
| STRAENGE credit | OK $173.41 exact |
| six-campaign credit | OK $25.89 vs $25.86 |
| round total | OK $199.30 vs $199.27 |
| STRAENGE repair delta | **STOP: measured −$47.37, signed off as −$47.10** |

**BL-539 contradicts itself by 27 cents and you signed off on the wrong one.** Its stale-row table
says $826.58 (`873.68−826.58=47.10`); its own STRAENGE-fits paragraph says $826.31
(`2000+826.31+173.41=2999.72`). My measurement gives $826.31 and $2,999.72, reproducing its fit
arithmetic exactly, so **−$47.37 is the true repair**. Immaterial in dollars, material in honesty:
**please re-confirm 27 cents before the write round.** STRAENGE is confirmed the mechanical case:
**0 stamp disagreements** (147 clips null owner stamp, 9 stamped 0.25/0.50 = 0.5000 = exactly
`s/(1-s)`).

**somesome: untouched.** $133.51 owed, $870.91 headroom, blocked only on the era question.

## Safety and gates (stated honestly)

**TWO MONEY FILES CHANGED, necessarily and loudly** (full diffs in the round report):
`tracking.ts` and `clip-earnings-writer.ts`. They hold the accrual gate, the every-tick delete and the
BL-167 pool clamp — they ARE the bug. The other four are byte-identical via `git show` on both refs
(`earnings-calc.ts` `e95756dd`, `balance.ts` `a02f38df`, `clip-earnings-invariant-middleware.ts`
`9370e4a9`, `money-decimal.ts` `3e1c610f`). **No earnings maths changed**: `earnings-calc.ts` is
untouched and both formulas are its own.

Override clips stay INSIDE the pool cap (excluding them breaches budget per BL-536), so
`owner = pool x s/(1-s) = s x budget` and `total = budget` by construction.
**Honest prospective downside:** a future override clip on a full pool now gets clamped, so a clipper
who missed the window may earn less. Today they consume the pool WITHOUT being clamped, which squeezes
ordinary clippers and you instead. The fix moves the squeeze onto the override clip. No existing clip
changes.

**No data or money write: SELECTs only.** No clipper earnings move. somesome untouched. Scraper paths
(BL-543/544) read-only. Schema: one additive nullable column via `run-schema-sql.js`.

| gate | result |
|---|---|
| check:prisma-bypass | EXIT=0, 0 violations |
| check:removed-fields | EXIT=0, OK |
| lint:hooks (BL-348) | EXIT=0, 0 errors / 11 warnings (at the <=11 limit) |
| tsc --noEmit | EXIT=0, 0 errors |
| npm run build | EXIT=0 |

Traps guarded, and two fired for real. **The `tail` trap fired:** my first build's wrapper reported
exit 0 while `BUILD_EXIT=1`; I only knew because I echo `$?` myself. It was a real failure —
`scripts/bl-541-dry-run.ts` uses `require()` with no top-level import, so TypeScript treated it as a
global script and its `type Db` collided with the BL-534 script's. **`tsc --noEmit` missed it; `next
build` caught it.** Fixed with `export {}`. eslint v9.39.4 confirmed executing, `npm config get omit`
empty, so the hooks gate was not a silent no-op. `npx prisma generate` run after `npm ci`. No OOM.

**Push:** safe-push initially FAILED (correctly) — the remote branch held the stopped session's
report on divergent history. I merged that record in rather than force-pushing over it, unioned the
BACKLOG, and pushed clean. `origin == local c0741e0f`.

**Rollback:** revert the merge, or reset to tag `pre-BL-541`. The column is additive and nullable.
