# BL-1169 — stopped at the precondition, and everything that could be done without those four files

**2026-08-06 21:24.** The brief was to build a download button for every dataset and make a
finished run announce itself. Its own precondition said:

> `git status --porcelain` on `dashboard/server.py`, `app.js`, `index.html`, `app.css` —
> BL-1159 has held these for hours; **IF ANY IS DIRTY OR HELD BY A LIVE ROUND, REPORT AND STOP.**

**All four are held by a live round. This round wrote no code.**

Every number below came from `.venv\Scripts\python.exe` — **Python 3.11.9**. The running
dashboard uses the same interpreter (`/api/now` → `python_version: 3.11.9`, full path under
`.venv`). System `python` (3.13, no fastapi/uvicorn) was not used. No lead address appears
here; none was needed, because every figure below is a count.

**Spend: $0.00.** The $0.20 cap was declared before starting. No paid leg was invoked —
everything here is a local file read.

---

## The stop, with its evidence

`tools/claims_read.py` at 21:23, then `claim.py brief` at 21:24:

```
BL-1159          438 min  dashboard/static/app.css, dashboard/static/index.html,
                          dashboard/static/app.js, dashboard/server.py  +11 more
```

Not flagged `** POSSIBLY STALE` — `brief` marks a claim stale only when its own files are
older than the claim, and `dashboard/server.py` was modified at **18:57:15**, two and a half
hours ago. By the tool's own liveness rule BL-1159 is **active**, seven hours and eighteen
minutes in.

The four files are **clean in git** (`git status --porcelain` returns nothing for them), so
this is not a mid-edit collision. It is the second half of the condition: *held by a live
round*. Every deliverable in the brief lands in those four files —

- §1 download buttons → `index.html` + `app.js` + `app.css` + new routes in `server.py`
- §2 run-finished announcements → `index.html` + `app.js` + `server.py`
- §3 all three bugs → `server.py:407-422`, `server.py:630`, and the send-list render in `app.js`

— so there is no subset I can safely build. The brief says "the files this round holds"; this
round holds none of them. Handing over is the whole job.

Also in flight: **BL-1167** (55 min) across `clippershq/run.py`, `control.py`, `run_resume.py`,
`ig_client.py`, `spotify_finder.py` — the run machinery §2 would have to hook into.

---

## The three bugs are all still present at HEAD — confirmed, with exact patches

### 1. The send list drops its own facts

`dashboard/static/app.js:1558-1566` renders the primary from three fields and no others:

```javascript
  if (f.primary) {
    h += '<div class="sendprimary">' +
         '<p class="sp-name">' + esc(f.primary.name) + '</p>' +
         ... f.primary.label ...
         '<a class="btn primary" href="' + esc(f.primary.download_url) + '">Download send list</a>' +
```

`f.primary.rows`, `f.primary.built_at` and `f.primary.stale` are never read. The server
already computes all three (`/api/files.primary`). **Confirmed unchanged since BL-1168.**

**And the gap is far worse than the brief states.** The brief says "ZERO of today's 43
leads". Measured now:

```
master_leads.csv rows added TODAY        : 1,225
   of those, carrying an email           :   654
   already in ALL_BOT_READY.csv          :     5
   MISSING from the send list            :   649
```

BL-1166/BL-1167's spotify runs added ~1,181 leads this evening. **649 contactable people
added today are absent from the file the page calls authoritative**, and the page shows no
row count, no build date, no stale flag.

A second direction nobody has looked at: **15 emails in the send list are not in master at
all.** The file is stale in both directions.

### 2. `server.py:406-423` — the marker row omits the two identity keys

Read at HEAD just now. The dict copies exactly fifteen keys:

```
funnel, status, progress, pages, target, leads, spend, elapsed, ts, epoch,
pid, calls, appended_incrementally, halted, out_path
```

`pid_created_at` and `boot_epoch` are not among them, though the marker on disk carries both.
BL-1168 measured the cost with `proc_identity.describe()`: without them `verdict=None`
("cannot be confirmed either way"); with them `verdict=False` ("that number has since been
reused by another python, which is why it still looks alive"). **The exact change, two lines:**

```python
             "out_path": rec.get("out_path"),
+            "pid_created_at": rec.get("pid_created_at"),
+            "boot_epoch": rec.get("boot_epoch"),
         }
```

### 3. `server.py:630-636` — the freshness allowlist

Read at HEAD just now, unchanged:

```python
_LIVENESS_SOURCES = (
    ROOT/dashboard/server.py,  ROOT/dashboard/static/app.js,
    ROOT/clippershq/proc_identity.py, ROOT/clippershq/run_status.py,
    ROOT/clippershq/spend_ledger.py,
)
```

Five files, while the launcher's watcher covers **every `*.py`** under `dashboard/` and
`clippershq/`. `index.html` and `app.css` are in neither list. The project's own guard is
still red — re-run just now:

```
tests/test_server_freshness.py
Ran 18 tests in 19.269s
FAILED (failures=1)
   test_a_server_older_than_its_code_says_so
   AssertionError: False is not true :
       a server older than every file it imports must report stale
```

**The exact change:** replace the tuple with the same walk the watcher uses — every `*.py`
under `dashboard/` and `clippershq/`, plus `dashboard/static/*.{js,css,html}` — excluding the
files the server itself writes (`dashboard/.running.json`, `.watching.json`,
`static/runs.json`), which is the boot-loop guard `_py_mtimes()` already implements in
`tools/dashboard_launcher.py:469-488`. Reusing that function is the smaller change and
removes the second definition of "the code".

---

## What §1 actually needs: the dataset census

This is the specification for the download page, measured rather than guessed. **Records, not
rows, in every row of this table** — the eleventh such check here.

| Dataset | Physical lines | Records | Ratio | Built |
|---|---:|---:|---:|---|
| `master_leads.csv` — all leads | 134,702 | **60,226** | 2.24x | 2026-08-06 20:06 |
| `output/ALL_BOT_READY.csv` — send list | 2,971 | **2,970** | 1.00x | **2026-08-02 21:45** |
| `exports_repost_accounts/*.csv` | 289 | **288** | 1.00x | 2026-08-06 12:14 |
| `clip_library/` (55 shards) | 9,741 | **2,761** | 3.53x | 2026-08-05 19:07 |
| `memebot/runs.jsonl` — renders | 1,429 | **801** | 1.78x | 2026-08-06 00:16 |
| `spend.json` — spend history | 1,530 | **1,530** | 1.00x | 2026-08-06 21:23 |
| `ground_truth/drop_offsets.jsonl` — drop marks | 52 | **21** | 2.48x | 2026-08-06 16:02 |

`master_leads.csv` is the one that bites: **134,702 physical lines, 60,241 CSV logical rows,
60,226 distinct people.** The 74,461-line gap is newlines inside quoted `bio` fields. Any
export that counts with `sum(1 for _ in fh)` overstates by **2.24x** — the same defect that
once reported 133,474 for ~59,016 real records.

**A blocker for "leads by run", found while counting.** The brief asks for leads downloadable
*by run*. That is currently near-impossible:

```
distinct run_id values in master : 16
rows carrying NO run_id          : 58,491  of 60,241   (97.1%)
```

A by-run download would reach **1,750 rows out of 60,241**. `/api/history` already says this
plainly — *"1522 of 1522 ledger rows have none, so they cannot be attributed to a run"* — and
**BL-1167 is fixing exactly this right now** (`run_id isolation`, `tests/test_run_id_isolation.py`).
The by-run download should be built *after* that lands, or it will ship as an empty feature.

**Feasibility notes for whoever builds it:** `openpyxl 3.1.5` is installed, so `.xlsx` on
demand is available in-process. `/download/{name}` exists at `server.py:1366` but the code
comments at 2535 note it is allowlisted by listing **a single flat directory**, so these new
datasets — which live in seven different places, one of them a 55-shard directory — need a
named-dataset route rather than an extension of that one.

---

## Three exports hand-verified against source

### 1. `output/ALL_BOT_READY.csv`

```
physical lines                 2,971
minus 1 header                 2,970
csv logical rows               2,970   delta 0 — no embedded newlines
distinct emails                2,970   ratio 1.00x — one row is one person
```

The file's own counting is honest. Its **contents** are four days old (above).

### 2. `exports_repost_accounts/` — double entry, closes exactly

```
sources     258 + 140 + 48 + 30 + 6            = 482 contributions
agreement   138x1 + 111x2 + 34x3 + 5x4         = 482      MATCH
union       138 + 111 + 34 + 5                 = 288      = declared union_accounts
csv         289 physical lines - 1 header      = 288      MATCH
```

Every identity closes. This provenance file remains the reference implementation of the
discipline — it publishes `clip_rows 9741 / clip_records 2761` and `render_lines 1429 /
render_records 801` alongside its own totals.

### 3. `clip_library/` — 55 shards

```
physical jsonl rows            9,741
unparseable                        0
DISTINCT clip identities       2,761
ratio                           3.53x   (9,741 / 2,761)
most-repeated identity             10 appearances
sum of all repeat counts       9,741   -> equals the row total exactly
```

The fold is complete and lossless: every one of the 9,741 rows is accounted for in the 2,761
records.

---

## What §2 would need, scoped from what already exists

Not built — but the four end-states the brief asks for are **already distinguishable in data
today**, which means the work is presentation, not plumbing:

| State | Where it already lives |
|---|---|
| **FINISHED** | marker `status: "completed"` + `/api/now` liveness `verdict:false, "pid has exited"` |
| **HIT ITS CAP** | `spend_ceiling_usd` / `spend_unflushed_usd` / `cap_usd` on the marker; `halted` field |
| **FAILED** | marker `status: "failed"` — seen on the `tiktok`/`google_play` rows in `test_headless.py` output |
| **KILLED** | marker `status: "died"` + the derived note *"it says 'running' but pid N has exited"* |

The gap is that the card collapses these into "not running — stale marker". Note also that the
per-pid *"why this says not running"* evidence is already rendered but sits inside a
`<details>` that BL-1168 measured as **closed by default**, along with all 10 others on the
page — so the announcement needs to be outside a fold to be seen at all.

Browser notification without an external request is available (`Notification` API, permission
prompted on a user gesture, zero network) and would satisfy the tab-switching case.

---

## Suites and config

**Not run.** This round changed no code, so there is nothing of mine to prove green, and a
331-second `--jobs 4` pass against a tree where **BL-1167 is mid-edit in five `clippershq`
files** would report their in-flight state as my result. BL-1168 measured the current
baseline 25 minutes ago: **4 red of 253 in 331.0s with 4 rounds in flight** —
`test_server_freshness.py` (bug 3 above), `test_headless.py` (BL-1167's live surface), and two
in the nested `memebot/` repo. The one guard this brief would turn green,
`test_server_freshness.py`, I re-ran individually and it is **still red**.

`config.json` parses, 162 keys. **Campaigns unchanged** — trivially, since this round performed
zero writes to anything but `reports/BL-1169.md`.

---

## What he should do next

The work is small, fully specified above, and blocked on one thing: **BL-1159 has held the
four dashboard files for seven hours and eighteen minutes.** Its last commit under its own id
was 16:31, five hours ago; the 18:57 change to `server.py` landed under BL-1165's commit
`04852a8` while BL-1159 held the file.

Either that claim gets released, or a round takes it with `claim.py start --force` and a
recorded reason — noting that `--force` **replaces** `will_write` rather than merging it, so
the forcing round must declare all four files plus whatever else it touches.

Once the files are free, the ordering that avoids shipping an empty feature:

1. **Bugs 1-3 first.** They are two lines, one tuple, and three fields in a template literal.
   Bug 3 turns a red guard green.
2. **Then the download page**, using the census above — every dataset except "by run".
3. **"Leads by run" last**, after BL-1167's `run_id` work lands. Today it would reach 1,750
   rows of 60,241.

The single number he should see first: **649 people with an email were added to master today,
and the file the page tells him to send from contains none of them.**
