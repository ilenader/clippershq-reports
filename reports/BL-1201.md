# BL-1201 — the audio is read: 1,238 clips measured, 107 pages are all-talking, and the churn is 38.1% not 15.8%

**The dialogue gate has fired for the first time.** It fired on **107 of 425 pages** where it
fired on **0 of 425** before, because `speech_frac` now exists: **1,238 of 1,255 sampled clips
measured**, 16 unmeasured, in **11.6 minutes**. And I have to correct a number I gave you last
round: the churn is **38.1%**, not 15.8%. The 15.8% was right for what it measured and wrong
for what you are about to see — §3 decomposes it.

Every figure came from **`.venv\Scripts\python.exe` (3.11.9)**. Bare `python` here is 3.13.14
and lacks the packages; it was not used.

**Spend: $0.00 against the $3.00 cap — zero vendor calls, and that is measured, not assumed.**
All 4,825 cached posts still carry a live `video_url` (CDN links expire 2026-08-10/11; probed
before starting, 4 of 4 returned bytes). The CDN is not a billed endpoint and Silero VAD is
local, so there was no receipt to bill from because nothing was billed. **Your marks file is
byte-identical to when I started** — 42,162 bytes, sha256 `96f5ff54…`, never opened for write.
**Your server was not restarted and you do not need to reload.**

---

## 1. THE RUN

425 pages, newest 3 posts each, Silero VAD on every sampled clip.

| | |
|---|---:|
| clips sampled | 1,255 |
| **clips measured** | **1,238 (98.7%)** |
| clips unmeasured | **16 (1.3%)** |
| wall clock | **695 s (11.6 min)**, 4 workers |
| vendor calls | **0** |

All 16 unmeasured clips failed for one reason — ffmpeg reporting *"Output file does not contain
any stream"*, i.e. **the post has no audio track at all**. They are recorded as `speech_frac:
None` with that reason, counted in their own column, and **never as 0%**. xHE-AAC did not appear
once, which matches BL-1195's 134 decodes at zero unmeasurable.

`speech_frac` across the 1,238 measured clips: min 0.000, p25 0.000, **median 0.162**, p75 0.594,
max 1.000. **47.2% sit at or above the 0.20 talking floor** — close to your own estimate that
roughly half of a typical page is talking.

## 2. YOUR RULE, AND THE THREE COUNTS

"If ALL of them talk, scrap the page. If even ONE is clean, keep it."

| page state | pages |
|---|---:|
| `has-clean` — at least one clean clip, **kept** | **316** |
| `all-talking` — every measured clip talks, **scrapped** | **107** |
| `unmeasured` — nothing decodable, **abstained** | 2 |

Per page, never collapsed, in `scratch/bl1201_rejudge.jsonl`: **talking 514, clean 576,
unmeasured 16** clips in total. The rescue holds — a single clean clip among unmeasured ones
still keeps the page, which is pinned by the 8 tests from BL-1198 and unchanged here.

**One limit you should know about.** This is a two-state read: above the speech floor, or below
it. Your third state — *dialogue with a song under it* — is **not** separated here, so "clean"
in these counts means "below the speech floor", which lumps genuinely clean clips together with
quiet talking. The energy read that would split them (`speech_dbfs − bed_dbfs`) was measured by
BL-1195 against all 149 labels and **failed**: 1.9 dB on pages you want against 2.7 dB on pages
you do not, best precision 29.2% [14.9, 49.2] against a 30.9% base rate. I did not rebuild it.

Also on record and unchanged: moviezar and the page you called best are **both 6 of 10 talking**.
Speech share alone cannot separate them, and nothing in this run claims otherwise — what this
run fixes is that the share was not being measured *at all*.

## 3. THE CHURN — I OWE YOU A CORRECTION

| comparison | pages moved | churn |
|---|---:|---:|
| 10 posts → 3 posts, **no audio** | 67 | **15.8%** ← what I told you |
| 3 posts no audio → 3 posts **with audio** | 115 | **27.1%** ← the audio |
| **10 posts no audio → 3 posts with audio** | **162** | **38.1%** ← what you will actually see |

My 15.8% was a projection of the *posts-per-page* change alone, measured on cached pages with no
audio — and it reproduces exactly, 67 pages. It was not wrong; it was **incomplete**, because it
could not include a signal that had never been taken. The honest headline is 38.1%.

Direction matters and is never summed:

- **145 pages moved pass → reject** (mostly the new dialogue rule)
- **17 pages moved reject → pass**
- Pass count: **256 → 128.** Reading the audio roughly halves what the filter passes.

That drop is the point of the change, not a side effect — but it is large, and you should see
it before it reaches a send list.

## 4. AGREEMENT — REFUSED, AND WHY

Your marks: **163**. Joined to this 425-page sheet: **14**.

**I am not quoting an agreement percentage on 14 pages.** The floor was set at 40 before the
number was computed, and 14 is noise wearing a decimal point. Your marks are almost all on the
earlier 100-page sheet; they do not join this one.

To make this answerable: mark ~40 pages on **this** sheet and the whole comparison re-runs
locally in seconds, free.

## 5. WHAT WENT WRONG, AND WHAT IS BLOCKED

**The first attempt threw away 12.5 minutes of VAD.** It imported `clippershq/meme_finder.py`
live; **BL-1202 claimed that file after my precondition check read it FREE and edited it at
20:54:53, mid-run**. Every worker spawned after that imported a half-written module and died
with `NameError: min_caption_chars` — 411 of 425 pages lost. Two changes fixed it and both are
worth keeping:

1. **The module is pinned to git HEAD** (`scratch/bl1201_mf_head.py`), so a long run cannot be
   broken by somebody typing into a shared file. Seven rounds share one working tree.
2. **Measure and judge are separate phases.** VAD results are fsynced to disk *before* any
   judging happens, so a defect in the judge can never again discard the expensive work. That
   is what the first attempt got wrong, and it is the same shape as the round that lost ~240
   pages and $0.5754 to an in-memory runner.

**Task 3 is blocked and I did not force it.** `clippershq/review_sheet.py` is **' M' (dirty,
mid-edit)** and the claims registry names a holder, so per the preconditions I stopped rather
than writing to it. The data the sheet needs is produced and on disk — per-page `talking`,
`clean`, `unmeasured` and `dialogue_state` in `scratch/bl1201_rejudge.jsonl`. Wiring it in is
a small change in whoever owns that file next; the three counts must stay three counts.

**Not done:** the three-state read (§2), and re-judging with OCR as well as audio — this run
re-used the cached drawn text rather than re-OCRing, so the 11.6 min is the audio leg only, not
the ~84 min end-to-end figure.

## 6. THE RECORD

- Preconditions: `tools/claims_read.py --holders` at start — `meme_finder.py`, `clip_speech.py`,
  `bl1196_pages.jsonl` **FREE**; `review_sheet.py` **held and dirty**, untouched.
  `ground_truth/review_marks.jsonl` **' M' (you are marking)** — read-only, verified
  byte-identical before and after. Claimed as BL-1201, every filename declared, 6 other rounds
  in flight at claim time. `meme_finder.py` was later claimed by BL-1202 mid-run (§5).
- **Suites: 278 PASS / 3 FAIL under `--jobs 4`, 330.9 s, 5 rounds in flight** (281 discovered).
  **All three failures are foreign, and the proof is that this round modified ZERO tracked
  source files** — every output is a new untracked scratch file plus this report.
  `memebot/meme/tests/test_render.py` and `memebot/meme/tests/test_ocr.py` are in the nested
  `memebot/` repo and were failing before this round. `tests/test_review_sheet.py` is newly
  red because **BL-1202 added `fired.append(short_captions)` as RULE 10 and `review_sheet.py`
  has no plain-language text for it** — the cross-check test catching an in-flight round
  mid-change, which is what it is for.
- Campaigns unchanged (`clippershq/run.py` shows no diff, 11 campaigns); `config.json` and
  `config.example.json` both parse.
- No lead data in this report: counts, states and distributions only — no handles, no addresses.
