# BL-546 — merge BL-543 (the ~$45/month saving) + four audit docs to main

**`origin/main` = `3af689a8`** · verified push (`origin == local`) · tags `pre-merge-BL-546` (b9147a92) / `post-merge-BL-546` (3af689a8).

**The saving is now live.** BL-543 was finished and pushed but sitting off main, so two Apify Instagram actors with a proven 0.0% win rate were still firing and still billing on every poll. As of this merge they are not.

---

## STEP 0 — the truth per branch (evidence, not assumption)

| Branch | On origin | Already on main? | Evidence |
|---|---|---|---|
| `checkpoint/BL-543` | **YES** @ `f7a13165` (matches your stated SHA) | **NO — genuinely unmerged** | `merge-base --is-ancestor` false; `rev-list --count origin/main..` = **1** |
| `checkpoint/BL-539` | **YES** @ `44ed5411` | **NO — genuinely unmerged** | same, **1** commit |
| `checkpoint/BL-540` | **YES** @ `9d9fb874` | **NO — genuinely unmerged** | same, **1** commit |
| `checkpoint/BL-544` | **YES** @ `b4d6ee62` | **NO — genuinely unmerged** | same, **1** commit |
| `checkpoint/BL-545` | **YES** @ `1a1e53c2` | **NO — genuinely unmerged** | same, **1** commit |

All five had `merge-base(main, branch) == b9147a92 == main`, i.e. each was cut from current main. Merged `--no-ff`, **one at a time, BL-543 first**, verified between each.

### Deliberately left alone

* **`checkpoint/BL-541`** exists @ `e11d98ba` with 1 unmerged commit, and its worktree `C:/bl541` is **live**. Not merged. Verified after the push that it is still **not** an ancestor of main.
* **`checkpoint/BL-538`** is **not on origin at all** — its worktree `C:/bl538` exists but nothing was ever pushed. Nothing to merge, nothing touched.

### Where I worked, and why it was safe

The primary tree was already on `main` and clean, so I merged there rather than in a worktree. Before running `npm ci` I checked the two live worktrees: **both `C:/bl538` and `C:/bl541` have their OWN real `node_modules`** (561 entries each, `junction=False`), so a reinstall in the primary tree could not break either live round. That check mattered: a junction would have made `npm ci` destructive to them.

---

## Conflicts — BACKLOG only, resolved as exact unions

Merge 1 (BL-543) was **conflict-free**. Merges 2 to 5 conflicted **only** in `BACKLOG.md`, each a single append-versus-append block.

**Capture-group corruption avoided.** No regex, no `String.replace`, no replacement string anywhere. Each resolution deleted exactly the three conflict-marker lines **by line number** (`sed -i '19194d;19180d;19124d'`), which cannot interpret `$2` in "$2,000" as a capture reference.

**Entry count is the exact union — 57 headings, checked at every step:**
```
origin/main   52
  + BL-543     ->  53 (conflict-free)
  + BL-539     ->  54   verified 54
  + BL-540     ->  55   verified 55
  + BL-544     ->  56   verified 56
  + BL-545     ->  57   verified 57
```
Post-merge checks: literal `$2,000` still present **11 times**, intact and not exploded. The only duplicated BL number is **BL-536 x2**, which is the known legitimate collision from BL-537 (two genuinely different items sharing a number), not corruption. Every other number appears exactly once. No conflict markers anywhere in `src/`, `scripts/`, `docs/`, or BACKLOG.

---

## STEP 6 — confirmed on the merged result

**`apify/instagram-reel-scraper` is gone from the call chain.** I checked this properly rather than trusting a grep: my first pattern flagged a hit, but it was matching the **explanatory comment** at `apify.ts:711`, not a call. Verified definitively:

* the only non-comment references left are its **price entry** in `apify-ledger.ts` (correct: it prices the actor if it ever ran) and the default inside `getInstagramActor()` (`apify.ts:126`);
* `getInstagramActor()` is now reached **only** from `fetchInstagramStatsBatchLegacy`, which appears solely at its own definition (`apify.ts:1491`) and in two comments — **never called**. Dead code, so reel-scraper cannot fire.
* Note: `APIFY_INSTAGRAM_ACTOR` **is** set in `.env.local` and feeds `getInstagramActor()`, but since that function only feeds dead code, the override is now inert.

**`apify/instagram-scraper` is skipped for reels, KEPT for `/p/`, and the guard fails open:**
```js
function legacyActorsCanResolve(postUrl: string): boolean {
  try {
    const path = new URL(postUrl).pathname.toLowerCase();
    if (path.startsWith("/reel/") || path.startsWith("/reels/")) return false;
    return true;
  } catch {
    return true;   // fails OPEN
  }
}
```
Only the two shapes proven dead are skipped. `/p/` posts keep the exact pre-BL-543 path, because BL-543's 0/30 sample was reel-dominated and it did not prove the leg dead there. Anything unparseable keeps the old behaviour.

**`logActorRun` is wired and does not double-count:** exported once from `apify-ledger.ts`, **8 call sites** in `apify.ts` (every exit path: HTTP error, empty, item error, silent-zero, success), and the admin route splits `actor:` rows **out** of the legacy rollups (`startsWith("actor:")`, 2 sites) into a separate authoritative `actorTruth` block.

### BL-543's own test, re-run on merged main: **12/12 pass**

```
PASS  apify/instagram-reel-scraper: 0 usable results out of 20 BILLED runs   not_found=20  win rate 0.0%
PASS  apify/instagram-scraper:      0 usable results out of 20 BILLED runs   not_found=20  win rate 0.0%

OK    stored=21384  new=21384  provider=apify-instagram-api  delta=0
OK    stored=2309   new=2309   provider=apify-instagram-api  delta=0
OK    stored=2450   new=2450   provider=apify-instagram-api  delta=0
OK    stored=12014  new=12014  provider=apify-instagram-api  delta=0
PASS  every sampled reel returned a real, non-regressing view count (4/4)
PASS  no sampled reel returned 0 (a silent zero would freeze earnings)

[INSTAGRAM TRACKING] BL-543 legacy tier SKIPPED — reel URL, legacy actors return not_found 100% of the time
[APIFY] Instagram result: NULL
PASS  dead reel → stats null (falls through; caller keeps last-known)   provider=none stats=null
PASS  dead reel did NOT return views=0
```

These are **fresh live reels** (different clips and view counts from BL-543's own run), and every one still comes back **delta=0** through tier 1. **A dead reel returns NULL, not 0**, so `tracking.ts` keeps last-known and no clip's earnings freeze.

---

## Safety

**6 money files byte-identical**, verified via `git show` on **both** refs (never worktree-vs-git-show — that CRLF artifact has faked alarms five times):
```
7a03d33b7c41 clip-earnings-writer   e95756dd9997 earnings-calc   a02f38dff8c4 balance
29471a799877 tracking               9370e4a9793f invariant-middleware   3e1c610f15d3 money-decimal
```
`tracking.ts` is **not in the diff** — which is exactly the constraint BL-543 was built around (5 of the 10 `logApifyUsage` sites live there, so it could not fix the winner-label contract at the caller and recorded truth at the actor invocation instead). `campaign-era.ts` not in the diff. **Poll cadence unchanged.** No clip's earnings changed by merging: the only DB write BL-543 adds is an `apifyUsageEntry.create` (a ledger row).

Merge diff: 10 files, +1730/-60 — 5 docs, 1 test script, 3 code files (`apify.ts`, `apify-ledger.ts`, `admin/apify-ledger/route.ts`), BACKLOG.

## Gates (honest, every trap guarded)

* `npm ci` exit **0** (`npm config get omit` empty, `NODE_ENV` unset, so dev deps installed).
* `npx prisma generate` exit **0**, run **before** tsc.
* `tsc --noEmit` exit **0**, **0 errors**.
* `npm run build` exit **0** — redirected to a log with `$?` echoed directly, **never piped through `tail`**. **61/61** pages. **No OOM** (0 hits for heap/FATAL/Killed; 6.2 GB free at build time).
* Hooks gate exit **0** → **0 errors / 11 warnings** (cap 11). eslint **genuinely present** (v9.39.4) and genuinely emitting findings, not a silent no-op.
* No dashes.

The proof script ran ~5 real Apify calls (~$0.02), disclosed rather than hidden.

## Rollback

`git reset --hard pre-merge-BL-546 && node scripts/safe-push.mjs main --force` (reverts all five merges). Nothing to undo in the DB.
