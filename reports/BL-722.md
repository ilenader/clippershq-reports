# BL-722 — STOPPED: tree is not quiet. No process is running, but another agent is writing control.py and main.py right now.

**Date:** 2026-07-29 · **Type:** Blocked — ownership · **Spend:** $0.00
**Files changed: NONE.** No backup written, no config touched, no wiring attempted.

---

## The distinction that matters here

The brief was explicit: *"only process exit is a valid quiet signal here; log silence is a
symptom, not proof (BL-715)."*

**The process condition is satisfied.** A full enumeration of `python`/`node` processes found
only two stale `http.server` instances from 2026-07-28 serving a static directory
(`--directory ryan_price`, 13 CPU-seconds each since yesterday). **No `main.py` is running.
No funnel process exists.**

**And that turned out not to be sufficient.** Other agents in this repo are not long-running
processes — they are tool-call driven, exactly like this one. They write files without ever
appearing in a process table. Process exit is a **necessary** quiet signal, not a sufficient
one, and this round is the counter-example: zero funnel processes, and **78 files written by
other agents in the preceding seven minutes.**

---

## Evidence

The ten files this work must touch were snapshotted, then re-checked after 75 seconds — none
of those ten moved in that window. But their mtimes already told the story, and a
repo-wide sweep confirmed it:

| file | last written | note |
|---|---|---|
| **`clippershq/control.py`** | **20:11:13** | needed for item 3 (menu). **Written 11 min after my own last edit at 20:00:08 — not mine** |
| **`clippershq/main.py`** | **20:09:50** | |
| `clippershq/secrets_guard.py` | 20:09:34 | **new module**, did not exist before |
| `tests/test_secrets_guard.py` | 20:14:29 | |
| `config.json` | 19:56:32 | |

Writes were still landing while the check ran:

```
20:14:38  scratch/bl722_run_gemini.py
20:16:03  logs/twitch_stall_traceback.log
20:16:08  scratch/bl722_results.jsonl     <- 44 seconds before the final check
```

**Another agent is mid-round on the Gemini benchmark** (`scratch/bl722_*`, ~40 audio and
video files plus `bl722_corpus.json`, `bl722_diagnose.py`, `bl722_probe_one.py`,
`bl722_results.jsonl`). That is the follow-up to BL-717, and it is running now.

This round requires editing `repost_finder.py`, `ig_client.py`, **`control.py`**, `main.py`
and `config.json`, and backing up `spend.json` and `master_leads.csv`. `control.py` is
demonstrably contended. Editing it would either clobber that agent's work or be clobbered by
it, and neither of us would get an error.

---

## What was verified, at zero cost

The engine BL-710 built is present and intact. Both suites were run — they are offline and
touch no shared state:

```
tests/test_clip_library.py   Ran 27 tests   OK
tests/test_clip_walk.py      Ran 31 tests   OK
                             --------------
                             58 checks green
```

That matches BL-710's claim exactly. `clip_walk.iter_stream_medias` exists and is a correct
drop-in.

**Item 1 is confirmed still unfixed**: `clippershq/repost_finder.py` contains no reference to
`iter_stream_medias`, so `iter_medias` still returns 0 on the `stream_rows` envelope. Nothing
downstream can work until that lands — which is why it is item 1, and why this round could
not be partially completed by doing the "safe" items first. Items 2–7 all depend on it.

---

## Nothing was done, and nothing was half-done

| # | item | status |
|---|---|---|
| 1 | `iter_medias` → delegate to `iter_stream_medias` | not started — `repost_finder.py` |
| 2 | `/gql/user/clips?sort_by_views=true` adapter | not started — `ig_client.py` |
| 3 | Menu option | not started — **`control.py` contended** |
| 4 | Config knobs | not started — `config.json` |
| 5 | Crossdedup before paid call | not started |
| 6 | Spend metering + proceed gate | not started |
| 7 | Seen-clip cache | not started |

**No backup was created.** Backing up `config.json`, `spend.json` and `master_leads.csv` is
itself a write, and a backup taken while another agent is mid-edit captures a torn state and
gives false assurance. `config.backups/` already showed six same-second `spend.json` backups
from a prior round; adding more noise there helps nobody.

The paging-token open question (envelope `next_page_id` vs
`stream_rows[*].data.paging_info.max_id`) **remains unanswered** — it can only be settled by a
live paid call, which needs item 2 wired first.

---

## To resume — a sharper check than "is a process running"

Process absence is not enough. All three must hold:

1. **No `python`/`node` process** running a funnel entry point *(this already holds)*.
2. **Zero writes repo-wide for a continuous 10 minutes**, excluding `.git`, `__pycache__` and
   `logs/`. This is the check that actually catches tool-call-driven agents. A sweep over
   `os.path.getmtime` across the tree, twice, ten minutes apart.
3. **`git status` clean under `tests/`** — otherwise `run_all.py` cannot certify anything,
   and the brief requires suites green.

Item 1 must land first regardless of who does it; 2–7 are dead code until `iter_medias`
returns rows.

---

## Method

Processes were enumerated via `Win32_Process` filtered on name, with CPU time and start time,
rather than inferred from logs — per the brief's BL-715 note. File ownership was established
by a 10-file mtime+size snapshot diff over 75 seconds, plus two repo-wide modification sweeps
(7-minute and 3-minute windows). The two engine suites were run because they are offline and
write nothing outside their own temp fixtures. No credential was read or printed. No file in
the repository was modified by this round.
