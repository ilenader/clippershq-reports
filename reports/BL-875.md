# BL-875 — **BLOCKED, as instructed.** `clip_vision.py` is claimed by BL-937 and dirty, so I did not land the split. The gap is no longer theoretical: a **new** tooling retirement accrued 34 minutes ago while the two-way rule was live

**Date:** 2026-08-02 · **Type:** Blocked + read-only measurement · **Spend:** **$0.00 · 0 paid calls**
Campaigns **unchanged under both conventions**. `config.json` valid, 161 keys. Claim `.claims/BL-943.json`.

Continues [BL-868](BL-868.md). The spec is [`scratch/bl939_tooling_spec.md`](.), still unlanded.

---

## The block, with the evidence that produced it

Item 5: *"If it is claimed and dirty, report blocked and stop."* Both conditions hold.

```
$ python tools/claims_read.py --holders clippershq/clip_vision.py
BL-937

$ git status --porcelain clippershq/clip_vision.py
 M clippershq/clip_vision.py
```

Re-confirmed at the end of the round: **still BL-937, still ` M`.**

The brief warned that a clean porcelain under a fresh claim means nothing. **This round is the opposite case, and it is worth naming**, because it looks like an opening and is not:

| | |
|---|---|
| claim age | **39 min** |
| claim `pid` | **None** |
| live BL-937 process | **none** |
| file content hash across 5 polls over 3 min | **identical every time** (`5e28a72c3216`) |
| last write to the file | **32 min ago** |

So the file is **dirty and static** — BL-937 stopped writing half an hour ago and never committed. That is not active work, and the temptation is to read it as abandoned and proceed.

**I did not, for a reason beyond the instruction.** The working tree holds **169 uncommitted lines** of BL-937's work (`clip_vision.py` +103, `test_clip_vision.py` +69). Any commit I made after editing that file would sweep those lines in as mine — I would be publishing another round's unreviewed work under my own claim, and the audit trail would say BL-943 wrote it. **There is no way to land my 20 lines without also landing their 169.** Stopping is not just the rule here; it is the only option that keeps authorship honest.

BL-868 made the same call from the opposite evidence (clean file, claim 30 seconds old, went dirty in 55). Both readings end in a stand-off, which is the repeatability item 5 asked for.

---

## What is blocked, precisely

| item | status |
|---|---|
| 1 — land the tooling-dependent category | **BLOCKED** — needs `record_failure()` in `clip_vision.py` |
| 2 — keep BL-937's transport behaviour | **already correct**, verified in BL-868, unchanged |
| 3 — stamp `PARSER_GEN` at failure | **BLOCKED** — same function |
| 4 — re-check the retirements under the finer rule | **DONE**, below — read-only, no file conflict |
| 5 — do not race | **honoured** |

---

## Item 4: the finding that makes this urgent

Re-checking every clip at the retirement ceiling under the three-way rule:

| class | clips | correct? |
|---|---:|---|
| **PERMANENT (media)** — 12 × no DASH manifest, 1 × oversize | **13** | yes — retire and mean it |
| **DELETED** — `clip not found in N pages` | 3 | yes |
| **TOOLING-DEPENDENT** | **1** | **NO — wrongly retired** |

**Answering item 4's actual question: none of the 13 "permanent" are tooling-dependent.** Every one names a media fault — `no video_dash_manifest on this media` or `video larger than 18MB`. Not one mentions a parser, a token ceiling or a truncation. The finer split does not reclassify any of them, and BL-868's permanent count stands.

**But the ceiling grew from 16 to 17, and the new arrival is the category that does not exist yet:**

| | |
|---|---|
| clip_id | `3950256644352314564_36488443501` |
| account | `hollywood_jan07` |
| reason | `unparseable` |
| `vision_last_error_kind` | **`content`** — filed as a verdict on the clip |
| attempts | **2 of 2 — retired** |
| last attempt | **34 minutes ago** |
| play_count | **67,366** |

**BL-868 reported zero tooling-dependent retirements outstanding. This one accrued after that report, under the two-way rule, while the fix sat specced and unlanded.** It is not a historical artefact to be swept up once; **the rule is manufacturing false retirements in real time**, at roughly one per half-hour of labelling activity.

And it is not a low-value clip — 67,366 plays puts it well inside the range the ranked selector pays for first.

BL-867 measured the retry rate on exactly this class: **4 re-opened, 3 labelled immediately, 0 still unparseable.** On that base rate this clip is about 75% likely to be recoverable, and under the current rule it will never be tried again.

---

## Why the third category is not a nicety

The two-way rule asks *"was this the network, or the clip?"* — and for an unparseable reply the honest answer is **neither**. It was **our parser**, reading a response we had already paid for. Filing that under `content` records a fact about the video that is not true.

Two tooling limits have already moved underneath these retirements:

- **BL-889** replaced every runner's private regex with the shared `clip_vision.parse_vision_json` (8/11 → 11/11 on a live probe)
- **BL-904** raised `max_tokens` 2000 → 5000 after measuring p95 at 3,795 on an uncensored sample

BL-867 found a clip carrying **67 beats** among the retirements — it had truncated at 2000 *and* failed to parse, so **two separate tooling limits produced one tombstone**, and neither was a fact about the video. Both limits have since changed. Nothing re-opened the clips they had already buried, because the ceiling has no memory of which generation failed them.

That is precisely what `PARSER_GEN` is for, and it is the part that stays blocked.

---

## Verification

| claim | result |
|---|---|
| `claims_read.py --holders` + porcelain + poll | **yes** — BL-937 holds it; ` M`; hash static across 5 polls |
| three-way split live | **NO — blocked**, not landed |
| `PARSER_GEN` stamped | **NO — blocked** |
| transport untouched, unrecognised still counting | **unchanged** — BL-937's behaviour verified in BL-868 |
| the retirements re-checked under the finer rule | **yes — 17: 13 permanent, 3 deleted, 1 tooling** |
| any of the 13 actually tooling-dependent | **no — 0 of 13** |
| campaigns unchanged | **yes** — `8e02f8d6f6307ae8` / `7a029ee5447cddd8`, both verified |
| `config.json` valid | **yes**, 161 keys |
| files written by this round | **`scratch/` only** |

---

## What should happen next

1. **Someone must resolve BL-937's 169 uncommitted lines.** They have been static for over half an hour with no owning process. They are one `git checkout` from gone — the exposure this repo has already paid for more than once. Committing them is a prerequisite for anyone landing the third category, because until they are committed the file cannot be edited by another round without commingling authorship.
2. **Then land `scratch/bl939_tooling_spec.md`** — a third `vision_last_error_kind`, `PARSER_GEN` stamped at failure, one branch in `should_skip()`. It is 20 lines of change and 4 tests.
3. **Re-open the one clip above**, and any that accrue in the meantime.

---

## Limits

- **This round changed nothing in the product.** Its output is one measurement and a refusal. That is the correct outcome under item 5, but it is the second consecutive round in which the headline deliverable did not land, and the cause both times was contention on a single file.
- **I did not verify the new retirement is recoverable.** Confirming it would cost one paid call and this round was specified as no-paid-calls, so the 75% figure is BL-867's base rate on n=4 applied to it, not a measurement of this clip.
- **"One per half-hour" is one observation, not a rate.** It is the interval between BL-868's zero and this round's one, on a library that is 99% labelled and therefore doing little labelling — the accrual rate during an active pass could be higher or lower.
- **The 3 deleted clips were not re-checked against the live feed**, same as BL-868.
- **BL-937's transport behaviour is taken from BL-868's verification**, not re-tested here, because re-testing it means importing the module in its uncommitted state and I did not want this round's numbers to depend on work that may still change.
