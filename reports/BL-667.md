# BL-667 — Merge BL-666 (the reviewer note layer) to main

**2026-07-25** · `origin/main` `5c667b25` → merged `91bf3759`, **verified pushed** · Tags `pre-merge-BL-667` / `post-merge-BL-667`

**Headline:** BL-666 was genuinely unmerged with a real code diff, and is now merged into main. The `main` worktree was still held dirty and stale by a concurrent round, so the merge was done in a **separate clean worktree** and pushed to `origin/main` without touching the dirty one. Every merged-result safety property was re-checked on the merged tree, not inherited.

---

## STEP 0 — truth, with SHAs

| Ref | SHA |
|---|---|
| `origin/main` before | `5c667b257a76340b00e3d92e2b5474cea68d0d9a` |
| `origin/checkpoint/BL-666` | `d24e68909fa7a8f1397d0e7682992f6cee8453e8` |
| merged `main` after | `91bf3759d0bed2b92fb577c868c66cf95b8dea49` |

```
$ git log --oneline origin/main..origin/checkpoint/BL-666
d24e6890 BL-666: BACKLOG entry (reviewer note layer)
134200ed BL-666: the reviewer note layer ...

$ git merge-base --is-ancestor origin/checkpoint/BL-666 origin/main
NOT MERGED (work to do)
```

**BL-666 was genuinely unmerged.** Its diff is **non-empty real code**, not a document:

```
 BACKLOG.md                                        |  14 +
 prisma/schema.prisma                              |   9 +
 scripts/bl666-live-proof.ts                       |  34 +
 scripts/test-bl-666-reviewer-note.ts              | 156 +
 src/app/(app)/admin/clips/page.tsx                |  10 +
 src/app/api/admin/reviewer-note/[clipId]/route.ts |  76 +
 src/app/api/clips/route.ts                        |  27 +-
 src/components/admin/ReviewerNoteCard.tsx         | 191 +
 src/lib/reviewer-note.ts                          | 373 +
 9 files changed, 889 insertions(+), 1 deletion(-)
```

**Nothing a live round holds was merged.** `checkpoint/BL-664` and `checkpoint/BL-665` were both checked individually and are **NOT on main**; only BL-666 was merged.

---

## How the dirty main worktree was handled (without touching it)

The `main` branch is checked out in a separate worktree, `C:/b575`, which was still **dirty (76 uncommitted tracked changes) and stale (HEAD `91b84410`, behind `origin/main` `5c667b25`)** from a concurrent round. Touching it, or checking out `main` elsewhere, was not an option.

So the merge was performed in a **fresh detached worktree at a short path** (`C:/wt/bl667`), created at `origin/main` (`5c667b25`), with its own `node_modules` (never junctioned) and copied `.env` / `.env.local`. The `--no-ff` merge commit was created there and pushed straight to the remote with `git push origin HEAD:main`, then verified `origin/main == local HEAD`. **`C:/b575` was never read from, written to, or checked out.** Its stale local `main` ref is now behind the remote, which is harmless: `origin` is the source of truth, and that worktree's owning round can reconcile on its own.

---

## The merge

Clean `--no-ff`, **zero conflicts** (BL-666 branched from the current `origin/main` and nothing else moved it in between). Verified on the merged tree:

* **Conflict markers:** `grep -rnE "^(<<<<<<<|>>>>>>>|=======$)"` across `src/`, `scripts/`, `prisma/`, `BACKLOG.md` → **0 hits.**
* **BACKLOG:** **97** `## BL-` entries, the `## BL-666` entry present **exactly once**. The union added only BL-666's 14-line entry. The two bare `$2` occurrences are the same pre-existing prose from earlier rounds (`"owner cap $2/account/10d"`, `"leaked $2"`); `git diff pre-merge-BL-667 HEAD -- BACKLOG.md | grep '^\+.*\$2[^0-9,.]'` returns **0**, so no capture-group corruption was introduced.
* **Schema:** BL-666's four nullable columns (`noteText`, `noteJson`, `noteSuggestion`, `noteConfidence`) were already applied to prod in BL-666 via `ADD COLUMN IF NOT EXISTS`. **Nothing was re-run.** `npx prisma generate` refreshed the client to match `schema.prisma`; **no `prisma migrate`.**

---

## Confirmed on the MERGED result (re-checked, not inherited)

| Requirement | Result on `91bf3759` |
|---|---|
| A note renders in the admin review queue | `ReviewerNoteCard` is rendered on PENDING clips in `/admin/clips`, fed by the note endpoint; build compiled the route + component clean |
| **No clipper-facing route can reach it (re-grepped)** | `grep` for `reviewer-note` / `ReviewerNoteCard` / `composeReviewerNote` outside owner/reviewer surfaces → **NONE**; `ReviewerNoteCard` imported by a non-admin page → **0**; endpoint gate is `requireOwnerOrCapability("CLIP_VIEW")` |
| `campaign-rules.ts` byte-identical | **OK** (blob OID identical, pre-merge vs merged) |
| Auto-reject still OFF, flag gates nothing | `auto-reject-flag.ts` reads `RULES_AUTO_REJECT_LIVE === "true"`; the flag is **set true nowhere** in `src/`, `.env`, `.env.local` |
| No clip status / earnings / payout path changed | the merge touches the shadow block + note layer + an owner UI; 6 money files below byte-identical; the submit block adds only note columns to the existing shadow insert |
| `cannot_evaluate` never collapsed into a reject | proven by `test-bl-666-reviewer-note.ts`, **43/43 pass** on the merged tree |
| Every certain rule prints its failure-mode caveat | same test suite, 43/43 |

**6 money files + `tracking.ts` + `campaign-era.ts` + `campaign-rules.ts` BYTE-IDENTICAL by blob OID** (`git rev-parse pre-merge-BL-667:<f>` vs `HEAD:<f>`): writer, earnings-calc, balance, tracking, middleware, money-decimal, campaign-era, campaign-rules — all eight match.

---

## Gates, honestly

Fresh detached worktree, `node_modules` never junctioned. `npm ci` exit 0; `npx prisma generate` run **before** tsc (npm ci wipes the client); no migrate. `npx tsc --noEmit` **exit 0, 0 lines**. `npm run build` **exit 0**, 61/61 pages, read from a redirected log with a real echoed exit code, **never piped through tail**. Hooks gate: prisma-bypass exit 0, removed-fields exit 0, **lint:hooks 0 errors / 11 warnings** (at the cap; **eslint v9.39.4 confirmed executing**, not a silent no-op). `test-bl-666-reviewer-note.ts` **43/43** on the merged tree.

`origin/main == local HEAD == 91bf3759`, verified after fetch. BL-664 and BL-665 untouched. No caption or clipper content appears anywhere in this report. NO dashes as bullets.

**Rollback:** `git revert -m 1 91bf3759`, or reset to `pre-merge-BL-667`. The note columns are additive and nullable (harmless if left); auto-reject was never on.
