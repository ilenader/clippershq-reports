# BL-603 — Merge BL-599 + BL-602 to main (merge only)

Date: 2026-07-20. Result: origin/main advanced `3e35954a` to `8ea418b0` (verified,
origin==local). Two `--no-ff` merges, one at a time, verified between each. No code
authored. 6 money files byte-identical by blob OID on every ref. Auto-rejection
remains OFF/shadow; the bot signal remains rank-only. Build fully green.

## STEP 0 — truth per branch (with SHAs)

Base at start: `origin/main = 3e35954a`.

• checkpoint/BL-599 = `fff2f513c1c48f0c7c2cabb3c3294478f8f598f3` (matches the
  expected @ fff2f513). `merge-base --is-ancestor BL-599 origin/main` = 1 (NOT an
  ancestor), 1 commit ahead of main → GENUINELY UNMERGED.
• checkpoint/BL-602 = `ab1648295bbcd6c403c65a0ff801e94e0daab750`.
  `is-ancestor` = 1 (NOT an ancestor), 1 commit ahead → GENUINELY UNMERGED.

Nothing a live code round holds was merged. BL-600 and BL-601 are audit branches
and were NOT touched. Only the two named branches were merged. The primary main
working tree was DIRTY with another round's staged file
(docs/SCRAPBADGER-TIKHUB-AUDIT.md, untracked .claude/ + docs/BL-533-MERGE-REPORT.md),
so the merge was performed in an ISOLATED worktree (/c/bl603, short path, no
node_modules junction) branched from origin/main and then fast-forwarded onto
origin/main — nothing another session holds was swept in or altered.

## 6 money files — byte-identical by blob OID on BOTH refs (git show)

Compared `origin/main` vs `origin/checkpoint/BL-599` vs `origin/checkpoint/BL-602`,
and again on the MERGED HEAD `8ea418b0` vs `origin/main` baseline. All identical:

```
clip-earnings-writer.ts            7aa6be4834
earnings-calc.ts                   797e20985a
balance.ts                         f8499e18b7
tracking.ts                        b311323c31
clip-earnings-invariant-middleware 61cef39395
money-decimal.ts                   ef5cdae757
```

Neither branch's diff touches a money file or `tracking.ts`. No clip status or
earnings changed (both branches are additive: a read-only rank column, and a
shadow log path).

## Merges + BACKLOG union

1. BL-599 `--no-ff` merged CLEAN (no conflicts): peer-engagement.ts (+ generated
   bands), fraud-review route + page, 2 scripts, BACKLOG (+16 lines).
2. BL-602 `--no-ff`: only BACKLOG.md conflicted. Resolved as a UNION keeping BOTH
   entries by DELETING the three conflict-marker lines BY LINE NUMBER (sed
   '19456d;19444d;19428d', descending) — no JS String.replace, so the "$2,000"
   capture-group corruption class cannot occur. Verified: 0 conflict markers
   remain (Grep), both `## BL-599` and `## BL-602` headers present exactly once.
   Entry count is the EXACT union: base main 75 + BL-599 + BL-602 = 77 `## BL-###`
   entries (each branch tip carried 76 = base 75 + its own).

Only overlap between the two branches was BACKLOG.md; no code file conflicted.

## rulesJson schema — generate, never migrate

The additive `Campaign.rulesJson jsonb` column was already applied to prod in
BL-602 as `ADD COLUMN IF NOT EXISTS`. Confirmed present via information_schema
(`rulesJson | jsonb`); NOT re-run. `npx prisma generate` (only) was run after
`npm ci` and before tsc. No `prisma migrate`.

## Confirmations ON THE MERGED RESULT (main)

• BL-599 bot signal ranks READ-ONLY: `/api/admin/fraud-review/route.ts` contains
  only read `where` filters (status selectors for the display lists) and NO
  `clip.update` / `updateMany` / status write; the module header states "All lists
  are ordered by a read-only rank". It never rejects, freezes, changes status, or
  shows the clipper anything.
• BL-602 rules form renders per-rule TYPE dropdowns (`req-type-${idx}` select over
  `RULE_TYPES`) and still handles a no-rules campaign (`parseRulesJson(NULL)` → [],
  rows default HUMAN_ONLY).
• BL-602 free checks run in SHADOW, LOG ONLY: the submit-path block emits only
  `console.log('[RULES-SHADOW] …')` (+ a fail-open `console.warn`); there is NO
  status write, NO writeClipEarnings, and NO auto-reject-live flag wired to status
  (grep for `RULES_AUTO_REJECT_LIVE` / `would_reject.*status` → empty). AUTO-REJECT
  IS OFF.
• 11-campaign backfill preserved the clipper-facing text: the backfill wrote only
  `rulesJson`; `requirements` was never modified (the merge changes no data). Auto-
  rejection off means no clip status can change from it.

## Build honesty

`npm ci` clean (exit 0; wipes the generated Prisma client). `npx prisma generate`
(exit 0) run BEFORE tsc. `npm run build` exit 0, captured from a LOG file (not
piped through tail). prebuild gate ran: check:prisma-bypass OK, check:removed-fields
OK (676 files), lint:hooks with eslint PRESENT (real gate, not a silent no-op) →
`11 problems (0 errors, 11 warnings)` = passes (max-warnings 11). `✓ Compiled
successfully` (tsc 0 errors). Next build fully ran: `Generating static pages
(61/61)`. The worktree needed `NEXTAUTH_URL` set for build-time page-data
collection (env trap, not code); with it the whole build is green.

## Ship

origin/main = local HEAD `8ea418b0` (verified after push, origin==local). Rollback:
`git push origin 3e35954a:main` (both merges are the top two commits). Report:
reports/BL-603.md. No dashes used as bullets.
