# BL-566 — merge BL-529 (the cron watchdog fix) to main, finally

**`origin/main` = `ae148f3d`** · verified push (`origin == local`) · tags `pre-merge-BL-566` (9e13650a) / `post-merge-BL-566` (ae148f3d).

BL-529 was finished and pushed for days but missed by every prior merge round, which is why the owner still received 15 false CRON_DOWN emails a day. It is now on main.

---

## STEP 0 — the truth

**`checkpoint/BL-529` @ `c1f102fd` was genuinely unmerged.** `merge-base --is-ancestor origin/checkpoint/BL-529 origin/main` returned false; `rev-list --count origin/main..origin/checkpoint/BL-529` returned **3** commits. Its merge-base with main was `ff15c985`, so main had moved a long way past it (through BL-530 to BL-538): this was a real 3-way merge, not a fast-forward, exactly as the brief expected.

**Left alone:** `checkpoint/BL-563` @ `55b88301` and `checkpoint/BL-564` @ `5d8b22d9` are both live and off main; I confirmed after the push that neither is an ancestor of main. `checkpoint/BL-565` is not on origin. Only BL-529 was merged.

**Worked in an isolated worktree at `C:/b566`** because the primary tree is held by the live BL-559 round.

---

## The merge

`git merge --no-ff origin/checkpoint/BL-529`. **Only BACKLOG conflicted** — `notifications.ts`, `watchdog/route.ts`, `notif-href.ts`, and the two new files (`cron-watchdog-policy.ts`, `test-cron-watchdog-policy.ts`) all auto-merged cleanly. 6 files, +893/-161.

**notifications.ts auto-merged without loss:** the merged file carries BL-529's full tiering (`CRON_STATUS` type, its `NEVER_OWNER_EMAIL_TYPES` membership, `CRON_DOWN` in `OWNER_EMAIL_TYPES`) alongside everything main added since. No side was dropped.

**BACKLOG union, corruption trap avoided.** HEAD carried BL-527 through BL-538; BL-529's side carried only its own `## BL-529` block. I resolved it with **line-number deletes only — no regex, no `String.replace`** — so `$2` in "$2,000" cannot be read as a capture reference. Verified after: headings = **66** (main's 65 + BL-529's 1); `## BL-529` present once; `$2,000` intact 13 times; the only duplicate numbers are the known-legit BL-536 x2 and BL-541 x2 from prior rounds; zero conflict markers anywhere.

---

## STEP 7 — BL-529's own test on the merged result: 36/36 PASS

`scripts/test-cron-watchdog-policy.ts` ran clean on the merged tree — **ALL CHECKS PASSED (36 PASS, 0 FAIL)**. It was 30/30 when written; main has moved nothing that breaks it. The load-bearing cases:

* the 15-email shape is unreachable; a total-outage burst caps at 3 alerts and 1 email;
* an ongoing tracking outage emails once per 24h, not every 30 min;
* tracking is never starved by the cap (critical-first ordering claims the budget first);
* never-seen (null all-time) never alerts; only went-stale may;
* BL-526's root cause (all dedup windows expiring together) now hits the global cap instead of the inbox.

---

## STEP 8 — confirmed on the merged result

**The 5 dormant kinds cannot be reported down.** `resolveWatchedKinds` builds the watch set from the `RAILWAY_NATIVE_CRON` allowlist plus tracking only. marketplace-expire-deadlines, marketplace-counter-recompute, marketplace-decay-strikes, cleanup-magic-links and notifications-cleanup are not in the registry, so the watchdog never evaluates them and cannot emit CRON_DOWN for them. The daily 15-email storm is structurally gone.

**Tracking is watched for EVERY allowlist value — the critical property survives.** `cron-watchdog-policy.ts` sets `ALWAYS_WATCHED_KIND = "tracking"` with source `"always-watched-money-cron"`, added "always, regardless of the allowlist or anything in it," before the allowlist is parsed. So empty, unset, "*" and typo'd all still watch tracking. No env edit can unwatch the money cron. BL-529's test asserts this and passes.

**The email tiers are structural.** In `notifications.ts` the gate reads `if (!NEVER_OWNER_EMAIL_TYPES.has(type) && OWNER_EMAIL_TYPES.has(type) && ...)` — **NEVER_OWNER is checked first**. `CRON_STATUS` is in `NEVER_OWNER_EMAIL_TYPES`, so the quiet tier structurally cannot email; `CRON_DOWN` is in `OWNER_EMAIL_TYPES`, so a real tracking outage still emails loudly. `CRON_STATUS` needs no migration because `Notification.type` is a free-text String.

---

## Safety

**6 money files byte-identical by blob OID**, via `git rev-parse` on **both** refs (pre-merge main and merged HEAD):
```
7aa6be48… clip-earnings-writer   797e2098… earnings-calc   f8499e18… balance
c2db8615… tracking               61cef393… invariant-middleware   ef5cdae7… money-decimal
```
`tracking.ts` is **not in the diff** — the diff is exactly the 6 BL-529 files, none a money file, no scheduler, no `instrumentation.ts`. No clip's earnings changed, no money was written, no job execution changed. `agency-monitor --fix` not run. BL-534's era rule and every live round untouched.

**Gates, honestly:** `npm ci` exit 0; `npx prisma generate` exit 0, run **before** tsc; `tsc --noEmit` **0 errors** — but I did not trust tsc alone; `npm run build` **exit 0** read from a log with `$?` echoed directly, never piped to `tail` (the exact trap BL-529's own round hit), **61/61** pages, **no OOM**; eslint confirmed present (v9.39.4); hooks gate **0 errors / 11 warnings**. No dashes.

---

## What the owner should now expect

**The 15 daily false CRON_DOWN emails stop.** Once Railway auto-deploys this main, the watchdog no longer watches the 5 dormant never-heartbeating jobs, so no more "Cron is down: marketplace-…/cleanup-magic-links/notifications-cleanup" emails.

**A genuine tracking outage still emails loudly.** tracking stays watched for every allowlist value and stays in `OWNER_EMAIL_TYPES`, so if the money cron actually stalls, the owner gets exactly one email (plus bells to every owner), not silence and not a storm.

**No alert kind can still fire falsely to the inbox.** CRON_STATUS is hard-blocked from email by `NEVER_OWNER_EMAIL_TYPES`; the dormant kinds are no longer watched at all; and the global rate limit caps any future burst at 1 email / 3 alerts per hour with tracking first in line. The remaining in-app CRON_STATUS bells are informational by design and never reach email. I am not aware of any kind that can still email the owner falsely; if a job is added to the allowlist but never scheduled, it would surface as an in-app never-seen note, not an email.

## Rollback

`git reset --hard pre-merge-BL-566 && node scripts/safe-push.mjs main --force`. Code only; nothing written to the database.
