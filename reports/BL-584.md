# BL-584 — Retire the confirmed-dead Instagram ghosts (re-verified live at the moment of retiring)

**Done, safely.** I re-probed all 734 of BL-580's gone candidates LIVE this round, retired only the
**710 that were still HTTP 404 right now**, and left the **24 that had come back alive** untouched and
polling. Every retirement is reversible (`videoUnavailable=true` + `videoUnavailableSince=now` only,
no `trackingJob.delete`). Global earnings across ALL clips held constant at **$9,357.82** through every
tranche: not one cent was zeroed. This was a pure DATA operation via the sanctioned runner
(`scripts/run-mutation-once.js`); NO code changed, so the 6 money files are byte-identical and no
build was run.

## Probes run (all disclosed; NO Apify actor run)
* **HikerAPI `/v2/media/info/by/code` (+ by/url on a 404), the 734 candidates, re-probed live this
  round.** Prepaid HikerAPI quota (balance ~$275, 399k+ requests), effectively $0; precedented by
  BL-550/BL-580. Honest note: the first run wrote its output to an invalid path (`C:\` root) and lost
  its summary, so the sweep was run twice (~1,468 prepaid calls total). Still effectively $0; disclosed
  rather than hidden.
* Supabase reads/writes via `run-select.js` / `run-mutation-once.js` (our DB, $0).
No Apify actor was run. Code read from `main` @ `e6a7b85d`.

## PART 1 — re-probe the full 734 LIVE (the re-verify rule in action)

| result this round | count |
| --- | --- |
| still gone (HTTP 404 now) | **710** |
| came back ALIVE (HTTP 200 with media) | **24** |
| transient / other | 0 |
| missing in DB | 0 |

The number moved from 734 to 710: **24 clips flickered back to life** since BL-580's probe hours ago
(19 REJECTED, 5 APPROVED). Exactly as instructed, I leaned MORE conservative and retired only the
fresh 404s. Had I trusted the stale list, 24 clips would have been wrongly frozen. This is the whole
reason the rule exists (BL-576 found 22 of 30 cadence-dead clips alive; a fresh 404 is proof, a stale
verdict is not). The 24 revived ids are recorded; a sample: `cmqz3wyn8000x0ppbkqdxonds`,
`cmr36um3h009r0pnsqy4h7els`, `cmr3hy0qd00jx0pnsxxgv2eti`.

## PART 2 — snapshot, dry run, and the exact rollback (before any write)

Eligible still-gone set: **710 clips, $3,507.71 in frozen earnings**, split into disjoint tranches:

| tranche | definition | clips | earnings (frozen) |
| --- | --- | --- | --- |
| A | REJECTED still-gone | 170 | $0.00 |
| B | APPROVED doubly-confirmed (live-404 AND consecutiveGone>=1) | 76 | $258.17 |
| C | APPROVED remaining (consecutiveGone=0) | 464 | $3,249.54 |
| total | | 710 | $3,507.71 |

Disjoint check: 170 + 76 + 464 = 710. A per-clip before-snapshot (id, url, userId, status, earnings,
savedEarnings, videoUnavailable, consecutiveGone) was captured before any write. The exact rollback,
published as the companion `reports/BL-584-rollback.sql`, is:
```sql
UPDATE clips SET "videoUnavailable"=false, "videoUnavailableSince"=null WHERE id IN (/* the 710 retired ids */);
```
A freeze KEEPS earnings, it does not remove them: `videoUnavailable=true` freezes at `savedEarnings`
and the stored `earnings` value is left exactly as-is (proven in PART 3/4). The global earnings total
across all clips was $9,357.82 before the first write and stayed $9,357.82 after the last.

## PART 3 — retire in tranches, verifying between each (the write)

Applied via `run-mutation-once.js`, each UPDATE guarded with `AND "videoUnavailable"=false`
(idempotent). Re-verified after EACH tranche:

| after tranche | rowCount | global earnings | clips unavailable | tranche earnings frozen |
| --- | --- | --- | --- | --- |
| A (rejected) | 170 | $9,357.82 (unchanged) | 176 (6 historical + 170) | $0.00 |
| B (doubly) | 76 | $9,357.82 (unchanged) | 252 | $258.17 (all 76 now unavailable) |
| C (approved rest) | 464 | $9,357.82 (unchanged) | 716 | $3,249.54 (all 464 now unavailable) |

Global earnings never moved. No tranche showed an earning move or a view move, so none was rolled
back. 716 unavailable = the 6 historical + the 710 retired this round.

## PART 4 — proof (id-matched)

* **The three PART 1 counts:** 710 still-gone, 24 came-back-alive, 0 transient (above).
* **A retired clip with earnings UNCHANGED (frozen, not zeroed):**
  `cmocubizw000x0pn5frr2bdg3` $1.23 before -> $1.23 after; `cmocubz2300100pn5i5uu09r3` $0.85 -> $0.85;
  `cmoi47zts000g0pr359ki0j0b` $0.96 -> $0.96. All now `videoUnavailable=true`,
  `videoUnavailableSince=2026-07-18 19:10`. Earnings identical to the penny.
* **A came-back-alive clip still polling, untouched:** all **24 of 24** revived clips remain
  `videoUnavailable=false`, so they stay in the poll set. None was frozen.
* **The poll set now excludes the retired clips:** the cron dueJobs WHERE (`tracking.ts:3539`) carries
  `videoUnavailable: false`, so all 710 (now `videoUnavailable=true`) are excluded with no code change.
  Active IG poll set is now 834 (the 710 are all excluded by construction; the count also reflects new
  clips submitted since BL-580's 1,535 snapshot).
* **Second run is a no-op:** re-running tranche C returned `rowCount=0` (the guard skips
  already-retired clips). Idempotent.
* **Ledger gone-lookups on retired clips cease from the next tick:** because the 710 leave the poll
  set immediately, the cron stops issuing their per-clip IG gone-lookups on the very next cron tick.
  (Forward statement, verified by mechanism: they are already out of the poll set now.)

**Reversibility, intact:** auto-restore (`tracking.ts:1726-1741`) still un-retires any clip that later
returns a 200 with views; auto-leave still applies if the counter resets; `trackingJob.delete` was
never used (every job row is kept for audit). Any of the 710 can be un-retired with the companion
rollback SQL.

## Realized saving and the remaining floor
The 710 dead IG clips were the bulk of the surviving Instagram Apify leak (the recurring per-clip
gone-lookups BL-577/BL-580 traced), and they now stop being polled entirely. That removes the largest
remaining Apify draw. The remaining floor: (1) newly-gone clips that accumulate over time, now caught
reversibly by BL-576's counter and the BL-580 persisted-skip as it matures; (2) the 24 legitimately
revived clips and all live clips, which keep polling as they must; (3) the TikTok slideshow sliver
(BL-583, awaiting its shadow proof); and (4) image-only IG carousels, which have no view count to
fetch anywhere. No clip's earnings changed, no view moved, no live clip was frozen.

## Safety attestations
6 money files BYTE-IDENTICAL by blob OID on both refs (no code changed). No `trackingJob.delete`.
Auto-restore and auto-leave intact. Snapshot + exact rollback captured before any write. Tranched with
per-tranche verification; nothing rolled back because nothing moved. `tracking.ts:2557-2582` BL-163
owner-lock, campaign-era.ts, and live-clip cadence untouched. Every probe disclosed; no Apify actor
run. Pure data operation via the sanctioned runner; no build claimed.
