# BL-889 — **STOPPED, as instructed.** Both named holders still have `clip_library/`, and it is not merely claimed: **28 shard writes in 10.1 minutes, +31 rows.** The $0.24 was authorised and not spent.

**Date:** 2026-08-01 · **Type:** Blocked (precondition) · **Spend:** **$0.00** of $0.30
**Wrote:** `scratch/bl887_*` only. **No `clip_library/` row, no paid call, no `clippershq` module.**
**Claim id BL-887; published as BL-889** — BL-887 and BL-888 were taken in the reports repo while I held that id.

---

## `tools/claim.py brief`, first, as asked

```
IN-FLIGHT CLAIMS  (paste into a brief)  2026-08-01 20:41
  BL-849       326 min  clippershq/clip_library.py, clip_library/, scratch/bl849_label.py   ** nothing written yet
  BL-867        73 min  clip_library/, scratch/, clippershq/clip_library.py
  MEMEBOT-036   27 min  memebot/scraper/duration.py, memebot/scraper/edit.py, config.yaml +4 more
  MEMEBOT-038    5 min  clippershq/clip_cuts.py, clippershq/song_library.py, clip_pipeline.py +6 more
  MEMEBOT-039    3 min  scratch/memebot039_fields.py, memebot039_guard.py, memebot039_measure.py +2
  MEMEBOT-040    1 min  scratch/songs.json, scratch/memebot040_verify.py, memebot040_findings.md  ** nothing written yet
  (** = the claim is older than any work under it. Ask the owner; nothing expires automatically.)
```

**The stop condition is met exactly as written.** The brief said: *"If BL-867 or BL-849 still
hold it, report and STOP."*

| | held since | writes | state |
|---|---|---|---|
| **BL-849** | 15:16 — **326 min** | `clip_library/`, `clippershq/clip_library.py` | `** nothing written yet` |
| **BL-867** | 19:28 — **73 min** | `clip_library/`, `scratch/`, `clippershq/clip_library.py` | live |

Both. So no backfill ran and no call was made.

---

## And it is genuinely live, not just claimed

The brief also asked for zero shard writes over 10 continuous minutes **verified by hash,
not mtime**. The claimant half already failed, but "held but idle" and "held and writing"
need different answers from the owners, so I measured it anyway — hashing every shard every
30 seconds for 10 minutes. Hash and not mtime because this tree is under OneDrive, where a
sync daemon touches mtime without changing bytes.

```
20:46:30  clips-2026-04.jsonl   changed   rows 192 -> 193
20:47:00  clips-2026-02.jsonl   changed   rows 127 -> 128
20:47:30  clips-2026-02.jsonl   changed   rows 128 -> 130
20:48:01  quiet  (3960 rows)
20:48:31  clips-2026-04.jsonl   changed   rows 193 -> 194
20:48:31  clips-2026-06.jsonl   changed   rows 501 -> 502
   ... 22 more ...
20:53:03  clips-2026-05.jsonl   changed   rows 248 -> 249

VERDICT after 10.1 min
  rows at start : 3944
  rows at end   : 3975  (+31)
  shard writes  : 28
  -> clip_library/ is BEING WRITTEN -- a backfill now would race a live writer
```

**28 writes across five different shards in 10.1 minutes — roughly one every 20 seconds.**
Exactly one 30-second sample was quiet. BL-849 is marked as having written nothing, so this
is BL-867, whose intent names "deep-page hidden clips" — a walk appending as it goes.

For scale: the library was **3,865 rows** when BL-883 surveyed it at 19:20 and **3,975** at
20:53. **+110 rows in ninety minutes**, still climbing.

**Both halves of the precondition fail.** Stopping was not a judgement call.

---

## What that would have cost had I run it anyway

BL-883's merge tool is designed for exactly this and would probably have survived — it
re-reads inside the lock and merges by `clip_id`, so an append during phase A is present at
merge time and keeps its own values. But phase A is **35 minutes of VAD** and the library is
taking a write every 20 seconds, so roughly **100 rows would land mid-run**. Those rows would
be correct-but-unbackfilled, the fill rate would be wrong the moment it was printed, and the
class distribution I was asked to compare against BL-853 would be measured over a moving
denominator. The result would be a number I could not stand behind.

That is a softer failure than data loss, and it is still the wrong time to run.

## The tool is still good — re-verified on this tree, today

```
derivation token for audio_class      : 80904b03
existing values overwritten           : none  (UNTOUCHED, correct)
appends by the concurrent writer      : 38
merges run against it                 : 12
appended rows LOST                    : 0     (NO ROW LOST, NO CORRUPTION)
shrink guard, planted byte loss       : refused -- "read 78 rows under the lock
                                        but the replacement holds 77"
original file after the refusal       : unchanged
```

**The rewritten shrink guard still fires on a plant** (item 4), and the derivation token
(item 5) is unchanged at `80904b03`. Nothing has rotted since BL-883; the round is ready to
execute the moment the library is quiet.

## Item 7, carried forward

For `clip_pipeline` the missing declaration was **not** a second failure. `_f()` tries
`clip_library.field()` and **falls back to a plain dict read by design**, so it would have
read an undeclared `audio_class` perfectly well. **The branch was dead for one reason only:
the field was never written.** MEMEBOT-035's "two failures hiding each other" was wrong
about that module. The declaration still had to be fixed for consumers that call `field()`
directly — it just was not what kept this branch unreachable.

---

## Verification

| check | result |
|---|---|
| `claim.py brief` | run and reported **first**, verbatim |
| BL-867 holds `clip_library/` | **yes — 73 min** |
| BL-849 holds `clip_library/` | **yes — 326 min**, `nothing written yet` |
| 10-minute quiet check, by **hash** | **FAILED — 28 writes, +31 rows, 5 shards** |
| backfill run | **no** |
| paid calls | **none** |
| `spend.json` `total_spent_usd` | **7.9769 — unchanged** |
| merge tool, concurrent-append race | **38 appends, 12 merges, 0 lost** |
| shrink guard on a planted byte loss | **refuses, original untouched** |
| derivation token | `80904b03` |
| `tests/run_all.py` | **94 of 95 suites green**; the 1 red is another round's — see below |
| **campaigns SHA** | **`8e02f8d6f6307ae8` — MATCH** |
| `config.json` | parses, 162 keys |

### The one red suite is not this round's

`tests/test_funnel.py::test_crawl_cluster_wiring` fails reproducibly:

```
FileNotFoundError: ...\crawl_spend_1de5s46v\spend.json
```

It drives `run_crawl` against a temp ledger via `main.SPEND_FILE_ENV` and the crawl no
longer writes it. **It has no reference to `clip_runner`, `clip_speech`, `speech_fn` or
`audio_class`** — I checked rather than assumed, because MEMEBOT-035 touched that area. It
was **green in BL-883's ALL-GREEN 92/92 run an hour ago**, and I have written only
`scratch/bl887_*` since. `clippershq/main.py` is held by **BL-880**, which is the surface
this test depends on. Real failure, someone's to fix, not mine — and I am reporting the
batch number rather than excluding it.

## To actually run this

The blocker is now purely social, and there are two owners to ask:

1. **BL-867** (73 min, actively appending). It needs to finish or pause its walk. Its own
   finding — 205 rows with the lock against 200 without — is why this matters.
2. **BL-849** (326 min, `** nothing written yet`). Nothing has been written under it in five
   and a half hours. The tool does not expire claims, so somebody has to decide whether it
   is still live.

With both clear and the hash check passing, the run is ~35 minutes of CPU and **$0.24**.
Everything else is built, raced and re-verified today.

## Limits

- **Nothing was backfilled.** `audio_class`, `speech_dbfs` and `bed_dbfs` remain 0.0% across
  the library; `speech_frac` remains ~49.5%.
- **No class distribution to compare against BL-853's ~51.9 / 43 / 7.6.** That needs the
  backfill.
- **No library-wide bed-gap distribution.** The best available remains BL-883's n=12 on the
  memebot corpus: mean 3.9 dB, median 3.6, 9 of 12 at ≥3 dB.
- **The race proof is synthetic** — a temp shard and a thread, at 40 rows rather than 3,975.
- **The quiet check is a 10-minute sample**, not a guarantee about any other window.
- **I did not contact the claim owners.** Reporting is as far as this round goes.

## Method

Ran `claim.py brief` first and reported it verbatim before doing anything else. Read both
blocking claims directly from `.claims/` rather than trusting the summary line. Measured the
quiet condition by SHA-256 over every shard at 30-second intervals for a full 10 minutes,
because mtime cannot distinguish a sync daemon from a writer on this tree. Re-ran BL-883's
race and planted-failure tests on today's tree to confirm the tool had not rotted. No paid
call, no key read, no library row written, and the authorised $0.24 is unspent.
