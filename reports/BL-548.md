BL-548 — BL-541 merged to main: the four override deletion paths are gone, so the money is now safe to write

# BL-548 — BL-541 merged to main. The shredder is off.

**`origin/main` @ `ee1f5b1f`, verified.** BL-541 is now an ancestor of main. Merge only: no code
authored, no money written.

**What this unblocks:** until this merge, four live paths deleted the owner's agency row from every
override clip, and two needed no owner action at all — `tracking.ts:2752` on every 10-minute cron
tick, and `gamification.ts:942` whenever a clipper opened their own progress page. Any credit written
before now was written into a shredder. **It is now safe to write the $199.30.**

## STEP 0 — truth per branch, with evidence

| branch | on origin? | on main before this round? | action |
|---|---|---|---|
| **checkpoint/BL-541** @ `c0741e0f` | yes | **NO — 3 commits ahead** (`git merge-base --is-ancestor` false) | **MERGED** |
| checkpoint/BL-538 | **does not exist on origin** (local worktree only, live round) | n/a | left alone |
| checkpoint/BL-547 | did not exist at STEP 0; **appeared mid-round** (`8fc4dea1`, its session pushed while I worked) | no | **left alone**, as required |
| **checkpoint/BL-543** @ `f7a13165` | yes | **ALREADY ON MAIN** (merge `06b26899`) | nothing to do |

**BL-543 is already on main, so its ~$45/month Instagram saving is realised, not pending.** The brief
asked me to note it if it were unmerged; it is not.

## The two money-file diffs, scrutinised

**`src/lib/tracking.ts` — 4 hunks, all one semantic change:**
1. import of `ownerShareApplies`
2. `2136`/`2141` the accrual gate: `!isOwnerOverride` becomes `ownerShareApplies(clip)` (both branches)
3. `2566` the locked-share reconcile: same swap
4. **`2752` the every-tick DELETE: removed**, override clips fall through to ordinary accrual

**`src/lib/clip-earnings-writer.ts` — 1 hunk, exactly ONE non-comment changed line:**
```
-              current.isOwnerOverride !== true &&
```
That is the BL-167 pool clamp losing its override exemption. Nothing else. `isMarketplaceClip !== true`
correctly remains.

**Confined to the accrual gate, the every-tick delete, and the pool clamp. Nothing beyond.** No
earnings maths changed.

## The four untouched money files, sha256 via `git show` on BOTH refs (never worktree-vs-git-show)

```
IDENTICAL  e95756dd9997e1eac16338d561d0df9c7033cba6986782bf051e40fd2e1a57a1  earnings-calc.ts
IDENTICAL  a02f38dff8c49ad46a1a41132ea0b4b23fbaba1c8b5eec27d4e187efda503687  balance.ts
IDENTICAL  9370e4a9793f0bf3fe8973efe49c75bd992c115ca87f0c666f9911e9c0e65b9a  clip-earnings-invariant-middleware.ts
IDENTICAL  3e1c610f15d3b4c5c5972caf914e1f58aceb5545d2872a25273971a888d3d9c7  money-decimal.ts
```
`earnings-calc.ts` byte-identical is the proof that no earnings maths moved: both formulas
(`calculateOwnerEarnings`, `calculateOwnerEarningsGuaranteed`) are its own and untouched.

## STEP 7 — traced on the MERGED result, not assumed

**1. No path deletes, zeroes or rewrites the owner's row on an override clip.** `agencyEarning.delete`
still exists in 13 places; I checked every one. **None is gated on `isOwnerOverride`.** They fire only
when the owner amount is genuinely zero (`tracking.ts:2781`, `force-recalc:321`, `override:289` — all
the `else` of an `if (amount > 0) upsert`), the video is gone (`tracking.ts:3155`), the clip is
rejected/destroyed (`review:878/1186/1209`, `destroy:65/70`), or `expected === 0`
(`agency-monitor:213`, with `v.isOverride ||` removed). **`gamification.ts` now contains no
`agencyEarning.delete` at all** — the clipper-triggered path is gone.

**2. The ambiguity guard is live**, so teaching the monitor the gross formula cannot arm the $932.49
platform-wide rewrite:
```
if (Math.abs(impliedByLockedShare - stampedRatio) > 0.01) { continue; }  // owner's decision
```
Gross applies only where a clip's stamped `oCpm/cCpm` agrees with the locked share's `s/(1-s)`;
rate-era mismatches are skipped entirely and surfaced as `skippedAmbiguous`.

**3. The pool cap holds.** The override exemption is gone from the clamp, so override clips are
clamped like any other and `owner = pool x s/(1-s) = s x budget`, `total = budget` by construction.

**4. An unconfirmed backdate grants no streak.** `streakCountsForClip` returns
`backdateVerifiedAt != null` for override clips, and `streakEligibleWhere()` mirrors it at query time
so the grid and the evaluation cannot drift. Fail closed.

**Column live:** `clips.backdateVerifiedAt`, `timestamp without time zone`, `is_nullable: YES` —
additive and nullable, confirmed in `information_schema`.

## No money moved

```
agency rows on override clips: 0     total: $0.00     (unchanged — the merge writes no money)
somesome: 483 rows, $3,427.03        (untouched, exactly as before)
```
No clipper's earnings changed. The 22 already-backdated clips got no retroactive streak.

## BACKLOG union — arithmetic, not hope

Merge-base `b9147a92` had **52** entries. Main added **5** (BL-543, BL-539, BL-540, BL-544, BL-545).
BL-541 added **2**. Predicted union: **59**. Result: **59**, with **0** duplicate headings, and every
one of main's 57 and BL-541's 54 entries verified individually present.

**The `$2` capture-group hazard was avoided by construction:** I deleted the three conflict markers
**by line number** (`sed '19124d;19193d;19215d'`) and performed no string replacement at all, so the
`"$2,000"` corruption that hit a prior round could not arise.

## Gates, stated honestly

| gate | result |
|---|---|
| `npm ci` | EXIT=0, 822 packages |
| `npx prisma generate` (generate only, never migrate) | EXIT=0, client regenerated after `npm ci` wiped it |
| check:prisma-bypass | EXIT=0, 0 violations |
| check:removed-fields | EXIT=0, OK |
| lint:hooks (BL-348) | EXIT=0, **0 errors / 11 warnings** (at the <=11 limit) |
| `tsc --noEmit` | EXIT=0, 0 errors |
| **`npm run build`** | **EXIT=0**, read from a log via `echo $?`, not a pipe |

Build genuinely reached every stage: "Compiled successfully in 14.0s", "Finished TypeScript in 27.5s",
"Generating static pages using 11 workers", 61 pages. **I did not trust tsc alone** — on this exact
branch BL-541 hit a real failure that `tsc --noEmit` missed and only `next build` caught. eslint
v9.39.4 confirmed executing and `npm config get omit` empty, so the hooks gate was not a silent no-op
(it reported real counts). No OOM.

## What happens next

The write round can now proceed safely: STRAENGE's stale-row repair (**−$47.37**, not the −$47.10 that
was signed off — BL-539 contradicts itself by 27 cents and the owner should re-confirm) then its
$173.41 credit, landing at owner $999.72 / total $2,999.72 of $3,000. **The six ACTIVE campaigns need
no manual credit at all**: the ordinary cron path now accrues their $25.89 through `writeClipEarnings`,
idempotently by construction. somesome's $133.51 stays blocked on the era question.

**Rollback:** `git reset --hard pre-merge-BL-541 && node scripts/safe-push.mjs main --force`
(`pre-merge-BL-541` = `3af689a8`). The column is additive and nullable, harmless to leave.
