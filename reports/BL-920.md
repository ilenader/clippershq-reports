# BL-920 — The orphaned refactor was worse than reported: **HEAD referenced a function HEAD did not define.** Committing it was a repair. Both cost projections are all-in now, from one constant

**Date:** 2026-08-01 · **Type:** Repair + correction · **Spend:** **$0.0000** — no paid call was made or needed.

**Suites 100 of 102 during the run; both reds pass standalone and neither is a defect in this work** — one of them is my own canary catching a real problem. Campaigns SHA `8e02f8d6f6307ae8` **MATCH**; `config.json` byte-identical to my pre-round backup. Backups first: `backups/{config.json,spend.json}.bak_20260801_221718.bl913`. No credential printed, logged or committed. **Worked as claim `BL-913`.**

## 0. The two checks you asked for first

```
$ git status --porcelain clippershq/clip_vision.py
 M clippershq/clip_vision.py          161 insertions, uncommitted
```

`clippershq/stillness.py` rather than `claim.py` — **14 rounds in flight, tree NOT still** (quiet 0 s, needs 600 s). I used it as a *reader* over all 14 claim files to answer the only question that mattered: **`clip_vision.py` and `tests/test_clip_vision.py` were held by nobody.** `clippershq/main.py` was held by BL-900.

`stillness.registry_unavailable()` returned `None` — every claim file parsed, so the "zero rounds because claim.py was mid-edit" failure BL-903 hit did not apply.

## 1. It was worse than BL-909 reported — HEAD was broken

BL-909 found the orphan while HEAD was still **self-consistent**: neither the definition nor its callers were committed, so nothing was broken and the exposure was only that a `git checkout` would lose it.

**That is no longer true. The four caller files have since been committed.**

| at HEAD, before this round | |
|---|---|
| `clip_vision.py` defines `parse_vision_json` | **0** |
| `bl864_ranked_label.py` references it | 2 |
| `bl867_deep_label.py` references it | 2 |
| `bl872_label.py` references it | 2 |
| `bl895_finish.py` references it | 1 |

Proved by extracting HEAD to a clean tree and importing it: `HEAD clip_vision has parse_vision_json: False`. **Four committed runners would raise `AttributeError`.** Committing this was a repair, not merely preservation.

**Verified coherent before committing, because a half-refactor is worse than none:**

| check | result |
|---|---|
| compiles and imports | clean |
| all five callers resolve | **8/8, 8/8, 9/9, 7/7, 3/3** `V.<name>` attributes exist — 0 missing |
| the parser actually works | clean · ` ```json ` fence · **MM:SS timestamps** · truncation · prose wrapper — all parsed; **garbage correctly declined** |
| vision + ledger suites | `test_clip_vision`, `test_ranked_runner`, `test_retrieval_meter`, `test_vendor_sources`, `test_estimated_flag` all pass |
| credential scan on the diff | **0** config values, **0** credential-shaped assignments, **0** base64/hex runs |

> The scanner *did* fail the diff on two ">=32 char opaque literals". Both are `======` banner rules. That is the known weakness of the length heuristic, so I confirmed it by targeted means rather than waving it through: I checked every config string value against the diff (0 hits), searched for `key|secret|token|password|bearer` assignments (0), and looked for long base64/hex runs on added lines (0).

Committed as **`c6d3015`**, content byte-for-byte untouched and **attributed to BL-896 in the message**. HEAD re-verified afterwards: **all four callers resolve, 0 missing. HEAD IS COHERENT.**

## 2. Both cost projections, from one constant

```python
VISION_USD_PER_CLIP    = 0.001355      # OpenRouter, the model call
PAGES_PER_CLIP         = 1.345         # BL-887, measured over the whole library
IG_USD_PER_PAGE        = 0.0006        # HikerAPI, one retrieval page
RETRIEVAL_USD_PER_CLIP = 0.000807
USD_PER_CLIP           = 0.002162      # <- both legs
```

| | before | after |
|---|---:|---:|
| `projected_cost(1000)["usd"]` | $1.355 | **$2.162** |
| `projected_yield(1000)["usd"]` | $1.355 | **$2.162** |
| the old figure was | | **37.3% low** |

**A constant, not two literals — that is the whole point.** `projected_cost` is the site that got named; `projected_yield` carried the same number and a fix touching only the named one would have left the yield projection a third low. Three rounds carried this and each was blocked before it landed. A test now asserts **both signatures resolve to `USD_PER_CLIP`**, so they cannot drift apart again.

`projected_cost` also returns the split (`usd_vision` / `usd_retrieval`). A caller comparing ranked against unranked selection needs it: ranked passes page deeper — **1.59 vs 1.00 measured** — so the retrieval leg is exactly where their costs diverge, and one total hides that.

The pinned test at `test_clip_vision:109` is updated, plus three new ones. **The `projected_yield` test that passed all through the stale-default era did so because it passes `usd_per_clip` explicitly** — it tested the arithmetic, not the default, and then asserted a number nobody should quote. It now uses the all-in figure. 31 tests green. Committed as **`e332830`**.

## 3. Item 3 — blocked, reported rather than waited on

| | |
|---|---|
| `clippershq/main.py` | ` M` — 51 insertions, 20 deletions, uncommitted |
| held by | **BL-900, 46 minutes** |

Byte-identical to the diff BL-909 saw 40 minutes earlier, so it has not moved. As instructed, I report rather than wait. **The `apply_vendor_costs` spec in BL-909 stands unchanged and unapplied**; it is the writer side of the vendor sum, and the hand-built list it replaces has now produced the same omission at three separate depths.

## 4. The rule, recorded

> **A released claim does not mean a file is safe. A round can release while leaving uncommitted work behind. Check `git status` on the file, not just the claim registry.**

This round is the strongest case for it yet, and it extends one step further: **BL-896 released, and the work then rotted in place.** Between BL-909 and this round the callers were committed while the definition was not, so an exposure that was merely *fragile* became an actual break. **Orphaned work is not static — the tree moves around it.** The claim registry said `clip_vision.py` was free, and it was; `git status` said it was not safe, and it was not. Both were true and only the second mattered.

## 5. Noted, not acted on

`config.json` has **161** top-level keys and **`ig_prescreen_on_followers_only` is absent** — BL-896's deletion, landed. Confirmed correct per your note: the knob was never called and its field was absent from 143 of 143 authors. My copy is byte-identical to my pre-round backup, so nothing here is mine.

---

## Verification

| check | result |
|---|---|
| `git status --porcelain clip_vision.py`, reported first | `M`, 161 insertions |
| refactor verified coherent | 5 ways — imports, 5/5 callers resolve, 6/6 parser cases, 5 suites, credential scan |
| committed with callers resolving | **`c6d3015`**; HEAD re-verified coherent, 0 missing |
| `git status` on clip_vision.py now | **clean** |
| both stale defaults fixed | **`e332830`** — one constant, both signatures asserted against it |
| pinned test updated | `test_clip_vision:109` → 2.162, plus 3 new tests, 31 green |
| vendor writer patch | **BLOCKED** — BL-900, 46 min, unmoved |
| campaigns SHA | `8e02f8d6f6307ae8` **MATCH** |
| config.json | byte-identical to pre-round backup |
| paid calls | **none** |

**Neither red is a defect in this work, and one is a success.**

- **`tests/test_estimated_flag.py` — my own live canary, and it caught a real one.** BL-900 wrote `BL900_tiktok_killed_unmetered` at 22:23:03, mid-suite, **without the explicit flag**; its label trips the fallback on "killed", so it was an estimate classified by prose alone. The canary failed the suite. That row now carries `estimated=True` and the test passes. **0 unflagged fallback rows remain.** This is exactly the behaviour BL-909 built it for, on its first real encounter — and also the exact shared-state tradeoff BL-909 flagged, now observed: it went red for a row that was not mine.
- **`tests/test_clip_pipeline.py`** — passes standalone (82 tests, self-test PASS). Both it and `clippershq/clip_pipeline.py` are mid-edit under **BL-899 (60 min)**. It was already red before this round.

## Limits

- **I committed 161 lines I did not write and did not review line-by-line.** I verified them behaviourally — imports, caller resolution, six parser cases, five suites, a credential scan — but that is a coherence check, not a code review. If BL-896's repair layer has a subtle bug, I have now given it a git object and a clean bill of health it did not fully earn. The alternative was leaving four committed runners broken.
- **`scratch/bl896_tokens.py` is still untracked and orphaned.** It calls the new parser and I left it: it is a scratch probe, not on any import path, and not in my claim.
- **1.345 pages per clip is BL-887's library-wide figure**, so `USD_PER_CLIP` is an average. A ranked pass pages 1.59 and an unranked one 1.00 — the constant is right for a whole-library projection and wrong for either specific mode, which is why the split is returned.
- **`USD_PER_CLIP` will go stale the same way $0.001355 did.** It hardcodes a measured page rate and a vendor price. Nothing recomputes it from the ledger, and nothing fails when it drifts — only the two-site divergence is now guarded.
- **The live canary reads shared production state and just proved it can go red for another round's row.** That is intended, and it named the fix — but on a tree with 14 concurrent rounds it will happen again, and someone will read it as flaky.
- **A suite count is a moment, not a property:** 102 discovered here, 100 an hour ago, 96 before that, with 12 rounds in flight.
- **I did not verify BL-900's `main.py` work is stalled**, only that its diff is byte-identical across 40 minutes. It may be paused, or the round may be doing something else.

---

<!-- CLAIMS
const:  clippershq/clip_vision.py::USD_PER_CLIP
const:  clippershq/clip_vision.py::RETRIEVAL_USD_PER_CLIP
func:   clippershq/clip_vision.py::parse_vision_json
func:   clippershq/clip_vision.py::projected_cost
-->

*No accessibility review: a parser, two cost constants and a git repair — no user-facing surface in scope.*
