# BL-641 — somesome is finished, and every clipper sees $9,750.00 of $9,750.00

**2026-07-23** · Base `9f5de583` → merged `d6a5ca46` · Branch `checkpoint/BL-641` · Tags `pre-BL-641` / `pre-merge-BL-641` / `post-merge-BL-641`
Display-only code plus a ONE ROW, ONE COLUMN status change. **No stored money moved. No clipper's earnings or balance changed by a cent.**

---

## PART 0 — COMPLETED or PAST? Answered from the code, not guessed

| What it does | COMPLETED | PAST |
|---|---|---|
| Clipper visibility | **VANISHES.** The clipper list is `status in [ACTIVE, PAUSED, PAST]` (`api/campaigns/route.ts:113`); `/api/campaigns/past` serves PAST only | **VISIBLE** in the "Completed campaigns" strip with a Completed badge |
| Earnings freeze | **NOT** in `campaignStatusBlocks` (`tracking.ts:1935-1940`) | **IS** an unconditional arm of it — the hardest freeze on the platform |
| Cron cadence | Excluded from the due-jobs sweep (`tracking.ts:3593`) | Excluded from the same sweep |
| Era boundary | None (predicate is `toStatus='ACTIVE' AND fromPauseSource='AUTO'`) | None, same reason |
| Balances / withdrawals | Zero references to campaign status in `api/earnings`, `api/payouts`, `balance.ts` | Same: zero |

**RECOMMENDED AND USED: PAST** — it is the only status that reads as finished AND keeps the campaign visible, and a hidden campaign cannot display 100% to anyone.

**COMPLETED would have hidden it, and would also have been LESS safe.** Because COMPLETED is missing from `campaignStatusBlocks`, it would have UNBLOCKED the somesome clips created after the 2026-06-22 era boundary, which are currently frozen only by the AUTO-pause arm. PAST freezes every clip unconditionally.

---

## PART 1 — the display change, derived not hardcoded

New `isCampaignFinishedForClippers` in `src/lib/campaign-clipper-view.ts` reads **`Campaign.status` and nothing else**. No campaign id, no budget figure, no date, no name. A PAST or COMPLETED campaign is terminal, so the gap between spend and budget is headroom nobody can ever reach — the identical phantom-headroom condition BL-535 closed for exhausted pools, arrived at through the campaign's LIFECYCLE instead of its spend. Both funnel into the same `Math.max` in `resolveClipperFacingSpent`, so the rule may only ever RAISE a figure, never lower one. `/api/campaigns/spend` selects `status` (read only; the response is a `{id: number}` map, so its shape cannot change).

**Which surface shows which number**

| Surface | Role | Reads |
|---|---|---|
| `/campaigns` Completed strip | CLIPPER | **$9,750.00 of $9,750.00, 100%** (NEW) |
| `/admin/campaigns` | OWNER / ADMIN | **$5,445.80 of $9,750** (UNCHANGED) |
| Clipper's own earnings line | CLIPPER | exactly what they earned (UNCHANGED) |

**HONEST CORRECTION TO THE BRIEF.** The owner surface does **not** show $8,879.09 and never did. It shows **$5,445.80** — BL-638's third definition: live clips only ($2,018.77) plus the FULL owner accrual ($3,427.03). The true total **$8,879.09** ($5,452.06 clipper + $3,427.03 owner) and the real **$870.91** headroom are computed figures that appear on **no surface**. This round leaves that exactly as it was; it is not made worse, and it is not fixed. **Own round.** What the owner surface *does* still show is a real, non-zero gap ($4,304.20 by its own definition), so the owner is not shown a full bar and does not lose the signal that this campaign is not fully consumed.

---

## PART 2 — the status change

Snapshot and exact rollback were printed BEFORE the write; a dry run confirmed exactly 1 matching row.

```sql
-- APPLIED (run-mutation-once, rowCount=1, idempotent):
UPDATE campaigns SET status='PAST', "updatedAt"=now()
 WHERE id='cmoaa70j2000z0ppfyaco13mj' AND name='somesome' AND status='PAUSED';

-- EXACT ROLLBACK:
UPDATE campaigns SET status='PAUSED', "updatedAt"='2026-07-20 11:28:37.103'
 WHERE id='cmoaa70j2000z0ppfyaco13mj';
```

`manualSpent` left **NULL on purpose** — writing 9750 there would be the hardcoded per-campaign number BL-535 forbids. `budget` (9750), `pauseSource` (AUTO) and `lastBudgetPauseAt` (2026-06-22 17:59:21.894) all untouched. **No `campaign_status_changes` row was written at all**, so the era predicate cannot match under any reading.

| Confirmed NOT to have happened | Evidence |
|---|---|
| Era boundary minted | still `2026-06-22 03:07:29.621`; 33 status-change rows before and after |
| eraExempt tripped | **0 of 33**, unchanged (BL-617, BL-638) |
| Owner row re-derived (the $933.94) | 483 rows, $3,427.03 — identical |
| Clip unfrozen | PAST is a STRONGER freeze than AUTO-pause; 953-clip md5 identical |
| Balance moved | per-clipper md5 identical across all 84 clippers |
| Pending withdrawal affected | 133 payout_requests md5 identical (2 REQUESTED $104.70, 3 UNDER_REVIEW $166.45) |

**The 419 (433 total) videoUnavailable clips and their $3,433.29 stay exactly as they were** — frozen, still APPROVED, earnings intact, every clipper keeping precisely what they earned. PAST cannot thaw them; it adds a gate, it never removes one.

---

## PART 3 — what the 84 clippers experience

The brief said 104; the measured figure is **84 clippers with clips on somesome** (953 clips), matching BL-638.

* **Campaign appears?** Yes, in the "Completed campaigns" strip with a Completed badge and a grayscale card.
* **Progress bar reads?** `$9,750 / $9,750` and `100%`.
* **Own earnings line?** **Unchanged, to the cent.** This is a CAMPAIGN-level figure only. `/api/earnings` and `/api/payouts` contain **zero** references to campaign status, so nothing a clipper sees about their OWN money moved.
* **Can still withdraw?** Yes, on exactly the same terms as yesterday. The withdrawal gate is status-blind.
* **No clipper is shown they personally earned more than they did.**
* **Two side effects to know:** view counts stop refreshing (PAST is excluded from the cron sweep; they were on a 72h cadence), and the card is no longer clickable (`api/campaigns/[id]/route.ts:67` returns 403 for PAST to non-OWNER). Both are existing, documented PAST behaviour.

**FLAGGED, NOT FIXED (own round):** BL-638 found `/api/earnings:64` lacks the withdrawal gate's `videoUnavailable:false` filter, so **20 clippers see $366.82 they cannot withdraw**. Verified this round neither worsens nor fixes it: neither route reads campaign status at all.

---

## PART 4 — the evidence

`npx tsx scripts/test-bl-641-finished-campaign-display.ts` → **19 passed, 0 failed**, against LIVE post-flip rows.

```
somesome status=PAST budget=$9750.00 clipperSide=$2018.77 total=$5445.80
LIVE (status PAST) clipper sees $9750.00 of $9750.00 = 100%
PASS  LIVE somesome is FINISHED, so a clipper sees the full $9750.00
PASS  LIVE progress bar reads 100%
OWNER / ADMIN keep: $5445.80 of $9750.00, headroom $4304.20
control "Panic Baby" ACTIVE budget=$3000.00 spend=$158.31
  as-is: $158.31  ·  same row with status=PAST: $3000.00
PASS  the SAME row, money untouched, reads full the moment its STATUS is finished
campaigns whose clipper-facing figure CHANGED: 1   -> somesome [PAST] $5445.80 -> $9750.00
PASS  ZERO unfinished campaigns change display (BL-535 control property intact)
PASS  PAST with NO / ZERO budget cannot fabricate a full claim
PASS  Math.max holds: a finished OVER-budget campaign keeps its real (over) number
```

**Stored values, BEFORE == AFTER by md5 over live rows**

| Set | Checksum | Result |
|---|---|---|
| Per-clipper earnings + base + bonus, all 84 | `ed87165bb10201085ae54fde5fe991d8` | identical |
| All 953 clips (earnings, videoUnavailable, status) | `bbc08a0fafee555c5ecddfbcded6dc00` | identical |
| All 133 payout_requests (status, amount) | `754790413b8900853aab3d4ba147c931` | identical |

Clipper total **$5,452.06** · owner accrual **$3,427.03** over **483** rows · **433** videoUnavailable clips holding **$3,433.29** — every figure identical to BL-638 and BL-570 to the cent.

**Campaigns the rule affects, now and in future:** today, **somesome only**. The other four PAST campaigns (Grateful Songs, Zhus, Hapday, Gainzalgo) have 0 clips and 0 agency rows so they never enter the spend map, and their card prefers `manualSpent` (already equal to budget) regardless. There are **0 COMPLETED campaigns**. In future the rule applies to any campaign the owner moves to PAST or COMPLETED, and to nothing else.

---

## Safety and gates, stated honestly

**6 money files + `campaign-era.ts` BYTE-IDENTICAL by blob OID** (`git rev-parse pre-merge-BL-641:<f>` vs `HEAD:<f>`): writer `7aa6be48`, earnings-calc `797e2098`, balance `e887f80a`, tracking `847dcf70`, middleware `61cef393`, money-decimal `ef5cdae7`, campaign-era `106e16ad`. **`tracking.ts` NOT in the diff.** 2 source files changed, 380 insertions total.

Gates: prisma-bypass **exit 0** · removed-fields **exit 0** · lint:hooks **0 errors, 11 warnings** (at the ≤11 cap; **eslint v9.39.4 confirmed executing**, not a silent no-op) · `tsc --noEmit` **exit 0** · `npm run build` **exit 0** on all three runs (pre-commit, post-commit, post-merge), each redirected to a log with a real `echo $?`, **never piped through tail**, 61/61 pages. `npx prisma generate` run after `npm ci` (never `migrate`). Isolated worktree at `C:/wt/bl641`, node_modules installed there, **never junctioned**. NO dashes as bullets. BL-639 / BL-640 untouched; the primary tree was clean and unchanged when the merge was taken there.

**Rollback:** `git revert -m 1 d6a5ca46` plus the UPDATE above, or `reset --hard pre-merge-BL-641`. The code revert **alone** returns somesome to $5,445.80 of $9,750 with no DB change, because the display is derived from status.
