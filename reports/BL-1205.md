# BL-1205 — REPORT AND STOP: the sheet's own file is dirty under a round that started two minutes ago

**2026-08-09.** The rebuild was refused on the precondition the brief set, and the round spent
itself instead on the measurement that rebuild will need.

**`clippershq/review_sheet.py` is DIRTY and HELD.** `git status` shows ` M` — eight
uncommitted lines — and `claims_read.py --holders` returns **BL-1204**, which started at
**21:39:47**, roughly two minutes before this round. It also claims **`output/REVIEW_500.html`**,
the sheet itself. Those are precisely the two files a rebuild writes.

**Every number below was produced by `.venv\Scripts\python.exe` (3.11.9).** **$0.00 spent** —
no billed request. No lead data below. **His server on 8777 was not touched** (still LISTENING,
pid 21428) and **his marks file was never written**.

---

## 1. His marks — backed up, unchanged, and not what the brief expected

| | brief said | actually on disk |
|---|---|---|
| lines | 168 | **176** |
| distinct handles | 163 | **171** |
| sha256 (16) | `96f5ff54fe8dd203` | **`037f1d40f0c0069e`** |

Not a discrepancy to worry about — **he has marked eight more rows since the brief was
written**, which is exactly what a live marking session looks like. Recorded because the brief
gave a hash to verify against and it no longer matches; anyone checking that hash later would
otherwise think the file had been tampered with.

Backed up before anything else to
`bl1203_marks_backup_20260809_214136.jsonl`, and re-hashed after every step:
**`037f1d40f0c0069e` → `037f1d40f0c0069e`, UNCHANGED.**

---

## 2. Why the rebuild stopped

The brief is unambiguous: *"review_sheet.py and meme_finder.py have both been dirty under
multiple rounds; IF EITHER IS HELD OR DIRTY, REPORT AND STOP"*, and *"TRUST git status OVER
THE REGISTRY"*. Here both agree for once:

| file | `git status` | `--holders` | verdict |
|---|---|---|---|
| `clippershq/review_sheet.py` | ` M` **dirty** | **BL-1204** | **blocked** |
| `clippershq/meme_finder.py` | clean | FREE | available, but not where the work is |

**BL-1204 is landing the exact fix item 3 anticipated.** Its intent: *"Make the free-text box a
complete answer on its own: typed text saves with no preset… stored in its own field alongside
reason/reason_code."* Its uncommitted diff already contains it:

```diff
-  mark:mark, reason:(free||label||''), reason_code:(code||''),
+  mark:mark, reason:(label||free||''), reason_code:(code||''),
+  if(free) rec.free_text=free;
```

The brief said *"that fix may be landing in a parallel round; if it is already in, keep it."*
It is in, uncommitted. **Writing the sheet now would have destroyed it** — eight lines that
exist nowhere but that working tree.

**Everything item 1 and item 3 ask for lives in `review_sheet.py`**: the three dialogue counts,
removing the text-derived dialogue line, RULE 10's plain-language label, the scroll-0px
behaviour, inline playback, windowing, presets, the counter. There is no version of this round
that does not write that file, so there is no partial build worth attempting.

**`tests/test_review_sheet.py` therefore stays red**, for the reason BL-1202 already recorded:
RULE 10 has no label. The one line is still

```python
"short_captions": ("Captions too short", "posts average under 400 characters of caption"),
```

and it belongs to whoever holds the file — today, BL-1204.

---

## 3. What I did instead: the measurement the rebuild needs

Keyed by **handle**, never by row — row numbers have already collided across three sheets.

### Mark preservation

| | |
|---|---:|
| handles he has marked | **171** |
| …present in the 425 re-judged pages | **22** |
| …**not** in that set (earlier sheets) | **149** |

The 22 matches exactly where the brief says he is. **The 149 are the trap**: they are not in
the new sheet at all, and a rebuild that keys on the new page list would drop the entire
history that every measurement in this project rests on. They must be carried, not regenerated.

### Verdict churn beneath his marks

| | |
|---|---:|
| verdict **unchanged** under the new rules | **17** |
| verdict **changed** — keep his mark, flag for re-review | **5** |

### Does the rebuild move toward him?

On the 22 pages he has actually marked:

| | agreeing with him |
|---|---:|
| old verdicts (what he is looking at now) | **15 of 22 — 68.2%** |
| new verdicts (BL-1201 re-judge) | **14 of 22 — 63.6%** |
| net | **−1** |

**This is worth stating plainly because it cuts against the brief's premise.** The brief opens
*"everything built in the last day is measured and on disk, and none of it is in the sheet he
is looking at"* — with the implication that the rebuild is straightforwardly better. On the
only 22 pages where his judgement and the new rules can be compared, the new rules agree
**slightly less**. At n=22 that is indistinguishable from noise (one page), and the new rules
measured better on the older, larger label sets. But it is not evidence that the rebuild
improves what he is doing right now, and it should not be sold as such.

The re-judge data itself is exactly as described: **425 pages, dialogue fires on 107** — a gate
that fired on 0 before.

---

## 4. One error of my own, caught before it reached the report

My first pass read the re-judge verdict as `p.get("passed")`. That key **does not exist** in
`bl1201_rejudge.jsonl` — the field is `passed_3` — so it returned `None` → `False` for all 425
pages and reported **11 of 22 changed** with no error anywhere. The real figure is **5**.

It is the benign-zero shape this project keeps meeting: a missing key that defaults to a legal
value and produces a plausible wrong number. The reader now tries the keys the producer
actually writes and **raises** if none is present, rather than defaulting.

---

## 5. What he should do

**Nothing, and keep marking.** His server is untouched, his marks are intact, and the sheet in
front of him still works. The rebuild is one round away and belongs to whoever holds
`review_sheet.py` when it is free.

**For that round, the three things this one established:**

1. **Carry all 171 marked handles, not the 22.** Keying on the new page list silently discards
   149 marks — the entire ground truth predating this sheet.
2. **5 of his 22 changed verdict.** Keep the mark, flag the row; do not discard either.
3. **Do not assume the re-judge is an improvement on his current pass.** It is −1 on the 22
   pages where the comparison exists.

---

## 6. Ledger

**Spend $0.00 of $1.00.** **Master, config:** unchanged. **Campaigns:** unchanged —
`7a029ee5447cddd8` and `8e02f8d6f6307ae8` are the same object under `(',',':')` and
`(', ',': ')`. **`ground_truth/review_marks.jsonl`:** backed up, read, hash-verified before and
after, never written. **No file held by another round was touched**, and `claim.py` was run
**with** the published-report check, not `--no-reports-check`.

**Not done, and why:** every item in §1 and §3 of the brief, because all of them write
`clippershq/review_sheet.py`. **Playwright was not run** — there was no rebuilt sheet to drive,
and driving the operator's live session while he marks would have risked his data for a
screenshot.

**Suites:** `.venv\Scripts\python.exe tests/run_all.py --jobs 4` — **278 of 281 green,
973.5 s, 6 rounds in flight at 22:00.** Output read whole, never piped through `tail`. The 3
red are the two nested `memebot/` suites needing `pytesseract` in the venv, and
`tests/test_review_sheet.py` — the RULE 10 label, in the file this round is blocked from
touching. This round changed no shipped code, so the suite describes the tree the other six
rounds are working in.

*Reproduce:* `scratch/bl1205_preserve.py` — mark preservation, churn, and the hash check.
