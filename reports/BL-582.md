# BL-582 — Apify 12h forensic: every dollar of 2026-07-18, line by line (AUDIT ONLY)

**Read only. No Apify actor run. One doc. Base main @ e6a7b85d (post BL-581 merge of BL-580).**
Probes: read-only `run-select.js` DB reads + free Apify billing/run-log reads only; **$0**, no actor invoked.

## Timezone correction (the +02:00 trap, caught before it corrupted the match)
The pasted invoice is **Belgrade (UTC+2)**, the honest ledger is **UTC**. Proof by id-cluster: the invoice
api-scraper burst at `12:33:09` maps exactly to the ledger cluster at `10:33:09` UTC. The `:01-past-the-hour`
TikTok cadence does NOT disambiguate (a whole 2h offset preserves it). So the invoice window `07:45-19:29`
Belgrade == **UTC 05:45-17:29**, and every ledger query below uses UTC 05:40-17:35 to align.

## PART 1 — every run classified (invoice = ground truth for actual dollars)

| Actor (invoice) | provider / code path | runs | results | invoice $ | verdict |
|---|---|---|---|---|---|
| **Fast Instagram Scraper** `apidojo/instagram-scraper-api` | `fetchApidojoInstagramSingle` (apidojo.ts:537) called as TIER 2 of `fetchInstagramStats` (apify.ts:674) | **80** | **79 return 0**, 1 returns 1 | **$0.80** | **THE SPEND. 79 gone-lookups.** |
| Instagram API Scraper `apify/instagram-api-scraper` | `fetchInstagramStatsApiScraperSingle` TIER 1 of `fetchInstagramStats` (apify.ts:~672) | ~90 visible (ledger 170) | 149 fail (gone) | ~$0.01 (one 2-result run) | near-free tier-1 companion |
| TikTok Scraper `clockworks/tiktok-scraper` | `fetchTikTokStatsBatchApify` fallback on LamaTok miss (apify.ts) | 14 | 1-3 pages | **$0.09** (9 runs at $0.01) | honest floor + slideshow-500s |
| Tiktok Scraper PPR `apidojo/tiktok-scraper` | `fetchApidojoTikTokBatch` (apidojo.ts:684), TT batch tier | 14 | 10 each | $0.00 | free, hourly :00 UTC cron |
| Instagram Profile Scraper | profile-verify gate (account check) | 1 | 1 | $0.00 | expected, gates verification |
| **TOTAL (12h)** | | | | **≈ $0.90** | apidojo-IG = **89%** |

**The 0-result tell (BL-554's method).** 79 of 80 apidojo-IG runs returned **0 results** = definitively-gone
posts. Add 149 failed api-scraper tier-1 runs (same gone clips, tier-1 leg). A live post returns >=1 result;
these returned nothing and were billed anyway. Extrapolated: **~$1.6-1.8/day** on this 12h (the owner's
~$1.30/day is the daily average across a lighter overnight). BL-580's diagnosis is **CONFIRMED**: the
surviving spend is the deleted-reel leak on the single `fetchInstagramStats` path, dominated by the apidojo
tier-2 leg.

## PART 2 — invoice vs the honest ledger (match by id-cluster, not row count)

| Category | Invoice (billed) | Ledger `apify_usage_entries` | Trust | Why |
|---|---|---|---|---|
| **apidojo/instagram-scraper-api singles** | ~80 runs / **$0.80** | **0 rows** (blind) | **INVOICE** | `fetchApidojoInstagramSingle` uses `console.log`, never `logActorRun` (BL-567's blind spot, still open). The #1 spender is invisible to the ledger. |
| apify/instagram-api-scraper singles | ~90 runs / ~$0 | **170 rows** (149 fail) | INVOICE for $, ledger over-counts runs | Ledger logs per-URL/attempt (or the paste is a partial console page); either way api-scraper is ~free so the $ conclusion is unaffected. |
| hikerapi-v2 / lamatok est_cost | not an Apify line | ledger shows $0.51 / $0.60 "est" | NEITHER as Apify $ | Hiker + LamaTok bill **prepaid quota**, not Apify dollars. The ledger's `estimatedCostUsd` is a phantom for these and over-states api-scraper too. |
| TikTok clockworks | 9 x $0.01 = $0.09 | `lamatok-tiktok+legacy-tiktok`, `none-tiktok`, `legacy-tiktok-only` | agree in shape | LamaTok primary, clockworks mops the misses. |

**Net:** the ledger cannot see the money (apidojo-IG) and mis-prices the free legs. For actual Apify dollars,
**trust the invoice**. The ledger is still useful for the gone-tell (149 api-scraper failures) and TT attribution.

## PART 3 — why each still fires, and what kills it

- **apidojo-IG $0.80 (89%) = the 616 deleted-reel leak.** These are gone IG reels re-looked-up on the single
  `fetchInstagramStats` path. **`apidojo.ts` is CLEARED**: `fetchApidojoInstagramSingle`/`Batch` are PURE
  functions of their URL argument (they never re-derive a set), so this is NOT the BL-554 caller bug. The
  gone-awareness gap lived in the single caller, and BL-580's `isClipPersistedGone` skip (apify.ts:2259,
  guarding `fetchInstagramStats` at 2263, returns NULL never 0) now closes it — but only for clips at
  `consecutiveGone >= 5`. Today that is **3 clips** (2 at k=7, 1 at k=5); 103 more sit at k=1-2 and climb
  ~1/day/clip, so coverage matures over ~the next week. Merged already (BL-581), so nothing more to push.
- **clockworks-TT $0.09 = the honest floor plus slideshow-500s.** Genuine LamaTok-miss rescues and the
  slideshow gap BL-579's `/v1/media/by/id` fallback targets (still SHADOW; not live). Small.
- **A minor NEW observability finding (not a new cost leak):** the ledger **over-counts** api-scraper
  (170 rows vs ~90 billed runs) while under-counting apidojo-IG to zero. Net, no dashboard built on
  `apify_usage_entries` can see the real spend. Fix is one line: make `fetchApidojoInstagramSingle`
  (apidojo.ts:537) call `logActorRun` like the api-scraper tier does. Named for a later round, not this one.
- No brand-new cost leak: every surviving dollar maps to the known 616 gone-leak or the known TT floor.

## PART 4 — did we forget to push anything? (direct answer: NO finished Apify fix is stranded)
- **BL-580** (the cooldown-immune 616 skip + slideshow shadow): **ON MAIN** (merged this morning via BL-581,
  commit e6a7b85d). ahead=0. The one finished Apify fix is shipped.
- **BL-579** (the slideshow `/v1/media/by/id` fallback that would actually flip a live value): **not on
  origin** — a live in-progress round, correctly unshipped until its shadow proof passes. Its saving is
  pending by design, not forgotten.
- **BL-574 / BL-577**: not on origin (live in-progress rounds).
- Every other unmerged `checkpoint/*` branch that touches a scraper path touches only a **doc** (BL-482,
  BL-549, BL-550, BL-567 audit reports) or is non-Apify (BL-573 growth). None strands an Apify code saving.
- The **734-clip retirement** is data awaiting the owner's `reports/BL-580-signoff.sql`, not a branch. NOT run here.

## PART 5 — verdict, floor, and the ordered next step
- **Real floor:** ~$0.10/12h (~$3-6/month) = genuine TT LamaTok-miss rescues + the rare genuinely-transient
  IG. It cannot reach exactly zero without breaking live clips (an unresolvable live clip must return NULL,
  never 0).
- **Killable tonight:** ~$0 additional from code — BL-580 is already merged; only the counter maturing and
  the retirement sign-off move the number. Nothing else is waiting to be pushed.
- **After the counter matures (~1 week):** the 616 skip covers the repeat-gone clips (most of the 80/day
  apidojo-IG runs are the same deleted reels re-polled), removing an estimated **$0.5-0.7/12h**.
- **After the 734-clip retirement sign-off:** the confirmed-dead clips leave the poll rotation entirely, so
  they are never looked up again. This is the decisive kill for the residual.
- **Projected monthly:** now ~$40/month (at ~$1.30/day) → after counter maturation ~$10-15/month → after
  the 734 retirement ~$3-6/month floor.
- **Exact ordered next step:** (1) OWNER runs `reports/BL-580-signoff.sql` under its own verified round
  (biggest single kill, retires the 734). (2) Let the gone counter keep maturing (already live on main). (3)
  Finish BL-579's slideshow shadow proof, then merge its `by/id` fallback to close the TT slideshow floor.
  (4) One-line ledger fix: `logActorRun` in `fetchApidojoInstagramSingle` so the dashboard can finally see
  the spend it is currently blind to.

## Safety
Read only. No Apify actor run. No code, config, data, or money change (one doc added on branch
checkpoint/BL-582). No prior claim inherited unverified: the +02:00 offset, the apidojo-IG ledger blindness,
and apidojo.ts being a pure non-re-deriving function were each re-checked against live data / source this round.
Nothing a live round holds was touched. Every probe disclosed; ids matched by time-cluster, not row totals.
