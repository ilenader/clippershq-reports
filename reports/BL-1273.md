# BL-1273 — what is actually broken, today

**Audit only. No product code was written.** $0.00 spent — no paid call was made by this round or
by any of the ten sub-agents it ran. Everything below was verified against the tree as it stands
on 2026-08-15, not inherited from the report that first said it.

The last audit of this kind was ~15 rounds ago. Every round since has left items in its closing
lines. This round harvested **139 open items from the last 26 reports**, verified each against the
tree, swept six recurring defect families for current instances, classified the suite, and checked
that nothing of his is at risk.

**The single most useful result is not any one defect.** It is that *nine* of the items still being
carried forward as "known broken" are either already fixed, mis-stated, or aimed at the wrong
thing — and two of the six "known open items" in the brief are among them. A list that is not
re-verified rots at roughly one item per two rounds.

---

## THE TOP TEN, RANKED BY WHAT THEY COST HIM

| # | What | Cost | Status |
|---|---|---|---|
| 1 | **989 outreach emails open `Hi there,`; every TikTok one names the wrong platform** | the revenue surface; free to fix *now*, expensive after the first send | STILL TRUE |
| 2 | **723 hand marks exist on exactly one disk** — 110 commits unpushed, `5e03fa8` on no remote | the only human ground truth in the project | NEW |
| 3 | **Both shipped briefs name 15 scored pages with their verdicts**; the leak guard points at a disjoint set | every dialogue number measured on a 18–29% contaminated arm | NEW |
| 4 | **Nothing builds the TikTok send file** | 8 paid addresses stranded and growing; 0 of 124 reach `[B]` | STILL TRUE |
| 5 | **`run.json` still exports after `discover()` returns** | a kill loses 100% of findings; the ledger fix landed 35 lines away | STILL TRUE |
| 6 | **RUN HEALTH says "source may be dry" when every API well errored** | he loosens filters and spends again on a dead API | NEW |
| 7 | **The Repost-accounts download is a frozen Aug-12 snapshot nothing rebuilds** | 13 confirmed DM targets missing; the gap is widening | REAL suite red |
| 8 | **Instagram profile double-buy** — the one-call fix exists with zero callers | $1.71 and ~70 min per 5,700 pages | PARTIALLY TRUE |
| 9 | **`found_via` blank on 70.1% of 2,688 pages** | no discovery channel can be compared; **85.1% recoverable for $0.00** | STILL TRUE |
| 10 | **Four dashboard charts render blank instead of "unavailable"** | spend, leads, CPL — the go/no-go numbers | NEW |

---

## 1. THE SIX KNOWN OPEN ITEMS — VERIFIED

### 1.1 `tiktok_send_list.build()` has no caller — **STILL TRUE**, and worse than stated

`build()` is at `clippershq/tiktok_send_list.py:93`, `write_csv()` at `:199`, and the only entry
point is `main()` reached from `if __name__ == "__main__":` at `:259`. Repo-wide there is **no
shipping caller** — the three hits are two comments and one throwaway in `scratch/bl1219/send.py`.
`clippershq/exporters.py:270-273` *reads* the built file and its own empty-state message says
*"No TikTok send file yet - run clippershq/tiktok_send_list.py"*. The code instructs the operator
to do by hand what every sibling does automatically (`control.py:3452-3474` wires
`all_bot_ready.build()`; `meme_send_list.py:327` is wired).

**Today:** master holds **124** `tiktok_pages` rows, all with an email. `output/TIKTOK_PAGES_BOT_READY.csv`
holds **115**, mtime 16:43. Of the 9-row gap, one (`@aceof_spadess`) is *correctly* excluded — it
belongs to the meme partition, and the three-way split at `:106,126-129` is doing its job. **The
other 8 are stranded**, all `date_added=2026-08-14`, and all attributable to one run:
`output/tiktok_sheets/bl1260_volume/run.json` records `master_appended: 8`, `billed_calls: 416`,
`billed_usd: 0.2496`, finishing at 21:32:39 — matching master's mtime exactly.

**The harm is wider than "8 stranded."** `[B] BUILD MY SEND LIST` does not rescue them:
**0 of the 124 tiktok master emails appear in `output/ALL_BOT_READY.csv`** (3,978 rows, rebuilt
21:57 — *after* master grew, so this is not staleness). All 124 carry `tiktok_handle` with
`verdict`, `curation` and `handle` missing, so none can pass the verdict gate in `bot_ready_rows`.
The manually-run script is the **only** path this funnel's output has to a send surface.

Fix cost: one free command.

### 1.2 The `run.json` export still sits after `discover()` returns — **STILL TRUE**

`clippershq/tiktok_finder.py:1454` calls `discover()`; the export is at **`:1491-1506`**, 35 lines
below it. Nothing inside `discover()` (defined `:1096`) writes a findings row — the only in-loop
disk writes are the contact-sheet PNGs at `:730` and `:783`. Two siblings share the shape:
`_append_to_master` at `:1476` (**master itself is all-or-nothing**) and `seen.record_many` at
`:1253`, inside `discover()` but after its loop. A *cap hit* breaks cleanly at `:1244-1246` and
reaches all three — the exposure is kill-only. `run.py:664` throws the return value away, as its
own comment at `:700` says, so nothing upstream can rescue `res`.

**The contrast with the fixed write is the finding.** The ledger checkpoint is *inside* the loop —
`reserve()` at `:1412-1425`, flushing every `SPEND_CHECKPOINT_CALLS = 25`. It is working right
now: `spend.json` shows campaign `TIKTOK_FINDER` with **33 rows / 816 calls / $0.4896**, 32 of them
`tiktok_finder_checkpoint`, all dated 2026-08-14, none before.

So a kill today costs **≤$0.015 of accounting** and **100% of findings**. Direct evidence on disk:
the killed 19:03 run bought 309 requests ($0.1854) and left *no* `run.json`; the run that ended on
a cap hit left 249 rows. Killing that one a page early would have lost 249 verdict rows, 135 bought
profiles, 14 emails, 8 master appends, **$0.2496**, and the seen-set advance — so a re-run re-buys
every page.

The fix landed on the cheap artifact and not the expensive one, 35 lines apart in one function.

### 1.3 Instagram profile double-buy — **PARTIALLY TRUE**; the claim overstates it

Both sites are live in the shipped funnel (`run.py:116-118` registers `meme_pages` →
`meme_finder.run_funnel`):

* **the check** — `meme_finder.py:2877` `profile_facts(...)`, buys `media_count` for Rule 2 and the
  post-floor gate. Fires on **every walked page**.
* **the fetch** — `meme_finder.py:2987` `resolve_contact(...)`, buys the email/bio-link. Sits inside
  the `else:` of `if not verdict["is_meme_page"]:` at `:2983`, so it fires **only on pages that pass**.

Both bottom out in the same `GET /v2/user/by/username` (`ig_client.py:550-556`). So it is **2 calls
per *passing* page, not per page** — rejected pages buy exactly one. That is why $1.37 reproduces
exactly: $1.37 ÷ $0.0006 = 2,283 wasted calls per 5,700 pages = BL-1230's 40.9% pass rate.

At today's rate (**1,342 / 2,688 = 49.9%** in `meme_pages_seen.json`) and today's unchanged
`ig_api.cost_per_call_usd = 0.0006`, the same 5,700 pages waste **2,844 calls = $1.71**, and
~70 min (or ~108 min at the shipped `DEFAULT_RPS = 0.44`).

**The fix exists, is verified, and has zero callers.** `profile_and_contact` at `meme_finder.py:2666`
composes both halves over one response; `scratch/bl1232/verify_onecall.py:87` asserts
`new_calls == 1 and old_calls == 2` across 10 payload shapes. Every reference outside its own
definition is in `scratch/bl1232/`, `reports/`, or tests. No cache dedupes the pair: `ig_client.py`
has no response cache, and `resolve_cache.py` is a cross-run *negative* cache storing no payload.

**A finding inside the finding:** `spend.json` records batched rows only — `meme_finder.py:2824` sets
`configure_autoflush(..., every=25)`, and rows carry no per-call or per-endpoint detail. **The ledger
structurally cannot confirm or refute a per-call waste claim.** That constrains every future cost
audit, not just this one.

### 1.4 `found_via` unrecorded on 70.1% — **STILL TRUE**, reproduces to the digit

**1,885 blank of 2,688 = 70.1%** in `meme_pages_seen.json["pages"]`. Not master — that file has no
such column, and its `source_hashtag` is populated on 62,092 / 62,092 rows.

| file | pages | set | blank | % |
|---|---:|---:|---:|---:|
| `meme_pages_seen.json` | 2,688 | 803 | **1,885** | **70.1%** |
| `tiktok_pages_seen.json` | 374 | 374 | 0 | 0.0% |

All 1,885 blanks share **one key-signature** — `{band, first_seen, judged, passed, round, walked}` —
and come from exactly two writers, both scratch harnesses writing to the *main* seen file:

* `scratch/bl1232/judge.py:180-181` → **1,604** pages (`round: BL-1232`)
* `scratch/bl1230/judge_stage_b.py:186-187` → **281** pages (`round: BL-1230`)

Every *shipped* writer sets it correctly (`meme_finder.py:3000`, `tiktok_finder.py:1254-1258`, which
is why TikTok is 100%).

**85.1% of the hole is free to close.** `scratch/bl1232/size.py:142` already wrote `found_via` into
`sized.jsonl` — **3,650 rows, 100% populated**. `judge.py` reads those exact rows (`:53`) and simply
does not copy the field forward. A handle-join recovers **1,604 of 1,885** at $0.00, taking the blank
rate **70.1% → 10.5%**. The remaining 281 are not recoverable from their own corpus.

The reader is `exporters.py:873` — `sn.get("found_via") or "UNRECORDED"`. At 70.1% UNRECORDED the
four discovery channels cannot be ranked on yield, pass rate, or cost-per-passing-page — so
BL-1230's conclusion that *"the money should go to discovery"* rests on the 29.9% minority that
happens to carry the field.

### 1.5 553 string durations — **STILL TRUE, exactly 553**; and it is not durations

The field is **`elapsed_s`** — seconds-per-page in the judge verdict logs — **not** a clip duration.
`clip_library/*.jsonl` is clean: 9,741 rows, **0 strings**. Run through the repo's own audit tool
(`scratch/bl1244_recorder.py audit`), today:

| file | rows | STRING | NUM |
|---|---:|---:|---:|
| `bl1240_iter1/2/3`, `bl1240_volume` | 213 | 213 | 0 |
| `bl1242_iter4`, `bl1242_iter5` | 100 | 100 | 0 |
| `bl1242_rejudge874` | 240 | 240 | 0 |
| **total (7 logs)** | **553** | **553** | **0** |
| `bl1244_live20`, `bl1244_model_ab`, `bl1251_qwen` | 227 | 0 | 227 |

**The damage is wider than recorded:** the same rows carry `followers`, `posts` and `following` as
strings on **550** rows, and `tiles_seen` / `low_band_tiles` on **340**.

Writers are fixed (`bl1240_record.py:144`, `bl1242_record.py:266`, `bl1244_recorder.py:244-245`
bounds-checked, `bl1259_record.py:89`). **No migration exists and none was ever written** — that was
a deliberate call at `reports/BL-1255.md:152`: do not rewrite another round's committed artefacts,
point consumers at a coercing accessor instead. `tools/migrate_derived.py` has zero matches for
`duration`/`elapsed`.

The downstream cost is family 4 landing on family 3's data. `bl1244_recorder.py:187-189` type-checks
with `isinstance(..., (int,float))`; run today it prints `rows: 240  non-numeric elapsed_s: 240` and
then **prints no distribution at all** — a silent empty stat, not an error. And
`scratch/bl1240_export.py:52` maps the raw string into a delivered column titled **"Seconds"**, so
his CSV sorts lexically and will not sum.

### 1.6 `bl1242_rejudge874.jsonl` stopped at 240 of 874 — **STILL TRUE, unmoved**

240 lines, 240 distinct handles, 0 duplicates. Denominator is `REJUDGE_874_ALL` in
`scratch/bl1242_queue.json` = **874**. Running the shipped reporter read-only:

```
python scratch/bl1242_record.py done --out scratch/bl1242_rejudge874.jsonl \
    --queue scratch/bl1242_queue.json --set REJUDGE_874_ALL
→ n: 240   key_used: REJUDGE_874_ALL   total: 874   done: 239   remaining: 635
```

(240 rows vs 239 matched — one row is off-list.) **Resume works but is a report, not a driver:** no
loop auto-skips done ids; a human runs `done`, takes `next_10`/`shard`, hands it to an agent. The
`.progress.json` marker BL-1255 added shipped *after* this stall, and **no `.progress.json` exists
anywhere in the repo**.

**The remaining 635 is free.** Browser judging makes no metered call (`reports/BL-1242.md:6`); the
$0.0522 booked under campaign `BL-1242` is all `bl1242_detector_eval`. Six downstream consumers
(`bl1246_gate`, `bl1247_build`, `bl1257_select`, `bl1257_controls`, `bl1248_throughput`) inherit
**27.5% coverage**.

### 1.7 Campaign is per-module — **PARTIALLY TRUE**, and the defect is not where the claim puts it

Module constants are real: `tiktok_finder.py:112` `TIKTOK_FINDER`, `meme_finder.py:57` `MEME_FINDER`,
`clip_pipeline.py:3705` `memebot`, `clip_label.py:58`, `editor_brief.py:103`. But three other shapes
coexist — call-site literals (`control.py:1668,2148,2436,2883,3209`), operator-chosen per-run
campaigns (ZHUS / PANICBABY / STRAENGE / DAYLIGHT / ANIME15K via `main._execute_run`), and never an
env var.

The two funnels that collapse are **`clip_render` and the paste path**, both → `"memebot"`
(`run.py:291`, `paste_batch.py:1305`). Today that bucket holds **905 rows / $1.07435**:
`paste_batch` 605 / $0.3630, `clip_pipeline_ig` 249 / $0.2394, `clip_pipeline` 17 / $0.0732, plus
`editor_brief` and eight scratch labels.

**The correction:** `label` **is** recorded — on **2,826 of 2,844 rows** — and it *does* separate the
two in the raw JSON. The defect is that **no shipping consumer reads it as a grouping key**.
`run.py:295` `_spend_now(spend_path, campaign, since_epoch)` has no label parameter at all;
`dashboard/server.py:1625` uses `r.get("campaign") or r.get("label")`, so campaign always wins.

**And the cost is worse than "two funnels aren't separable" — it corrupts a spend cap.**
`_spend_now(path, "memebot", t_start)` is called at `run.py:407,538,722` for both the status card
*and the per-run cap delta*, so **any paste batch a dashboard user starts mid-run counts against the
render run's `clip_pipeline_max_run_usd`**. `dashboard/server.py:1692-1697` confirms `run_id` is
present on **0** of the 2,844 rows, so there is no second key to fall back to.

---

## 2. THE SIX RECURRING FAMILIES — CURRENT INSTANCES

### Family 1 — a field name that no longer matches its writer: **7 current instances, all silent**

Denominator so the negatives count: **3,248** JS property reads across 10 files, **139**
renderer↔endpoint pairings, **274** `DictReader`-bound column reads across 16 files, **2,667** JSONL
field-read sites across 19 modules, **1,700** python `.get("x", default)` sites. Every finding was
checked against the real file on disk.

**All seven are the silent variety** — a `.get()` default or a JS `undefined`. None raises. None
ever will.

**1. 989 outreach emails open `Hi there,`.** `meme_send_list.py:221` and `tiktok_send_list.py:153`
read `display_name` → `full_name` → `name`. Neither fallback is a master column — they are
in-memory API field names that were never persisted. Measured directly:

```
output/MEME_PAGES_BOT_READY.csv    rows=874  display_name_filled=0
output/TIKTOK_PAGES_BOT_READY.csv  rows=115  display_name_filled=0
```

`send_guard.REQUIRED_COLUMNS` checks column *presence*, not fill — **the guard built to catch this
cannot see it.**

**2. Every TikTok email names the wrong platform.** Verified on disk: the TikTok file has
`profile_url` and no `profile_link`; the meme file has the opposite.

```
TIKTOK_PAGES_BOT_READY.csv  profile_url=True   profile_link=False
MEME_PAGES_BOT_READY.csv    profile_url=False  profile_link=True
```

`tiktok_send_list.py:70` writes `profile_url`; `message_template.py:1170,1179,1181,1198` reads
`profile_link` four times. At `:1198`, `_platform(g("profile_link")) or "short-form"` falls through,
so every TikTok send reads *"I found you through your edits on **short-form**"* while the URL one
column over says TikTok. Handle recovery and the agency `ref` fallback die the same way. **Meme sends
are unaffected** — this is TikTok-only.

**3. `exporters.py:410` — a 3.4× over-count in the registry whose own commit headline is "COUNT
RECORDS, NOT ROWS".** It folds drop marks on `d.get("path") or d.get("clip_id") or len(recs)`. The
only writer, `dashboard/dropmark_routes.py:184`, has never emitted either — the journal's identity is
`(song_id, hook_id)`. Both miss, the key falls to a unique counter, and the declared last-wins fold
is a **complete no-op**: **82 marks reported against 24 real windows**, every superseded mark shown
beside its replacement, and 26 of the 82 are `action` entries that are not marks at all.

**4. `/api/outcomes` returns `NOT_ENOUGH_DATA` permanently while 94.1% of the data is present.**
`outcome_loop.py:561` reads `duration_s` / `clip_len_s`; the ledger writes `source_duration_s`
(1,106 of 1,175) and nested `gate.duration_s` (547). Both reads return nothing on **all 1,175**
records → one `unknown` bucket → `duration_band` can never answer. **A reader-side rename fixes it.**

**5. Three TikTok columns are structurally always empty.** `tiktok_send_list.py:162-164` reads
`avg_views`, `videos_observed`, `promo_in_bio`; `tiktok_finder._append_to_master:1302` writes 8 keys
and drops all three (master's nearest names are `avg_views_sampled` / `videos_sampled`). **115/115
blank.** The data is measured upstream at `tiktok_finder.py:513` and thrown away at the append — so
the reach evidence for ranking and pitching the TikTok list never arrives.

**6. `content_genre`** — same permanent `NOT_ENOUGH_DATA` at `outcome_loop.py:553`, but genuinely
absent: it is a `clip_library` field (9,741/9,741) that `clip_pipeline._base_record()` never copies
onto the ledger. 0 of 2,007 lines. Needs a **writer** change, not a rename.

**7. `accounts.html:195` reads `render_rows`; `repost_export.py:285` writes `render_lines`** (its
*local variable* is named `render_rows` — the page was written against the variable name).
`esc(undefined)` returns `''`, so the footer renders *"the render ledger is &nbsp; rows but 1069
records"* — the blank falling in the exact sentence whose purpose is the rows-vs-records
distinction. Real value: **1,890**. Born mismatched in `c664b62`; the page's five tests never check
that the keys it reads exist.

> **Why this family keeps recurring.** A naive "is this name written anywhere in Python?" sweep
> returns **zero** findings — it would have missed BL-1249 itself, and it misses 6 of these 7. Each
> one required pairing the *specific* reader to the *specific* writer on its own path. Any future
> guard that greps globally will report green forever.

Adjacent: `vision_tooling_gen` is written on 93 `clip_library` rows but is not in `CLIP_FIELDS`, so
`clip_library.field()` returns `None` for it forever — the sixth recurrence of that documented trap.
`review_marks` has **three writer shapes** (the server path stamps `source`/`row`/`key`;
browser-download files carry no `source`; `BL-1257-FRESH100_marks.jsonl` carries neither `row` nor
`filter_verdict` nor `reason`), so any union keyed on `row` or filtered on `source` silently drops
whole sheets.

### Family 2 — a guard that cannot fail

Denominator: **344 test files** (318 discovered by `run_all.py` — 292 `tests/`, 16
`memebot/scraper/tests/`, 10 `memebot/meme/tests/` — plus 3 support and 7 excluded scratch files) and
**21 guard scripts**.

**The zero-collection instance is interpreter-dependent, which is why it survived.**

```
py311  python -m unittest tests.test_config_contract  -> "Ran 0 tests ... OK"   rc=0
py313  python -m unittest tests.test_config_contract  -> "NO TESTS RAN"         rc=5
```

Ten files are module-level `def test_*` with no `TestCase`, hiding **6, 83, 77, 28, 28, 20, 13, 9, 8
and 8** tests respectively. Python 3.12+ fixed the silent OK — but **3.11 is the interpreter with
PIL/numpy**, i.e. the one that can actually run the vision suites, and there it still exits 0.
*Mitigated in practice*: all ten have a `__main__` runner, `run_all.py` runs each as a script, and
`_emit` (`tests/run_all.py:398`) fails any suite reporting zero checks. **No file is currently
green-while-testing-nothing under the real runner.**

**Tautologies:** `test_manifest_prose_refused.py:201` `assertTrue(True)` (declared, in a class named
`ReportedNotFailed`, but it inflates the check count) and `test_clip_audio_fields.py:86`
`assert not CA.eligible(rec)[0] or True` — unconditionally true, and it sits in the test whose own
comment names the uncovered case. **Cleared:** all 8 `assertEqual(f(x), f(x))` sites evaluate the call
twice — genuine determinism checks, not the `src_line.text == src_line.text` shape. **That instance
is dead.** No `try: assert… except: pass` anywhere in 344 files.

**Self-fulfilling setup — the canonical shape is live.** `tests/test_input_population.py:93-99`
clears `PRODUCER_FUNCS`, injects a hand-made key, then asserts the keys are tuples. The producer
(`scratch/mb090_population.py:228`) never runs — reverting it to the exact bare-name collision this
file's docstring says it pins leaves the test **green**. Same class:
`test_silent_zero_fixes.py:277-304` replaces `server._read_ledger` wholesale in both arms, so the
INFRA-025 fix inside `_read_ledger` can be reverted and both pass; and
`test_bounce_entry_point.py:53-65` is satisfied by a **docstring** at `run.py:741` mentioning the
name, so deleting the real call at `:774` keeps it green — the identical defect BL-1220 named and
BL-1223 fixed by AST walk.

**A skip rendered as a PASS — and two of the preconditions fail today.** Five sites turn a missing
fixture into an affirmative `[OK ]` instead of a loud skip, breaching `docs/TESTING.md` rule 6:
`test_funnel.py:1913-1916`, `test_ocr_features.py:158-159`, `test_song_library.py:693,712,724`, and
`test_song_library.py:907` — the last a true self-disarm, since the store is built *inside* the test,
so a change that parks everything reports a pass.

* **`_probe_samples/` is gitignored with 0 files in HEAD.** So `run_all.py --head` — which the
  runner's own docstring calls *"the only run that describes what anybody else actually gets from a
  clone"* — runs `test_funnel.py`'s REAL PROBE **as a reported pass**, and silently skips 4 tests in
  `test_clip_walk.py`, 2 in `test_harvest_accounts.py`, and a class in `test_envelope.py`. Same for
  `output/ALL_BOT_READY.csv`: 3 tests in `test_send_identity.py`, 2 classes in `test_send_superset.py`.
* **Default `python` (3.13) has no PIL/numpy/pyyaml**, so `test_ocr_features.py:159` **fires today**
  and the behavioural half of the BL-736 36.4%-vs-81.8% regression guard prints `[OK ]` without
  executing.

Also live: `test_envelope.py:370,382` are unconditionally `@unittest.skip`("the per-client flush clock
was reverted") — proven by `OK (skipped=2, expected failures=1)`.

**Four guards fail open, and every one is the same shape — a staged-set or registry reader returning
an empty collection on failure:**

| guard | line | failure | consequence |
|---|---|---|---|
| `tools/verify_claims.py` | `:548-550` | `git diff --cached` rc≠0 → `return []` | `main --enrolling` prints "no manifest would be newly enrolled" and returns 0; **`pre-commit:27` waves the commit through**. Index-lock contention across 5 live rounds is the realistic trigger. |
| `tools/claims_read.py` | `:178`, `:201-203` | discards the `torn` flag | prints **`FREE` on a torn registry**, rc=0 — while `docs/PRECONDITIONS.md:248` tells operators to run exactly this and expect `FREE`. A round starts writing a file another live round holds. |
| `tools/write_point_guard.py` | `:126-141`, `:311` | no store → `{}` fingerprints | **structurally cannot refuse in a clone** (`LEAD_STORES` are all gitignored, 0 in HEAD); prints "…0 in a gitignored lead store", returns 0. Its own `_selftest` handles this correctly at `:238-240`; `main` has no equivalent. |
| `tools/repo_guard.py` | `:487-489` | rc discarded → `int(behind or 0)` = 0 | a git failure becomes "not behind" and the push proceeds at `:494`. `git(..., check=True)` exists at `:105-106` in the same file, unused here. |

**And the guard that exists cannot fire in this repo's normal state:** `tools/guard_amend.py`'s
cross-round refusal (`:232-238`) is gated on `this_round()` returning an id, which needs
`GUARD_AMEND_ROUND` (set nowhere but a test) **or exactly one live claim**. There are **6**.
*The multi-round condition the guard exists for is the condition that disables it.*

Guards with no non-zero exit at all: `silent_zero_sweep.py` (survey only — enforcement lives in
`tests/test_silent_zero_shape.py`), `sweep_stale_markers.py:236` (`return 0`, **and no caller**),
`scratch_gc.py`, `env_audit.py:469` (returns 0 with `rep["parse_errors"]` non-empty). Orphans with
good refusals and zero callers: `tools/clone_check.py:110`, `tools/gen_manifest.py --check:189`.

Hook wiring is sound: `core.hooksPath = tools/githooks`, `pre-commit` reads `$?` with no pipeline at
all three checks (`:33,:58,:85`).

### Family 3 — a predicate that never fires, reading as a clean negative

Denominator: **892** `isinstance(` sites (172 numeric guards), **6,445** `ast.Compare` nodes, **211**
judged-page records, **62,092** master rows.

**The known instance is FIXED, and the fix was measured.** `clippershq/presort.py:74-101` now has
`_as_number()` handling digit strings and `1.5K`/`2.3M`. Evaluated offline over **19,097**
author-shaped records, old `isinstance(v,(int,float))` vs fixed:

| signal | before | after |
|---|---:|---:|
| `follows_1500_plus` | 51 | **125** |
| `ratio_under_5` | 55 | **87** |

All four signals now fire. Honest denominator: only **914** of the 19,097 carry `following` at all,
so the real rate is 125/914 — **the limiter is now field absence, not the type bug**.

**The three BL-1240 drawn-text rules are still dead**, re-measured today at 0/100 on the frozen sample:

* `caption_is_promotional` (`meme_finder.py:279-281`) — **0/100, 6/211**; `PROMO_DRAWN_ONLY=True`
  (`:981`) counts it only when `source=="top_text"`, still 0/25 after BL-1239 wired the reader
* `narrates_action` (`:704-718`) — **2/211**, and `:1667-1673` deliberately deleted its rejection, so
  it **structurally cannot reject a page**
* `looks_like_dialogue` (`:1483-1486`) — **1/211**, display-only, cannot gate

**Newly found dead:** `ocr_features.py:237 account_features()` and its OCR cue banks; `filters_free.py`
is *half*-dead — `recent_ok():61` is wired from `main.py:1782`, but `view_ok():52` and
`follower_ok():75` have no callers; `creator_signals.py:81,99,120,133` and `promo_vocab.py` are
documented non-ships that fire 0 times ever.

**Seven shipped `master_leads.csv` columns are populated 0 of 62,092 (0.00%):** `vision_verdict`,
`vision_confidence`, `paid_partnership_frac`, `repost_frac`, `audio_reuse`, `median_comments`,
`median_caption_len`, `views_hidden_frac`. On the 56 crawl rows that could carry them
`deep_posts_checked` is blank 56/56 — **the deep-vet branch at `crawl_suggested.py:661` has never
succeeded once.** And the `niche_flag = "DEEP_UNKNOWN"` marker set at `:700` **has no column in
master**, so the one flag that would separate "deep-check failed" from "no instrumentation" never
reaches the CSV.

**One live type asymmetry:** `crawl_suggested.py:560` writes `followers_from_feed` **raw** while
`:786` writes the same value only `if isinstance(..., int)`; `writer.py:1968-1974` uses bare `int`
for followers/following/total_likes/media_count but `(int,float)` at `:1966` for `avg`. Four
producers feed these uncoerced (`crawl_suggested.py:435`, `main.py:2563`, `ig_follower_scrape.py:189`,
`discovery_search.py:214`). A float or `"1.5K"` reaching `:1968` becomes a **blank CSV cell**,
indistinguishable from "the API never returned it". Latent today (56/56 pass), guaranteed the moment
any of those four returns a non-int.

**Clean negatives:** zero `== True`/`== False` across 6,445 comparisons; the 93 `is True`/`is False`
are legitimate tri-state checks; only 3 ordered comparisons against an uncoerced `.get()`, all safe.
All 10 judge disqualifiers **do** fire (`business` 36, `creator` 34, `dialogue` 14 … `promo` 2, /211)
— but **`dialogue` is measured anti-predictive** (precision 0.500 against a 0.560 base). It fires and
must not become a gate. Read that alongside Family 5 below.

### Family 4 — a failure that reads as a benign zero

`tools/silent_zero_sweep.py` **is** called — by `tests/test_silent_zero_shape.py:192-216,273`, which
enforces the BOUNDARY rule over an explicit **14-file** `GUARDED` list (`:56-73`).

**The guard is green on its 14 files. There are 75 violations of the identical rule outside them**,
across 181 files / 1,330 `except` handlers. The guard is not broken; **its scope is.**

Ranked by what it costs him:

1. **`clippershq/main.py:1538-1540` — a total API failure reads as "source may be dry."** The early
   return skips the well-error check at `:1568-1572`. It surfaces at `main.py:5203`, in the
   `=== RUN HEALTH ===` block printed **last, as the takeaway**. If every source well errors (caught
   per-well at `:3066-3084`), he is told to loosen his filters when the API was down — **and he
   spends again.**
2. **Four dashboard charts render blank instead of "unavailable."** `dashboard/server.py:3995-3996,
   4017-4018, 4030-4031` populate `out["unavailable"]`; `dashboard/static/app.js:882,893,899,916`
   bypass the `chartRows()` helper at `:869-878`, and **`series.unavailable` is read nowhere in
   `app.js`**. It hits `chSpend`, `chLeads`, `chLeadsFunnel`, `chCPL` — the go/no-go numbers. The
   file's own comment at `:855-860` says this must never happen; the fix landed for 2 of 6 series.
3. **`clippershq/ig_client.py:287` — the rate limiter fails open.** `except (OSError, ValueError):
   nxt = 0.0`. A torn shared-slot file means **zero wait**, so every process fires immediately at the
   paid API.
4. **Five dedup caches read empty on a torn file → re-spend on already-seen pages:**
   `repost_finder.py:447`, `clip_runner.py:285`, `caption_finder.py:303/604/1210`,
   `resolve_cache.py:51`.
5. `send_suppress.py:138` — the suppression list empties silently (documented as deliberate, but the
   failure mode is "nothing hard-excluded on MX grounds").
6. `repost_export.py:192-196` — `except Exception: pass` on `clips_rendered`, which surfaces as a CSV
   column at `:307` **and drives the row sort** at `:278`.
7. `review_sheet.py:132-137` returns `{}`, so `:2017` renders *"0 page(s) were excluded"* when the
   summary is unreadable.

### Family 5 — in-sample results presented as general

**The headline: both briefs shipped today name 15 scored pages with their verdicts, and the guard
built to prevent exactly this is pointed at a disjoint set.**

`scratch/bl1269_leakguard.py:78` hardcodes `ground_truth/BL-1257-FRESH100_marks.jsonl` (100 handles),
and `tests/test_bl1269_presort.py:97` does the same. **That set has zero handle overlap with
`ground_truth/review_marks.jsonl`** (669 rows / 623 unique) — measured, `|H100 ∩ HRM| = 0`. The guard
is structurally incapable of seeing a leak from the larger corpus.

Re-running the guard's own `find_leaks()` against the union of all scored sets (953 handles):

| brief | leaks vs his 100 | **leaks vs review_marks (623)** | status |
|---|---:|---:|---|
| `bl1242_brief_v4.md` | 0 | **15** | scored the 86.7% |
| `bl1258_brief_v5.md` | 0 | 5 | |
| `bl1258_brief_v6.md` | 0 | 0 | clean |
| `bl1260_brief_addendum.md` | 0 | 0 | clean |
| `bl1262_brief_v7.md` | 6 | **15** | the known case — 21 total, not 6 |
| **`bl1265_brief_v8.md`** | 0 | **15** | **SHIPPED (Instagram)** |
| `bl1260_brief_tt.md` | 0 | **15** | |
| **`bl1270_brief_tt2.md`** | 0 | **15** | **SHIPPED (TikTok)** |

The 15: `_diddy_oilup_`, `_ravelab_`, `absolutftbl`, `delivermeme`, `flash.aez`, `hexamv`,
`historyyshack`, `hoodculturevoice`, `ivorclips`, `meme_.graveyard`, `memezar`, `roastoday`,
`scenetoprint`, `supermovie.clips1928`, `technobangersdaily`.

They are not passing mentions. Several carry his verdict *and* the verbatim evidence — *"`hexamv` —
every line on a white strip above the picture. Nothing low."*, *"You rejected `@_ravelab_` and
`@technobangersdaily` on `business`."*

**Where it actually bites — and where it does not.** The scoring queue is built from
`review_marks.jsonl` (`reports/BL-1242.md:55`); all 15 are in `bl1242_queue.json`'s `truth` map:

| arm | n | of the 15 | share |
|---|---:|---:|---:|
| `D` — **the n=50 that produced 86.7%** | 50 | **0** | **0% — this number is clean** |
| `E` | 50 | 1 | 2.0% |
| `DIALOGUE_CHIP` | 34 | **7** | **20.6%** |
| `DIALOGUE_MENTION_WANT` | 7 | **2** | **28.6%** |
| **`F_DIALOGUE_TARGETED`** | 50 | **9** | **18.0%** |

So the headline **86.7% [73.8, 93.7] survives**. **The dialogue numbers do not.** BL-1242's
*"25.0% [7.1, 59.1] → 58.1% [40.8, 73.6]"* and *"pages he WANTS despite dialogue | 7 | 71.4%"* are
measured on arms where roughly one page in five is named in the judge's own instructions with the
answer written out — and `reports/BL-1248.md:359-371` promotes precisely that instrument to the
operator: *"Run at the current instrument."*

Combined with Family 3's finding that **`dialogue` is anti-predictive** (0.500 vs 0.560 base), the
dialogue work rests on a contaminated arm *and* a signal that underperforms chance.

**A shipped module claims a validation that was a refutation.** `clippershq/presort.py:23` says
*"(BL-1261, re-validated out of sample by BL-1269)"*. BL-1269 did not re-validate it — it measured
**124 killed of 922, 13 of 148 wanted (8.8%)**, verdict *"DOES NOT SHIP"* as a gate. And `:34-36`
leads its MEASURED block with the flattering in-sample number in prose (*"kills 28 of 81 rejects,
0 of 19 WANTs"*) while the damaging one is a **bare file pointer** — *"see `scratch/bl1269_oos.json`"*.
The out-of-sample labels are also weaker and the module does not say so: that file's own label reads
*"the JUDGE's own verdict (no human marks exist here) -- ADVISORY about his taste"*. In-sample is
human marks; out-of-sample is judge verdicts. `reports/BL-1267.md:164` still carries the refuted
line — *"Shippable, $0.00, 100% precision, 0 WANTs lost"* — unannotated.

**16 shipped thresholds are tuned on labels with no held-out number**, including
`CREATOR_EXEMPT_FOLLOWERS = 300000` (n=94, five splits with no n per split),
`DEFAULT_MAX_DIALOGUE_SHARE = 0.10` (**n=2 fires**, CI [34%, 100%]), `MIN_TOTAL_POSTS = 13` (n=33,
**positives only, no rejects**), `TOP_BAND_MAX = 0.40` (n=42, module says *"FITTED ON 42 LABELS, AND
THAT IS THE LIMIT"*). Six more have **no denominator at all**. **Two are provably unable to fire on
their own evidence:** `DEFAULT_MAX_DIALOGUE_SHARE` (BL-1210: *"fires on 0 of 623 pages — never"*) and
`INFOGRAPHIC_BAND_H_MIN = 0.30`, whose two labelled positives measure **0.233 and 0.147**.

**And the whole `quality_gate` surface — ~120 numeric config keys — has no labelled provenance
anywhere in 143 reports.** Grepping every one for `faceless_score_pass|cut_garbage_threshold|
editor_ratio_pass|likes_ratio_pass|follower_hard_min|…` returns **0 hits**. One is unexplained:
`cut_garbage_threshold` is **41** for ZHUS (`config.json:603`) and 40 everywhere else, with nothing
on disk saying why.

**The good news:** none of these contested numbers reached the operator. `README.md`, `OPERATING.md`,
`docs/**`, `dashboard/**` grep clean for every one of them. They live in `reports/` and module
docstrings only.

### Family 6 — a hidden file

**Live and costing accuracy now.** `clippershq/tag_yield.py:49` `LOG_GLOB = "*.log"` → `:70`
`glob.glob(...)`:

```
scratch/*.log   glob=382   listdir=394   MISSED=12  (3.0%)
```

Twelve dot-prefixed logs (`.bl1240_suite.log`, `.infra022_server.log`, …). This module's own
docstring at `:52-58` exists to prevent *"a ratio whose numerator and denominator were measured over
different populations, which looks like a finding and ranks like noise."* **Its exposure denominator
is doing that today, by 3.0%.**

**A glob over a handle-named directory, in the merge that produced BL-1270's headline.**
`scratch/bl1270_rejudge.py:40` globs `output/bl1270_rejudge/agent*/*.json` — directories holding one
file **per handle**:

```
glob=175   listdir=176   MISSED=1  ->  output/bl1270_rejudge/agent01/.boovkanio.json
```

The verdict inside is **`WANT`**. The round survived only because a human noticed and hand-wrote a
duplicate under a safe name. **The rescue was manual; the call site is unchanged**, so the next batch
of per-handle agent writes hits the same wall.

**The original hidden sheet is still on disk and still invisible.**
`output/tiktok_sheets/20260814_190349/.boovkanio.png` — `glob("*.png")` returns **34**, `os.listdir`
returns **35**. The *writer* is fixed (`tiktok_finder.py:696-716` `sheet_filename()` prepends `_`,
pinned by `tests/test_bl1260_tiktok_run.py:262-281`) and both readers were converted to `os.listdir`
— **but the pre-fix directory was never renamed**, so any shell glob or new script over that path
reads 34 of 35 of a paid capture.

**Today's rate, and the shipped figure is wrong.** `tiktok_finder.py:703` says *"roughly 1.6% of them
do (2 of the 125…)"*. That file now holds 374 handles, and the real population rate is:

| source | n | dot-prefixed | % |
|---|---:|---:|---:|
| `master_leads.csv` tiktok_handle, platform=tiktok | 55,232 | **1,291** | **2.34%** |
| `tiktok_pages_seen.json` | 374 | 4 | 1.07% |
| all Instagram surfaces (`meme_pages_seen`, `repost_seen`, master, marks) | 13,414 | **0** | **0.00%** |

**It is a TikTok-only hazard** — Instagram forbids a leading period, so every IG surface is
structurally safe. The quoted 1.6% is **32% low** against a denominator that has since tripled.

`dashboard/`'s three dotfiles (`.running.json`, `.cleared_runs.json`, `.watching.json`) are **clean**:
nothing globs that directory; they are read by exact path only. The one remaining production glob
whose pattern is operator-supplied is `tools/claim.py:914` — a claim declaring
`output/tiktok_sheets/*/*.png` gets a wrong `last_touch` mtime. **No `Path.glob`/`rglob` exists in
`clippershq/`, `dashboard/` or `tools/` at all.**

---

## 3. THE SUITE, CLASSIFIED

`pytest` is **not installed anywhere on this machine.** The runner is `tests/run_all.py`, which
spawns each suite as its own process. Run once, serially, on **system Python 3.11.9 by full path**:

```
FAILED -- 3 red of 318 suite(s) (2281.7s)
315 PASS, 3 FAIL, 0 TIMEOUT, 0 errored, 8,061 checks
```

Discovery is by walk and **already includes the nested dirs** — 292 `tests/`, 16
`memebot/scraper/tests/`, 10 `memebot/meme/tests/`. **There is no CI skip today.** The 79
`test_*.py` under `scratch/*/tree*/` are stale worktree copies, correctly excluded.

| test | verdict | evidence |
|---|---|---|
| `test_filelock.py::test_resolve_cache_concurrent` | **TIMING** | **Passes standalone 3/3** (5.2s). In-run 14.6s (2.8×) with `PermissionError: [WinError 5]` on `os.replace` in `%TEMP%`. 3 rounds in flight; peers respect `filelock.file_lock`, so the handle was a non-peer (Defender/indexer). |
| `test_ocr_environment.py::test_the_engine_is_the_installed_package…` | **ENVIRONMENTAL** (stale) | `assertIn(".venv", where)`; engine resolved to system `site-packages`. **Passes 5/5 under `.venv`.** The load-bearing half — not resolving via `scratch/bl1061_pkgs` — **passes**; the vendored-tree risk is gone. |
| `test_ocr_environment.py::test_the_provider_set_has_not_changed` | **ENVIRONMENTAL** (stale) | Baseline `{opencv-python, opencv-python-headless}`; machine now has `{opencv-contrib-python, opencv-python-headless}`. Still two overlapping providers, so the `pip uninstall` landmine **is not cleared** — it changed identity. |
| `test_repost_export.py::test_both_files_exist_and_have_one_row_per_account` | **REAL** | `327 != 340`. Deterministic standalone; the only failure of 24 in that file. |

**LIVE-ROUND MID-EDIT: zero reds — and the premise in the brief is stale.** `clippershq/run.py` and
`clippershq/tiktok_finder.py` are **no longer modified**; 17 commits landed after the snapshot's HEAD,
committing them via BL-1259/1260/1268/1270. The only dirty shipped-code file anywhere is
`memebot/scraper/edit.py`, and its entire diff is a **test artifact** —
`def BL921_WORKING_TREE_ONLY_MARKER(): pass`, appended by `tests/test_verify_claims.py:59` and
normally removed in a `finally`. Residue of a killed run. **Worth flagging: if any round commits
`edit.py` while that marker is present, `test_verify_claims.py` goes permanently red — that has
happened three times** (BL-1226, MEMEBOT-092/094).

### The named "environmental pair" — both are GREEN, and the diagnosis was wrong

`memebot/meme/tests/test_ocr.py` **PASSED** (154 checks) and `test_render.py` **PASSED** (91 checks).
They fail only under `.venv`, reproduced exactly:

* `test_ocr` → **missing package `pytesseract`**. *Not the binary*: `tesseract.exe` **is** installed at
  `C:\Program Files\Tesseract-OCR\` and `ocr._locate_tesseract({})` finds it.
* `test_render` → **missing package `fonttools`**. *Not the font*: `C:\Windows\Fonts\seguiemj.ttf` is
  present (12.4 MB). `memebot/meme/render.py:30` does `from fontTools.ttLib import TTFont` inside a
  bare `except`, so without fontTools no glyph coverage is read and no emoji run is ever detected.

Both are listed **optional and deliberately unpinned** at `requirements.txt:95`, so any `.venv` built
from that file will never have them.

**The sharpest finding: no interpreter on this machine makes the suite green.** Under system 3.11 the
memebot pair passes but `test_ocr_environment` fails; under `.venv` `test_ocr_environment` passes 5/5
but the memebot pair fails; 3.13 lacks PIL/numpy/pyyaml entirely. **Three interpreters, three
different red sets** — which is exactly what made these look like "pre-existing reds all week" across
BL-1209/1212/1214/1215/1227.

### `test_repost_export` — the four-round red is REAL

**Refuted: it does not build its own fixture.** It drives the real shipped module over real on-disk
data — `clip_library/*.jsonl` (55 files), the render ledger via the one accessor
`clippershq/ledger.py`, `repost_rejections.jsonl`, and two `scratch/` discovery files.

**Confirmed: it excludes `master_leads.csv` by construction** — but that is a *deliberate, documented
property of the shipped module* (`repost_export.py:47-54`: `sources()` omits it because the lead file
mixes 59,016 people with pages and has no separating `lead_kind`). The test verifies it **empirically**,
wrapping `io.open` and `builtins.open` around a real `build_rows()` and asserting no byte is read from
the lead file. **That check passes and is meaningful.** It is not vacuous, and it is not the failure.

**The failure is a live operator harm.** `clippershq/exporters.py:63,221` registers `repost_accounts`
in the dashboard download registry with `build_repost_accounts()`, which merely `_read_csv`s the
frozen export — **it never rebuilds**. Nothing calls `repost_export.export()`: no caller in
`clippershq/`, no menu entry, and the CSV is gitignored. Last rebuild: **BL-1235 on 2026-08-12**
("rebuild both stale exports"), stale again within two days.

The gap has been **288/300 → 288/309 → 327/340** across four rounds. **It is widening**, and each time
it was correctly observed to be "not mine" and moved past. He downloads **327 DM targets when the
system knows about 340**, from a file with no staleness indicator.

Fix: make `build_repost_accounts()` call `repost_export.build_rows()` instead of reading the frozen
CSV. Stopgap: one free `python clippershq/repost_export.py`, which also turns the suite green.

---

## 4. NOTHING OF HIS IS AT RISK ON DISK — BUT NONE OF IT HAS LEFT THE MACHINE

### The marks

| set | file | count | sha256 | committed | matches HEAD |
|---|---|---|---|---|---|
| **100** | `ground_truth/BL-1257-FRESH100_marks.jsonl` | **100** rows / 100 handles | `151d5d7d5aa392d1…` | **yes** — `5e03fa8` | **byte-for-byte** |
| 100 | `output/BL-1257-FRESH100_marks.jsonl` | 100 | same bytes | no (`.gitignore:98`) | identical to committed |
| **623** | `ground_truth/review_marks.jsonl` | **669 rows / 623 handles** | `3b06a611a55c2a92…` | yes (HEAD holds 154) | **+515 / −0** |
| 623 | `ground_truth/review_marks_merged_BL-1210.jsonl` | 623 / 623 | `ccb6f521…` | **yes** — `374d61b` | exact |
| — | `review_marks.jsonl` (repo root) | 51 rows / 49 handles | `cda0d75f…` | **untracked**, not ignored | stale fragment; all 49 already inside the 623 |

Counts were taken three ways (`wc -l`, per-line `json.loads`, `set()` on `handle`) and agree. **The
"623" is a distinct-handle count over 669 rows** — and its `row` field is *not* a key (424 distinct
values across 669 rows), the same rows-vs-records confusion that inflates the badge elsewhere. The
two sets are **disjoint**: 0 overlap, union **723**.

**Commit `5e03fa8`'s claim verified on all three axes** — 100 rows, 100 distinct handles, sha256
`151d5d7d…`, and 97 GOOD / 3 BAD. **The historical "no committed copy because output/ is gitignored"
is closed for the 100.**

**The modification is a pure append — 0 lines lost.** `git diff --numstat` = 515 insertions, 0
deletions; the HEAD blob is a **byte-exact prefix** of the worktree file (`head -154` LF-normalized
hashes to the HEAD blob exactly). Worth recording why that needed proving: `core.autocrlf=true` with
**no `.gitattributes`**, so a raw line-comparison shows *zero* matches and looks catastrophic. It is
CRLF, not corruption. Anyone auditing these files must normalize first.

**The real exposure — and it is the one action worth taking from this audit.**
`BACKUP_THESE_6_FILES.md` covers **none** of the marks files (grep for `review_marks`, `ground_truth`,
`FRESH100`, `marks` → 0 hits), justifying the omission at line 184: *"Anything in git — the code is
safe once pushed to a remote."*

**That premise does not hold right now:**

```
git rev-list --left-right --count @{u}...HEAD   ->  0   110      (unpushed on this branch)
git rev-list --count origin/main..HEAD          ->  737
git branch -r --contains 5e03fa8                ->  (empty)
```

**`5e03fa8` is on no remote branch at all.** The only human ground truth for the 100 exists in three
places — the working tree, the ignored `output/` copy, and local `.git` objects — **all on the same
C: drive**. The 623 is marginally better off (a byte-identical `.bak` in `ground_truth/backups/`),
on the same disk. One `git push` closes this.

### Claims and orphaned work

**Nothing staged** (`git diff --cached` empty), **no stashes**, **one worktree**. The seven-file
shared-index incident is resolved.

**No claim holds paths that are provably gone.** Every absent declared path returns **0 commits in
all history** — never written, not written-then-lost. `will_write` is a declaration of intent, so
absence is the normal in-flight state.

**Two claims are genuinely orphaned — abandoned, not broken.** **BL-1262** (289 min) and **BL-1265**
(169 min) both *shipped commits* (`137e379`, `5e03fa8`) and neither has a `reports/` entry —
`reports/` jumps BL-1261 → BL-1263 → BL-1264 → BL-1266. Eight later rounds have since started *and
finished*. Both are past `STALE_AFTER_MIN` (45) but under `STALE_TAKEOVER_MINUTES` (480), so neither
is takeable yet. BL-1265's claim covers `ground_truth/BL-1257-FRESH100_marks.jsonl` — **that file is
committed and clean, so the open claim poses no risk to the marks.**

**`round_of_path` is basename-only — confirmed, and the casualties are worse than "directories map to
nobody."** `tools/claim.py:1085` — `base = os.path.basename(p)` — discards every path component but
the last, and `_PATH_ROUND_PREFIX` at `:1042` requires a separator *after* the digits. Consulted at
`rounds_owning:1117-1118`, which feeds `dirty_declared` and the pre-commit `check_staged_single_round`.
Measured by running the real function over the real paths:

| form | result |
|---|---|
| top-level `outputs_*/` dirs | **25 of 25 map to NO round** — `outputs_` is a prefix the anchored regex cannot see past |
| `scratch/` subdirs, trailing slash | **290 of 290 map to nothing** — basename of a trailing-slash path is `""` |
| `scratch/` subdirs, bare | 197 map; **93 do not**, of which **71 carry a round id** |
| **any file inside a per-round dir** | **always `None`** — the id lives in a parent component line 1085 throws away |

The same directory maps or does not purely on a trailing slash (`output/bl1270_rejudge` → `BL-1270`;
`output/bl1270_rejudge/` → `None`) — **and `BL-1271` currently declares `scratch/bl1271_curve/`, the
invisible form.** This is a known defect deferred three times (`BL-1234.md:162,184`,
`BL-1236.md:398`, `BL-1238.md:281`). One caution for whoever fixes it: the `.claims/` exclusion at
`:1082-1083` must survive, or `claim.py end` can never release a round — claiming would create the
dirt that blocks the release. Pinned by `tests/test_staged_cross_round.py:55-71`.

**Orphaned work.** 1,692 untracked entries: **737 map to a round across 206 rounds, but only 28 belong
to a live claim** — so **709 entries across 202 departed rounds are owned by nobody**. A further
**955 map to no round at all**, which is where the basename defect bites:

| directory | files | size | maps to a round |
|---|---:|---:|---|
| `outputs_bl1156_drop/` | 2,279 | **1.8 GB** | **No** |
| `outputs_teach_20260805_151235/` | 685 | 638 MB | **No** |
| `outputs_teach_20260805_132600/` | 305 | 423 MB | **No** |
| `outputs_paste_*` (12 dirs) | ~2,900 | ~1.9 GB | **No** |
| `outputs_bl1163_drop_pairs/` | 32 | 26 MB | **No** |

**No marks, no ledger and no human judgement sits unprotected there.** The derived halves
(`*__OURS.mp4`, `_frames/`, 1,902 PNGs) are re-renderable at compute cost. The exposure is the
`*__ORIGINAL.mp4` downloaded source media and the paid-for measurement results in `_batch.json` /
`_frames` manifests: provenance survives (permalinks, spend, per-item refusal reasons), but re-fetching
costs money, depends on posts still being up, and **the stored CDN urls have all expired**. Roughly
**4+ GB of paid-for, not-free-to-regenerate media is unclaimed, invisible to the commit guard, and on
one disk.**

Four unstaged deletions (`scratch/bl1178_{ocr.py,pull.py,standard.md}`, `scratch/bl980_expiry_probe.py`)
belong to departed rounds; all four exist in HEAD and are fully recoverable. **Zero data at risk.**

---

## 5. WHAT THE CARRIED LIST GOT WRONG

Nine items still being carried forward as "known broken" are not, or not as stated. This is the
argument for re-verifying rather than inheriting:

| carried as | actually |
|---|---|
| the memebot `pytesseract` + emoji-font pair is environmental and unfixable | **both PASS** under system 3.11. Missing *packages* (`pytesseract`, `fonttools`), not the binary or the font — both of which are installed. |
| `test_repost_export` is a stale-fixture red that excludes master by construction | **excludes master deliberately and correctly; the red is REAL** and describes a live operator harm |
| the pre-sort isinstance bug disables two signals | **fixed** — `_as_number()` handles digit strings; fires 125 vs 51 |
| `test_config_contract` collects zero and prints OK | **true only on 3.11**; 3.13 exits 5, and `run_all.py`'s zero-check catches it |
| a readback compared `src_line.text` to itself | **dead** — all 8 `assertEqual(f(x), f(x))` sites evaluate the call twice |
| "2 profile calls per page" | **2 per *passing* page**; rejects buy one |
| "553 rows carry string durations" | correct count, wrong field — `elapsed_s` in judge logs, not clip durations, and **550 rows also carry string `followers`/`posts`/`following`** |
| campaign is per-module so funnels aren't separable in the ledger | **`label` is recorded on 2,826 of 2,844 rows**; the defect is that no consumer groups by it — **and it corrupts a spend cap** |
| live-round mid-edit is causing suite reds | **zero** — `run.py` and `tiktok_finder.py` were committed 17 commits ago |

And one carried *fix* that was a refutation: `presort.py:23` claims BL-1269 "re-validated" the
pre-sort out of sample. BL-1269 **refuted** it.

---

## Preconditions

| check | result |
|---|---|
| product code written | **none** — this round wrote `reports/BL-1273.md` only |
| spend | **$0.000000** — no paid call by this round or its ten sub-agents |
| `config.json` | parses, **164 keys**, **5 campaigns** |
| campaigns fingerprint | `8e02f8d6f6307ae8` (default) / `7a029ee5447cddd8` (compact) — **unchanged**, both reproduced |
| git index | **clean** — nothing staged, no stashes, one worktree |
| claims | claimed `BL-1273` (BL-1271 and BL-1272 were held); no path conflicts; 6 live at start |
| suite | **3 red of 318**, 8,061 checks, serial, system 3.11 — 1 REAL (`test_repost_export`), 2 stale-environmental, 1 timing |
| his marks | **723 across two disjoint sets**, counted, hashed, both committed, **0 lines lost** |

**A suite count is a moment, not a property — and on this machine it is not even one number. It is
three, one per interpreter.**
