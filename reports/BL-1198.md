# BL-1198 — the dialogue read was never taken: 0 of 4,825 posts had audio measured

**You are right, and the cause is not a threshold. The audio read never ran.** `speech_frac`
is absent on **4,825 of 4,825 posts** in the 425-page sheet, so the audio dialogue rule
abstained on **425 of 425 pages** — silently. The number the sheet printed as "dialogue" is a
different, older, **text-only** heuristic that reads the OCR'd drawn line and fires on **1 of
3,004** non-empty lines. You were shown a text statistic and read it as an audio one. A page
that is 80% talking by ear reporting 0% is exactly what that produces.

Every figure came from **`.venv\Scripts\python.exe` (3.11.9)**. Bare `python` here is 3.13.14
and lacks the packages; it was not used. **Spend: $0.00** against the $1.00 cap — no paid call
was made, because nothing in this diagnosis required one.

---

## 1. THE DIAGNOSIS — all three of your candidates are ruled out by measurement

You asked me to measure rather than assume, and the measurements say it is none of the three.

| candidate | verdict | evidence |
|---|---|---|
| reads only a few seconds of each clip | **not the cause** | the 20 s cap in `extract_wav` is real, but it cannot yield 0% — no audio was read at all for this sheet |
| reads the wrong audio stream | **not the cause** | nothing was decoded for this sheet; there is no stream to be wrong about |
| ~35% xHE-AAC undecodable, scored 0% instead of UNKNOWN | **MEASURED FALSE** | BL-1195 decoded **134 clips** from these same pages: **0 unmeasurable**, 41 genuinely no-speech |

The real chain, four counts, all reproducible via `scratch/bl1198_diagnose.py`:

1. **0 of 4,825** posts carry `speech_frac`. The judging pass that built the sheet never
   listened to anything.
2. The audio gate correctly abstains when nothing is measured — so it fired on **0 of 425**
   pages. Correct behaviour, invisible.
3. The sheet prints `dialogue_share`, produced by `looks_like_dialogue()` over the **drawn
   text**. It requires two `Name:` speaker labels or a long unanswered quote.
4. That rule fires on **1 of 3,004** non-empty drawn lines, so it reads **0.00 on 416 of 425
   pages**.

**This is instance ~19 of this project's most repeated defect**: a measurement that was never
taken rendering as a confident zero rather than as UNKNOWN.

### On the 20 hand-checked clips

The brief asked for 20 clips hand-checked against what VAD said. **I did not do that, and the
diagnosis is why**: for the pages you are marking there is no VAD output to check against —
the field is empty on every post. Hand-checking would have measured my own fresh run, not the
thing that produced the 0% you saw. The decode question those 20 clips were meant to settle is
answered instead by BL-1195's 134 real decodes, at 0 unmeasurable.

## 2. THE ENERGY READ YOU DESCRIBED IS ALREADY BUILT — AND ALREADY FAILED

Before proposing it again: **BL-1195 built and measured exactly this**, ~7 hours before this
round, and published it. `contrast_db = speech_dbfs − bed_dbfs`, scored against all 149 labels
on a held-out split:

| median contrast | |
|---|---:|
| pages you want | **1.9 dB** |
| pages you do not | **2.7 dB** |

**They sit on top of each other.** Best precision anywhere in its sweep was 29.2% [14.9, 49.2]
against a **30.9% base rate** — the lower bound does not come close to clearing. Speech-versus-
music energy, as a page-level gate, is closed with numbers. I did not rebuild it, and
`clippershq/audio_balance.py` is held by that round, so I did not touch it.

What is *not* closed is the same idea at **clip** level scored against **clip** labels — a
different measurement. I did not get to it (§5).

## 3. WHAT SHIPPED — four states, and "unmeasured" is one of them

`judge_page` now returns `dialogue_state`, the only key a caller should render:

| state | meaning |
|---|---|
| `no-posts` | nothing to judge |
| **`unmeasured`** | **we did not listen. Not clean, not talking, not zero.** |
| `all-talking` | every measured clip is speech-led → reject |
| `has-clean` | at least one clean clip → keep |

Plus `dialogue_measured_posts`, `dialogue_unmeasured_posts`, `dialogue_clean_posts`, and the
legacy text number re-exposed under the honest name `drawn_text_dialogue_share` so nothing
breaks while callers migrate. `dialogue_share` is unchanged and still there.

**Your rule is implemented as you stated it** — "if all ten talk, scrap the page; if even one
is clean, keep it" — and pinned by tests, including the case that matters most: one clean clip
among nine unmeasured still rescues the page, because unmeasured posts abstain rather than
counting as talking.

8 new tests in `tests/test_dialogue_state.py`, all passing.

## 4. THE POSTS-PER-PAGE TRADE

Measured over all 425 pages by re-judging at each depth (`scratch/bl1198_cost.py`).

| posts read | pages passed | verdicts changed vs 10 | churn | est. wall clock |
|---:|---:|---:|---:|---:|
| 3 | 223 | 67 | **15.8%** | **83.5 min** |
| 4 | 261 | 47 | 11.1% | 93.0 min |
| 5 | 262 | 44 | **10.4%** | **102.5 min** |
| 10 | 256 | — | 0% | 150.0 min |

The clock model is the one you identified: the paid leg (~55 min) is per **page** and does not
shrink; the OCR leg is per **post** and does. **Five posts buys back 47 minutes for a 10.4%
verdict churn.** Three posts buys 66 minutes for 15.8%.

**The agreement column is not usable and I am not going to quote it as if it were.** Only **12**
of your 161 marks join this 425-page sheet — the rest are on the earlier 100-page sheet. Twelve
pages cannot separate 41.7% from 58.3%. If you want the quality half of this trade measured
properly, mark ~60 pages on *this* sheet and it becomes answerable in one local pass, free.

## 5. WHAT I DID NOT DO

- **No 40-clip three-state hand-labelling, and no clip-level classifier.** The diagnosis
  consumed the round, and it changed what the next step should be: there is no point scoring a
  clip classifier for these pages until the judging pass actually records audio. That is the
  next round's first job, and it is cheap — the decode is free, it is local CPU.
- **The sheet still displays the misleading number.** `clippershq/review_sheet.py` is held by
  BL-1197. The one-line change is to render `dialogue_state` instead of `dialogue_share`;
  the verdict now carries it.
- **I did not re-run the judging pass to populate `speech_frac`.** That is the actual repair
  for what you are looking at, and it needs a decision from you on posts-per-page (§4) so it
  is not run twice.

## 6. THE RECORD

- Preconditions: `tools/claims_read.py --holders` — `clip_speech.py`, `meme_finder.py`,
  `tests/test_audio_balance.py` **FREE**; **`clippershq/audio_balance.py` held by BL-1195**, so
  it was not touched. `ground_truth/review_marks.jsonl` was **' M' (mid-edit — you are marking
  right now)**, so it was read only, never written. Claimed as BL-1198, every filename declared,
  5 other rounds in flight.
- I did not restart your server and did not touch your marks file.
- **Suites: 276 PASS / 2 FAIL under `--jobs 4`, 364.1 s, 7 rounds in flight** (278 discovered).
  Both failures are `memebot/meme/tests/test_render.py` and `memebot/meme/tests/test_ocr.py` in
  the nested `memebot/` repo — foreign to this round and failing before it, unchanged by it.
  `tests/test_meme_finder.py` 70 pass, `tests/test_dialogue_state.py` 8 pass (new).
- Campaigns untouched (`run.py` shows no diff), `config.json` and `config.example.json` both
  parse.
- No lead data in this report: counts and states only, no handles.
