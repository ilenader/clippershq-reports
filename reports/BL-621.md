# BL-621 — Merge BL-620: multiposting moved into the "Submit a clip" modal

**Status: SHIPPED.** Merge commit `9f950eca` is on `origin/main` (verified `origin/main == local HEAD`). Merge only; no new code authored. The owner will now see the new layout once this deploys.

## STEP 0 — TRUTH (with SHA)
* `checkpoint/BL-620` tip = `b0123729f7815bc555edab0b6354f549b0b34b0e`.
* **Genuinely unmerged:** `git merge-base --is-ancestor origin/checkpoint/BL-620 origin/main` returned 1 (not an ancestor). Exactly one commit was on the branch and not on main: `b012372` itself.
* **The merge-base WAS origin/main HEAD** (`adfef927`), so BL-620 = main + 1 commit with zero divergence. (Local `main` was stale at `c784ec33`; I merged against the true origin/main `adfef927`.) The `--no-ff` merge had **zero conflicts** and the diff is exactly the 5 paths BL-620 changed; nothing a live round holds was swept in (the working tree held only untracked `.claude/` + two stray docs, never staged).
* Merge commit `9f950eca` parents: `adfef927` (main) + `b0123729` (BL-620). Correct.

## Conflicts
**None.** merge-base == origin/main HEAD, so git did a clean three-way merge with no manual edits, including the file MOVE (delete `src/app/(app)/clips/BatchSubmitSection.tsx`, create `src/components/clips/BatchSubmitSection.tsx`). No conflict markers in any changed file. BACKLOG.md took BL-620's single appended entry cleanly (1 `BL-620` occurrence, no `$2` capture-group corruption, since nothing had to be resolved).

## Confirmations on the MERGED result
* **The "Submit a clip" modal carries the add-another-clip control and the embedded batch UI.** `SubmitClipPremium.tsx` references "Add another clip" (3), `setBatchMode` (3), and `BatchSubmitSection` (4); `BatchSubmitSection.tsx` has the `embedded` mode (12 references).
* **The old bottom-of-page accordion is GONE, no orphan left.** `src/app/(app)/clips/BatchSubmitSection.tsx` no longer exists (No such file), and `page.tsx` has **0** references to it (no import, no `<BatchSubmitSection>` JSX).
* **Single-clip submission is unchanged and still routes through `/api/clips`.** The single form's `onFormSubmit` still hands off to the page's `handleSubmit` (POST `/api/clips`); the batch route and clipper core are byte-identical, so a one-clip submit is unchanged and every clip lands in the identical per-clip state.
* **BL-601 traps hold:** per-row `row.clipAccountId` (5 references, never a batch-wide account); `processOwnerSubmitLink` referenced **0 times** in the batch route (CLIPPER core only, no auto-approve / earnings write / placeholder minting); case-insensitive platform (the shared `campaign-platforms` helper); server-side per-campaign limit pre-check (`todayCount`, 2), platform constraint (`campaignAllowedPlatforms`, 2), and in-batch duplicate rejection (1) all run before any write; drafts persist per (clipper, campaign) via `/api/clips/batch/draft`.

## Money-file + path safety
**6 money files + `tracking.ts` + `campaign-era.ts` + the clipper core + `clips/route.ts` + the batch route BYTE-IDENTICAL by blob OID** on both refs and on the merged HEAD (`git rev-parse HEAD:$f` == `git rev-parse origin/main:$f`). The verify path is not in the diff (untouched); the tracking path is untouched. No clip's earnings or status can change. No `prisma migrate`.

## Build (real exit codes, from logs, not piped)
`npm ci` exit 0 (wiped the generated Prisma client) → `npx prisma generate` exit 0 → `npx tsc --noEmit` exit 0 → `npm run build` **BUILD_EXIT=0**. Prebuild gates: check:prisma-bypass 0 violations; check:removed-fields OK; hooks gate `eslint --max-warnings 11` = **11 problems (0 errors, 11 warnings)** with eslint present (gate real). `/api/clips/batch` and `/api/clips/batch/draft` both in the route table. tsc and `next build` both ran.

## Ship
Verified push per BL-288: `git push origin bl621-merge:main` (`adfef927..9f950eca`), then fetch + assert `origin/main == 9f950eca`. Tags `pre-merge-bl621` / `post-merge-bl621` pushed.

**Rollback:** `git reset --hard pre-merge-bl621` (`adfef927`) then force-push, or `git revert 9f950eca`.
