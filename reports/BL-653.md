# BL-653 — merge BL-650 and BL-651 to main (both were pushed but not landed)

**2026-07-24 · Merge only.** Two finished branches were on origin but not on main, which is why the owner saw no change. Both are now merged and on main. No money, schema, data or key change.
**Base main:** `91b84410` → **merged main:** `16028f1f` · Tags `pre-merge-BL-653` / `post-merge-BL-653`.

## STEP 0 — truth per branch (with SHAs)

| Branch | Head SHA | On origin? | Genuinely unmerged? | Code diff |
|---|---|---|---|---|
| checkpoint/BL-651 | **21c772d** | yes | **YES** (`merge-base --is-ancestor` exit 1) | **non-empty**: 1 `.tsx` (`CampaignsRedesign.tsx`, 140 lines) + BACKLOG |
| checkpoint/BL-650 | **c9955b83** (code at `8a408e40`) | yes | **YES** (exit 1) | **non-empty**: 6 `.ts/.tsx` (952 insertions) + BACKLOG |

Both carry real code, not a document only (the BL-643 failure mode was checked for explicitly). BL-650's files: `src/lib/creator-scan/screenshot-metrics.ts` (new), `screenshot-ocr.ts`, `screenshot/route.ts`, `ScreenshotAutoRead.tsx` (new), `CreatorScanPanel.tsx`, and the test. BL-651's file: `CampaignsRedesign.tsx`.

**The dirty main worktree at C:/b575, resolved without touching it.** `main` is checked out in worktree `C:/b575`, and that worktree is **DIRTY**: 76 lines of a concurrent round's uncommitted work (`M BACKLOG.md`, `M prisma/schema.prisma`, and deleted `docs/*` and `public/splash/*.png`). Those changes are **uncommitted working-tree edits only**; the `main` ref itself is clean at `91b84410`, equal to `origin/main`. I did **not** stash, revert, or modify anything in C:/b575. Instead I created a **separate clean worktree at `C:/b653`, detached at `origin/main`**, merged both branches there, and advanced `origin/main` by a **fast-forward** (`91b84410..16028f1f`). A fast-forward pushes only new commits on top of the exact base; it never rewrites history and never touches another worktree's files. C:/b575's uncommitted work is completely intact; its local `main` ref is now behind `origin/main` and will reconcile on that round's next pull, which is that round's own concern. **Nothing a live round held was merged or disturbed.**

## The merges (one at a time, verified between)

1. `pre-merge-BL-653` tagged at `91b84410`.
2. `git merge --no-ff origin/checkpoint/BL-650` → `0f25d16f`, **clean, zero conflicts**.
3. `git merge --no-ff origin/checkpoint/BL-651` → **one conflict, BACKLOG.md only** (both branches appended an entry). Resolved as a **UNION**: the three conflict markers (one `<<<<<<<`, one `=======`, one `>>>>>>>`, verified exactly one each) were removed and **both** the BL-650 and BL-651 entries kept in full. No `$2`/capture-group corruption (a literal marker-line deletion, not a regex substitution). Result `16028f1f`.
4. Full-tree conflict-marker scan (`git grep -nE "^(<<<<<<<|>>>>>>>|=======)$"`) and `git diff --check`: **clean, zero markers.**

## Confirmed on the merged result (`16028f1f`, re-verified, not inherited)

**BL-651 — the Completed campaigns row**
* `CompletedRow` is present (`CampaignsRedesign.tsx`, 9 matches for `CompletedRow`/`data-no-swipe`/`scrollByCards`). Desktop arrow controls at `sm+`, edge fades `from-[var(--bg-primary)]` shown only when scrollable that way and gone at each end, `pointer-events-none` so they never block a tap, `data-no-swipe` on the scroll container and the arrows.
* somesome still reads **$9,750.00 of $9,750.00 = 100%**: BL-641's own test re-run on the merged tree, **19 passed, 0 failed** (`LIVE clipper sees $9750.00 of $9750.00 = 100%`, `progress bar reads 100%`, owner keeps `$5444.68 of $9750.00`). BL-535's fully-spent rule intact (`Math.max` control property, PAST-with-no-budget cannot fabricate a full claim). The cent difference from BL-641's $5,445.80 is live view drift, not this merge (no card number is computed here).

**BL-650 — the screenshot reader (re-grepped on the merged tree, not inherited)**
* **No path to any verdict, score or clip status:** `screenshot-metrics.ts` / `ScreenshotAutoRead.tsx` are imported by **none** of `scoring.ts`, `fraud.ts`, `peer-engagement.ts`, and by no other review path (grep empty). The reader has **no** `clip.update`, `.status =`, `writeClipEarnings` or earnings write (grep empty). Structurally incapable of accusing, flagging or changing a clip.
* **Owner-only, never clipper-visible:** the screenshot route is gated by `requireOwnerOrCapability("CREATOR_SCAN_REVIEW")` (three handlers) with the header comment `OWNER / CREATOR_SCAN_REVIEW only. Never clipper-visible.`
* **Parses correctly + flags uncertainty:** BL-650's test re-run on the merged tree, **43 passed, 0 failed** (TikTok/Instagram/YouTube detection, K/M/B expansion e.g. 12.4K→12400, 42% stays 42%, uncertain values flagged).
* **Inert without its key:** `screenshot-ocr.ts` gates on `!!process.env.OCR_SPACE_API_KEY` (line 51); `OCR_SPACE_API_KEY` was **NOT set** by this round (nor any other key). The feature stays inert until the owner sets it, so nothing changes for anyone now.

## Safety and gates (honest)

* **6 money files + `tracking.ts` + `campaign-era.ts` BYTE-IDENTICAL by blob OID** on the merged HEAD vs `origin/main` (and identical on both source branches before merge): writer `7aa6be48`, earnings-calc `797e2098`, balance `e887f80a`, tracking `847dcf70`, middleware `61cef393`, money-decimal `ef5cdae7`, campaign-era `106e16ad`. `tracking.ts` is NOT in either diff. No clip's earnings or status can change.
* No `prisma migrate`. `npm ci` **exit 0** (wiped the generated Prisma client) → `npx prisma generate` **exit 0** BEFORE tsc → `npx tsc --noEmit` **exit 0** → `npm run build` **BUILD_EXIT=0** read from a captured log, never piped through `tail`. `check:prisma-bypass` 0 violations, `check:removed-fields` ok, `lint:hooks` **11 problems (0 errors, 11 warnings)** at the ≤11 cap with **eslint present** (real gate, not a silent no-op). Compiled successfully.
* BACKLOG unioned exact (both entries retained, no capture-group corruption); no conflict markers anywhere.
* No key set. Isolated worktree `C:/b653`, `node_modules` installed there, never junctioned. C:/b575 untouched.
* **main pushed and origin == local HEAD `16028f1f`** (fast-forward `91b84410..16028f1f`, verified by `git ls-remote`/`rev-parse origin/main` == local HEAD; the safe-push branch-verify form could not be used because `main` is checked out dirty in C:/b575, so the equivalent origin==local assertion was run directly with retries). Tags `pre-merge-BL-653` / `post-merge-BL-653` pushed.
* No dashes as bullets.
* **Rollback:** `git revert -m 1 16028f1f` (BL-651 merge) and `git revert -m 1 0f25d16f` (BL-650 merge), or `reset --hard pre-merge-BL-653` then re-push.
