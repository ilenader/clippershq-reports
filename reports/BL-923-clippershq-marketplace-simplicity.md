**Nothing unremovable.** Every bl923sbx- row is gone: 28 ledgered ids plus 94 rows the app wrote for sandbox people, and a sweep of 341 id columns and 3 address columns finds 0. Two things to know: production sent two owner alerts to the sandbox OWNER's undeliverable `.invalid` address (disclosed below), and 3 real notifications vanished during the window for reasons I could not find. The evidence says this round did not remove them.

# BL-923: the marketplace a person can read on a phone

Base `main` @ `78d12570`. Branch `checkpoint/BL-923`: `0b3c1bd7` (the work), then `b91ca6ec` (the fixes the proofs found). Merge `2f87742c`. Tags `pre-BL-923`, `post-BL-923`, `pre-merge-BL-923` and `post-merge-BL-923`, all pushed and verified (`safe-push`: origin == local, three times). The merge tree `759a39d7` equals the branch tree, so build #2 on the branch IS the merge build. `checkpoint/BL-723` is not an ancestor. **Ships to everyone, not behind isTestUser: these surfaces were already live.** Railway deploys `main`; confirm `/api/version` reads `2f87742c`.

## What a clipper now reads, side by side

| Where | Before | After |
|---|---|---|
| Home tiles (poster) | "Post a clip: Put somebody else's clip on your own account." / "Your money: What you have earned here, and what is still coming." / "Change what you do: Move between making clips and posting them." | "Post a clip" / "Money" / "Change what you do" (the locked switch keeps its sentence) |
| Above the campaign list | "Every campaign the owner has opened appears here. Posting a clip joins you to it, so there is nothing to join first." | (deleted) |
| Campaign card | name as a link, "Instagram", "You earn $0.09 per 1,000 views on your own post here.", "19 clips you have not posted, 0 clips you posted, 1 clip you skipped. You are not on this campaign yet, and you do not need to be." | the campaign's picture (or none), name, platform icon, **"You earn about $9.00 per 100,000 views"**, quiet "19 clips you have not posted • 0 clips you posted • 1 clip you skipped", one button **"See clips"** |
| Clip card | "Not posted yet. You can still earn 45 percent.", "Instagram", "You earn ~$0.09 per 1,000 views", "That is 45 percent of what YOUR post makes. What other people's posts make does not change yours.", "Post this clip: <title>", "Skip this clip: <title>" | picture, title with "Not posted", platform icon, **"You earn about $9.00 per 100,000 views"**, **"Post"**, **"Skip"** |
| Clip page | "Read this before you download" box (4 sentences), "Post it to Instagram. A post on anything else cannot be counted for this campaign.", "~$0.09 per 1,000 views" plus the 45 percent sentence | the campaign's rules above step one; **How to post this**: 1 "Open the file in Google Drive and download it." 2 "Choose which of your accounts you will post it from." 3 "Post it to that account on Instagram or TikTok." 4 **"Come straight back to this page and paste the link here. Nowhere else counts."**; "You have 30 minutes from the moment your post goes live to paste the link here. Downloading and editing can take as long as you like. Miss it and only this one post goes uncounted."; "The same clip on two of your own accounts is fine and earns on both. The same clip twice on one account is refused."; picker options "@handle on Instagram" with the platform icons beside it |
| Rules, first time | (none inside the marketplace) | "Rules for this campaign" open: "Read these before you post. Posts that break them do not get paid.", the lines, **"I have read these rules"** |
| Rules, afterwards | (none) | a quiet "Campaign rules" link that reopens the lines with "You have read these." |
| No rules document | (none) | "This campaign has no written rules yet. Nothing is blocked: you can post on this campaign now." |

**Words to learn what he earns and what to press** (home tiles + one campaign card + one clip card, names and titles excluded): **136 before, 44 after.** Money and buttons alone: 50 before, 18 after.

## PART 1 (from the first session, re-verified)
• **`campaign-rules.ts` restoration, confirmed first:** blob `006c4d1a` on disk == `main` == `78d12570`. **My mistake:** in the first session I overwrote BL-602's shadow rule evaluator `src/lib/campaign-rules.ts` instead of creating a new file. tsc caught it, I restored it byte for byte from git, and this round's code lives in `src/lib/campaign-rules-seen.ts`.
• 36 campaigns: 19 carry `cardImageUrl`, 4 carry `imageUrl`. Of the two marketplace campaigns, ANGIE BROWN has a picture and "Marketplace test campaign" has none. **20 of 36 campaigns have no rules document** (16 have one). Of the 2 marketplace campaigns, 1 has none (the owner's test campaign), and it blocks nobody.
• The 30 minute window runs from the post going live on the platform: `clip-freshness.ts:103` (Instagram), `:152` (TikTok) and `:178` (YouTube) compare `Date.now()` with the provider's publish time. The walkthrough says exactly that.
• No "has read the rules" record existed anywhere, so `campaign_rules_seen` was created (`scripts/migrations/BL-923-campaign-rules-seen.sql`, applied in the first session, **not applied again**). Real rows at open 0, at close 0. **Real rows this code will write after deploy: one per person per campaign, when a clipper presses "I have read these rules" or ticks the ordinary page's requirements box. Nothing else.**

## PARTS 2 to 6: what was built
• **Money:** `posterRateUnrounded` feeds both `posterRatePer1k` and the new `posterRatePer100k`, which is rounded once, at the end. No screen multiplies anything: three client fallbacks of `yourRatePer1k * 100` were removed and the field is required. The screens show only the poster's own figure: no gross, no maker's leg, no percentage beside a gross figure.
• **Cards (PART 3):** tiles are a label and an icon, and both paragraphs are deleted. A campaign card shows its own picture or none (a picture that fails to load removes itself), the name as a heading, the platform as an icon plus a spoken sentence, one large figure, the quiet counts in BL-920's shape (a dot to the eye, a comma to the ear), and ONE button named "See clips <campaign>".
• **Rules (PART 4):** `CampaignRulesPanel` sits under the campaign page's h1 and above step one on the clip page, reads the server's answer (so nothing flashes unlocked), and is never a dialog. The post route answers **409 `CAMPAIGN_RULES_NOT_SEEN`** after the side lock and before the link is touched. Only the post route asks: browsing, listing, opening, skipping, the money pages and the per campaign side lock are untouched, and the deleted platform wide gate was not rebuilt. **Both doors write the record:** the marketplace panel, and the ordinary campaign page through one shared setter (see mistake 3).
• **Walkthrough (PART 5):** the four steps and the one-line rules above. The account picker is the same native select, with the platform icons beside it, "@handle on Instagram" options, and a description it is linked to. **The submit path's validation is unchanged.** The only new refusal it can meet is the rules precondition PART 4 required, and G2 and G3 show the URL refusal is unchanged word for word.
• **PART 6:** closed drawers take **0 of 40 Tab stops** (both panels `inert`). **All 8 sidebar sections use CountPill.** The final review moved the digits to dark-on-accent at 5.86:1 (they were 3.40:1). A ninth badge, Help Requests (OWNER only, not a SidebarSection), still has bare digits and is logged.

## PART 7: proofs, each with what made it pass
• **Per 100,000, exact (`bl923-per100k.ts`):** 36 campaigns across 7 distinct real CPMs, 0 mismatches against round2(unrounded × 100), and 0 against 100 × the per 1,000 figure computed the same way (rounded once from the unrounded rate). Three real CPMs show why this matters: 0.5 × 0.45 = 0.225, so the page shows $22.50 where 100 × $0.23 would print $23.00; 1.5 gives 0.675, $67.50 not $68.00; 2.5 gives 1.125, $112.50 not $113.00. **8 of 36 campaigns would have printed a wrong amount by the rounded route.** Also 0.2 gives 0.09, $9.00 (ANGIE BROWN) and 1 gives 0.45, $45.00. The sandbox CPMs, chosen not to divide evenly, render as 0.37 → **$16.65** (not $17.00), 0.19 → **$8.55** (not $9.00), 0.23 → **$10.35**, 0.41 → **$18.45**.
• **No subtraction (`bl923-no-subtraction.ts`, 4 of 4):** every key of all three poster payloads was walked (67, 88 and 33 keys). The only money keys are `yourRatePer1k` and `yourRatePer100k`, and no CPM, gross, budget, fee, owner figure or other earner's share appears. The walker catches a planted `clipperCpm` and `editorShare`, so it can fail.
• **Rules gate (`bl923-gate-proof.ts`, 22 of 22 on the final build, one person per path, every link `example.com`, so a misfire could never reach a vendor):** G1 unread poster refused 409 with the typed code and no row written. G1d the same request after reading meets the unchanged URL refusal. G2 a reader gets the URL refusal. G3 no-rules campaign blocks nobody. G4 an unread poster lists, opens, skips and loads both pages, all 200, with nothing written. G5 missing campaign 404, id over 64 characters 400. G6 banned: refused, 403 from `checkBanStatus` on the first run and 401 on the final one, because `auth.ts:768` re-reads the row after 5 minutes and `:827` ends a banned session. G7 signed out 401. G8 two presses through both doors write ONE row and keep the first `seenAt`. G9 a maker is refused first by the side lock (`OTHER_SIDE_ON_THIS_CAMPAIGN`, 403, never 409). G10 once read, the page is told so. **0 of 19 responses were 429.** Setup checks S1 to S4 are real rows, not flags I minted.
• **Renders (`bl923-render.mjs`, production build, minted Auth.js cookies, bypass off), 162 checks at 320/375/414/1280/1440,** with innerWidth and the URL read back, pan measured by scrolling (**0 px everywhere**) and `--bg-page` 0 on every page. A 59/59: pictures present, absent (no picture fields) and broken (404 removed itself), longest name, one control, one figure, quiet counts. B 45/45: rules unread, the confirm pressed in the UI (focus to the link, announced, the server agrees), rules read after a fresh load (not asked again), reopened, no rules, empty catalogue, longest name. C 58/58: the clip page unread with four steps, the window, the duplicate rule, the picker and the pointer; Submit while unread refused with focus on the confirm, the link not marked wrong and **0 post requests sent**; no rules, pending picture, absent picture with its reason, longest title; **the ordinary page's box ticked in the UI, and the marketplace reads false before and true after.** 3 parallel subagents (**cap 3, no DB connection opened by any**) also looked at the pictures. Extra: `bl923-server-refusal-probe.mjs` 4/4 (the server's own 409 path by keyboard lands focus on the confirm; 6 visible pills at 5.86:1). `bl923-obscure-probe.mjs` 9/9 (a focused confirm or Submit is never under the phone's bottom bar).
• **Money files:** by `git rev-parse HEAD:<path>` == `78d12570:<path>`: `5b40d49e`, `00410634`, `67c30c89`, `672d2ab3` (tracking.ts), `61cef393`, `ef5cdae7`, all IDENTICAL, and no money file appears in the diff.
• **Invariants, full population, open and close (15 of 15):** 10,242 live clips, 0 breaches, 0 negative. 39 v2 maker rows, 0 breaches. 0 double pay. 21 budgeted campaigns, 0 over. Paid is final for maker and poster (0, 0). **ANGIE BROWN: one row, $2,700.00, ACTIVE, spent $7.28.** **Both reconciliation forms return 0 rows.** Clip earnings, clip status, campaign state, agency, v2 maker, v2 platform and payout fingerprints are all identical at open and close.
• **Guards:** every prebuild guard passes, including `check:cron-wiring`, `check:css-tokens` and `check:page-titles`. **New `check:v2-rules-gate`** (R1 the post route asks, refuses with the code, after the side lock and before the link; R2 no other API route asks; R3 both doors write; R4 the 100,000 figure comes from the unrounded rate; R5 no file multiplies a rounded per 1,000 by 100; R6 the Premium page every clipper gets receives the recording setter). `bl923-guard-demo.mjs` **8 of 8**: each rule broken alone fails exactly that rule; a comment and `* 1000` do not trip it; the tree is restored with sha256 equal each time. The 11 BL-678 guards are untouched. **Hooks gate: 0 errors, 10 warnings against a cap of 11.** The brief said 11; main and branch both measure 10 in the same 8 files, so this round changed nothing there and there is 1 warning of headroom. eslint is present. tsc: 0 errors. Builds were run from logs with the exit code echoed: #1 0, a failed attempt (my proof script's two redundant comparisons, fixed), then 0, 0 and 0.
• **Accessibility:** the accessibility lead reviewed twice. The first review's 6 blockers were applied. The final review's 1 blocker (pill contrast) and 3 minor points were applied: the stagger restored, "or" not "and", the confirm focus retrying on the server's 409. Targets are 44 to 48 px, focus rings are visible (`V2_FOCUS`), the campaign picture has `alt=""` beside its heading, icons are hidden with words beside them, and confirm, refusal and load are announced.
• **Connections:** the server ran with `connection_limit=1`; at most 2 connections were open at once (server plus one script). No vendor call was made and no Apify actor was run. 0 `SERVER_ERROR` rows in the window.

## Teardown
Opening snapshot at T = `2026-09-23 08:19:24.91+00` (db now `::text`). Removed: 12 users, 4 campaigns, 6 v2 clips, 6 accounts (the 28 ledgered ids), plus rows the running app wrote for sandbox people: 58 activity events, 2 notifications, 1 poster state, 1 rules row, **10 `user_lifecycle` rows** (no relation to users, so no cascade; the lifecycle cron made them) and **2 send-ledger rows**. Closing snapshot at the same T: users, campaigns, accounts, campaign accounts, v2 clips, posts and poster states, rules rows, clips, activity and audit are all identical in count and id fingerprint. **Notifications: 15,614 → 15,611.** The only app paths that delete notifications are the 04:00 retention cron (no run in the window, per `cron_runs`) and the user cascade (the users fingerprint is identical). My teardown selects and re-checks every row by the prefix. Unexplained, and logged.

**Disclosed: production owner alerts reached the sandbox OWNER.** "[Clippers HQ] Cron tick processed nothing" (send ledger `cmudu1ls`, 08:20:03) and "Payout overdue (18h late)" (`cmudufev`, 08:30:48) fan out to every OWNER-role user, so each went to `bl923sbx-…@example.invalid` as well as to the real owner (3 real sends in the window). The provider accepted both copies, and `.invalid` delivers to no one. The lesson is in BACKLOG: no sandbox OWNERs.

## Every mistake of mine, and every check that passed for the wrong reason
1. The `campaign-rules.ts` overwrite, above.
2. **Three gate checks passed for the wrong reason:** the world file keyed clips as `clip_pic-<suffix>`, every post hit a clip that does not exist, and the URL refusal looked like the gate letting him through. S4 now proves every clip id is a real row.
3. **The ordinary door wrote nothing.** `campaigns/[id]/page.tsx` sets `const isTestUser = true`, so every clipper gets `CampaignDetailPremium`, and the legacy checkbox I wired renders for nobody. R3 passed anyway. The render found it; one shared setter now serves both, and R6 guards it.
4. I moved the card's stagger into a class no utility defines, which switched the animation off for everyone. The final review caught it.
5. Harness faults, each fixed and re-run: the counts check merged visible and spoken text; a card shot matched "Rules campaign" inside "No rules campaign"; `getByRole('checkbox').first()` found a disabled checkbox; one B run's first 320 shot measured 848 wide with a 33 px toggle (unstyled; not reproduced, and the harness now waits for the stylesheet).

Follow-ups for the owner are in BACKLOG BL-923 (7 items). `## BL-` entries: 231 → 232 (BL-723 has no entry of its own). Worktree `C:\w\b923` removed after this report.
