# BL-750 — Fix NOT applied: three agents are writing every ~30s and one took a `pre_bl749` backup of config.json. Item 1's fill rate re-measured through the real guards: 8.0%, not 10.0%.

**Date:** 2026-07-31 · **Type:** Blocked — gate · **Spend:** $0.00
**No file edited. No backup written. No new quota spent.**

---

## The gate

| condition | result |
|---|---|
| zero repo writes for 10 continuous minutes | **FAIL** — 7 write events in the 10-minute window, and 8 more in the 4 minutes after |
| no funnel process | **FAIL** — PID 31216 still up (1,193 min) |

**On PID 31216 I did not stop.** You assessed it as parked at a menu and tied clearing it to
the *paid* audits; this round is edits plus a free sample. I re-verified it: **0.00% CPU over a
25-second sample, all 13 threads in `Wait`, CPU 7,117.5s → 7,117.5s.** It has consumed roughly
one CPU-second in twenty hours. It is not what stopped this round.

**What stopped this round is a live edit collision on `config.json`.** At **10:06:04** another
agent wrote:

```
backups\config.json.20260731-100548.pre_bl749.bak
```

A `pre_bl749` backup of the exact file items 4 and 5 require me to modify. Writes have
continued since, from at least three concurrent rounds:

```
10:06:57  scratch/bl749_xlink_measure.py
10:08:45  scratch/bl749_xlink_rows.json
10:10:07  scratch/bl749_followers_measure.py
10:10:54  scratch/bl749_followers.json
10:07:13  scratch/bl722_run_gemini.py       <- Gemini round
10:10:41  scratch/bl722_results.jsonl
10:09:31  scratch/field_sweep.py
10:10:56  scratch/bl747_send_path_audit.py  <- a third round
```

**The honest nuance:** `config.json` itself has **not** been modified today — its mtime is
still 2026-07-30 14:02:35. Only the backup was taken. In this repo a pre-run backup is standard
safety and does not guarantee an edit follows. But an agent that has just snapshotted a file
in preparation is precisely the agent whose write would silently drop my added keys, and
config is where items 4 and 5 land.

**`clippershq/youtube_finder.py` is clear** — untouched since 2026-07-25 12:35:29, and no
running script references it. The code half of this work is uncontended; only the config half
is at risk. I did not take the partial, because items 4 and 5 straddle both files and a
half-applied niche change (code preset without the config default) is worse than none.

---

## Item 1 re-measured, and BL-748's number needs correcting

This needed no edits and no new quota, so I did it. BL-748 reported **10.0%** raw regex
matches. Routed through the guards the brief specifies — `bio_parser.valid_email`, the
junk-domain blocklist, `PLATFORM_HOP_DOMAINS`, then the agency/role classifier — the real
number is lower:

| stage | n |
|---|---|
| raw regex matches | 5 |
| rejected by `valid_email` | **1** |
| rejected as junk domain | 0 |
| rejected as `PLATFORM_HOP_DOMAINS` | 0 |
| **surviving** | **4** |
| — personal | **3** |
| — role | 1 |
| — agency | 0 |

**Usable: 4/50 = 8.0%. Personal only: 3/50 = 6.0%.**

Survivors (masked — this repo is public):

```
The EO Business Podcast         personal   li…@outsourcingangel.com.au
The American Business Podcast   personal   Pa…@Americanpodcast.tv
2 Cents Podcast                 personal   bu…@gmail.com
Think School                    role       co…@thethinkschool.com
```

**So the projection in the brief should be revised down: ~89 extra emails/day becomes ~71
usable and ~53 personal** at the measured 891 qualified channels/day. Still the largest win on
the list, still free — but 10.0% was the pre-guard figure and the brief carries it forward as
if it were the deliverable rate.

Two things worth noting for the implementation:

- **`valid_email` earns its place** — it rejected 1 of 5 on a 50-channel sample. The guards are
  not ceremony.
- **`2 Cents Podcast` is `country=BD`** and is dropped by the country gate before the email
  would ever be used. On this sample the *net* new personal emails among channels that survive
  the other gates is smaller again. The description parse should sit **after** the country gate,
  not before, or the fill rate will be reported against a population that never ships.

---

## What was not done

| # | item | status |
|---|---|---|
| 1 | parse `snippet.description` for email | **not implemented** — but fill rate measured and corrected (above) |
| 2 | size floor sees views (`subs >= 50k OR views >= N`) | not implemented — `youtube_finder.py` |
| 3 | honour `hiddenSubscriberCount` | not implemented — `youtube_finder.py` |
| 4 | new default niche + fitness/gaming presets | not implemented — **straddles code + `config.json`** |
| 5 | `max_subscribers` into `config.json` | not implemented — **`config.json` contended** |
| 6 | keep `customUrl` + `publishedAt` | not implemented — `youtube_finder.py` |
| 7 | record the self-declared-country limit | **noted below** |

**No backup was written**, because nothing was changed — and because another agent had just
snapshotted `config.json`; adding a second backup of a file mid-contention records a state
neither of us owns.

`tests/run_all.py` was **not run**: three rounds are writing concurrently, so a suite result
would certify their in-flight tree rather than this round's.

---

## Item 7, recorded as instructed

The country gate reads `snippet.country`, which is **self-declared by the channel owner**.
BL-748 measured 5/5 sampled drops correct (BD, MA, GR, ZA, DE), so the gate is working. But
`MERI PODCAST` passed with `country=US` while its listed contact domain is `meri.et`. That is
not a gate defect and there is no free fix — the API exposes no verified geo. **Known limit,
not a bug.** A paid cross-check against the contact domain's TLD would catch this class, but it
is not worth a call at this yield.

---

## One thing I would add to the plan before it is built

Item 2 changes the floor to `subs >= 50,000 OR viewCount >= N`, and **N is not specified**.
From the BL-748 sample, the case to rescue is 26,900 subs / 16.8M views, and the channels the
floor correctly drops sit at 0.4M–1.5M views. Anything in the **2M–5M** range separates them on
this sample — but that is one query of 50 channels, and N is the whole behaviour of the gate.
**It should be picked from a wider sample than the one that motivated it**, or the fix repeats
the original error in the opposite direction.

---

## To resume

`clippershq/youtube_finder.py` is uncontended and all six code changes are specified. The block
is `config.json`. When the BL-749 round settles:

1. Re-run the gate — it should pass quickly, since only config is contended.
2. Apply items 1–3 and 6 to `youtube_finder.py`, items 4–5 across both files.
3. Put the description parse **after** the country gate (see above).
4. Pick N for the view floor from a multi-niche sample, not the 50-channel one.

---

## Method

Process state from `Win32_Process` with paired CPU samples and per-thread state. The write gate
sampled mtime+size across 4,417 files every 20 seconds. The item-1 re-measurement reused the
BL-748 payload already on disk and the repo's own `bio_parser` / `role_policy` guards — **no new
API call, no quota, no dollars**. No credential was read or printed; no repository file was
modified.
