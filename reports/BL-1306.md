# BL-1306 — what is actually left, verified against tonight's tree

**Round:** BL-1306 · **Date:** 2026-08-15 · **RESEARCH ONLY — no product code was written.**
No rule, brief, config or shipped module was changed.
**Interpreter:** `.venv\Scripts\python.exe` — **CPython 3.11.9**, by full path, for every number.
The `python` on PATH is 3.13 and lacks this project's packages; nothing here used it.
**Vendor spend: $0.000000.** No billed call of any kind.
**NO LEAD DATA.** No address appears here, redacted or otherwise.

**The judge is done for now** — 92/100 Instagram and 83/100 TikTok against marks you formed
independently (BL-1303), the first scores in this project that could have come out badly. This
report is everything else, re-measured. **Nothing below is inherited: every carried item was
re-run against the tree tonight, and four of them are now closed.**

---

## THE TOP TEN, RANKED BY WHAT IT COSTS YOU

| # | what | what it costs | fix size |
|---:|---|---|---|
| **1** | **The brief with the dialogue veto is the newest file on disk and nothing marks it refused** | **7 of every 100 pages judged** | **XS — one line** |
| **2** | **MX-first is not enforced on `[B]`, the button you actually press** | live bounce risk on every send | **S — one call** |
| **3** | **60 paid, deliverable addresses held back for want of a declared `lead_kind`** | money already spent, sitting unusable | **M** |
| **4** | `found_via` blank on **70.1%** of 2,688 pages | blocks the only lever that raises the 19% want rate | **M** |
| **5** | `scratch/` is **23.33 GB** and its cleaner **hard-deletes**, unscheduled | disk, and it breaks your quarantine rule | **M** |
| **6** | A **PNG the judge reads** names four of your marked pages | contaminates any score that reuses it | **S** |
| **7** | `triage.py` and `tiktok_triage.py` have **no caller** | their measured savings are not running | **S each** |
| **8** | `parallel_judge` has **no caller** | the only complete OCR judging path is orphaned | **M** |
| **9** | Three drawn-text predicates fire **8 / 19 / 2** times in 3,689 lines | rules that read as clean negatives | **S** |
| **10** | **88 benign-zero sites** baselined outside the 14-file guard | each can turn a failure into a plausible number | **L** |

**Closed since the list was written:** the stale `LATEST_BOT_READY.meta.json`, the leak guard's
hardcode, the `st["links"]` field mismatch, and — after investigation — the taskkill incident.

---

## 1. THE FOUR THAT ARE NOW CLOSED

**Say these out loud so nobody re-reports them.**

| carried item | tonight |
|---|---|
| *`LATEST_BOT_READY.meta.json` reads `campaign: DAYLIGHT, row_count: 2` from August 1 beside a 4,270-row CSV* | **CLOSED by BL-1304, which landed while I was measuring.** Both metas now read `campaign=ALL, row_count=4270` against 4,270-row CSVs, built 22:25:15 |
| *`bl1269_leakguard.py:78` still hardcodes his 100 and still cannot fail* | **CLOSED by BL-1282.** It discovers **15 marked sets / 823 handles** and 15 briefs. The string `BL-1257-FRESH100_marks.jsonl` survives only inside the docstring that explains the old defect — verified by AST that it is not a code literal |
| *`st["links"]` against a writer writing `items`, reporting 0 links for every batch ever run* | **CLOSED by BL-1249.** `dashboard/paste_routes.py:127` now prefers `counts` and falls back to counting `items`; the docstring records the defect in the past tense and `tests/test_bl1249_panel_fields.py` pins it |
| *A sub-agent ran `taskkill /F /IM python.exe` at 20:56 and killed two processes that were not its own* | **INVESTIGATED — no work was lost.** §2 |

---

## 2. THE TASKKILL, INVESTIGATED

BL-1295 owns it and disclosed it honestly: a sub-agent of that round ran `taskkill /F /IM
python.exe` with a filter matching every Python process on the box and **terminated 2 besides its
own**, with six rounds in flight.

**I checked whether anybody lost work. Nobody did.**

| evidence | result |
|---|---|
| every round live at 20:56 | **all published a report afterwards** — BL-1295 21:53, BL-1296 21:39, BL-1297 21:20, BL-1298 21:13, BL-1299 21:36. BL-1288 (20:55) and BL-1291 (20:53) had already filed |
| orphaned temp beside the master | `master_leads.csv.tmp`, **20:34:36** — 22 minutes *before* the kill, so not from it |
| is a lead stranded in it? | **No.** Both files hold **62,675 records**; the set difference is **0 in each direction**. The 105-byte size gap is formatting, not content |
| master integrity | parses cleanly, 62,675 records |
| pending render record | 1, written `2026-08-01T13:48:07Z` — a fortnight old, not tonight |
| git | reflog intact; no lost commits around the window |

**The cost was wall clock, not work.** The lasting item is the instruction, not the damage:
**a sub-agent's stop must be one recorded pid, never `/IM python.exe`.** BL-1148 wrote that rule
down in this same repository and a sub-agent that had not been told it broke it anyway.

---

## 3. THE ITEMS THAT ARE STILL TRUE

### 3.1 MX-first is not enforced on the button you press — **and it is not a refusal anywhere**

Verified tonight, three ways:

```
refresh_mx                     0 matches in clippershq/control.py
send_suppress.table_health     all_bot_ready.py:316, inside main()  (main = lines 281-459)
[B] calls                      all_bot_ready.build(...)   control.py:3504
[B] calls all_bot_ready.main() NO
```

So the MX table-health check lives in the **command-line** path and `[B]` never reaches it.

**And it is weaker than the carried item says.** Even on the CLI path it is not a refusal — it is
a `print` wrapped in `try/except Exception` that swallows and continues (`all_bot_ready.py:
314-322`). Nothing in the send path exits non-zero on a stale or absent MX table. `send_suppress`
contains exactly two non-zero exits and both are in its own `_self_test`.

**What it costs:** you press `[B]`, you get a file, and nothing has checked that the domains in it
were ever probed. BL-962 shipped a file whose domains were 83.8% absent from the table and nothing
said so — that is the failure this check exists to prevent, and it is not on your path.

**Fix size: S.** One call from `control.py:3504`'s neighbourhood, plus a decision about whether
staleness should warn or refuse.

### 3.2 Sixty deliverable addresses are held back — not fifty

Measured by running the real builder over the real master tonight:

| | rows |
|---|---:|
| exported to the send file | 4,270 |
| **held back: no declared `lead_kind`** | **60** |
| suppressed, `role_inbox` | 369 |
| suppressed, `dead_mx` | 31 |
| suppressed, `no_mx_route` | 17 |
| suppressed, `fossil_fit_test_day` | 107 |
| quarantined, bio-link custom domain | 58 |

**The carried figure was 50; it is 60 and rising.** The other four causes are good ones — a role
inbox and a dead MX genuinely should not be mailed. **`lead_kind` is not a quality judgement.** It
is a funnel forgetting to say whether the person is someone to hire or someone to sell to, and the
address was paid for either way.

**Fix size: M.** The funnels that omit it have to declare it; `all_bot_ready` already reports the
count loudly (`UNROUTABLE`, `all_bot_ready.py:329-333`) — on the CLI path only, per §3.1.

### 3.3 `found_via` is blank on 70.1% — reproduces to the digit

**1,885 of 2,688** meme pages carry no `found_via`. Unmoved since BL-1273.

This is item 4 rather than item 8 because of what it blocks: **the only lever that raises the 19%
want rate is better discovery**, and you cannot compare discovery channels when seven pages in ten
cannot say where they came from. Every filter that has been tried instead removes wanted pages.

### 3.4 `scratch/` is 23.33 GB, and its cleaner deletes

| | measured tonight |
|---|---|
| size | **23.33 GB** (carried figure: 17.60 GB) |
| directories / files | **6,355 / 31,361** |
| `tools/scratch_gc.py` | `shutil.rmtree(p)` at **:318** |
| manifest of what it removed | **none** |
| restore path | **none** |
| quarantine | **none** |
| scheduled anywhere | **no** — no task, no hook |

**Your standing rule is quarantine, never delete.** The cleaner violates it, has no record of what
it took, and is not scheduled — so the directory grows and the only tool for it is one nobody dares
run. Both halves of that are the problem.

**Fix size: M.** Move-to-quarantine plus a manifest plus a schedule; the deletion logic already
exists and is the part to remove.

### 3.5 Three modules ship with no caller

| module | callers outside itself |
|---|---|
| `clippershq/triage.py` | **0** — its measured 5.3% drop bucket is not running |
| `clippershq/tiktok_triage.py` | **0** |
| `clippershq/parallel_judge.py` | **0** — the repo's own note at `meme_finder.py:2447` says so, and it is still true |

**A correction to my own sweep, because it is the exact defect family under audit.** My first pass
reported `triage.py` as *having* a caller at `tiktok_triage.py:2` and `parallel_judge` as having
one at `meme_finder.py:860`. Both are **prose inside a docstring** — a module title line and a
paragraph naming the function. A text sweep that matches its own subject matter is the thing
INFRA-018 recorded; I reproduced it and am reporting the correction rather than the first answer.

---

## 4. THE FIVE RECURRING FAMILIES, CURRENT INSTANCES ONLY

### Family 1 — a field name that no longer matches its writer

**The named instance is CLOSED** (BL-1249, §1). **My own sweep for current instances found none,
and reported five that were its own bug** — it probed `output/bl1257_fresh100/capture.json` at the
top level for a `handle` key, when that file is `{round, interpreter, lanes, gap_s, wall_clock_s,
pages}` and the records live under `pages`. **A sweep that reports its own reading error as a
finding is family 1 happening to the auditor.** Corrected here; the honest result is **no current
instance found by this method**, which is a weaker statement than "there are none".

### Family 2 — a guard that cannot fail

**The leak guard's LOGIC is fixed; its SCOPE is not.** It reads **15 marked sets / 823 handles** —
and scans only files matching `*brief*.md` under `scratch/`, which is **15 files**.

**`scratch/bl1240_reference_card.png` — 930 KB, which the judge READS — names four of your marked
pages with your verdicts, and no regex can look inside a PNG.** A guard whose scope excludes the
one input format its subject actually uses is a guard that reports clean forever.

**Clean on the other half:** **0** tools with a `main()` and no non-zero exit path. That
sub-family has been closed since BL-1273 listed four.

### Family 3 — a predicate that never fires, reading as a clean negative

Measured on **3,689 drawn lines** across the two page corpora:

| predicate | fires |
|---|---:|
| `caption_is_promotional` | 8 |
| `narrates_action` | 19 |
| `looks_like_dialogue` | 2 |
| `is_news_report` *(control)* | **25** |

**The control fires, so a low number is a property of the predicate and not of the corpus.** These
were re-wired by BL-1282 and are honestly near-silent rather than dead — the page-level effect is
documented there. Plus the three modules with no caller in §3.5.

### Family 4 — a failure that reads as a benign zero

**The ratchet is holding, and that is the finding.**

| | tonight |
|---|---:|
| files in scope | 214 |
| violations inside the 14 GUARDED files | **0** |
| sites baselined outside | **88** |
| **NEW since the baseline** | **0** |
| baselined but now fixed (stale entries) | **0** |

Two rounds have added code since the ratchet went in and neither added a site. **88 remain
baselined, not fixed** — that is item 10, and it is last because each is individually small and
collectively unreviewable in one round.

### Family 5 — an in-sample result presented as general

> ### **This is item 1, and it is the newest example of the oldest mistake here.**

The dialogue veto was justified at **92/100** — measured against `BL-1257-FRESH100_marks.jsonl`,
the file whose "truth" is the v4 judge's own verdict plus your GOOD/BAD button on it. Agreement
there is `baseline − broke + fixed`, so **that 92 was an identity, not a measurement**. On that
basis the veto was recommended and built into `scratch/bl1293_brief_v9.md`.

**BL-1303 then scored it against marks you formed independently:**

| | score on the fresh 100 |
|---|---:|
| v9 **without** the veto | **92/100** |
| v9 **with** the veto restored | **85/100** |
| **the veto is worth** | **−7 pages** |

**And here is what is still live tonight.** `scratch/bl1293_brief_v9.md` (33,270 bytes, 20:18
today) is **the newest full brief on disk**, and its first 4 KB carry **no "do not ship", no
"refused", no "superseded"**. Nothing in shipped code points at any brief — a round picks one up by
reading the directory. **The next round that reaches for "the latest brief" gets the one measured
to be seven points worse.**

**Fix size: XS.** One line at the top of v9. It is item 1 because it is the cheapest fix on this
page and the most expensive omission.

*(Also still open in this family: `docs/THRESHOLDS.md` classifies 24 rows, **21 of them
FITTED-ONLY** — tuned on labels with no held-out number.)*

---

## 5. THE SUITE

```
ALL GREEN -- 331/331 suites passed, 8279 checks   (669.8s)
3 round(s) in flight at 22:51
```

**Zero reds, so there is nothing to classify as environmental, concurrency-flaky, another round's
mid-edit, or real.** That is the second consecutive all-green tonight and it is worth one caution:
**a green suite is a moment, not a property.** Three rounds were writing while it ran. Earlier
tonight the same command returned 2 red twice, and on both occasions the reds were the guards
correctly reporting a live fact — a send file behind its master, and a new silent-zero site.

---

## 6. WHAT TO DO NEXT — IN PLAIN LANGUAGE

**Do these three first. Together they are about an hour.**

1. **Write one line at the top of `scratch/bl1293_brief_v9.md` saying it is refused and why.**
   It is the newest brief in the folder and it makes the judge seven points worse. Anyone who
   picks up "the latest brief" tomorrow picks up the wrong one. One line prevents that.

2. **Make `[B]` check the MX table.** Right now the check exists, works, and lives on a path you
   never use. You press `[B]`, get a send file, and nothing has confirmed the addresses in it were
   ever probed for deliverability. This is the only item on the list that can cost you a domain
   reputation rather than money.

3. **Ask the funnels to say `lead_kind`.** Sixty addresses you have already paid for are sitting
   in the master unusable — not because they are bad, but because nothing recorded whether each
   person is someone to hire or someone to sell to.

**Then, when there is a longer stretch:** record `found_via` (it is the only route to a better
want rate, and 70% of pages cannot say where they came from), and give `scratch/` a cleaner that
quarantines instead of deleting — 23 GB is real, and the current tool breaks your own rule.

**What you can stop worrying about:** the judge, for now. The meta files. The leak guard's logic.
The panel's batch counts. And the taskkill — it cost time, not work.

---

## PRECONDITIONS

| check | result |
|---|---|
| product code written | **none** — `reports/BL-1306.md` and five `scratch/bl1306_*` files only |
| spend | **$0.000000** |
| interpreter | `.venv\Scripts\python.exe` CPython 3.11.9, by full path |
| preconditions read with | `tools/claims_read.py --count` (**2**) and `git status --porcelain` — not `claim.py` |
| targets held or dirty | none; nothing I claimed was ` M` |
| `config.json` | parses, **164 keys**, **5 campaigns** |
| campaigns fingerprint | `7a029ee5447cddd8` (compact) / `8e02f8d6f6307ae8` (default) — **the same object under different separators, unchanged** |
| rounds in flight | 2 at start (BL-1304, BL-1305); 3 by filing |
| suite | **ALL GREEN 331/331, 8,279 checks, 669.8 s, `--jobs 4`** |

**Declared paths:** `reports/BL-1306.md`, `scratch/bl1306_sweep.py`, `scratch/bl1306_sweep.json`,
`scratch/bl1306_families.py`, `scratch/bl1306_families.json`, `scratch/bl1306_suite.log`.
**`scratch/bl1306_suite.log` is gitignored** (`.gitignore:117`, `*.log`) and cannot be committed;
`tools/commit.py` refuses a whole commit rather than dropping it quietly, which is correct, so it
is omitted from the commit and its footer is quoted in §5.

**Two corrections this round made to its own output**, both recorded rather than quietly dropped:
the five family-1 "mismatches" that were my probe reading the wrong level of an artefact, and the
two "callers" that were prose inside docstrings.
