# BL-1131 — every subscription, key and recurring charge: the cancel list

Read-only. **Nothing was changed, cancelled, revoked or rotated.** No paid call was made.
No credential value appears anywhere in this report.

---

## The answer in one line

**You were right, and the thing you are paying for is not in this project.**

This Python project holds **9 credentials across 8 vendors**, and only **one** is dead here —
a Google Gemini key nothing has ever called. But **the other repository on your C: drive
holds a second, larger set of credentials for five subscription vendors that this project's
ledger cannot see at all**: Apify, ScrapeBadger, Supabase, Sentry and an email sender.

And one thing that reframes every number in the last report:

> **This ledger has never seen an invoice.** It records `calls × a price written in
> config.json` (`cost_per_call_usd: 0.0006`). It does not know what plan you are on, and a
> monthly subscription fee is invisible to it **by construction**.

---

## 1 & 2. Every credential, and whether anything calls it

Classified by **AST** — real `.get("block")` / `["block"]` / `"block" in cfg` nodes — and by
**import reachability** from declared entry points, run against a clean `git archive HEAD`
snapshot. **Not by grep**, which returns the vendor name from comments, prose, fixtures and
docstrings explaining removals.

**Analysed against HEAD, not the working tree**, because three of the files involved are
mid-edit under BL-1130's live claim right now. Reading those would be analysing someone
else's half-finished work and calling it shipped.

| vendor | key name | file | tracked? | verdict | lifetime |
|---|---|---|---|---|---|
| **Google Gemini (direct)** | `gemini_api.key` | `config.json` | no | **DEAD** — 4 AST reads, **all four in `scratch/`**, zero in shipping code | **$0.00, never a single ledger row** |
| HikerAPI | `ig_api.key` | `config.json` | no | **LIVE** — 8 reachable readers incl. `ig_client.py`, `control.py` | $8.4924 |
| OpenRouter | `openrouter_api.key` | `config.json` | no | **LIVE** — `control.py` (the vision label) | $2.2550 |
| TikHub | `tikhub_api.key` | `config.json` | no | **LIVE** — `tikhub_client.py` | $0.6320 |
| LamaTok | `api.key` | `config.json` | no | **LIVE** — 8 reachable readers incl. `api_client.py` | $0.3392 |
| Google / YouTube Data v3 | `youtube_api.key` | `config.json` | no | **LIVE, free** — `youtube_finder.py`, `control.py`; 10,000 units/day | $0.00 |
| Twitch | `twitch_api.client_secret` | `config.json` | no | **LIVE, free** — `twitch_api.py`, `twitch_finder.py` | $0.00 |
| Anthropic | `anthropic_key.txt` | `memebot/meme/` | no | **LIVE but opt-in and UNMETERED** — `meme/reword.py`, imported by `meme/cli.py`, behind `--reword` which is off by default | **unknown — nothing meters it** |
| HikerAPI (2nd copy) | `hikerapi_token.txt` | `memebot/meme/` | no | duplicate of a live key in a second home | — |

**The one DEAD key, proven:**

```
gemini_api AST reads: 4
   scratch/bl722_diagnose.py:9      scratch/bl838_gemini_n100.py:44
   scratch/bl722_probe_one.py:11    scratch/filmid_gemini.py:56
NON-scratch, NON-test readers: NONE
```

It is a leftover from when Gemini was probed **directly**, before the vision path settled on
reaching the same model **through OpenRouter**. The model is still used; this key is not the
way it is reached.

**Honest caveat on cancelling it:** a Google AI Studio key is pay-as-you-go, not a
subscription, so revoking it **saves $0.00**. It belongs on the *revoke* list for attack
surface, not the *cancel* list for money.

**Coverage limit, stated:** `git archive HEAD` on the parent excludes `memebot/`, which is a
nested repo. The memebot side was analysed separately against its own HEAD — that is how the
Anthropic path was resolved.

---

## 3. The money cross-check, both directions

**Every vendor with ledger rows has a credential. There is no orphan vendor.**

| ledger key | vendor | rows (of 880) | lifetime | credential present |
|---|---|---|---|---|
| `ig_usd` | HikerAPI | 764 | $8.4924 | yes |
| `vision_usd` | OpenRouter | 83 | $2.2550 | yes |
| `tiktok_usd` | LamaTok | 37 | $0.3392 | yes |
| `tikhub_usd` | TikHub | 10 | $0.6320 | yes |
| `link_usd` | plain HTTP fetches | 0 | $0.0000 | n/a — unbilled |

**Four credentials have never produced a ledger row**, and they are not the same case:

| key | rows | what zero rows means |
|---|---|---|
| `youtube_api.key` | none | **expected** — free within quota. Zero rows is correct. |
| `twitch_api.client_secret` | none | **expected** — free, points-limited. |
| `gemini_api.key` | none | **NEVER USED.** Nothing ships that could produce a row. |
| Anthropic | none | **proves nothing** — the path is unmetered, so a row could not exist even if it had run. |

Render ledger folds as expected: `memebot/runs.jsonl` 1,036 rows → **521 records**, raw
$0.6936 → **folded $0.3492**, 1.99×. Those are HikerAPI dollars already inside the $8.4924.

---

## 4. What could be billing without a ledger row — your actual worry

**This is the section that matters, and the answer is: quite a lot.**

The ledger meters **calls**, at **a price written in `config.json`**. Two consequences:

1. **A subscription fee is invisible.** A dormant paid plan bills monthly whether or not one
   line of code runs. Nothing in this system would ever record it.
2. **Even the per-call figures are modelled, not billed.** `cost_per_call_usd: 0.0006` is a
   constant somebody typed. If the real plan price differs, **every dollar in the $11.72 is
   wrong by that ratio** — including the figures in the last report.

**Vendors whose model is subscription or prepaid-credit rather than pure per-call, and which
this ledger therefore cannot see:**

| vendor | where the key lives | model | ledger can see it? |
|---|---|---|---|
| **HikerAPI** | this project | paid plan / prepaid credit | **no — only its own call estimate** |
| **TikHub** | this project | paid plan / prepaid credit | **no** |
| **LamaTok** | this project | paid plan / prepaid credit | **no** |
| **OpenRouter** | this project | prepaid credit balance | **no** |
| **Apify** | the other project | **monthly subscription + credits** | **no — not even a key here** |
| **ScrapeBadger** | the other project | paid plan | **no** |
| **Supabase** | the other project | **monthly subscription** | **no** |
| **Sentry** | the other project | **monthly subscription** | **no** |
| **email sender** (`EMAIL_API_KEY`; `.env.example` names `RESEND_API_KEY`) | the other project | **monthly subscription** | **no** |

**I did not check any balance.** Every one needs a live authenticated call, and this round
has a $0.00 cap. **NOT CHECKED** is the honest status for all of them, and checking them is
the operator's next action, not mine.

---

## 5. The other project — a second bill

**93 of the 140 folders at `C:\` are worktrees of `ilenader/Clippershq.git`** (a Next.js /
Prisma app that merely shares the naming convention). A further 21 are worktrees of the
public reports repo.

**60 of those 93 hold their own `.env.local`.** They are 3 distinct files (52 identical, 7
identical, 1 unique) — the same credential set duplicated **60 times on disk**.

**Nothing is committed: 0 of 93 track a `.env` or `.env.local`.** Only `.env.example` is
tracked, and it holds placeholders. That repo's history is clean.

**Vendors found there — names only, values never read:**

| variable name | vendor | billing |
|---|---|---|
| `APIFY_API_KEY`, `APIFY_INSTAGRAM_ACTOR`, `APIFY_TIKTOK_ACTOR`, `APIDOJO_*` | **Apify** | **subscription + credits** |
| `SCRAPEBADGER_API_KEY` | **ScrapeBadger** | paid |
| `DATABASE_URL`, `DIRECT_URL`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` | **Supabase** | **subscription** |
| `NEXT_PUBLIC_SENTRY_DSN` | **Sentry** | **subscription** |
| `EMAIL_API_KEY`, `EMAIL_FROM` (`.env.example`: `RESEND_API_KEY`) | **Resend** or similar | **subscription** |
| `HIKERAPI_KEY`, `LAMATOK_KEY`, `TIKHUB_API_KEY` | **the same three as this project** | **possibly a SECOND plan on each** |
| `AUTH_DISCORD_ID/SECRET`, `DISCORD_CLIENT_ID/SECRET` | Discord OAuth | free |
| `GITHUB_GIST_TOKEN` | GitHub | free |
| `AUTH_SECRET`, `NEXTAUTH_SECRET`, `CRON_SECRET` | self-generated | not a vendor |

**The line worth reading twice:** that project carries `HIKERAPI_KEY`, `LAMATOK_KEY` and
`TIKHUB_API_KEY` — the same three vendors this project bills against. Either they share one
account, or **you are on two plans with each of three vendors**. I cannot tell which without
comparing key values, which I will not do, or looking at the invoices, which you can.

---

## 6. THE CANCEL LIST — dead first

### Cancel or downgrade — costing money for nothing here

| # | vendor | where to check | what breaks if you cancel |
|---|---|---|---|
| **1** | **Apify** | console.apify.com → Billing | **Nothing in this Python project** — it has no Apify key and no Apify code. Only the Next.js app. If that app is not running, this is a pure saving. **Most likely candidate.** |
| **2** | **ScrapeBadger** | that vendor's dashboard | **Nothing here.** Same as Apify. |
| **3** | **Sentry** | sentry.io → Settings → Subscription | **Nothing here.** Error monitoring for the Next.js app only. The free tier is usually enough. |
| **4** | **Resend / email sender** | resend.com → Billing | **Nothing here** — this project's own email path uses its own finder and does not read `EMAIL_API_KEY`. |
| **5** | **Supabase** | supabase.com → Project → Billing | **Nothing here.** But it is the Next.js app's **database** — cancelling deletes that app's data. Downgrade to free rather than cancel unless you are done with that app. |
| **6** | **Google Gemini (direct key)** | aistudio.google.com → API keys | **Nothing.** 4 references, all in `scratch/`. **Revoke it** — but it is pay-as-you-go, so the saving is $0.00. This is attack surface, not money. |

### Keep — these are load-bearing

| vendor | lifetime | what breaks if you cancel |
|---|---|---|
| **HikerAPI** | **$8.4924 (72.5%)** | **Everything.** Every clip page, every profile, and the retrieval leg behind every render and every vision label. This is the system. |
| **OpenRouter** | $2.2550 (19.2%) | All clip labelling stops. No vision label ⇒ no song match ⇒ no renders (the matcher requires vision evidence). |
| **TikHub** | $0.6320 | TikTok deep-checks stop. TikTok discovery degrades; Instagram is unaffected. |
| **LamaTok** | $0.3392 | TikTok pages and resolves stop. Instagram unaffected. |
| **YouTube Data API v3** | $0.00 free | YouTube creator discovery stops. **Costs nothing — no reason to cancel.** |
| **Twitch** | $0.00 free | Twitch discovery stops. **Costs nothing.** |
| **Anthropic** | unknown | Only memebot `--reword`, which is off by default. Safe to revoke if you never use that flag — **but nothing meters it, so check the console for actual usage rather than trusting this project.** |

### Check the plan, do not cancel

**HikerAPI, TikHub, LamaTok and OpenRouter** — log in and compare the plan fee against the
call volume above. This project thinks it spent $11.72 in total across all four. **If any of
them is a monthly plan larger than that, the plan — not the usage — is your bill**, and this
ledger has never been able to tell you so.

---

## 7. Rotation, not cancellation

**Two tracked files each carry all seven credentials**, confirmed by the project's own
`secrets_guard.looks_like_credential` test:

* `scratch/bl1041_config_stage1.json` — 7 live values
* `scratch/bl979_config_ranked.json` — 7 live values

Both hold `api.key`, `ig_api.key`, `tikhub_api.key`, `youtube_api.key`, `gemini_api.key`,
`twitch_api.client_secret`, `openrouter_api.key`.

**Deleting the files does not help.** They are in the private parent repo's history and stay
there. **Rotation is the only thing that makes them harmless** — issue a new key at each
vendor and revoke the old one.

**Rotation is cheap here:** `config.json` is gitignored, so a new key is a paste into
Settings. **No code change, no deploy, no test run.**

**What is NOT exposed, checked and cleared:**

* `config.example.json` is tracked and holds **placeholders only** — correct.
* **20 other tracked files** matched a credential-shaped scan and are all **false positives**
  — render records and field lists with a column literally named `key`. Named here so nobody
  re-raises them: `bl952_discarded`, `bl967_fields`, `bl983_paths`, `bl1107_leakcheck`,
  `bl806_grow`, `bl829_tagmeasure`, and the `mb0*_render` / `mb0*_verify` families.
* `memebot/meme/anthropic_key.txt` and `hikerapi_token.txt` are **not tracked** by the
  memebot repo. Local only. Still worth deleting `hikerapi_token.txt` — it is a second home
  for a key that already lives in `config.json`, against the one-home rule.
* The other project: **0 of 93 worktrees commit a `.env`.** Clean.

---

## Off-brief, stated

* **No balance was checked, for any vendor.** Every one needs a live authenticated call and
  the cap is $0.00. **NOT CHECKED** is the status for all nine.
* **I did not compare key values between the two projects**, so I cannot say whether the
  shared HikerAPI / LamaTok / TikHub keys are one account or two. That needs the invoices.
* **Billing models are stated from how these vendors are known to work, not from your
  invoices.** I have not seen a bill for any of them. Treat the subscription column as *what
  to go and check*, not as *what you are being charged*.
* **I read one sample worktree's `.env` files** and then only variable names across the rest.
  No further reading into the other project.

## Got wrong

* **My first credential sweep reported 56,283 "credential-shaped entries".** It counted every
  JSON leaf named `key` on the whole tree — mostly `config.backups/` snapshots and render
  records with a `key` column. Filtering through the project's own
  `looks_like_credential` test brought it to **2 tracked files with real values**, which is
  exactly what the brief said was there. The raw number was noise wearing a finding's clothes.
* **My first version spawned one `git ls-files` per file** and did not finish in two minutes.
  One call per repo answers the same question exactly.

## Still broken, and whose

* **`clippershq/operator_pairs.py` is dirty, mid-edit, and held by NO live claim.** BL-1130's
  claim does not name it. Unowned work in progress — whoever left it should claim or revert
  it. I touched nothing.
* **The ledger's unit prices are config constants, never invoiced amounts.** Nothing in this
  system reconciles against a bill. Whoever owns the spend path.
* **The Anthropic leg is unmetered** (`memebot/meme/reword.py`). A cap cannot see it.
* **60 copies of the same credential set on disk** in the other project's worktrees. That
  repo's owner.

## Suite and spend

* **No suite run and none needed** — no product code changed. Only scratch scripts and their
  JSON outputs, all inside this round's claim. **BL-1130 holds `scratch` as a whole
  directory**; the tool advised rather than blocked, my files are `bl1131_*` and cannot
  collide, and I am saying so here as instructed.
* **`config.json` valid and untouched:** 162 keys, 5 campaigns, `7a029ee5447cddd8` (compact)
  and `8e02f8d6f6307ae8` (default) — one object under two spellings, both reproduced.
  `spend.json` untouched.
* **Spend: $0.0000 against a $0.00 hard cap. No paid call was made. Nothing was changed,
  cancelled, revoked or rotated.**
