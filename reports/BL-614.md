# BL-614 — Supervised Apify cutover watch (live, read-only)

## NOTHING IS BROKEN. Apify is now confirmed DEAD in production, and every path is failing safe. No clip stored a 0 since the cutover, verification works on all three platforms, submit is unblocked, slideshows are safe, and earnings did not drop. Do NOT restore the key. (Follow-up to the earlier watch, which caught the cutover before it had propagated; it has now taken effect.)

**2026-07-22 · AUDIT ONLY. READ ONLY on code, config, data and money. Nothing changed, fixed, restored, or built. No Apify actor run. Every probe disclosed. Ids matched, never rows.**
**Base:** origin/main `3c59fdc` (BL-613, verification off Apify) · **Branch:** checkpoint/BL-614 · **Observation window:** 2026-07-22T11:12Z (cutover) to 12:15Z.

## PART 0 — is Apify actually dead in prod? YES.
`APIFY_API_KEY=DISABLED` is a truthy string, so the code still ATTEMPTS a few Apify fallback calls, but Apify rejects them (HTTP 401) and no run is created. Read live from Apify's own run-log API (free read, no actor run):
```
CURRENT UTC: 2026-07-22T12:15:42Z
newest actor run: 2026-07-22T11:11:48Z  SUCCEEDED  (apidojo/tiktok-scraper)  = 64 minutes ago
total runs on account: 21244  (UNCHANGED across the whole window)
```
The tracking tick had been firing Apify TikTok actors every ~10 minutes (runs at 11:01 and 11:11). Since **11:11:48Z there have been ZERO new runs across ~6 expected tick windows (11:21 / 11:31 / 11:41 / 11:51 / 12:01 / 12:11)** and the account run total is frozen at 21244. So the cutover took effect at approximately **11:12Z** and Apify is dead: no runs, no billing.

Corroboration + the important nuance: prod's own ledger (`apify_usage_entries`) after 11:12Z DOES show a handful of `actor:apify/instagram-api-scraper` log rows (9 rows, 11:20 to 11:57Z, estimated cost $0.009). These are the code LOGGING an attempted IG fallback call, not real runs — the run-log proves no run was created, so each was a 401 auth rejection that the code treats as a fail-safe miss (null, keep last-known). The $0.009 is the ledger's price ESTIMATE for the attempt, not actual billing (actual is $0, run total frozen). Those attempts also stopped by 11:57Z (none in the 12:00 hour), i.e. declining, not looping.

## PART 1 — view tracking, watched live with Apify dead
The tick kept running on the cheap providers throughout the window (ledger, 11:12 to 12:11Z): Instagram 100% HikerAPI (`hikerapi-v2`, 67 clips), YouTube 100% free Data API (`youtube-api-batch`, 44 clips), TikTok LamaTok (`fanout:tiktok` 39 + `lamatok-tiktok-only` 3). The only Apify-bound clips were the 9 IG fallbacks that 401'd (above), which fail safe.
* **ClipStat rows written SINCE cutover (11:12Z): 111. Written as > 0: 111. Written as 0: ZERO.**
* **The 0-hunt (the one failure that matters most): 0 rows.** No clip's latest stat is 0 where a prior stat was greater than 0, anywhere, since the cutover. The BL-543 / BL-605 / BL-610 NULL-never-0 invariant HOLDS with Apify actually gone: an unresolvable IG fallback returns null and the clip keeps its last-known value, never a coerced 0.
* **Cadence steady, no gap, no error loop:** 88 stats in the post-cutover part of the 11:00 hour, 23 so far in the 12:00 hour, zero zeros in both.
* **Coverage did not fall:** before the cutover TikTok leaned on the Apify legacy fallback for only ~6% of clips; after, those resolve on LamaTok or defer one tick as null (keep last-known). IG and YouTube were already Apify-free. Net resolve rate is unchanged and no clip is stuck.
* **Earnings did not drop:** global APPROVED earnings (`videoUnavailable=false`) is **$5,770.03 across 2,741 clips**, versus $5,769.84 at the pre-propagation watch, i.e. up $0.19 from natural view growth, never down. Consistent with the 0-hunt: no view was zeroed, so no earnings froze or fell.

## PART 2 — account verification (the earning gate), probed live with Apify dead
Verification reads bios off HikerAPI / LamaTok / the YouTube Data API (BL-612, deployed at `3c59fdc`), so it never depended on Apify. Probed the exact endpoints:
* **Instagram (HikerAPI `/v2/user/by/username`):** `@6had0ww` → HTTP 200 RESOLVED, bio contains its code `59C6` (would verify); `@zzz_not_a_real_handle_9xqp7` → HTTP 404 NOT_FOUND (link problem, never "code missing"); bad-key → HTTP 401 TRANSIENT (retry, refund the slot, never "code missing").
* **TikTok (LamaTok `/v1/user/by/username`):** `@ozclip16` → HTTP 200 RESOLVED, signature contains its code `KBBG`; `@bingoclips3` → HTTP 404 NOT_FOUND (falls through to the existing cascade); bad-key → HTTP 401 TRANSIENT.
* **YouTube (free Data API):** the key is prod-only (absent locally), but the ledger shows `youtube-api-batch` serving 44 clips SUCCESS in-window, so the same Data API key the verify tier uses is live and healthy.

**The three outcomes stay distinguishable on both probed platforms: resolved / not_found / transient. A fetch failure (401) is a transient retry, never a missing-code verdict.** Verification is not broken on any platform.

## PART 3 — clip submission (traced; no live submit occurred in-window)
No clip was submitted between 11:12Z and 12:15Z, so there is no live post-cutover submit to observe; traced from code instead. Submit is Apify-free at the decision layer (BL-552): TikTok → LamaTok, Instagram → HikerAPI, YouTube → Data API. The submit freshness retry only reaches Apify for an IG `createdAt`; with the key disabled that 401s → the caller treats it as transient → the clip is ACCEPTED and the cron verifies later. The first ClipStat SKIPS on a NULL resolve (BL-605 / BL-609), so it is never written as 0. **Submit is not blocked or throwing on any platform.** The only consequence of a real cut for IG submit is that its 30-minute freshness gate softens (the clip is still accepted), a soft effect, not a break.

## PART 4 — slideshows and carousels
* **TikTok `/photo/` slideshows: none exist in the live approved population** (0 rows) — blocked at submit (BL-278 `CAROUSELS_DISABLED`), so there is nothing to break.
* **IG carousels:** BL-610 quarantines an uncountable carousel as NULL (keep last-known, not added to the gone counter, no error loop). The 0-hunt above found **no carousel dropped to 0**, and the cadence shows no error storm, so quarantined carousels are keeping their last-known views exactly as designed.

## PART 5 — THE VERDICT
**Is anything broken? NO. Do NOT restore the key.** Apify is confirmed dead (zero runs for 64 minutes, total frozen at 21244), and every path is failing safe: 111 ClipStat rows written since the cutover, all greater than 0, zero written as 0; the 0-hunt is empty; earnings are $5,770.03 (up, not down); Instagram / TikTok / YouTube verification all return distinguishable resolved / not_found / transient (a fetch failure is a retry, never a missing code); submit is unblocked and fail-safe on the first-stat null-skip; no TikTok slideshow exists and no carousel dropped to 0; the tick cadence is steady with no error loop. The handful of IG fallback calls that still hit the disabled key 401 harmlessly and keep last-known, and they are declining tick over tick.

### What to keep watching over the next 24 hours
* **Apify run-log** (`GET https://api.apify.com/v2/actor-runs?desc=1&limit=10&token=<key>`): should stay frozen at zero new SUCCEEDED runs. Any new run means the key was re-enabled somewhere.
* **Apify billing:** should read ~$0/day now that runs have stopped.
* **The late-0 query (run daily):** the exact check for any NULL→0 coercion:
```sql
WITH ranked AS (
  SELECT "clipId", views, "checkedAt",
         row_number() OVER (PARTITION BY "clipId" ORDER BY "checkedAt" DESC) AS rn,
         max(views) OVER (PARTITION BY "clipId") AS max_views
  FROM clip_stats)
SELECT "clipId", views AS latest_views, max_views AS prior_peak, "checkedAt"::text
FROM ranked
WHERE rn = 1 AND views = 0 AND max_views > 0
  AND "checkedAt" > now() - interval '24 hours'
ORDER BY "checkedAt" DESC;
```
Any row here whose stat history is NOT steady-zero (a clip that had real views and suddenly reads 0) is a genuine break; keep-last-known is being violated. None was found in this window.
* **Earnings floor:** global APPROVED earnings should never step down; it is $5,770.03 now.
* **Verification:** IG / TikTok / YouTube should keep returning resolved / not_found / transient; a spike in verify failures would point at HikerAPI / LamaTok, not Apify.

## Safety and probe disclosure
READ ONLY; one doc; no code / config / data / money change; **no fix attempted and the key was NOT restored or altered by me** (owner's action, and none is needed). **No Apify actor was run** — only Apify's free run-log read. Live probes: ~3 Apify run-log reads, 3 HikerAPI + 3 LamaTok `/user/by/username` verify reads (prepaid, ≈ $0, paced ≥1.3s, no rate limit hit), plus read-only DB `SELECT`s via the sanctioned runner (timestamps cast to `::text`). Ids matched to their clips, never counted as rows. No handle's personal bio text reproduced beyond a verification-code substring. A markdown-only round changes no tsc/build, so none was run. NO dashes used as bullets.
