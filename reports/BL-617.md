# BL-617 — Whole-site health and money-flow check, end to end, on live data

## VERDICT (one line): YES, the money is correct and the site is healthy. No dollar was found created, destroyed, or paid to the wrong party; every major flow is WORKING; the only issues are the same small latent items from BL-542, unchanged and not actively leaking.

**2026-07-22 · AUDIT ONLY. READ ONLY on code, data, schema, config and money. Nothing changed, fixed, pushed, merged or flipped. `agency-monitor --fix` NOT run, no owner rows re-derived, BL-538's repair SQL NOT run, the Apify key NOT restored.**
**Base:** origin/main `c784ec33` · **Branch:** checkpoint/BL-617 · **DB now() = 2026-07-22 12:45 to 12:55 UTC** (every timestamp cast `::text` and compared to DB now(), so the local +02:00 offset never fakes an outage). **Probe cost: $0** (read-only `run-select.js`; 3 Apify run-log reads; 0 Apify actors).

## PART 1 — following every dollar, tested on live data
Traced view to payout and TESTED each junction against production. **No junction was found where a dollar can be created, destroyed, or paid to the wrong party.**
* **View counted:** the tracking tick writes real ClipStat views (PART 4); a null resolve keeps last-known, never 0 (verified in the fresh 0-hunt, PART 2).
* **Earnings off the clip's OWN stamped CPM:** `cpmAtSubmissionDecimal` (the frozen clipper CPM) is populated on **all 3,299 approved clips (0 null)**, so every clip earns off its own submission-time rate, not a live campaign value. `baseEarnings` derives from that stamped CPM; `earnings = baseEarnings + bonusAmount` holds (PART 2).
* **Owner share:** for `guaranteeOwnerSplit` campaigns the owner accrues off the campaign's `lockedOwnerShareDecimal` (s), applied as gross `s/(1-s) x clipperEarnings` behind BL-563's `decideOwnerGross` guard. The per-clip `ownerCpmAtSubmissionDecimal` is null on 2,989 clips, which is EXPECTED: the locked share, not a per-clip owner CPM, drives owner accrual on split campaigns. On non-split campaigns owner accrual is base-only via the campaign owner CPM.
* **CPM chain vs locked share (the owner's question):** measured the stamped `ownerCpm/(clipperCpm+ownerCpm)` against `lockedOwnerShareDecimal` on every split campaign. **Only somesome disagrees (179 of 726 rows > 0.02).** This is a LEGITIMATE rate-era difference, not a defect: somesome's ownerCpm moved 1.00 to 0.49 mid-campaign, so one locked share cannot describe two eras. Every other split campaign (STRAENGE, WinGram, bees.n.honey, GainzAlgo, Panic Baby, CROCS, Deja Shoe, both BAD BITCH ANTHEM) shows **0 disagreements**. somesome is PAUSED and frozen; this is the known era question, not a live mispricing.
* **Platform fee:** taken once at payout creation (9%, 4% referred), not at balance formation, so it cannot double-subtract (confirmed by the payout net = gross minus fee minus express fee, PART 3). `feePercentAtApproval` is null on 89 old approved clips, a stale-field cosmetic (the fee is charged at payout regardless), not a leak.

## PART 2 — the invariants, right now
| Invariant | Result | Evidence |
|---|---|---|
| Earnings invariant `\|earnings-(base+bonus)\|>0.01` | **0 violations on every paying clip** | APPROVED: 3,299 rows, 0 violations, total drift **$0.01**, max drift $0.01. |
| Rejected-clip "violations" | cosmetic, $0 money | 9 REJECTED rows carry stale base/bonus while `earnings=0` ($42.15 stale-field gap); every one pays $0. Same cosmetic BL-592 note B flagged (was 10). |
| Never-decrease guard | **committed and ON** | `earnings-never-decrease.ts` imported in force-recalc; needs an explicit `allowDecrease:true` to decrease. |
| Idempotency vs the 10-min cron | **no double-write** | **0** clips carry more than one agency row. |
| Rounding | **no cents created or lost** | total drift across 3,299 approved clips is $0.01 (one sub-cent). |
| Budget vs spend | **no campaign over budget** | every campaign's clipper+owner spend is under its budget; STRAENGE $2,996.04/$3,000, somesome $3,169.72/$9,750, bees.n.honey $2,063.48/$3,000, all others well under. |

Total live APPROVED earnings (videoUnavailable=false): **$5,770.27** across 2,741 clips.

## PART 3 — the known open items, re-measured (not inherited)
* **somesome $933.94 exposure: still latent, still safe.** The `eraExempt` "keep existing earning" button is **UNTRIPPED (0 of 32 `campaign_status_changes` rows)**, so somesome's rows have NOT been re-derived. Owner accrual $3,427.03 unchanged. The simple `s/(1-s)` re-derive gives $990.04 vs the stored $1,149.23 on live rows, i.e. re-deriving would CUT the owner, exactly the exposure direction. Confirmed do-not-touch.
* **STRAENGE: healthy under its reserve.** Owner available $998.48 (raw incl. frozen $1,000.54), clipper $1,997.56, total **$2,996.04/$3,000**. BL-563's shared gross guard is live; the $173.41 credit is protected for the eventual move to PAST.
* **The ~$48 owed: refined.** The clean, actionable owner under-accrual on the ACTIVE/non-somesome split campaigns is now only about **$4** (GainzAlgo -$2.57, bees.n.honey -$0.87, STRAENGE -$0.30, WinGram -$0.25, Panic Baby -$0.22; BAD BITCH ANTHEM exact). BL-542's $48.10 was dominated by **$44.95 of stamp-agree under-accrual INSIDE frozen somesome**, which is entangled with somesome's era (the cron cannot credit its agree rows without also touching its ambiguous rows). An exact per-row re-split needs `decideOwnerGross` replicated; BL-542's $48.10 stands as the last precise figure, of which only ~$4 is cleanly actionable today.
* **force-recalc raw-campaign caps bug: STILL LIVE.** `force-recalc-earnings/route.ts:259-261` passes the RAW `clip.campaign` (live `minViews`/`maxPayoutPerClip`) into `recalculateClipEarningsBreakdown`; `resolveClipEconomicFields` is called (`:331`) but its result is used only for `guaranteeOwnerSplit`/`ownerUserId` (`:370`), never the caps. Latent (admin-manual recompute only). BL-538 added the never-decrease guard beside it but did not fix this.
* **PWA predicate mismatch: STILL LIVE.** `tracking.ts:2082/2089` passes the RAW `isPWAUser` flag; force-recalc + cpm-restamp require PWA activity within 2 days. A clip's PWA bonus still flips by which path last ran. Latent.
* **In-flight payouts: 10 (was 8), all sound.** 7 REQUESTED + 3 UNDER_REVIEW, $418.30 gross / $374.57 net. Every net = gross minus 9% fee minus any express fee (confirmed: the residuals are `expressFeeAmount` on EXPRESS-speed rows), all positive, none negative or stranded; all USDT (rule-compliant). Three UNDER_REVIEW are STALE (24 to 47 days old) — an ops backlog, not a money hole; the money is correctly reserved, just not yet paid.

## PART 4 — every major flow, WORKING / INERT / BROKEN (live evidence + timestamp)
| Flow | State | Evidence (UTC) |
|---|---|---|
| View tracking | **WORKING** | tracking cron 143 fires/24h, last **12:51:13**; 324 ClipStats in the last 3h, **0 written as 0**, fresh 0-hunt (latest=0 where prior>0) returns 0. |
| Account verification (IG/TikTok/YouTube) | **WORKING** | probed live at 11:15 (BL-614, same session): HikerAPI IG + LamaTok TikTok both resolve 200 with the code found, 404 not_found, 401 transient (distinguishable, never a false "code missing"); the ledger shows `hikerapi-v2` + `lamatok-tiktok` + `youtube-api-batch` all serving through 12:11, so the same keys are healthy now. |
| Clip submission | **WORKING** | submits flow through the clipper core; recent PENDING/APPROVED clips created; first ClipStat skips on null (BL-605/609), never 0. |
| Money cron heartbeat | **WORKING** | tracking last 12:51, lifecycle 12:45 (97/24h), watchdog 12:30 (49/24h). |
| retire-dead-clips cron | **WORKING (BL-592's INERT finding fixed by BL-593)** | registered in CRON_JOBS (due 06:00 UTC) and it FIRED: retirements at exactly **06:00 on 07-20 (2), 07-21 (1), 07-22 (4)**. 728 total retired. |
| Growth engine | **WORKING** | 9 live email sends in 24h (last 12:00), well within the 50-per-sweep cap. |
| Bot-detection rank | **WORKING (no auto-reject)** | 8 bot alerts in 48h (firing, sane rate); **0** auto-rejections (REJECTED with `reviewedById` null) in 48h. |
| Structured rules | **SHADOW, auto-reject OFF** | `clips/route.ts:911` logs `[RULES-SHADOW] realOutcome=PENDING wouldReject=...`; **0** clips rejected citing a rule in 7 days. |
| CRON_DOWN false emails | **none** | 0 cron/down rows in email_events; consistent with the crons heartbeating. |

## PART 5 — look where nobody has looked (the BL-539 category-inconsistency shape)
Measured clipper earnings + owner agency rows across every clip category:
* **Retired (videoUnavailable): CONSISTENT, no leak.** 728 retired clips hold $3,528.58 frozen clipper + $2,361.61 frozen owner earnings, but the withdrawal gate (`/api/payouts/route.ts:119,162`) and `/api/earnings` both fetch clips with `videoUnavailable:false`, so retired earnings are excluded from every withdrawable balance and every live-spend sum. They keep last-known frozen and never re-enter a money path. The poll set also excludes them.
* **FLAGGED: the one residual inconsistency, unchanged.** 6 FLAGGED clips hold $113.50 clipper + $92.17 owner (3 agency rows). Clipper FLAGGED earnings are explicitly bucketed OUT of withdrawable balance (`balance.ts:175`); the $92.17 owner agency rows still exist (BL-542 #4), latent. Same shape and amount as BL-542.
* **Banned users:** $7.60 of approved-clip earnings, blocked by the ban check at withdrawal. Stranded, harmless.
* **Test-user / the new batch path:** exactly 1 approved clip by a test user; `isTestUser` gates only UI visibility, and the batch path uses the clipper core (PENDING, no force-approve, no earnings write), so a test user's clip is treated as a normal clip with no money leak.
* **Marketplace:** 0 clips, 0 earnings, structurally unexercised (as BL-542 noted).

## PART 6 — the scoreboard and verdict
**Is the money correct and the site healthy? YES.** Every paying-clip invariant holds (0 violations, $0.01 drift), no campaign is over budget, the fee is taken once, no double-write, every flow is working, and the withdrawal path correctly excludes retired and flagged earnings. Nothing is actively leaking.

| # | Issue | Money / user impact | Live or latent | Confidence | Next step |
|---|---|---|---|---|---|
| 1 | somesome rate-era exposure (`tracking.ts:2151` gross-without-guard) | owner LOSES $933.94 if unfrozen/re-derived | **latent** (eraExempt untripped) | high | leave frozen; owner's era decision |
| 2 | force-recalc raw-campaign caps bug (`route.ts:259`) | mis-prices on a manual recompute | latent (admin-manual) | high | build `campaignForCalc` snapshot before recompute |
| 3 | PWA-bonus predicate mismatch (`tracking.ts:2082` raw flag) | small clipper over-bonus, path-dependent | latent | high | one shared 2-day PWA freshness helper across the 3 paths |
| 4 | FLAGGED owner phantom ($92.17) + clipper ($113.50) | owner over-accrued; clipper bucketed out already | latent | high | owner ruling on flagged-clip policy |
| 5 | Stamp-agree under-accrual (~$45 in frozen somesome, ~$4 clean) | owner OWED | live (blocked on somesome era) | medium | credit the ~$4 clean; the rest waits on somesome |
| 6 | 3 UNDER_REVIEW payouts stale 24-47 days | none (reserved, unpaid) | live | high | ops: process or void the backlog |
| 7 | 9 REJECTED clips + 89 null-fee stale fields | $0 (cosmetic) | live | high | optional cosmetic cleanup |
| 8 | Marketplace path (no Y1/Y2 caps in code) | $0 today | untested | n/a | implement caps only if it is a policy, before first use |

### What could not be measured, and why
* **The exact stamp-agree under-accrual split within somesome** — needs `decideOwnerGross` replicated per row; the aggregate nets its agree-under against its era-over, so BL-542's $44.95/$48.10 remains the last precise figure.
* **The marketplace path** — 0 submissions, so its behaviour is read-only inference, not measurement.
* **Exact PWA-attributable dollars** within the bonus — `bonusAmount` does not break out level vs streak vs PWA.
* **A this-minute verification re-probe** — cited the BL-614 probe from ~1h earlier (same session, all three platforms distinguishable) plus the current provider ledger, rather than re-running the Apify-free HikerAPI/LamaTok calls a second time.

## Safety and disclosure
READ ONLY. One doc on branch checkpoint/BL-617. No code, data, schema, config or money change; nothing fixed, pushed, merged or flipped. `agency-monitor --fix` NOT run, no platform-wide owner re-derive, BL-538's repair SQL NOT run, the Apify key NOT restored, nothing a live round holds touched. Every number came from a live SELECT via the sanctioned read-only runner with timestamps cast `::text` against DB now(); the pre-fix BL-523/525 snapshots were not used. No handle, bio or address appears here. A markdown-only diff cannot change tsc/build, so none was run and none is claimed. NO dashes used as bullets.
