# BL-585 — Merge the two finished branches so they deploy

**Outcome, honest:** only ONE of the two named branches exists on origin. **checkpoint/BL-573 was
merged to main** (verified push, build green). **checkpoint/BL-574 does NOT exist on origin** (it was
never pushed), so there was nothing to merge for it; I did not fabricate a merge. Details below.

## STEP 0 — truth per branch (with SHAs)
* **checkpoint/BL-573 = `81af6c9b`** (matches the brief's "@ 81af6c9b or later"). On origin: yes.
  Genuinely not on main before this round: `git merge-base --is-ancestor origin/checkpoint/BL-573
  origin/main` exited 1 (not an ancestor), and `git log origin/main..origin/checkpoint/BL-573` showed
  exactly one commit: `81af6c9b BL-573: truthful growth feed + stop the 90k/day bloat + per-sweep
  volume ceiling; stage STALLED_7D`. MERGED this round.
* **checkpoint/BL-574: NOT FOUND on origin.** `git fetch origin checkpoint/BL-574` returned
  `couldn't find remote ref checkpoint/BL-574`, and a full `git ls-remote --heads origin` case-
  insensitive scan for `574` and `verif` found no BL-574 branch (only old `f-verify-*` /
  `bl-161-clips-100-verify` era branches). So BL-574's verification fix for the 114 stuck clippers was
  never pushed and cannot be merged. Reported, not merged. The owner needs to push BL-574 (or point me
  at its real branch name) for a follow-up round.
* **Live rounds left untouched:** BL-583 and BL-584 (checkpoint/BL-584 = `3763494c`, a data-op branch)
  were neither read nor merged.

## The merge (BL-573)
* Merge-base is `94f3cb17` (the BL-576 merge). Against ITS merge-base, BL-573 touches ONLY growth
  files, its own doc, and BACKLOG: `admin/growth/page.tsx`, `api/admin/growth/events/route.ts`,
  `lib/growth/config.ts`, `lib/growth/engine.ts`, `docs/BL-573-GROWTH-FEED-AND-STALLED7D.md`,
  `BACKLOG.md`. It touches NONE of the 6 money files and NONE of the scraper files.
* The direct main-vs-BL-573 tree diff showed apify.ts/lamatok.ts/apify-ledger.ts as "-50/-57/-6", but
  that was purely BL-580's delta showing through (BL-573 branched before BL-580). Confirmed the 3-way
  merge PRESERVED BL-580's code: post-merge `apify.ts` still has `isClipPersistedGone` /
  `persisted-gone-skip` and `lamatok.ts` still has `shadowProbeSlideshowById`. Nothing was reverted.
* `--no-ff` against current main. The ONLY conflict was `BACKLOG.md` (both sides appended a different
  block). Resolved as a UNION by deleting the three conflict-marker lines with literal string edits
  (no regex, so the "$2 in $2,000" capture-group corruption that bit a prior round cannot occur). Both
  the BL-580 and BL-573 entries are present, exactly once each. No conflict markers remain anywhere.
* Schema: BL-573 changes NO Prisma model (no `prisma/schema.prisma` in its diff), so there was no
  column to apply. `npx prisma generate` was run (to rebuild the client after `npm ci`); `prisma
  migrate` was NOT run.

## Confirmations on the merged result
* **Growth feed defaults to LIVE-only:** `events/route.ts:43` defaults `scope` to `"live"`, which maps
  to `{ dryRun: false }` (line 45). The owner sees the 3 real sends, not the 270,645 practice rows.
* **The 50-per-sweep ceiling exists:** `growth/config.ts:105` `MAX_LIVE_EMAILS_PER_SWEEP: 50`.
* **Per-trigger counter replaced the bulk dry-run write:** `growth/engine.ts` uses `dryRunByTrigger`
  (5 references) instead of the per-candidate-per-sweep `recordEmailEvent`; no existing rows were
  deleted (a merge deletes nothing, and BL-573's own change deletes nothing).
* **No env var changed by the merge:** the diff touches no `.env*` file; `GROWTH_ENABLED_TRIGGERS`
  stays exactly as the owner set it (BL-573 only documents the value to set, it does not set it).
* **No email sent:** merging code sends nothing; the growth engine only runs on its own sweep schedule
  in production.
* **BL-574 confirmations (verification tier observations, profile-link-at-entry, non-accusatory
  message) CANNOT be made** because BL-574 is not on origin and was not merged. Stated plainly rather
  than implied.

## Build (actually run, real exit codes from a log)
Eslint binary confirmed PRESENT. `npm ci` exit 0; `npx prisma generate` exit 0 (before tsc);
`npx tsc --noEmit` exit 0 (0 type errors); `npm run build` BUILD_EXIT=0 with prebuild gate from the
log: check:prisma-bypass 0 violations, check:removed-fields OK, lint:hooks 11 problems (0 errors, 11
warnings, at the ceiling -> PASS), then `Compiled successfully`. Not trusting tsc alone; the full
`next build` ran. Built in an isolated worktree at a short path; node_modules was a real `npm ci`,
never junctioned.

## Ship
Verified push (BL-288): `e6a7b85d..bac606ac  bl585-merge -> main`, then re-fetched and asserted
`origin/main == bac606aca5de81de365fec6df5689f0e02c2ae35`. Railway auto-deploys from main, so the
truthful growth feed, the per-trigger counter, the 50-per-sweep ceiling, and the STALLED_7D stage go
live on this deploy. STALLED_7D still requires the owner to add it to `GROWTH_ENABLED_TRIGGERS`
(unchanged by this merge).

## Safety attestations
Merge only. 6 money files BYTE-IDENTICAL by blob OID (`git rev-parse` HEAD vs origin/main). BL-580's
persisted-skip and slideshow shadow preserved. No email sent, no env var changed,
`GROWTH_ENABLED_TRIGGERS` untouched, no rows deleted, `prisma migrate` not run, no conflict markers,
BACKLOG unioned exactly. No clipper bio or handle appears in this report. BL-574 not merged (absent
from origin); nothing a live round holds was touched.
