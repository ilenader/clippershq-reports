# BL-613 — Merge BL-612: account verification fully OFF Apify (the earning gate)

**Merged 2026-07-22.** main `3c59fdc` (merge of checkpoint/BL-612 `3b114d6`). Tags: pre/post-merge-BL-613. Rollback: `git revert 3c59fdc`.

## STEP 0 — truth (with SHA)
- checkpoint/BL-612 `3b114d6` was **genuinely unmerged**: `git merge-base --is-ancestor 3b114d6 main` returned false; `git log main..origin/checkpoint/BL-612` showed exactly one commit (`3b114d6`).
- The **merge-base was main HEAD itself** (`4f1279e`), so BL-612 = main + 1 commit. The `--no-ff` merge had **zero conflicts** and **nothing a live round holds was swept in** (diff is exactly the 5 files BL-612 changed).
- Files: `verify/route.ts` (586, mostly deletions), `hikerapi.ts` (+78), `lamatok.ts` (+79), `scripts/test-bl612-verify.ts` (new), `BACKLOG.md` (+3). No conflict markers anywhere.

## What BL-612 does (confirmed on the merged result)
1. **IG verify path calls HikerAPI, Apify ZERO times.** `checkInstagramBio` (verify/route.ts:135) dynamically imports and calls only `fetchHikerInstagramProfileByUsername` (hikerapi.ts). The IG branch (route.ts:423) never enters the tier cascade where apidojo/Browserless live. Every remaining `apify`/`browserless` token in the file is a comment documenting the removal, not a call.
2. **TikTok LamaTok tier is ADD-ONLY, prior path intact.** route.ts:432 tries `checkTiktokBioViaLamatok` (LamaTok `signature`, tier-0) first; a HIT approves; **any** non-hit (route.ts:440 else) falls through to the unchanged `tier1 = checkBioForCode` + `runHigherTiers` cascade (the existing plain-fetch → apidojo → Browserless). It can only turn a miss into a hit, never regress TikTok. YouTube unchanged (YT Data API, already free).
3. **Transient is distinguishable from a missing code.** IG: `transient`/`no_key` → `transient=true` (refund the rate-limit slot, "try again", never accuse); `not_found` → `transient=false` link-problem message; `resolved` but code not in bio → `transient=false` genuine "we read your bio, code not found". TikTok cascade: `transient = !cascade.found && cascade.allErrored` (only when every tier errored). A fetch failure can never read as a missing code.
4. **Code-match logic unchanged.** IG `bio.toUpperCase().includes(code.trim().toUpperCase())` (route.ts:164); LamaTok `sig.toUpperCase().includes(code.trim().toUpperCase())` (route.ts:196) — the same case-insensitive substring semantics as before, now run against the clean structured bio field.

Verification is **not less reliable** after the merge (a real fetch failure fails safe, a real profile-not-found is stated as a link problem, and a real read-but-absent code is stated non-accusatorily), so the earning gate is satisfied.

## Safety
- **6 money files + `tracking.ts` + `campaign-era.ts` byte-identical by blob OID on BOTH refs** (`git rev-parse main:$f` == `HEAD:$f`, and pre-merge-BL-613 == merged HEAD). None appear in the diff. No earnings/code-match logic touched.
- BACKLOG unioned exact: the single BL-612 entry is present, no `$2` capture-group corruption, no lost entries.
- No `prisma migrate`. Merge only.

## Build honesty
`npm ci` exit **0** (eslint confirmed present, so the hooks gate is real, not a silent no-op). `npx prisma generate` regenerated the client that npm ci wiped. `npx tsc --noEmit` exit **0** (0 type errors). `npm run build` exit **0** from a captured log: check:prisma-bypass 0 violations, check:removed-fields OK, hooks gate `eslint --max-warnings 11` exit **0** (≤11 warnings, all pre-existing exhaustive-deps in UI components this merge never touched), "Compiled successfully". Both tsc and `next build` actually ran; nothing was piped through tail.

## What is now TRUE for the owner
BL-612 was the **last FAIL-BROKEN Apify path**: before it, an Apify fetch hiccup during Instagram verification could read as a **missing code** and wrongly reject a real clipper. That path now reads the bio via HikerAPI and treats any fetch failure as transient (refund + try again). Per BL-611, **every remaining Apify call already fails safe** (returns NULL, never 0, so tracking keeps last-known views and no earnings freeze or drop to 0). So turning Apify off is now safe.

### Exact Railway step
There is **no `APIFY_HARD_OFF` variable wired in the code** — the real kill switch is **removing the Apify key**:
1. Railway → the Clippers HQ project → the app service → **Variables**.
2. **Delete `APIFY_API_KEY`** (and `APIFY_TOKEN` if present), or set the value empty.
3. Save. Railway redeploys automatically (or press **Deploy**).

### What to watch after
- **Apify billing** → should fall to ~$0/day within a day (no actor runs, no apidojo `$0.01` lines).
- **Tracking cron logs** → `[SCRAPER-DECISION]` shows HikerAPI hits; Apify-bound clips resolve as fail-safe null misses (keep last-known). **No clip should drop to 0 views or freeze.**
- **Account verification** → IG still verifies via HikerAPI, TikTok via LamaTok tier-0 + plain fetch (the apidojo/Browserless higher tiers go quiet without the key but fail safe as transient), YouTube via YT Data API.
- If anything looks off, restore the variable — the switch is fully reversible.
