# BL-660 — Merge BL-658 (cron auth) + BL-659 (shadow persistence) to main

## HEADLINE: NO CRON DIED. After the deploy, all three heartbeat crons fired again, including both routes changed by BL-658 (lifecycle at 13:15 UTC, watchdog at 13:30) and the tracking money cron on its separate Railway service (13:10). Both branches merged cleanly to main (`5c667b25`, origin==local), auth is now bearer-only on lifecycle and watchdog with no other route affected, the shadow persists, and auto-reject stays off.

**2026-07-24** · Merged `origin/checkpoint/BL-658` (`8c7327e1`) and `origin/checkpoint/BL-659` (`d47560ab`) into `main`, which advanced `16028f1f` → `5c667b25`, origin==local confirmed. Tags `pre-merge-BL-660` / `post-merge-BL-660`.
**tsc exit 0. `npm run build` exit 0. Hooks gate 0 errors, 11 warnings (cap 11), eslint present.** Exit codes echoed from redirected logs, never piped through `tail`.
**Money-critical safety:** the 6 money files plus `tracking.ts` and `campaign-era.ts` are **byte-identical by blob OID** across both refs.

## Worktree note
The `main` branch was checked out in a stale, dirty leftover worktree (C:/b575 at `91b84410`, 5 commits behind origin with staged deletions from an abandoned round). I did **not** disturb it. I ran the merge in the primary repo directory (which holds `node_modules`), checked out `origin/main` in detached HEAD, merged both branches there, and pushed the result with `git push origin HEAD:main`. origin/main is the source of truth Railway deploys from; the stale local `main` ref in C:/b575 is unaffected and irrelevant to the deploy.

## STEP 0 — truth per branch
| Branch | Tip | Genuinely unmerged? | Non-empty code diff |
|---|---|---|---|
| checkpoint/BL-658 | `8c7327e1` | YES (`merge-base --is-ancestor` false) | YES: `lifecycle/route.ts` and `watchdog/route.ts`, +31/-37 |
| checkpoint/BL-659 | `d47560ab` | YES | YES: `schema.prisma` +57, `clips/route.ts` +55/-12, `auto-reject-flag.ts` +21 |

Nothing a live round holds was merged; only these two named branches.

## The merges
- **BL-658** merged clean, no conflicts.
- **BL-659** conflicted only in `BACKLOG.md` (both branches appended a new entry at the tail). Resolved as a **union of both sides**: the BL-658 entry and the BL-659 entry are both kept, in order, with only the three conflict markers removed. Verified afterward: **0 conflict markers**, exactly **1 BL-658 entry and 1 BL-659 entry**, no `$2` capture-group corruption. No conflict markers anywhere in `.ts`, `.tsx`, `.prisma` or `BACKLOG.md`.

Merge commit `5c667b25` has parents `98169401` (main + BL-658) and `d47560ab` (BL-659 tip).

## Confirmed on the merged result
- **lifecycle and watchdog reject the header and require the bearer.** No active `x-vercel-cron` read remains in either route (the only remaining mentions are explanatory comments). The live auth in both is now solely `if (req.headers.get("authorization") !== \`Bearer ${cronSecret}\`) return 401`, with a fail-closed 500 when `CRON_SECRET` is unset.
- **No other route lost its auth.** Only lifecycle and watchdog ever referenced the header; every other cron route was already bearer-only and is byte-unchanged. `retire-dead-clips` auth is untouched by this merge.
- **The shadow persists.** `clips/route.ts` calls `ruleShadowDecision.create(...)` (1 site) inside the fail-open `after()` block. The `rule_shadow_decisions` table already exists in prod (BL-659, `CREATE TABLE IF NOT EXISTS`); this merge re-created nothing and ran no `prisma migrate`. `npx prisma generate` confirmed the model is in the client.
- **Auto-reject remains OFF.** `RULES_AUTO_REJECT_LIVE` exists only as `isAutoRejectLive()` (off by default) and gates nothing; there is no reject path. Each shadow row records `autoRejectLive=false`. No clip status is changed, nothing is rejected or flagged, and there is no clipper-facing surface.

## AFTER THE DEPLOY — every cron still fires
Push at ~13:04 UTC triggered the Railway auto-deploy. Pre-deploy baseline (db now 13:03): tracking last 13:00:53, lifecycle 13:00:09, watchdog 13:00:09.

Post-deploy fire times, cast `::text` against DB now:

| Cron | First fire after deploy (::text UTC) | Latest fire | Fires post-deploy | Verdict |
|---|---|---|---|---|
| tracking (money cron, separate Railway service) | 2026-07-24 13:10:11.348 | 2026-07-24 13:30:15.494 | 3 | FIRING |
| lifecycle (BL-658 changed this route) | 2026-07-24 13:15:00.735 | 2026-07-24 13:30:00.741 | 2 | FIRING |
| watchdog (BL-658 changed this route) | 2026-07-24 13:30:00.907 | 2026-07-24 13:30:00.907 | 1 | FIRING |

DB now at final check: 2026-07-24 13:31:17 UTC. Push was ~13:04 UTC. All three fired well after the deploy, so the bearer-only auth is accepting the live scheduler on both changed routes, and the money cron is untouched.

`retire-dead-clips` writes no heartbeat row (it runs once daily at 06:00 UTC) and its authentication was not touched by BL-658, so it could not have been affected by this merge; it will fire on its normal 06:00 schedule.

The **tracking money cron** runs in-process on a **separate Railway service** (`run-tracking-cron.ts`) and never hits the HTTP route auth, so BL-658's change could not touch it; its post-deploy heartbeat above confirms it kept firing regardless.

## Gate results, honest
`npm ci` exit 0; `npx prisma generate` run before tsc (model present in client). `npx tsc --noEmit` exit 0, zero errors. `npm run build` exit 0, "Compiled successfully", prebuild gates all ran (prisma-bypass 0 violations, removed-fields, hooks lint). Hooks gate: 0 errors, 11 warnings against a cap of 11, eslint present.

## Safety and disclosure
Merge only. No money file, clip earnings or status changed; all 8 protected files byte-identical by blob OID across both refs. No route lost authentication; lifecycle and watchdog are now bearer-only. Auto-reject stays OFF with no clipper-facing surface. No `prisma migrate`, no table re-created, no secret set or printed, no clipper caption in this report. BACKLOG unioned exact. NO dashes used as bullets.
