# BL-592 the last 30 rounds, verified live: merged, deployed, and behaving?

**2026-07-19 · AUDIT ONLY. READ ONLY on code, config, data and money. Nothing changed, pushed, merged or flipped.**
**Base:** origin/main @ `5096fd4f` · **Branch:** checkpoint/BL-592 · **DB now() = 2026-07-19 18:22 UTC** (all timestamps ::text, compared to it). **Probe cost: $0** (read only `run-select.js`; 0 Apify actors; 2 Apify billing calls, both rejected by an invalid token, $0; no HikerAPI probe).

## THE HEADLINE
Most fixes are live and behaving. **One real failure, and it is the owner's main worry: the `retire-dead-clips` cron is deployed but INERT (never registered), so BL-590's Apify-leak kill is dormant.** One measurement fix (verify observations) has zero rows and cannot be confirmed. The money core is healthy: invariant clean on every paying clip, STRAENGE gross-correct under its reserve, somesome untouched, the payout rule holding on live data.

## SCOREBOARD

| # | Fix | Merged | Deployed | Live behaviour | Evidence (live, ::text) |
|---|---|---|---|---|---|
| 1 | STRAENGE + BL-563 shared gross guard | Y | Y (07-17) | **PASS** | `owner-share-guard.ts` live, `decideOwnerGross` wired in gamification + force-recalc. Live owner accrual (available clips) **$998.48**, under the $1,000 reserve; clipper pool **$1,997.56**/$2,000; live total **$2,996.04**/$3,000. somesome $3,427.03 unchanged. See note A. |
| 2 | Earnings invariant 0 / sub-cent (BL-542) | Y | Y | **PASS** | APPROVED clips **3,235, 0 violations, max drift $0.01**. See note B. |
| 3 | somesome $933.94 reachable only via untripped eraExempt (BL-570/572) | Y | Y | **PASS** | `campaign_status_changes` eraExempt=true = **0 of 32** (button NOT tripped). somesome owner $3,427.03 unchanged, 953 clips not collapsed. See note C. |
| 4 | Payout USDT rule, 3 paths, net, chain="Ethereum" (BL-556) | Y (07-17 15:26) | Y | **PASS** | `validatePayoutMethod` at all 3 create paths (route.ts, referral-request, actions/payouts.ts); `SOLANA_MIN_NET_USD` one constant. Live: **2 post-deploy payouts, both USDT / Ethereum, min $12.82**. No USDC / TRON / BEP20 / sub-threshold Solana since deploy. |
| 5 | Never-decrease guard committed (BL-538) | Y | Y | **PASS (code); live-behaviour UNVERIFIED** | `earnings-never-decrease.ts` committed, imported in force-recalc, ON by default (needs explicit `allowDecrease:true`). No force-recalc run occurred to observe a block; consistent with item 2 being clean. |
| 6 | apidojo-IG ~80% leak reduced vs ~$1.23/day (BL-590) | Y (07-19 18:05) | Y | **FAIL (not yet reduced)** | Counter fix + K=3 deployed **~17 min before this audit**. The reduction depends on the retirement cron, which is INERT (#7). Ledger today: `apify/instagram-api-scraper` 144 runs $0.40; the dominant apidojo-IG leg is ledger-blind (BL-588), real $ UNVERIFIED (billing token invalid). By mechanism the leak persists. |
| 7 | retire-dead-clips cron REGISTERED and FIRING | Y | Y (route only) | **FAIL / INERT** | Route `api/cron/retire-dead-clips` exists, but `retire-dead-clips` is **absent from CRON_JOBS** in `railway-cron-scheduler.ts` (list: tracking, lifecycle, watchdog, expire-deadlines, discord-role-reconcile, notifications-cleanup, decay-strikes, counter-recompute) and not in `RAILWAY_NATIVE_CRON`. **0 retirements since BL-584** (716 unavailable, last retire 07-18 19:10). Classic BL-568 unregistered-cron failure. See fix D. |
| 8 | consecutiveGone climbs past 3 for dead clips (BL-590) | Y | Y (~17 min) | **PASS (early)** | Live distribution: k>=3 = **31 clips** (k3:7, k4:2, k5:1, k7:4, k8:15, k31:2). Not stuck at 0. Reset-fix is only ~17 min live, so most of this climb predates it; the fix's own effect is not yet independently provable. |
| 9 | 710 retired clips stayed retired (BL-584) | Y | Y | **PASS** | `videoUnavailable=true` total **716** (6 historical + 710), unchanged since BL-584; **0 retired or revived since 07-18 19:10**. Poll set excludes them (`dueJobs` carries `videoUnavailable:false`). |
| 10 | TikTok holds at ~$0.09/day floor (BL-588) | Y | Y | **UNVERIFIED** | Billing API unreadable (token invalid). Ledger shows LamaTok-primary TT (prepaid) + small clockworks rescue; code mechanism (LamaTok-miss + slideshow floor) unchanged. Exact $0.09 not confirmable here. |
| 11 | Money tracking cron heartbeat (BL-566) | Y | Y | **PASS** | `cron_runs` tracking last fired **2026-07-19 18:20:26 UTC, 5 min ago** (fires every 10 min). Lifecycle 10 min, watchdog 25 min ago. Alive. |
| 12 | CRON_DOWN false-email storm stopped (BL-566) | Y (07-17 17:33) | Y | **PASS** | Last CRON_DOWN notification **2026-07-17 14:30 UTC**, BEFORE the deploy; **0 CRON_DOWN since** (~2 days). CRON_STATUS (never-email tier) 3 rows, last 07-18. The 15/day storm is gone. |
| 13 | Growth feed truth + per-trigger counter + 50-cap (BL-573) | Y (07-19 14:27) | Y | **PASS** | Feed scope defaults `live` (dryRun:false) so it shows the ~37 real sends, not 377k practice. `dryRunByTrigger` counter live; **dry-run bloat STOPPED** (last dryRun row 14:31 UTC, 0 written after deploy). `MAX_LIVE_EMAILS_PER_SWEEP=50` in config and enforced (engine.ts:408). |
| 14 | STALLED_7D sending within cap (BL-573) | Y | Y (env set by owner) | **PASS** | STALLED_7D dryRun=false: **16 email + 17 in-app, last 18:00 UTC**. Owner set the env var; it is sending. 16 <= 50 cap. Open/bounce UNVERIFIED (no Resend-webhook rows in email_events). |
| 15 | Verify persist observations + validate link at entry (BL-586/587) | Y (07-19 15:20) | Y | **P2 PASS; P1 UNVERIFIED** | PART 2 link validation: `verify-handle.ts normalizeHandleInput` wired in POST /api/accounts (code live). PART 1 persistence: `lastVerifyAt` **NULL on all 999 accounts**; no clip_account updated after the 15:20 deploy (latest 14:58), so no verify attempt has occurred to record. Cannot confirm the write without triggering a verify (runs the forbidden Apify actor). See note E. |

## FAILURES, RANKED BY IMPACT
1. **#7 retire-dead-clips cron INERT (Apify money, owner's #1 worry).** The whole BL-590/BL-584 automatic-retirement path is dormant. It cannot self-fire.
2. **#6 apidojo-IG leak not yet reduced** — a direct consequence of #7 plus the 17-minute-old deploy.
3. **#15 PART 1 verify observations empty** — the one-SELECT measurement the owner is relying on to size the 114-stuck problem has 0 rows; unprovable until a real verify runs.

## EXACT NEXT STEPS
* **D (fixes #7, unblocks #6):** a CODE round adds to `CRON_JOBS` in `src/lib/railway-cron-scheduler.ts`, e.g. `{ name: "retire-dead-clips", path: "retire-dead-clips", due: (n) => n.getUTCHours() === 6 && n.getUTCMinutes() === 0 }`; **then the owner sets `RAILWAY_NATIVE_CRON` to include `retire-dead-clips`** (comma-separated; `*` alone will not enable it until the CRON_JOBS entry exists). Confirm first with a manual `GET /api/cron/retire-dead-clips?dryRun=1` + `Authorization: Bearer <CRON_SECRET>`.
* **#15 PART 1:** trigger ONE real IG verify (owner action, not this audit; it runs the Apify profile actor) then re-run the one SELECT on `clip_accounts."lastVerifyAt"`. If still NULL after a genuine attempt, the best-effort write is broken.
* **#6/#10 exact dollars:** read the Apify billing API with a valid account-scoped token (the `.env.local` APIFY_API_KEY is truncated/invalid, 30 chars).

## NOTES (the honest detail behind the PASS/FAIL)
* **A. STRAENGE is NOT the claimed $999.72 to the cent, and that is fine.** Live owner accrual on available clips is **$998.48** (under the $1,000 reserve); one clip's owner row was recomputed today (+$0.82), which is exactly BL-563's guard writing GROSS, not eroding. The raw sum including 16 frozen (videoUnavailable) clips is owner $1,000.54 / clipper $2,001.71; the $2.06 of frozen owner rows and $4.15 of frozen clipper earnings do not count as live spend (owner display = $0 when videoUnavailable). The guard SURVIVES and the money is correct; the specific figure has simply drifted from the BL-562 snapshot.
* **B. The 10 whole-population invariant violations are all REJECTED clips** (earnings correctly 0, `baseEarnings`/`bonusAmount` left non-zero; max stale-field gap $17.02). Zero of them pay a cent. Among APPROVED clips the invariant is perfect. Worth a cosmetic cleanup (zero base/bonus on rejection), not a money risk.
* **C.** eraExempt=0 proves the keep-existing-earning boundary was never tripped and somesome's rows were not re-derived. The exact "108 rows / $933.94" figures originate in BL-570/572, which were not in this audit's read set, so those specific counts are UNVERIFIED-in-detail; the guardrail they protect (untripped, somesome stable at $3,427.03) holds.
* **E.** BL-586/587 shipped 07-19 15:20 UTC; the newest clip_account touch is 14:58 UTC, so genuinely nobody has verified since it went live. 0 rows is consistent with "no attempts yet," NOT proven-broken. It is also not proven-working: it stays UNVERIFIED until a real attempt records a row.

## Safety
READ ONLY. One doc on branch checkpoint/BL-592. No code, config, data or money change. No Apify actor run. `agency-monitor --fix` NOT run, no owner rows re-derived, no retirement SQL, nothing a live round holds touched. Every probe disclosed ($0). Timestamps cast ::text and compared to DB now() (18:22 UTC). Report claims are verified against live state, never inherited from a report. No handle, bio or address appears here. A markdown-only diff cannot change tsc/build; none was run.
