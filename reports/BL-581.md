# BL-581 — merge BL-580 to main (deploy the cooldown-immune 616 skip + slideshow shadow)

**2026-07-18 · merge-only round · worktree C:/b581 (RAN ALONE) · Haiku-scoped, no new code written.**

Merged `checkpoint/BL-580` into `main` so BL-580's already-shipped fixes deploy. Wrote no data, retired
no clip, ran no signoff SQL, changed no money.

## STEP 0 — truth (with SHAs)
- `origin/checkpoint/BL-580` = **290a2443**. `origin/main` (before) = **94f3cb17**.
- `git merge-base --is-ancestor origin/checkpoint/BL-580 origin/main` exited **1** ⇒ BL-580 was
  genuinely NOT on main. `git log origin/main..origin/checkpoint/BL-580` = the single commit 290a2443.
- merge-base(origin/main, BL-580) = **94f3cb17** = origin/main itself ⇒ no divergence, so the merge
  brought ONLY BL-580's one commit. Nothing a live round holds (BL-573/574/577/579) was merged.
- BL-580 touched 4 files (+119, -0): BACKLOG.md, apify-ledger.ts, apify.ts (+50), lamatok.ts (+57).

## The merge
- `git merge --no-ff origin/checkpoint/BL-580` → "Merge made by the 'ort' strategy", **0 conflicts**
  (main == merge-base, so BACKLOG did not conflict; the $2,000 capture-group corruption risk did not arise).
- Merge commit **e6a7b85d**, parents p1=94f3cb17 (main) p2=290a2443 (BL-580). No conflict markers anywhere.
- BACKLOG entry count: origin/main **80** → merged **81** (exact union, +1 for BL-580, no duplication).

## Confirmed on the MERGED result
- **apify.ts:616 reads the PERSISTED counter, fails open to NULL.** `isClipPersistedGone(clipUrl, 5)`
  queries `db.trackingJob.findFirst({ where: { clip: { clipUrl } }, orderBy: { consecutiveGone: "desc" },
  select: { consecutiveGone: true } })` and returns `(job?.consecutiveGone ?? 0) >= 5`. On a hit the
  fetch path logs `[SKIP-APIFY] reason=persisted-gone` and returns `{ stats: null, provider:
  "persisted-gone-skip" }` — NULL, never 0 (BL-543 keeps last-known views on NULL). It reads a DB field,
  so the cooldown short-circuit that made BL-571's live recheck a no-op cannot silence it.
- **Slideshow by/id fallback is SHADOW only.** `shadowProbeSlideshowById(url): Promise<void>` fires the
  LamaTok `/v1/media/by/id` call and its ONLY output is `console.log("[SLIDESHOW-SHADOW] ...")`; the code
  itself states "no stored view, no earning, no stats object is produced or returned." Fire-and-forget.
- **No retirement path.** Neither change sets `videoUnavailable`; nothing new writes it. No clip is retired.
- **6 money files byte-identical by blob OID on BOTH refs** (`git rev-parse origin/main:f` vs `HEAD:f`):
  clip-earnings-writer, earnings-calc, balance, tracking, clip-earnings-invariant-middleware, money-decimal
  all IDENTICAL. No earnings change, no stored view moves.

## Build (clean install, per brief; real exit codes from logs, not a pipe)
- `npm ci` exit **0** (clean install from lockfile). eslint binary **PRESENT** (hooks gate is real, not a
  silent no-op). `npx prisma generate` (generate only, never migrate) exit **0** (Prisma Client 7.8.0).
- Standalone `npx tsc --noEmit` exit **0** (0 type errors).
- `npm run build` (`next build`) BUILD_EXIT=**0**: "Compiled successfully", "Finished TypeScript" (0 errors).
- Hooks gate (eslint present): prisma-bypass **0 violations**, removed-fields **OK**, lint:hooks
  **11 problems (0 errors, 11 warnings)** ≤ 11 ⇒ **pass**.

## After the push (Railway auto-deploys) — stated plainly
- The 616 persisted-gone skip is now **live**. It is NOT fully dormant: live `tracking_jobs` today show
  `max(consecutiveGone)=6` with **3 clips already at consecutiveGone>=5** (99 above zero). So on deploy the
  skip immediately spares the billed Apify actor for those ~3 confirmed-gone clips, and it grows as more
  clips cross K=5. This is a small, growing saving, not an instant one.
- The slideshow `by/id` shadow is now **logging** (`[SLIDESHOW-SHADOW]`); it writes no stored view and
  flips no live value. It begins the matched-id proof for a later round.
- **Apify spend will NOT drop to zero from this merge alone.** The skip saves only for the clips already
  at K>=5 (a handful today) and grows slowly; the 734 confirmed-dead IG clips are NOT retired here. They
  await the owner running `reports/BL-580-signoff.sql` under a separate, later verified round — which this
  round did NOT run.

## Safety ledger
Merge only. No signoff/retirement SQL run. No `prisma migrate`. No data written, no clip retired, no
stored view moved, no earning changed. 6 money files byte-identical by blob OID on both refs. No conflict
markers. BACKLOG unioned exactly (80→81). Nothing a live round holds was merged. main pushed and verified
origin==local.
