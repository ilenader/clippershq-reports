# BL-855: The ranking is one term now. The tag bank was already spent, and a test had quietly deleted a live config key.

**Date:** 2026-08-01 · **Type:** Act on BL-850 · **Spend:** **$0.1308 of a $0.50 budget** · Two paid runs, both dollar-capped, both metered on real billed requests

Acting on [BL-850](https://raw.githubusercontent.com/ilenader/clippershq-reports/main/reports/BL-850.md), which tested 40 hypotheses within account with permutation nulls and Benjamini-Hochberg correction. Nothing here re-derives that; it is taken as given.

Honesty tiers: **SHIPPED** (in code, tests green) · **MEASURED** (run here) · **CORRECTED** (a figure in circulation that was wrong) · **REFUTED** (an assumption in the brief that the data contradicts) · **NOT DONE**.

---

## Verdict first

| # | Asked | Result |
|---|---|---|
| 1 | Simplify the ranking | **SHIPPED** — `log10(play_count)` + provisional sub-10s bonus + 2 gates. The engagement panel is now forbidden by an AST guard. |
| 2 | Grow the page pool toward 300 | **PARTIAL, and the reason is the finding.** 189 → **206**. The tag bank was already exhausted at its configured depth; +13 came only after changing depth. |
| 3 | Fix the dedup | **REFUTED — there is no dedup bug.** 0 same-rev duplicates, 0 id collisions. But the correction exposed a real one: the declared panel is **0.6% stored against 21–23% available**. |
| 4 | Record the `follower_count` artefact | **SHIPPED** in `clip_walk.py`, the module whose traversal causes it. |
| 5 | Note layout is underpowered | **SHIPPED** as a comment where it can stop a future edit, not just in a report. |
| 6 | Cost a below-floor sample | **MEASURED — $0.036** for 300 clips across 20 accounts. |

**And one thing nobody asked for, which is the most urgent item here:** `tests/test_dashboard.py` writes the **live `config.json`** and had already **destroyed `twitch_finder.min_viewers`**. Restored from this round's backup; the test is fixed so it cannot recur.

---

## 1. The ranking — SHIPPED

`clippershq/clip_pipeline.py::rank_score`, before and after:

```python
# BEFORE — engagement_per_follower was the PRIMARY sort key, above play_count
return (0 if old else 1, epf if epf is not None else 0.0, pc), why

# AFTER
score = math.log10(pc) if pc > 0 else 0.0
if d is not None and d < SHORT_CLIP_S:            # 10.0
    score += SHORT_CLIP_BONUS                     # 0.15, PROVISIONAL
return score, why
```

**What that changes in practice.** The old key meant a 30,000-view clip with a good ratio outranked a 9,000,000-view one. The test suite *asserted* that — `test_bonus_outranks_raw_views_when_present`. That assertion is now inverted and named `test_views_outrank_engagement_INVERTED_BY_BL850`, with the reason in the body.

**Removed, each with its measurement in a comment at the removal site:**

| term | why removed |
|---|---|
| `engagement_per_follower` | r = +0.593 only because its numerator contains views. The per-view ratios are flat: `saves_per_view` r = −0.001 (p = 0.99), `reshares_per_view` r = +0.069 (p = 0.27). |
| age demotion (`AGE_DEMOTE_DAYS`) | r = −0.008, q = 0.987 — the flattest of 40. The constant survives, reporting-only; age is still printed in `score_why`, never scored. |
| `like/comment/save/reshare_count`, `follower_count`, `valence_text`, `audio_type` | added to `_RANK_FORBIDDEN` |

**The provisional term is labelled where someone will actually read it** — at the constant, not only in a report:

```python
# PROVISIONAL, and the only clip feature in the score. BL-850: clips under 10 seconds sit
# +0.184 within-account rank percentile above their own account's other clips, q = 0.037
# after Benjamini-Hochberg over 40 hypotheses ... It rests on n = 24 clips across 10
# accounts ... Six of those accounts contribute exactly ONE short clip.
# Sized DELIBERATELY BELOW the measured effect (0.15 against 0.184 of a rank percentile, on
# a log10 scale where one decile of the library is ~0.35). RE-TEST BEFORE TRUSTING IT.
SHORT_CLIP_S = 10.0
SHORT_CLIP_BONUS = 0.15
```

**The second data-quality gate** is new: a cover frame that is one flat field of colour (`strip_frac ≥ 0.90 and bar_mid_frac ≥ 0.90`) is now gated out. **Stated plainly because it is a real cost:** BL-850 measured those 24 clips at mean within-account rank **0.555 — slightly above their accounts' median.** They are not weak. The gate is about downstream frame-readability only, it is named `_BLANK_STRIP_MIN`, and it is one line to delete.

**Verified live:**

```
SHORT_CLIP_BONUS   = 0.15
epf forbidden      = True      follower forbidden = True
score(1M views, 50s) = 6.0000      score(1M views, 8s) = 6.1500
forbidden_fields_read(rank_score, gate) = set()
```

### Two things happened mid-round that changed the shape of this work

**MEMEBOT-017 deleted `loop_runner.py` while I was editing it.** Its claim said it would ("pick one, absorb/delete the other"), and it did. My edit to `rank_clips` — dropping the `engagement_per_follower` tie-break for a stable `clip_id` one — went with it. That is the correct outcome: the surviving implementation is `clip_pipeline.py` and the change lives there. Reported rather than quietly re-applied.

**A concurrent writer replaced my text-based forbidden-field guard with an AST one, and it is better.** My first version failed its own test: `inspect.getsource(rank_score)` was grepped for every forbidden name, so a docstring explaining *which* fields were rejected turned the suite red. A guard that punishes documenting an exclusion makes deleting the explanation the cheapest way to go green. `forbidden_fields_read` now walks the AST and reads only real field accesses.

---

## 2. The page pool — the bank was already spent

**The brief's premise needs one correction before the numbers.** "moviefacts was 67% of a recent round's yield while BL-732's old ranking had it absent from the top three." The 67% is true of BL-828 and **means nothing as a ranking**: that run stopped after 8 of 24 tags on a *page target*, tags are walked in bank order, and moviefacts sits near the front. BL-842 walked all 24 and moviefacts came out at **20/158 = 13%**. It is still the best tag. It is not two thirds of the funnel, and no tag ranking taken from a target-truncated run measures anything but list position. That is BL-842's finding, not this round's.

### Costed before spending, as required

| | |
|---|---|
| pool at round start | **189** confirmed pages |
| target | 300 → **need 111** |
| cost per confirmed page | **$0.00126** (BL-828's real billed run) / $0.00145 (hold-out, gate 0.40) |
| projected | **$0.14**, ~233 calls |
| cap set | **$0.25** — half the budget, ~1.8× the projection |

The pool count itself needed a fix first: keying on "`source_tag` is non-empty" returned **648**, because Spotify's funnel fills `source_tag` too (`playlist:37i9…`, `related:luvcat`). Keying on `source_hashtag == "repost_finder"` — the literal string the writer stamps — gives 189. A contaminated denominator would have made the target look already met and the run would have been skipped for the wrong reason.

### Run 1 — the bank is exhausted at depth 2

Two tags walked (#batman, #moviefacts), 4 pages, 120 clips, **$0.0156**. Then, for **$0**:

```
pages_per_tag                        2
tags configured                     20
reachable posts per full pass    1,200   (20 tags x 2 pages x 30 clips)
seen-cache entries               1,423
seen-cache / reachable           118.6%
```

**The seen-cache is larger than everything one full pass can reach.** BL-842 swept every tag to depth 2 and the cache remembers it. Re-running the same bank at the same depth walks posts already paid for. The supply is not "24 tags"; it is "24 tags × 2 pages", and it was spent.

### Run 2 — the depth lever, and it works

`pages_per_tag` 2 → **6** on the four top-yielding tags. Pages 3–6 have never been walked, so the seen-cache cannot cover them by construction.

```
4 tags x 6 pages = 720 clips seen
  10 already known, 39 NEW to check
  gate outcomes:  13 MEME PAGE  |  7 not English  |  6 below the 40% bar  |  1 too few distinct clips
```

**Pool 189 → 206.** Measured against this round's own timestamped master backup:

| tag | before | after | new |
|---|---:|---:|---:|
| ryangosling | 12 | 16 | **+4** |
| scifi | 11 | 15 | **+4** |
| moviefacts | 22 | 25 | **+3** |
| literallyme | 13 | 15 | **+2** |
| batman | 6 | 9 | +3 |
| superman | 3 | 4 | +1 |

**+13 is cleanly attributable to the depth run** — those are the only four tags it walked. The +4 on batman/superman is not mine to claim: #batman was walked at depth 2 by run 1 before I stopped it (its kill-safety persists partial results, which is the behaviour working), and **#superman was walked by neither of my runs** — with 13 rounds in flight, another round plausibly ran discovery concurrently. Reported as unattributed rather than counted.

### The projection was right about money and wrong about time

**Cost: $0.1308 total against a $0.50 budget** — projection $0.14, actual close. **Time: badly underestimated.** I modelled 4.2 s per candidate from thumbnail fetches. The account gate runs EasyOCR over 12 thumbnails per candidate at ~1.34 s each — **~16 s of local CPU per candidate**, so the real figure is nearer 20 s. Run 1 took ~55 minutes to spend 1.5 cents.

> **This funnel is TIME-bound, not dollar-bound.** A $0.25 cap is not a meaningful constraint on it; wall-clock is. Reaching 300 pages needs ~7 more tags at depth 6, roughly **$0.07 and 3–4 hours**, and the useful knob is concurrency or `use_ocr`, not the budget.

**NOT DONE:** the pool is at 206, not 300. Getting there is now a known, cheap, boring operation — the finding was that the old configuration could not have got there at any price.

---

## 3. The dedup — REFUTED, and what it exposed instead

The brief's hypothesis was "either dedup is not firing or clips arrive under different ids." **Neither.** Measured on a frozen snapshot (BL-851 was appending live):

```
rows                                 1,019
distinct clip_id                       664
clips with multiple rows               348
  same rev  (DEDUP NOT FIRING)           0     <-- the bug hypothesis
  differing rev (deliberate update)     348
rev histogram              {1: 664, 2: 348, 3: 7}
same clip_pk under different clip_id     0
same permalink under different clip_id   0
last-wins kept the POORER copy           0     <-- BL-710's trap, not present
```

Every extra row is a `rev`-2 or `rev`-3 update from the vision-labelling passes. `read_all()` last-wins by `(clip_id, rev)` exactly as documented, and `stats()` has always returned the right number. **"755 clips" was a row count quoted as a clip count** — a reporting error, not a code defect. The true figures at snapshot time: **664 distinct clips, 68 accounts, 1,019 rows.**

### The real defect the correction uncovered

`clip_library.py` claimed the rescued declared panel was filled at *"genre_declared 41.6%, cast 31.3%, genre 30.1%…"*. Re-running `caption_parser.analyze_caption` over the captions **already stored in the library**, next to what is actually on disk:

| field | available from stored captions | actually stored |
|---|---:|---:|
| cast | **23.5%** | 0.6% |
| genre | **22.4%** | 0.8% |
| where_to_watch | **21.4%** | 0.8% |
| ratings | **21.1%** | 0.5% |
| summary | **20.3%** | 0.5% |
| title | 3.2% | 0.5% |
| content_medium | **0.0%** | 0.2% |

**The panel BL-838 → BL-843 rescued from being discarded is still, in practice, not stored** — by a factor of ~35. The wiring in `clip_runner.py` is correct and does pass `caption_fields`; what is missing is a **backfill of the rows written before it**. That backfill is **$0 and makes no network call** — the captions are on disk and re-running the analyzer over them is local CPU. Roughly 150 clips per field are waiting.

`content_medium` is a separate, still-open problem: 0.0% *available*, so the analyzer is not producing it at all.

**NOT DONE deliberately:** the backfill writes `clip_library/`, which BL-851 and BL-854 are both appending to right now. Diagnosing under a live writer is safe; rewriting under two is not. The corrected table is in `clip_library.py` so the next round starts from the real number.

---

## 4. The `follower_count` artefact — SHIPPED into `clip_walk.py`

Recorded in the module whose traversal *causes* it, not in the module that was fooled by it, with the raw rows that make it obvious:

```
richardthedis6067    followers   play_count
  6198                           1,929,194   <- page 1, ranked descending
  ... (7 rows at 6198)
  6247                             567,624   <- page 2, follower count re-read
  ... (5 rows at 6247)
```

`follower_count` is re-read from every page response; the pages are ordered by view count; so every page boundary is also a view-rank boundary and the field steps down the ranking in lockstep. r = −0.566, p < 0.0001, and **circular**. Within an account it varies by a median of 1.1% across 2–3 distinct values, while on the same rows `posted_at` gives r = −0.056, p = 0.325.

It is also in `_RANK_FORBIDDEN`, so the AST guard fails the suite if anything reads it again. The note says explicitly that it **remains correct at account level** — the artefact is in its *within-account variation*, not the field.

---

## 5. Layout — noted, not acted on

Recorded as a comment on `_RANK_FORBIDDEN` explaining why layout is deliberately **not** on that list:

> `gate()` reads it to reject a cover frame with no imagery, which is a usability check, never a score term. BL-850 found layout UNDERPOWERED rather than refuted (79.3% one class, 7 crossing accounts, 91% chance of missing a real one-decile effect), so it must not be scored — and must not be written off.

The distinction matters because "not in the ranking" and "refuted" would otherwise look identical to the next reader, and layout remains the best-attributed and least account-confounded field in the library (2.6% top-2 share).

---

## 6. The below-floor sample — $0.036

`DEFAULT_VIEW_FLOOR = 20000` **ends the walk**; it is not a post-collection filter. Minimum observed play_count is 20,029. Nothing below was ever fetched.

| design | accounts | clips/account | below-floor clips | paid calls | **cost** | walk time |
|---|---:|---:|---:|---:|---:|---:|
| minimal | 10 | 15 | 150 | 30 | $0.018 | ~1 min |
| **recommended** | **20** | **15** | **300** | **60** | **$0.036** | **~1 min** |
| deeper | 20 | 25 | 500 | 80 | $0.048 | ~2 min |
| broader | 30 | 20 | 600 | 90 | $0.054 | ~2 min |

Spread across 20 accounts rather than deep on 5, because the tests need **within-account contrast** and BL-850's power table wanted ≥15 accounts contributing both classes. The paid unit is the **page**, not the clip, so 12 clips cost one call whether above or below the floor. Layout, the engagement panel and the caption panel are all $0 or local CPU on top.

> **$0.036 — 14% of one discovery run — is what it costs to make every within-account question answerable.** Every dollar spent above the floor buys more winners and no contrast. This is the highest-value purchase available and it was not made this round only because it was scoped as a costing.

---

## 7. UNASKED AND URGENT: a test suite was deleting live config keys

`tests/test_dashboard.py::test_put_writes_a_block_scoped_knob_into_its_block` PUTs to the live `/api/settings`, which writes the **real `config.json`**, restoring the old value in a `finally`. It hard-coded `twitch_finder.min_viewers` and restored with `int(before)`.

Diffed against this round's own timestamped backup:

```
twitch_finder.min_viewers    backup = 20     live = MISSING
```

**The failure is self-perpetuating.** When the key is absent, `before` is None, `int(None)` raises in the body — and raises *again* in the `finally`, so the restore never runs. The first crash removes the key; the missing key guarantees the next crash.

- **Restored** to 20 via `main.save_config` (own timestamped backup, validate, atomic replace).
- **Fixed** the test: pick a knob that is actually present and integer-valued, skip if none, and assert the live config is left as found.
- **Not reverted:** `spotify_finder.run_target` also differs from the backup (700 → 900). That is a plausible deliberate change by one of the rounds in flight and is not mine to undo. Reverting everything that moved would clobber other people's work, which is why the diff was inspected key by key.

---

## Proof

| check | result |
|---|---|
| **Suites** — `tests/run_all.py` | **ALL GREEN — 87/87 suites, 3,724 checks (490.9 s).** `test_clip_pipeline` (82 checks), `test_clip_walk` (31), `test_clip_library` (36), `test_dashboard` (32) included. |
| **Campaigns SHA** | `json.dumps(campaigns, sort_keys=True)` → **`8e02f8d6f6307ae8` — MATCH** |
| **Config** | parses, 162 top-level keys, `config_defaults` imports, `twitch_finder.min_viewers` = 20 |
| **Spend** | **$0.1308** of $0.50. Both runs capped at $0.25, metered on `client.http_requests` |
| **Backups** | `config.json` (101,793 B), `spend.json` (48,011 B), `master_leads.csv` (24,450,993 B) → `scratch/bl855_backups/20260801-161244/` |
| **Credentials** | none printed, logged, written or committed. Both run scripts set their cap on an **in-memory** config copy; `config.json` on disk was written only by the one-key restore, through `save_config` |
| **Ranking live** | `score(1M, 50s) = 6.0000`; `score(1M, 8s) = 6.1500`; `forbidden_fields_read(rank_score, gate) = set()` |

**Recorded because it cost real time to diagnose:** the first full-suite run came back with `test_claims_manifest.py` and `test_filelock.py` red. Both passed when run individually (12 and 4 tests, OK), and the second full run was **87/87 green**. Both do git and lock operations, and thirteen rounds were writing the tree concurrently. **In this repo a red suite is not evidence of a break until it reproduces** — but neither is one green run evidence that a flake is gone, so they are named here rather than forgotten.

### Concurrency, since "RUN ALONE" was not available

Seven rounds were in flight at claim time and thirteen by the end. `tools/claim.py` is advisory, so BL-855 proceeded with conflicts declared in the claim itself and a per-file mtime guard (`scratch/bl855_guard.py`) that **fired once** — MEMEBOT-017 had moved both ranking files — and was re-baselined rather than overridden. Final guard state: all three files CHANGED **by this round**, none by another writer. `clip_library.py` was edited for the one comment correction despite BL-849/BL-853 holding it; it is comment-only, has no behavioural effect, and was verified present afterwards.

---

## What to do next, in value order

1. **Buy the bottom — $0.036.** Nothing else unblocks a within-account question.
2. **Backfill the declared panel — $0, local CPU.** ~150 clips per field of `cast`, `genre`, `ratings`, `where_to_watch`, `summary` are sitting in captions already on disk. Run it when `clip_library/` has one writer.
3. **Finish the pool to 300 — ~$0.07, 3–4 hours.** Depth 6 on the remaining ~7 producing tags. Budget is not the constraint; OCR wall-clock is.
4. **Re-test the sub-10s bonus** on the next 200 clips. It is 24 clips today and it is the only clip feature in the score.
5. **Audit every test that writes live state.** `test_dashboard.py` was found by accident because this round happened to diff a backup. Nothing was watching.

---

## Verdict

1. **The ranking is one term** — `log10(play_count)` — plus a labelled provisional bonus and two data-quality gates. The engagement panel is forbidden by an AST guard, not a comment.
2. **The tag bank was spent before this round started.** The seen-cache covered 118.6% of what a full pass could reach. Depth, not another pass, is the lever; it moved the pool 189 → 206 for 13 cents.
3. **There is no dedup bug** — 0 same-rev duplicates, 0 id collisions. True count: **664 clips, 1,019 rows**.
4. **The declared panel is 0.6% stored against 21–23% available.** That is the real defect, and the fix is free.
5. **`follower_count` is the crawler's page position**, documented in the module that creates it.
6. **A test suite had deleted a live config key.** Restored and fixed.
