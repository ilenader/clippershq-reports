# BL-555 — the gone-leak is on main

**2026-07-17 · MERGE ONLY. `origin/main` @ `730d4a28`, verified.** No code authored. Nothing switched.
**6 money files byte-identical by blob OID. `tracking.ts` not in the diff. No clip's earnings changed.**

---

## STEP 0 — truth per branch, with evidence

| branch | on origin | on main before | action |
|---|---|---|---|
| **checkpoint/BL-554** @ `4bb1d14c` | yes | **NO, 1 commit ahead** (`merge-base --is-ancestor` false) | **MERGED** |
| checkpoint/BL-538 @ `044d2172` | yes | NO, 1 commit ahead | **LEFT for its own round** |

**BL-554 was genuinely unmerged**, so the gone-leak was live and still billing until this merge.

**BL-538 was deliberately not merged.** A live worktree holds it at `C:/bl538`, and it is a 15-file
schema-plus-migration round. Outside this round's remit and untouched: **0 of its files appear in my
diff**.

---

## What landed

**The fix, one line at `apify.ts:1502` (was 1482 pre-merge):**

```ts
// before — every URL Hiker did not resolve, INCLUDING the gone-404 clips
const missing = urls.filter((u) => !primaryMap.has(normalizeUrlForMatch(u)));
// after — the gone-aware set the fan-out already computed
const missing = urlsForApify.filter((u) => !primaryMap.has(normalizeUrlForMatch(u)));
```

BL-275's `[SKIP-APIFY] reason=gone-404` protected the api-scraper tier but never the apidojo tier
below it, which re-derived its own set and was gone-blind. **Caught red-handed on one tick:** the
ledger logged `toApify=0 gone=15` at 12:30:26.313, and Apify's own invoice shows
`apidojo/instagram-scraper-api` running on 15 deleted reels at 12:30:26.688 for $0.075. BL-550 had
already probed 37 such clips against Apify directly: **Apify resolved 0 of 37.**

**Confirmed on the merged result, and TikTok's correctness is the proof of the shape.** The three
other `urls.filter` sites are the TikTok paths, which never had this bug and say so out loud
(`apify.ts:1057-1061`):

```ts
// Only transient/carousel/ambiguous misses fall back to Apify; gone URLs
// are dropped from the Apify batch entirely.
const missing = urls.filter((u) => {
  const k = normalizeUrlForMatch(u);
  return !lamatokMap.has(k) && !goneSet.has(k);
});
```

**Instagram now matches TikTok.**

**Dead code gone.** `fetchInstagramStatsBatchLegacy` and `getInstagramActor` both deleted:
**0 live definitions remain**. `getInstagramActor` was the only reader of `APIFY_INSTAGRAM_ACTOR` and
defaulted to `apify/instagram-reel-scraper`, the actor BL-543 removed for a measured 0.0% win rate.
**That env var is now permanently inert**, so the stale value still in `.env.local` can never
resurrect a dead actor.

**The LamaTok 5xx retry is present and fails open.** `LAMATOK_RETRY_DELAY_MS = 600`,
`fetchLamatokTiktokByUrlOnce` + an `isRetryable` gate on `verdict === "transient"` and a 5xx or
network status. A 404 is not retried (definitive). A 429 is not retried (needs backoff above clip
level). **Both attempts failing yields `stats: null`, never 0**, so tracking keeps the last known
value and no clipper's views are zeroed.

---

## Safety

**6 money files, blob OID via `git show` on BOTH refs** (never worktree-vs-`git show`; the CRLF
artifact has faked five alarms):

```
IDENTICAL  797e20985ad57475ef321afcf3cb1ea7b0d6ab84  earnings-calc.ts
IDENTICAL  7aa6be48345dfad1b785ccb9ad30cd41f128f753  clip-earnings-writer.ts
IDENTICAL  f8499e18b7950530d934ced3b22736b42b7f289f  balance.ts
IDENTICAL  c2db86158f8c138a0a5f01490ef45b115a125f4b  tracking.ts
IDENTICAL  ef5cdae757b9ad3c23380ee8b63e279f98d0b6ac  money-decimal.ts
IDENTICAL  61cef39395363c31f0c902dd4c64e8c06b3e6449  clip-earnings-invariant-middleware.ts
```

**`tracking.ts` occurrences in the diff: 0.** Diff scope is 3 files: `BACKLOG.md`, `src/lib/apify.ts`,
`src/lib/scraper-providers/lamatok.ts`. **`campaign-era.ts` and BL-538's files: 0 touched.** The
cadence is unchanged. **No clip's earnings changed and no stored view moved**: this round only changes
which endpoint is asked, and the money files are identical. **The Apify integration is disabled, not
deleted.**

## BACKLOG

Merge-base was current main (`0259298d`), so BL-554 branched from it and main added nothing since.
**No conflict arose and no marker deletion was needed.** Base 61, main 61, BL-554 62.
**Union = 62. Result = 62.** Zero duplicate headings, zero conflict markers repo-wide. The `$2`
capture-group corruption could not occur: **no string replacement was performed at all.**

## Gates, stated honestly

| gate | result |
|---|---|
| `npm ci` | EXIT=0, 822 packages |
| `npx prisma generate` (generate only, never migrate) | EXIT=0, client regenerated after `npm ci` wiped it |
| check:prisma-bypass | EXIT=0, 0 violations |
| check:removed-fields | EXIT=0, OK |
| lint:hooks (BL-348) | EXIT=0, **0 errors / 11 warnings** (<=11 limit) |
| `tsc --noEmit` | EXIT=0, 0 errors |
| **`npm run build`** | **EXIT=0**, read from a log via `echo $?`, never piped through `tail` |

Build reached every stage: "Compiled successfully in 18.9s", "Finished TypeScript in 36.1s", 61 static
pages. **I did not trust tsc alone** (BL-541 hit a real failure here that tsc passed and only
`next build` caught). **eslint binary confirmed present** at `node_modules/.bin/eslint` v9.39.4 with
`npm config get omit` empty, so the hooks gate was not a silent no-op: it reported real counts. No OOM.

---

## PROOF: did apidojo/instagram-scraper-api actually stop?

**PRE-MERGE BASELINE, from Apify's own run log.** In the last hour before this merge (since 13:00 UTC):

```
apidojo/instagram-scraper-api    runs=1  $0.0100   <- STILL RUNNING pre-merge
clockworks/tiktok-scraper        runs=1  $0.0070
apidojo/tiktok-scraper           runs=1  $0.0030
newest Apify run overall: 2026-07-17T13:01:06Z
```

**Push landed 13:06 UTC.** Ledger's newest row at push time: 13:01:42.

**THE VERDICT: apidojo/instagram-scraper-api HAS STOPPED RUNNING.** Measured, one full post-deploy
tick, not assumed.

**`apidojo/instagram-scraper-api` has run ZERO times since the 13:06:19 push, across three
post-deploy ticks (13:10, 13:14, 13:20).** It was 66% of the bill. It is gone.

**CORRECTION, made after this report was first published.** The first version of this section said
"runs since the push: 0" for ALL actors. That was true when written and is **no longer true**: at
13:14:46 `clockworks/tiktok-scraper` ran once for **$0.0040**. I am correcting rather than leaving it,
because a stale zero reads as "Apify is at zero" and **Apify is not at zero.**

**Apify runs since the push (13:06:19), complete:**

| time | actor | $ |
|---|---|---|
| 13:14:46 | clockworks/tiktok-scraper | $0.0040 |
| | **apidojo/instagram-scraper-api** | **none** |
| | apidojo/tiktok-scraper | none |
| | apify/instagram-api-scraper | none |

**What the clockworks run is, and why it is not the gone-leak.** BL-554 deliberately did not touch the
TikTok path, because TikTok never had this bug. That run fired **between** batch ticks (13:11 and 13:20
both logged `toApify=0`), so it is the **single-clip** path (`apify.ts:480`, "only when LamaTok
misses") catching a genuine transient LamaTok miss and failing open. **That is the fall-through working
as designed, not a leak**, and it is exactly the residue BL-554 predicted would remain.

The decisive comparison is a tick that had gone-404 clips to leak and did not leak them:

| tick (UTC) | code | IG gone-404 in the tick | apidojo/instagram-scraper-api |
|---|---|---|---|
| 12:30:26 | old | **15** | **RAN, $0.0750** |
| 12:50:39 | old | (not captured) | **RAN, $0.0350** |
| 13:00:55 | old | (not captured) | **RAN, $0.0100** |
| **13:10:56** | **new** | **5** | **DID NOT RUN** |
| **13:20:46** | **new** | **1** | **DID NOT RUN** |

Before the fix, every tick with gone-404 clips produced an apidojo run. **The 13:10:56 tick had 5 gone
clips and the 13:20:46 tick had 1, and neither produced an apidojo run.** That is the leak closed, in
production, across two independent ticks.

The same tick's ledger rows, for completeness:

```
13:11:05  instagram  hikerapi-instagram-only  clipCount=0     <- nothing reached Apify
13:11:05  tiktok     lamatok-tiktok-only      clipCount=0     <- nothing reached Apify
13:10:56  instagram  fanout:instagram   total=14 hits=9  gone=5 toApify=0
13:11:05  tiktok     fanout:tiktok      total=11 hits=10 gone=1 toApify=0
13:20:46  instagram  fanout:instagram   total=12 hits=11 gone=1 toApify=0
13:20:59  tiktok     fanout:tiktok      total=15 hits=14 gone=1 toApify=0
```

**Honest scope of this proof:** it is THREE post-deploy ticks plus the run log, not a day. The
remaining question is only whether it holds, and tomorrow's invoice line answers it: today measured
**$2.3599** (99.9% `PAID_ACTORS_PER_EVENT`, of which `apidojo/instagram-scraper-api` was **$1.349,
66%**), against $9.10 on 07-13. **If tomorrow is not roughly a third of today, something I did not
find is still calling Apify.** The queries below are how you check, and I would rather you check than
take my word.

### The exact queries to settle it

**1. The ledger:**

```sql
SELECT provider, SUM("clipCount") AS clips, ROUND(SUM("estimatedCostUsd")::numeric,4) AS cost
FROM apify_usage_entries
WHERE "occurredAt" > NOW() - INTERVAL '1 hour'
GROUP BY provider ORDER BY cost DESC;
```

**2. Apify's own run log** (the one that decides it):

```
GET https://api.apify.com/v2/actor-runs?token=<APIFY_API_KEY>&limit=200&desc=1
  filter startedAt > the deploy time; actId VLKR1emKm1YGLmiuZ = apidojo/instagram-scraper-api
```

**3. Apify's invoice**, for the day-over-day:

```
GET https://api.apify.com/v2/users/me/usage/monthly?token=<APIFY_API_KEY>
  dailyServiceUsages[].totalUsageCreditsUsd
  Measured today pre-merge: $2.3599, of which PAID_ACTORS_PER_EVENT $2.358 (99.9%),
  apidojo/instagram-scraper-api $1.349 (66%).
```

**BL-554's own words, and they stand: if `apidojo/instagram-scraper-api` does not fall off the run
log, the fix did not work.** Tomorrow's invoice line is the honest scoreboard: today is $2.36 against
$9.10 on 07-13, and the gone-leak was 66% of what remains.

**What remains, measured rather than hoped:**

1. **The ~$0.29/month of IG profile calls** (`verify/route.ts:233`, `:836`,
   `account-profile.ts:132`), which BL-554 deliberately left on Apify because they gate live clipper
   verification.
2. **The TikTok single-clip fall-through**, now observed once at $0.0040. This is a genuine LamaTok
   miss being caught, which is the fall-through doing its job. BL-550 measured this class at 7 in 400
   (1.75%), of which 6 were the http_500 the new retry should now absorb, leaving ~0.25%. **Whether
   the retry actually absorbs them is the next thing worth measuring**, and one run is not enough to
   say.

**So Apify is not at zero, and I am not going to claim it is.** The 66% that was pure waste is gone.
What is left is a small, real safety net doing real work.
