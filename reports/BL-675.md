# BL-675 — merge BL-673 (Instagram cover capture) to main

**2026-07-29 · MERGE ONLY. Merged to main at `d6373647`, verified on origin.** Base main `0a69fcc4` (Merge BL-671) + `74fcab74` (checkpoint/BL-673). Tags `pre-merge-BL-675` / `post-merge-BL-675`.

## STEP 0 — truth, with SHAs

| Question | Answer |
|---|---|
| Is `checkpoint/BL-673` on origin? | **YES**, `refs/heads/checkpoint/BL-673` = **`74fcab74431c2d7c79a4abe64d79f56c5572b93c`**, exactly the SHA the brief named |
| Was it genuinely NOT on main? | **YES.** `git merge-base --is-ancestor 74fcab74 origin/main` → **NOT MERGED**. main was `0a69fcc4` |
| Is the code diff non-empty? | **YES.** From the merge base `c8321777`: **172 diff lines under `src/**/*.ts`**, 3 files, 423 insertions / 7 deletions |
| Anything a live round holds? | **NO.** `checkpoint/BL-674` (`7c2a1567`) exists on origin and was left alone; `merge-base --is-ancestor 7c2a1567 HEAD` → **correctly NOT merged**. No BL-676 or BL-677 ref exists on origin |

BL-673's own contribution, from the merge base:

```
 BACKLOG.md                             |  15 ++
 scripts/test-bl-673-instagram-cover.ts | 293 +++++++++++++++++++++++++++++++++
 src/lib/clip-thumbnail.ts              | 122 +++++++++++++-
 3 files changed, 423 insertions(+), 7 deletions(-)
```

**A caution worth recording.** A naive two-endpoint `git diff origin/main 74fcab74` reports 7 files and shows BL-671's work as *deleted*, because BL-673 was branched from `c8321777`, before BL-671 merged. That is an artefact of comparing tips, not a real change. The merge-base diff above is the true contribution, and the post-merge diff confirms it: `git diff 0a69fcc4 HEAD` is the same 3 files.

## The dirty main worktree, and what I did about it

**`C:/b575` holds `main` and is BOTH stale and dirty. I did not touch it.**

* It sits at **`91b84410`**, many merges behind the real main (`0a69fcc4`).
* It has **77 dirty entries** staged, including `M BACKLOG.md`, `M prisma/schema.prisma`, and a large number of `D` deletions across `docs/`, `public/splash/`, `scripts/` and `scripts/migrations/`. Merging there would have swept another session's staged work into this merge, and could have committed schema and migration deletions.

So the merge was done in a **fresh, clean worktree at a short path, `C:/m675`**, created detached at `origin/main`, with its own `node_modules` (installed by `npm ci`, **never junctioned**), and pushed with `git push origin HEAD:main`. `C:/b575` is exactly as it was found: same SHA, same 77 dirty entries, nothing staged, unstaged, stashed or checked out by me.

## Conflicts, and the BACKLOG union counted properly

One conflict, `BACKLOG.md`, at a single hunk: HEAD carried BL-671's entry, the branch carried BL-673's. Resolved as a **UNION keeping both**, HEAD side first so the file stays chronological. The three marker lines were removed with an exact line-equality filter in Python, deliberately **not** a `sed` substitution, because a capture-group replacement is what produced the `"$2"` corruption BL-672 caught.

| Check | Before | After | Verdict |
|---|---|---|---|
| `^## BL-` entries | 99 | **100** | +1, exactly BL-673. No entry lost |
| `## BL-671` heading | 1 | 1 | kept |
| `## BL-673` heading | 0 | 1 | added |
| Total lines | 19,646 | **19,661** | +15, exactly BL-673's 15 added lines |
| `$2` occurrences | 73 | **73** | identical, so no capture-group corruption |

The 73 `$2` hits are real dollar amounts, not artefacts: `$2,000` ×15, `$2,000.00` ×9, `$25.86` ×6, `$20` ×6, `$2000` ×5, `$240` ×4 and so on. **Counted with `grep -c`, never with `head`.**

**Conflict markers repo-wide after resolution: 0** (`grep -rn` over `*.ts`, `*.tsx`, `*.md`, `*.json`, `*.prisma`, excluding `node_modules`, counted with `grep -c`).

The merge commit has exactly two parents, `0a69fcc4` and `74fcab74`.

## STEP 6 — the three claims, re-confirmed ON THE MERGED RESULT

**Byte-identical by blob OID, checked on the merged tree against main `0a69fcc4`:**

```
SAME cba6a277  src/lib/scraper-providers/hikerapi.ts
SAME 5ab4cefb  src/lib/apify.ts
SAME fd1f99c0  src/components/clips/ClipCardNew.tsx
```

BL-673's claim holds exactly as stated. The whole merge touches one source file.

**The Instagram path cannot trigger an Apify actor.** Structural, from the merged file: there is exactly **one** `fetchClipStats` call site, `clip-thumbnail.ts:394`, and it is inside `if (!coverUrl && clip.platform === "tiktok")`. The Instagram branch is at `:379` and returns on failure at `:385`. So Instagram is double-guarded, by an early return and by the TikTok-only condition, and can never reach the Apify tier chain.

**A thumbnail failure cannot throw on the submit path.** The capture body opens `try {` at `:357` and closes `} catch (e: any) {` at `:420`, returning `{ url: null, reason: "capture-error" }` at `:422`. Every new branch returns a reason inside that block rather than raising. Independently exercised: the deleted-post probe returned `hiker-not-found` and did **not** throw. (Worth restating: capture is not on the submit path at all. It is triggered lazily by the clip card, per BL-670.)

**TikTok and YouTube still work.** BL-673's harness re-run on the merged tree: **35 passed, 0 failed.**

```
PASS  TikTok's free oEmbed source still serves covers  3 of 6 live posts served a thumbnail_url
PASS  TIKTOK still finds its cover after the shared-path change (0j1025)
PASS  TIKTOK still finds its cover after the shared-path change (5nclha)
PASS  YouTube short-circuits as unsupported-platform (zero provider calls)
PASS  the card's derived i.ytimg.com cover still resolves (free, no key)  status=200 bytes=4760
PASS  INSTAGRAM now gets PAST cover-extraction (no more scrape-empty / no-cover)
PASS  the cover DOWNLOADS with the re-host headers  status=200 bytes=63816 ct=image/webp
PASS  an IMAGE-ONLY CAROUSEL still yields a usable cover
PASS  a deleted/nonexistent post does NOT throw  ·  reason=hiker-not-found
PASS  and it stores NOTHING (never an expiring CDN url)
PASS  it can never be a provider CDN url
PASS  global clip earnings sum unchanged  9901.08999999999 -> 9901.08999999999
PASS  clip count unchanged  4381  ·  PASS  payout_requests count unchanged  137
```

The 3 TikTok oEmbed misses answer HTTP 400 because those posts are deleted. They were equally uncapturable before, and are not a regression.

**No Apify actor ran this round.** Read from Apify's own free run-log API after the merge and after the harness: total frozen at **17253**, newest runs still the two from BL-673's round at 10:47:41Z and 10:48:41Z. Nothing new.

## AFTER THE PUSH — the one step BL-673 could not prove

BL-673 could not execute `storage.upload()` because `SUPABASE_SERVICE_ROLE_KEY` is absent from `.env.local` and lives only in the Railway env. Production has it, so the deploy is where that link closes.

**Production is up and the endpoint is live:** `clipershq.com` answers (307 to the auth redirect), and `POST /api/clips/<id>/thumbnail` answers **401** unauthenticated, so the auth gate is intact.

**Baseline immediately before the deploy, 2026-07-29 11:22Z:**

```
| ig_post_cutover | with_cover |
|            170  |         0  |
```

170 Instagram clips submitted since the Apify cutover, **zero** with a stored cover. That is BL-670's "0 of 169", now 0 of 170.

**RESULT: NOT YET CONFIRMED ON LIVE PRODUCTION, and I am not going to claim otherwise.** Polled once a minute for nine minutes after the push:

```
11:23:39Z  post-cutover IG clips with cover: 0
11:24:39Z  0     11:25:40Z  0     11:26:40Z  0
11:27:41Z  0     11:28:41Z  0     11:29:42Z  0
11:30:42Z  0     11:31:43Z  0
```

**The reason is simply that there has been nothing to capture yet.** Instagram clips submitted since the deploy window (after 11:20Z): **0**. The newest Instagram clip on the platform is `cms5yt6ty000a0poczg7t8qk7` from **10:51:55Z**, half an hour *before* this merge. So no new Instagram clip exists to prove the fix on, and no clipper has opened a clips page in the window to trigger a capture on an existing one. This is precisely the case the brief told me to state plainly rather than dress up as success.

**One limitation I should be explicit about:** `/api/health` returns only `{"ok":true,"ts":...}` with no commit or build marker, so I could not confirm from outside which SHA Railway currently has live. Production is up and the capture endpoint answers 401 unauthenticated, but "the deploy has landed" is inferred from the push, not proven.

**Why a cover does not appear the moment the deploy lands, and this matters for reading the number above.** Capture is **lazy**, not automatic. BL-670 established that `captureClipThumbnail` has exactly two callers: the clip card's POST and the backfill script. The card fires that POST only when a clipper actually opens their clips page and sees a card with no cover. So the first restored Instagram cover appears the first time a clipper with a post-cutover Instagram clip opens `/clips` after the deploy, which is outside my control and cannot be forced from here without impersonating a clipper.

**The exact query for the owner to check this himself, any time:**

```sql
-- Have any post-cutover Instagram clips captured a cover since the BL-675 deploy?
SELECT count(*) AS ig_since_cutover,
       count("thumbnailUrl") AS with_cover,
       max("createdAt")::text AS newest_clip
FROM clips
WHERE "clipUrl" ILIKE '%instagram%'
  AND "createdAt" > '2026-07-22 11:12:00';

-- And the covers themselves: host must be supabase.co, never cdninstagram/fbcdn.
SELECT id, "createdAt"::text, "thumbnailUrl"
FROM clips
WHERE "clipUrl" ILIKE '%instagram%'
  AND "thumbnailUrl" IS NOT NULL
  AND "createdAt" > '2026-07-22 11:12:00'
ORDER BY "createdAt" DESC
LIMIT 10;
```

The second query is the acceptance test: **every row's `thumbnailUrl` must be on `<project>.supabase.co/storage/v1/object/public/uploads/clip-thumbnails/`, and no row may be on `cdninstagram.com` or `fbcdn.net`.** Paste any returned URL into a browser; it must return HTTP 200 and an image. A faster manual trigger, if the owner wants one now: open `/clips` as any clipper who has submitted an Instagram clip in the last week, and the card fires the capture on render.

**Already proven, so the remaining risk is narrow.** The harness confirms on the merged tree that the cover is found, that it downloads (63,816 bytes, `image/webp`), that `getPublicUrl` builds a `supabase.co` URL under `/uploads/clip-thumbnails/`, and that an already-stored Instagram cover on that exact path still serves HTTP 200 (49,891 bytes). BL-670 separately established that all 1,280 stored covers are on Supabase with 60 of 60 returning 200. The only untested link is the `upload()` call, using the same unchanged `rehostImage` that has been writing those 1,280 objects.

## Gates, stated honestly

* **`npm ci` exit 0**, then **`npx prisma generate` exit 0**, run in that order and **before** typecheck, so the wiped Prisma client was regenerated first.
* **`npx tsc --noEmit` exit 0**, with **0 lines** of output.
* **`npm run build` BUILD_EXIT=0**, read from a captured log and echoed directly, never piped through `tail`. `check:prisma-bypass` 0 violations, `check:removed-fields` OK, `lint:hooks` **11 problems (0 errors, 11 warnings)** at the ≤11 cap, compiled successfully, **61/61** static pages.
* **eslint v9.39.4 present** (`npx eslint --version`), so the hooks gate is real and not a silent no-op.
* **No `prisma migrate`** was run; only `prisma generate`.

## Safety

* **6 money files + `tracking.ts` + `campaign-era.ts` BYTE-IDENTICAL by blob OID on BOTH refs** (`0a69fcc4:<f>` vs the merged tree): writer `7aa6be48`, earnings-calc `797e2098`, balance `e887f80a`, tracking `847dcf70`, middleware `61cef393`, money-decimal `ef5cdae7`, campaign-era `106e16ad`. `tracking.ts` is not in the diff.
* **No clip's status, earnings or payout changed.** Verified live across the harness run: global clip earnings `$9901.09` unchanged, baseEarnings and bonusAmount unchanged, clip count 4381 unchanged, 137 payout_requests unchanged, and each probed clip's status unchanged.
* **The 436-clip backfill was NOT run**, per the brief. It remains spec'd in BL-673's BACKLOG entry, filtered by `videoUnavailable = false` at roughly $0.44.
* **Still flagged, and it should ship before that backfill:** the card sets its session dedup marker only on a *successful* capture, so a permanently uncapturable clip re-fires its POST on every visit. That was free while Apify 401'd; each retry is now a paid HikerAPI call. Exposure is the 714 `videoUnavailable=true` Instagram clips with no cover.
* **a11y-lead reviewed the merged tree: CONFIRMED, MUST-FIX list EMPTY.** No `.tsx`, no CSS and no component changed, and `ClipCardNew.tsx` is byte-identical. It also answered the one new question this merge raised, namely whether BL-671 landing in main creates any interaction with covers now appearing: **it does not.** `useDragScroll` has exactly two call sites, both rendering *campaign* cards, while `ClipCardNew` renders only in two vertical lists that never import the hook, so no clip thumbnail is inside a drag container. The one pre-existing nit stays logged and out of scope: the YouTube platform corner mark reads about 2.1:1 over a near-white cover (`ClipCardNew.tsx:292-296`), while Instagram's tint passes at about 3.2:1.
* **NO dashes** used as bullets.
* **Rollback:** `git revert -m 1 d6373647`, or `reset --hard pre-merge-BL-675`. Reverting restores the previous behaviour exactly, which is Instagram covers failing as `scrape-empty`.
