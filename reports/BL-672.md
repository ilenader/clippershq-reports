# BL-672 — MERGE of BL-671 (one shared drag engine for both card rows, image-drag bug killed, two clipper buttons raised)

**Merge commit:** `0a69fcc4` on `main` (origin == local, verified).
**Merged branch:** `checkpoint/BL-671` @ `b281627f`.
**Tags:** `pre-merge-BL-672` (c8321777), `post-merge-BL-672` (0a69fcc4), both pushed.
**Rollback:** `git revert -m 1 0a69fcc4`, or `git reset --hard pre-merge-BL-672`. Deleting `src/lib/use-drag-scroll.ts` on its own would break BOTH rows, so revert the whole merge.

## STEP 0 — was BL-671 genuinely unmerged?

Yes, with a real, non empty code diff.

* `git merge-base --is-ancestor b281627f origin/main` → **NO** (not already on main).
* `git log origin/main..checkpoint/BL-671` → exactly one commit, `b281627f`.
* `git merge-base origin/main checkpoint/BL-671` → `91bf3759`, which is **not** the current tip (`c8321777`, the BL-669 merge), so this was a genuine three way merge and not a fast forward.
* Non empty `.ts` / `.tsx` diff: **5 code files, +694 / −150**.

| file | change |
| --- | --- |
| `src/lib/use-drag-scroll.ts` | NEW, +270, the shared engine |
| `src/app/preview/preview-campaign-row.tsx` | −145 net, its private drag block removed and replaced by the import |
| `src/app/(app)/campaigns/CampaignsRedesign.tsx` | +50, `CompletedRow` wired to the engine |
| `src/components/layout/sidebar.tsx` | +38, the two buttons |
| `scripts/test-bl-671-drag-scroll.ts` | NEW, +341, the proof harness |

**Nothing a live round holds was merged.** Only `checkpoint/BL-671` entered this merge. BL-673 and BL-674 have **no branch and no worktree** on this machine (`git branch -a --list "*67*"` lists only BL-267, BL-367, BL-467, BL-567, BL-670, BL-671), so there was nothing of theirs to touch. `C:/b671` is parked exactly at `b281627f` with **0 modified files**, i.e. its round is finished and pushed; `C:/b670` is likewise clean and was not merged.

## The dirty main worktree, handled without touching it

`C:/b575` holds the local `main` branch and is still **dirty**: 77 tracked entries, with its `main` ref stale at `91b84410` versus `origin/main`. It was left completely untouched: not checked out, not reset, not staged, not cleaned.

The merge ran in a **fresh detached worktree at a short path**, `C:/b672`, created from `origin/main`. `.env` and `.env.local` were copied in and a real `npm ci` was run; **no `node_modules` junction was created**. The result went up with `git push origin HEAD:main`, so the stale local `main` ref in `C:/b575` was never written.

Verified push (BL-288 semantics): push succeeded on attempt 1, then `git fetch origin` and `origin/main == local HEAD == 0a69fcc4` was asserted. Both tags pushed.

## Merge mechanics

* One conflict only: **`BACKLOG.md`**, both sides having appended an entry after the shared base. Resolved as a **union, both sides kept**, BL-668 first then BL-671, by deleting only the three marker lines.
* **Exact union count:** base `91bf3759` had **97** `## BL-` entries, `origin/main` **98** (+BL-668), `checkpoint/BL-671` **98** (+BL-671), merged result **99**. 97 + 1 + 1 = 99. Both new entries verified present exactly once.
* **No `$2` capture group corruption.** `$2` occurs **112 times** in `BACKLOG.md` and the count is **identical on all four refs** (base, main, BL-671, merged), so none was introduced or lost. Every occurrence is a real dollar amount; the only two not followed by a digit are `$2/account` and `$2 via campaignBudgetLiability`, both pre existing prose.
* Conflict marker grep over `*.ts` / `*.tsx` / `*.md` / `*.prisma` / `*.json` / `*.css` on the merged tree → **none**, and `git diff --diff-filter=U` is empty.
* **No schema change at all.** `prisma/schema.prisma` is not in this diff, so no migration question arises. `npx prisma generate` was still run (after `npm ci`, before `tsc`) because `npm ci` wipes the generated client. **No `prisma migrate` of any kind.**

## Gates — real exit codes, read from logs

| gate | result |
| --- | --- |
| `npm ci` | **exit 0** |
| `npx prisma generate` | **exit 0**, Prisma Client 7.8.0, run **before** `tsc` |
| `npx tsc --noEmit` | **exit 0**, log 0 lines |
| `npm run build` | **BUILD_EXIT=0**, echoed from `$?` directly, never piped through `tail`; "Compiled successfully in 18.5s" |
| `check:prisma-bypass` | ran (prebuild) |
| `check:removed-fields` | ran (prebuild) |
| hooks gate `lint:hooks` | **eslint present and actually executed** (`node_modules/.bin/eslint`, eslint **9.39.4**); `eslint --config eslint.hooks.mjs --max-warnings 11` → **11 problems: 0 errors, 11 warnings**, at the cap, passing |

## Money files — blob OID on BOTH refs

`git rev-parse <ref>:<path>` on `origin/main` (c8321777) and on the merged `HEAD` (0a69fcc4). All **IDENTICAL**, and the same seven are also identical against `checkpoint/BL-671` itself:

| file | blob |
| --- | --- |
| `src/lib/clip-earnings-writer.ts` | `7aa6be48` |
| `src/lib/earnings-calc.ts` | `797e2098` |
| `src/lib/balance.ts` | `e887f80a` |
| `src/lib/tracking.ts` | `847dcf70` |
| `src/lib/clip-earnings-invariant-middleware.ts` | `61cef393` |
| `src/lib/money-decimal.ts` | `ef5cdae7` |
| `src/lib/campaign-era.ts` | `106e16ad` |
| `src/lib/campaign-rules.ts` (extra, checked too) | `fc91216f` |

The complete merged diff against main is 6 files: the 5 code files above plus `BACKLOG.md`. No API route, no query, no money or status path is in it. **No clip's earnings or status can change**, because nothing outside those files moved.

## Confirmed on the MERGED result

The proof harness `scripts/test-bl-671-drag-scroll.ts` was re run **on the merged tree**, importing the real shipped hook and driving it through `react-dom/server`: **33 passed, 0 failed** (BL-671's own report said 30/30, so the merged run is 3 assertions richer, all green). Every claim below is either one of those assertions or a direct read of the merged source.

**1. The shared hook is imported by BOTH surfaces.** `grep` on the merged tree:

| surface | import |
| --- | --- |
| landing preview | `src/app/preview/preview-campaign-row.tsx:40` → `import { dragScrollClassName, useDragScroll } from "@/lib/use-drag-scroll"`, used at line 235 |
| Completed row | `src/app/(app)/campaigns/CampaignsRedesign.tsx:25` → same import, used at line 311 |

Harness assertions: `landing carousel imports the shared engine`, `Completed campaigns row imports the shared engine`, `landing carousel keeps NO private drag physics`, `Completed row keeps NO private drag physics`, `landing carousel spreads the shared handlers`, `Completed row spreads the shared handlers`. All PASS.

**2. Dragging the Completed row scrolls it and does NOT drag a card image.** `dragHandlers` is spread onto the Completed row's scroll container and includes `onDragStart` → `preventDefault()`. `dragstart` bubbles, so one container handler cancels the native drag of every descendant `<img>` and `<a>`; `MiniCard` needed no edit and is byte unchanged. Harness: `a 80px drag SCROLLS the row` (scrollLeft 80), `native image / link drag is preventDefault-ed`, `images and links opt out of native dragging`, `text selection suppressed while pulling`. The CSS belt and braces reached the **built bundle**, not just the source: the compiled chunk contains `-webkit-user-drag:none`, `cursor:grab` and `cursor:grabbing`.

**3. BL-651's arrows and edge fades still work, and the fades still never block a tap or drag.** The arrow and fade markup is **untouched by this merge**: filtering the diff for `pointer-events-none`, `ArrowButton`, `tabIndex={-1}`, `opacity-0`, `hidden sm:grid`, `w-7` and `fade` returns only comment lines, no markup lines. On the merged file the two fades are still `aria-hidden="true"` + `pointer-events-none` + `w-7`, so a tap or a drag passes straight through to the card underneath; the arrows are still `tabIndex={-1}`, still `hidden … sm:grid`, still `pointer-events-none opacity-0` at each extreme. The only behavioural wiring change is that `scrollByCards(dir)` now calls `scrollByDir(dir, 0.85)` on the shared hook. Harness: `Completed row arrow scrolls 85% of the viewport` (left=425 on a 500px row), `preview row arrow scrolls 80% of the viewport` (left=−400), `prefers-reduced-motion: arrows jump (behavior auto)`, `normal motion: arrows glide (behavior smooth)`, `the glide keeps the edge fades in sync` (46 updates).

**4. Every card's numbers are unchanged, including somesome.** `scripts/test-bl-641-finished-campaign-display.ts` re run live against prod on the merged tree: **19 passed, 0 failed**. It reports somesome at `status=PAST budget=$9750.00`, and a clipper sees **$9,750.00 of $9,750.00 = 100%**, with the progress bar at 100%, both as LIVE PAST and forced AS PAST and AS COMPLETED. BL-535's control property is intact: **zero** unfinished campaigns change display, and the named control still shows its real `$841.00` while not finished. Owner headroom stays real and non zero at `$4,379.88`. Structurally this is expected: `MiniCard` and `effectiveSpent` are byte unchanged, and `CompletedRow`'s only edits are the hook wiring and the container class string.

**5. The LANDING page carousel still drags exactly as before.** This was the real risk of the merge, since its logic was MOVED out, so it was checked line by line rather than assumed. The old private block in `preview-campaign-row.tsx` and the new `useDragScroll` are **constant for constant identical**: 8px threshold, 0.7/0.3 velocity low pass, 0.15 px/ms minimum flick, ±3 px/ms clamp, 0.9 friction per 16ms, 48ms frame cap, 0.02 px/ms stop, mouse only (`pointerType !== "mouse"` and `button !== 0` return early), reduced motion read LIVE at release, `setPointerCapture` in a try/catch, glide cancelled on unmount. All nine handlers survive the move: `onPointerDown`, `onPointerMove`, `onPointerUp`, `onPointerCancel`, `onClickCapture`, `onDragStart`, `onWheel`, `onKeyDownCapture`, `onFocusCapture`. The arrow step is still `0.8` of the client width, now passed explicitly as `scrollByDir(-1, 0.8)`, and the harness measures it at −400 on a 500px row.

Two deltas the landing row does inherit, both stated rather than glossed:

* `onClickCapture` now disarms the latch first and returns early when `e.detail === 0`. Previously a `pointercancel` mid drag (right click, alt tab, gesture takeover) left the latch armed with no click coming, so the next thing to arrive, possibly a keyboard Enter on a card link, was silently swallowed. A real mouse drag still swallows its own trailing click, proven by `a drag SWALLOWS its trailing click`, `after pointerCANCEL, a KEYBOARD activation still opens the card`, `a keyboard click (detail 0) is never swallowed` and `and the latch is spent, so the NEXT mouse click works`.
* `dragScrollClassName` adds `[&_img]:[-webkit-user-drag:none] [&_a]:[-webkit-user-drag:none]` to both rows. The landing row already had the `onDragStart` preventDefault, so this is CSS hardening with no visible change there.

**6. The two buttons' destinations are unchanged.** The Discord link keeps `href="https://discord.gg/JVC3JMrxGf"`, `target="_blank"` and `rel="noopener noreferrer"` byte for byte; the Download App button keeps the identical BL-155 install `onClick` handler and `PWAInstallInstructions` modal. Only weight changed: a card surface with a border, full strength label text, an accent icon tile, a hover border lift and an `active:translate-y-px` press, plus `py-3` for a 44px target.

## What was deliberately NOT touched

* **The 403 on completed cards.** `MiniCard` still returns a plain `<div>` when `isPast`, because `/api/campaigns/[id]` 403s a PAST campaign to a non owner (BL-641). Nothing inside a completed card is clickable and this merge did not change that.
* **The two deferred a11y findings on BL-651's arrows.** `opacity-0` at the extremes still leaves a named button in the accessibility tree with no unavailable state (SC 4.1.2), and `hidden sm:grid` still leaves a sub 640px or 400 percent zoom mouse user without a visible non drag affordance (SC 2.5.7). Both are inherited from BL-651, both were consciously ring fenced by BL-671's brief, and both remain open. Verified untouched by the diff filter in point 3.

## Observations, reported not changed

* `sidebar.tsx` introduces `text-[#09090b]`, a hardcoded hex rather than a CSS var. It is **not a new pattern**: the same literal already appears **52 times** across `src/`, including `CampaignsRedesign.tsx:96`, `help-redesigned.tsx` and `PayoutsRedesign.tsx`, and it is the established dark ink for `bg-accent` surfaces. Flagged for a future round, not altered here.
* The visible `+2%` pill carries `aria-hidden="true"` with a separate `sr-only` string `, +2% earnings bonus`. The glyph does survive into the accessible name, so SC 2.5.3 holds, but the visible text and the accessible name are now maintained in two places. Flagged, not altered.

## Correction to the BL-669 report

While auditing `$2` for this round it turned out that the published BL-669 report understated the evidence: it said the grep "found 5 hits" when 5 was simply the `head -5` truncation limit. The true figure is 112 occurrences, identical across every ref involved. `reports/BL-669.md` has been corrected in place with that note; its conclusion of no capture group corruption was and remains correct.
