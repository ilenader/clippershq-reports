# BL-788 — SEND DRY-RUN: **No — do not send this copy.** Not because it is broken (it parses perfectly and all 2,636 addresses are valid) but because it is a stale snapshot that still contains 35 rows from a retired funnel. Regenerate and rows 1–1,352 are ready. **Personalisation is impossible from this file for every row in it**

**Date:** 2026-07-31 · **Type:** Dry-run audit, READ-ONLY · **Spend:** **$0.0000 · 0 API calls · 0 network requests**
Nothing written to master, config, spend or any pipeline file. Audited artifact: `output/ALL_BOT_READY.csv`, written **13:55:21**, unchanged since.

---

## Answer, first line

**NO — do not hand this copy to a sending tool.** Three reasons, in order of severity:

1. **It still contains 35 Kick rows** from a funnel retired on 20 July, plus 56 other rows from the same fit-test day. The fossil-day suppression that removes them **has since landed in `send_suppress.py`, but this file was written before it** — so the fix exists and this artifact predates it.
2. **It is stale.** Master has gained **81 rows** since the export was built (57,386 → 57,467) and is still being written.
3. **41 rows carry a fabricated `profile_link`** and zero personalisation tokens.

**Regenerating fixes 1 and 2 outright.** After that, **rows 1–1,352 (the ALIVE clipper block) are ready to send today** with a non-personalised message. The client block is a different decision — see §3.

**The mechanical verdict is clean, and that is worth stating plainly:** the file parses to spec, every one of the 2,636 addresses is well-formed and unique, and every suppression guarantee holds. Nothing here is a *defect* in BL-783's work. What fails is freshness and personalisation.

---

## 1. Opening the file as a sending tool would

| property | value |
|---|---|
| size | 228,505 bytes |
| columns | **10** — `email, profile_link, score, intent_score, activity, lead_kind, agency, role, days_since_post, flags` |
| data rows | **2,636** |
| ragged rows | **0** — every row is exactly 10 fields |
| BOM | **none** (neither UTF-8 nor UTF-16) |
| encoding | **valid UTF-8**, 2 non-ASCII bytes in the entire file |
| line endings | **2,637 CRLF, 0 bare LF** |
| **embedded newlines** | **ZERO** |
| NUL bytes | 0 |
| trailing newline | yes |
| duplicate addresses | **0** |

**The embedded-newline concern does not materialise.** Master genuinely holds 129,982 physical lines for 56,178 records because bios contain newlines — but `bio` is not an exported column, and the exporter's `csv.writer` emits nothing multi-line. 2,637 CRLF for 2,636 rows + 1 header is exact.

### The one parsing hazard

**6 cells contain a comma, all in `profile_link`, all correctly quoted:**

```
nba2kmobilesupport@2k.com,"https://www.tiktok.com/@2K, Inc. - a Take-Two Interactive affiliate",0,0,UNKNOWN,client,,,,
```

An RFC-4180 parser (Excel, Google Sheets, pandas, Mailchimp, n8n's CSV node) reads these correctly — verified: `csv.reader` returns 2,636 rows at width 10 with zero ragged rows. **A naive `split(',')` tool would break on 6 rows.**

**One homoglyph**, line 53: `profile_link` contains U+0456 (Cyrillic і) inside `.../@dіed`. The *email* on that row is pure ASCII, so the send is unaffected; the link is unclickable.

### 10 real rows, verbatim (local-parts masked)

```
HEADER: email,profile_link,score,intent_score,activity,lead_kind,agency,role,days_since_post,flags

    2: *********@gmail.com,https://www.tiktok.com/@areqaep,91,0,ALIVE,clipper,,,47,
    3: **********@yahoo.com,https://www.tiktok.com/@hoodieguyofficial1,52,0,ALIVE,clipper,,,16,
    4: *********@gmail.com,https://www.tiktok.com/@onytitned,65,0,ALIVE,clipper,,,15,
 1354: **********@gmail.com,https://www.tiktok.com/@.arzuna_,62,0,UNKNOWN,clipper,,,34,
 1453: **********@2k.com,"https://www.tiktok.com/@2K, Inc. - a Take-Two Interactive affiliate",0,0,UNKNOWN,client,,,,
 1454: *********@activision.com,"https://www.tiktok.com/@Activision Publishing, Inc.",0,0,UNKNOWN,client,,,,
 1501: *******@kennzy.tv,https://www.instagram.com/kennzy,0,0,UNKNOWN,client,,,,
 2001: **********@gmail.com,https://www.instagram.com/kristinemaia11,0,0,UNKNOWN,client,,,,
 2532: *********@gmail.com,https://www.tiktok.com/@rak_ji,71,0,DORMANT,clipper,,,169,
 2637: ********@gmail.com,https://www.instagram.com/maxsaep,95,15,DORMANT,clipper,,,91,
```

Note lines 1453/1454: **four consecutive empty trailing fields**. That is a client row — no score, no intent, no recency, no flags.

---

## 2. Is every address sendable?

**Yes. 2,636 of 2,636 pass every check, with zero defects of any kind.**

| check | result |
|---|---|
| leading/trailing whitespace | **0** |
| not exactly one `@` | **0** |
| leading apostrophe / formula prefix (`'`, `=`, `+`, `-`) | **0** |
| consecutive dots (`..`) | **0** |
| empty local part / empty domain / domain without a dot | **0** |
| local part starting or ending with a dot | **0** |
| illegal characters `( ) , : ; < > [ ] \ " space` | **0** |
| non-ASCII inside an address | **0** |
| over 254 chars, or local part over 64 | **0** |
| duplicate addresses | **0** |

**BL-770's 16 Excel text-escape artifacts are gone.** They survive in master's raw cells (`'-name@gmail.com`) and are stripped by the email cleaner on the way out — confirmed in BL-783, and this is the independent verification at the export boundary.

Top sending domains: gmail.com 1,931 · outlook.com 62 · hotmail.com 31 · icloud.com 25 · yahoo.com 17. **73% of the file is Gmail**, which matters for warm-up pacing.

---

## 3. The merge fields — the finding that should change the plan

**BL-774's "94% of the clipper list carries name, fandom, followers, median views and recency" is true of MASTER and false of THE EXPORT.** The export has 10 columns and carries none of them.

| merge field | in the export? |
|---|---|
| name / display_name | **NOT PRESENT** |
| fandom / niche | **NOT PRESENT** |
| followers | **NOT PRESENT** |
| median views | **NOT PRESENT** |
| country | **NOT PRESENT** |
| bio | **NOT PRESENT** |
| handle | derivable from `profile_link` — **98.4% clean** |
| recency | `days_since_post` — **58.2%** |

Column population:

| column | populated |
|---|---|
| `email`, `profile_link`, `activity`, `lead_kind` | 100% |
| `score` | 59.1% (**clipper 100%, client 0%**) |
| `days_since_post` | 58.2% (**clipper 98.5%, client 0%**) |
| `flags` | 10.0% |
| `agency` | 6.3% |
| `intent_score` | **5.2%** |
| `role` | **0%** — every role row was excluded, so no survivor carries the flag |

**The only personalisation token available for any row is the handle.** Not the name, not what they edit, not how big they are. A first line reading *"Hi @areqaep — saw your edits"* is the ceiling for the entire file, including the 1,352 excellent clipper rows whose name, fandom and 8M median views are sitting in master one join away.

**This is a one-column-block fix, not a data problem.** Everything BL-774 counted is already in master and already computed inside `bot_ready_rows`; `write_bot_ready_csv` simply does not emit it.

---

## 4. What a generic row would look like

Scoring each row 0–5 on usable tokens (clean handle, score, recency, intent, provably ALIVE):

| tokens | rows |
|---:|---:|
| 5 | 72 |
| 4 | 1,322 |
| 3 | 163 |
| **1** | **1,038** |
| **0** | **41** |

**1,079 rows — every client row — have one token or none.**

### The 5 worst rows, and what a message could honestly say

All five are 20 July Google Play rows with **zero** tokens:

| line | address | `profile_link` | what a message can say |
|---:|---|---|---|
| 1453 | `********@2k.com` | `.../@2K, Inc. - a Take-Two Interactive affiliate` | nothing |
| 1454 | `********@activision.com` | `.../@Activision Publishing, Inc.` | nothing |
| 1455 | `********@garena.com` | `.../@GARENA INTERNATIONAL I` | nothing |
| 1456 | `********@garena.com` | `.../@GARENA INTERNATIONAL I` | nothing |
| 1457 | `********@ubisoft.com` | `.../@Ubisoft Nova` | nothing |

Every one has `score=0, intent=0, activity=UNKNOWN, days=blank, flags=blank`. The only human-readable string is a **company name embedded in a fabricated TikTok URL**.

**`profile_link` is wrong for all 53 Google Play rows, not just these 5.** The Play funnel writes the developer *name* into `tiktok_handle`, and the writer builds `tiktok_url = "https://www.tiktok.com/@" + handle`. The 41 detectable ones contain spaces or commas; the rest (`.../@Trackzio`) look plausible and are equally fabricated — they resolve to nothing. Anyone clicking to verify a lead lands on a dead page.

**Recommendation:** rows with 0 tokens must not receive a personalised template at all. They need a plain "we make clips for app launches" message, or they should be held back. There is no first line that can honestly reference anything about them.

---

## 5. Send order — and a correction to how BL-786 reads

**The ordering in `ALL_BOT_READY.csv` is correct.** BL-786's finding that *"rank 1, 2, 3 score −3 and are among the worst in the file"* was measured on **`DIRECT_SEND.csv`** and does **not** transfer to this file. That distinction was not drawn clearly enough, and it matters here.

### File structure — the sort actually applied

```
rows     1 – 1,352   ALIVE  (all clipper, best reach first)
row      1,353       first UNKNOWN
row      1,452       first CLIENT row
row      2,531       first DORMANT
```

### Rows 1–20 vs rows 500–520

| | mean followers | mean median views | ALIVE |
|---|---:|---:|---|
| **rows 1–20** | 465,664 | **2,134,164** | 20/20 |
| **rows 500–520** | 123,915 | **23,078** | 21/21 |

**The top rows are 92× better on measured reach.** Row 1 is a 2.5M-follower account with 8,095,740 median views; row 500 has 24,213. Monotonic across deciles:

| rows | mean median views | %ALIVE | %client |
|---|---:|---:|---:|
| 1–263 | 406,528 | 100% | 0% |
| 264–526 | 43,022 | 100% | 0% |
| 527–789 | 13,308 | 100% | 0% |
| 790–1052 | 4,560 | 100% | 0% |
| 1053–1315 | 1,361 | 100% | 0% |
| 1316–1578 | 40 | 14% | 48% |
| 1579–2367 | 0 | 0% | 100% |
| 2368–2630 | 52,271 | 0% | 62% |

The clipper block sorts correctly, the client block (zero reach by construction) sits after it, and the final decile is the DORMANT tail — high-reach accounts correctly flagged and sunk rather than cut. **That is the never-cut rule working as designed.**

**So: send top-down from this file and the best leads genuinely come first.** The ranking defect is confined to `DIRECT_SEND.csv`, where every sort key is zero.

---

## 6. Suppression proof

| guarantee | result |
|---|---|
| dead-MX / no-mail-route addresses | **0** (table covers 20 such domains) |
| role inboxes present | **0** — independently recomputed via `role_policy.quality()`, not just read from the flag |
| `role` column populated | 0, correctly — role rows are excluded, so no survivor can carry the flag |
| agency rows flagged | **166**, and **every one that should be flagged, is** |
| other flags | `stale_180d` 60 · `stale_180d_unverified` 39 |

**The agency check was done adversarially.** Recomputing from the address alone flags 202 rows against the file's 166. All 166 are a strict subset, and the 36 differences are the export being **more** correct, because it also had the handle and display name:

| address | handle / name | email-only | with context |
|---|---|---|---|
| `*******@irenees.com` | `irenee.s.music` / IRENEE S | agency | **not agency — own domain** |
| `**@elenakitic.com` | `elenakitic` | agency | **not agency — own domain** |
| `********@senidah.com` | `senida_h` | agency | **not agency — own domain** |
| `********@gmail.com` | `aikenedit` / Aiken | agency | **not agency — own booking inbox** |

`role_policy` defines a creator's own `booking@`/`business@` as personal, not third-party. **Zero false negatives, zero false positives.**

### The one suppression gap in this artifact

**91 rows dated 2026-07-20 are still present** — 38 Twitch (good, keep), **35 Kick (a retired funnel)**, 18 pre-ceiling Google Play. `send_suppress.py` has since gained a `FOSSIL_DAYS` rule covering exactly this date, but **this export was written at 13:55, before that landed.** The file on disk is a snapshot of the old policy.

---

## What to do before sending

1. **Regenerate the export.** Picks up the fossil-day suppression (−91 rows including all 35 Kick) and the 81 new master rows. This is the whole of reasons 1 and 2.
2. **Add the merge columns** — `handle`, `display_name`, `niche`, `followers`, `deep_median_views`. All are already computed inside `bot_ready_rows` and discarded at the final line. Without them no row in the file can be personalised.
3. **Fix or blank `profile_link` for Google Play rows.** A fabricated TikTok URL is worse than an empty cell.
4. **Send rows 1–1,352 first** (ALIVE clipper block, ordered correctly). Hold the client block until it has merge fields.
5. **Route the 0-token rows to a generic template** or exclude them.
6. **Pace for Gmail** — 73% of recipients are on one provider.

---

## Limits

- **Audited one artifact at one moment.** `output/ALL_BOT_READY.csv` as written 13:55:21. Master was being written throughout (57,386 → 57,467) and `send_suppress.py` and `send_list.py` were modified by another agent during this round; none of that is reflected in the file examined.
- **No address was validated against a mail server.** Every check is syntactic — RFC shape, character legality, duplication. A syntactically perfect address can still bounce; only the 20 known dead-MX domains are established fact, and BL-768's ceiling of ~1.2% certain versus ~10% observed still stands.
- **The 92× reach gap in §5 is measured on master's `deep_median_views` and `followers`**, which the export does not carry. A sender cannot see those numbers — they can only trust the row order.
- **"Would a mail-merge tool parse this cleanly?" was tested with Python's `csv` module**, an RFC-4180 parser. Excel, Sheets and n8n follow the same rules; a bespoke splitter may not.
- **Token counting in §4 is my own construction** for this audit, not a pipeline concept.
