# BL-623 — Merge BL-622: multi-clip submit UI redesign

**Status: SHIPPED.** Merge commit `bdc8c1d4` is on `origin/main` (verified `origin/main == local HEAD`). Merge only; no new code authored.

## STEP 0 — TRUTH (with SHA)
* `checkpoint/BL-622` tip = `e475e8826d55cf41684300c29756a59db1ebb82a`.
* **Genuinely unmerged:** `git merge-base --is-ancestor origin/checkpoint/BL-622 origin/main` returned 1 (not an ancestor). Exactly one commit was on the branch and not on main: `e475e88` itself.
* **The merge-base WAS origin/main HEAD** (`9f950eca`), so BL-622 = main + 1 commit with zero divergence. (Local `main` was stale at `c784ec33`; I merged against the true origin/main `9f950eca`.) The `--no-ff` merge had **zero conflicts** and the diff is exactly the 3 paths BL-622 changed; nothing a live round holds was swept in (the working tree held only untracked `.claude/` + two stray docs, never staged).
* Merge commit `bdc8c1d4` parents: `9f950eca` (main) + `e475e88` (BL-622). Correct.

## Conflicts
**None.** merge-base == origin/main HEAD, so git did a clean three-way merge with no manual edits. No conflict markers in any changed file. BACKLOG.md took BL-622's single appended entry cleanly (1 `BL-622` occurrence, no `$2` capture-group corruption, since nothing had to be resolved).

## Safety gate — ALL logic files byte-identical (no STOP)
Every logic file is **BYTE-IDENTICAL by blob OID** on both refs and on the merged HEAD (`git rev-parse HEAD:$f` == `git rev-parse origin/main:$f`): `api/clips/batch/route.ts`, `api/clips/batch/draft/route.ts`, `clipper-submit-core.ts`, `api/clips/route.ts`, the 6 money files (`clip-earnings-writer`, `earnings-calc`, `balance`, `tracking`, `clip-earnings-invariant-middleware`, `money-decimal`), and `campaign-era.ts`. None is in the diff, so nothing had to trigger a STOP. The verify + tracking paths are untouched. No clip's earnings or status can change. No `prisma migrate`.

## Confirmations on the MERGED result
* **The add-another-clip control sits directly under the link input** (a single `setBatchMode(true)` control), and the old full-width dashed button at the bottom of step 2 is gone (0 matches for its class).
* **Each row renders icon + handle + full-width link + remove, platform derived from the account:** `PlatformIcon platform={acct.platform}` (1), the link's `basis-full sm:basis-0` reflow (1), and **no platform dropdown** remains (0 platform `<label>`/id matches).
* **Single-clip submission is unchanged and still routes through `/api/clips`:** the page passes `onSubmit={handleSubmit}` to the modal (2 matches), and the single form + `handleSubmit` are byte-identical.
* **BL-601 traps hold** (batch route byte-identical): `processOwnerSubmitLink` referenced **0 times**, per-row `row.clipAccountId` (5), and the server-side `campaignAllowedPlatforms` platform guard (2) plus the per-campaign limit pre-check and in-batch dedup, all before any write. The icon-only platform retains its accessible name via the row-group aria-label + the per-row reminder both controls point at.

## Build (real exit codes, from logs, not piped)
`npm ci` exit 0 (wiped the generated Prisma client) → `npx prisma generate` exit 0 → `npx tsc --noEmit` exit 0 → `npm run build` **BUILD_EXIT=0**. Prebuild gates: check:prisma-bypass 0 violations; check:removed-fields OK; hooks gate `eslint --max-warnings 11` = **11 problems (0 errors, 11 warnings)** with eslint present (gate real). "Compiled successfully". tsc and `next build` both ran.

## Ship
Verified push per BL-288: `git push origin bl623-merge:main` (`9f950eca..bdc8c1d4`), then fetch + assert `origin/main == bdc8c1d4`. Tags `pre-merge-bl623` / `post-merge-bl623` pushed.

**Rollback:** `git reset --hard pre-merge-bl623` (`9f950eca`) then force-push, or `git revert bdc8c1d4`.
