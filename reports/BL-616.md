# BL-616 — Merge BL-615: multiposting visible, correct, and forgiving

**Status: SHIPPED.** Merge commit `c784ec33` is on `origin/main` (verified `origin/main == local HEAD`). Merge only; no new code authored.

## STEP 0 — TRUTH (with SHA)
* `checkpoint/BL-615` tip = `90345f3e29a1146f6af2d2b9855e2f4b22a287d2`.
* **Genuinely unmerged:** `git merge-base --is-ancestor origin/checkpoint/BL-615 origin/main` returned 1 (not an ancestor). Exactly one commit was on the branch and not on main: `90345f3` itself.
* **The merge-base WAS main HEAD** (`3c59fdce`), so BL-615 = main + 1 commit with zero divergence. The `--no-ff` merge therefore had **zero conflicts**, and the diff is exactly the 10 files BL-615 changed (nothing a live round holds was swept in; the working tree held only untracked `.claude/` + two stray docs, never staged).
* Merge commit `c784ec33` parents: `3c59fdce` (main) + `90345f3e` (BL-615). Correct.

## Conflicts
**None.** merge-base == main HEAD, so git performed a clean three-way merge with no manual edits. No conflict markers anywhere (`*.ts` / `*.tsx` / `*.md` / `*.prisma` swept). BACKLOG.md took BL-615's single appended entry cleanly; the 71 `$2` occurrences in BACKLOG are pre-existing dollar amounts in older entries, not `$2` capture-group corruption (no sed/regex ran on BACKLOG because there was nothing to resolve).

## Schema (batch_submit_drafts) — confirmed, NOT re-run
BL-615 added the `batch_submit_drafts` table via `CREATE TABLE IF NOT EXISTS` and it is already in prod: `to_regclass('public.batch_submit_drafts')` is non-null with **6 columns** (id, userId, campaignId, data, createdAt, updatedAt). Per the instruction I confirmed it and did NOT re-run the migration; only `npx prisma generate` (exit 0) picked up the `BatchSubmitDraft` model. No `prisma migrate`.

## Confirmations on the MERGED result
* **Batch UI in the LIVE layout, test-user gated.** `page.tsx` defines `realIsTestUser = Boolean(session.user.isTestUser)` and renders `<BatchSubmitSection>` under `{realIsTestUser && (…)}` inside the premium branch that actually renders (line 307); the dead legacy copy is gone. `POST /api/clips/batch` returns **404** when `isTestUser !== true` before any work (batch/route.ts:48), so the feature stays invisible and unreachable for every non-test user, and single-submit is unchanged for them.
* **Pickers constrained + server rejects a disallowed-platform row.** The batch route imports and uses `campaignAllowedPlatforms` (2 uses) and rejects, before any write, a row whose resolved account platform is not one the campaign accepts, on top of the core's RULE 1b. Per-row `clipAccountId` is preserved (17 references; never a batch-wide account). `processClipperSubmitLink` is the only submit path (3 references).
* **Single-submit carries the same constraint + empty state.** `SubmitClipPremium.tsx` uses `eligibleAccounts` (5) + `campaignAllowedPlatforms` (3) to constrain the account radiogroup and show a clear "connect a {platform} account" message, with the account-platform check made case-insensitive.
* **Drafts per clipper + per campaign, re-validated on restore.** `/api/clips/batch/draft` is test-user-gated (`isTestUser`) and keyed on `userId_campaignId`; the client re-validates every restored row against the current platforms, accounts, and remaining limit, flagging stale rows and clearing only on explicit Cancel or a successful submit.
* **Owner force-approve core referenced 0 times** in both `batch/route.ts` and `batch/draft/route.ts`. Platform comparisons case-insensitive (`campaign-platforms.ts` lowercases both sides). The per-campaign daily limit is enforced server-side before any write (unchanged pre-check).

## Money-file safety
**6 money files + `tracking.ts` + `campaign-era.ts` + the clipper core + `clips/route.ts` BYTE-IDENTICAL by blob OID** on both refs and on the merged HEAD (`git rev-parse HEAD:$f` == `git rev-parse origin/main:$f`, pre-merge). None appear in the diff. No clip's earnings or status can change. No `prisma migrate`.

## Build (real exit codes, from logs, not piped)
`npm ci` exit 0 (wiped the generated Prisma client) → `npx prisma generate` exit 0 → `npx tsc --noEmit` exit 0 → `npm run build` **BUILD_EXIT=0**. Prebuild gates: check:prisma-bypass 0 violations; check:removed-fields OK; hooks gate `eslint --max-warnings 11` = **11 problems (0 errors, 11 warnings)** with eslint present (gate real). `/api/clips/batch` and `/api/clips/batch/draft` both in the route table. tsc and `next build` both ran.

## Ship
Verified push per BL-288: `git push origin bl616-merge:main` (`3c59fdce..c784ec33`), then fetch + assert `origin/main == c784ec33`. Tags `pre-merge-bl616` / `post-merge-bl616` pushed.

**Rollback:** `git reset --hard pre-merge-bl616` (`3c59fdce`) then force-push, or `git revert c784ec33`.
