# BL-587 — merge BL-586 to main (make the verification unstick live)

**2026-07-19 · merge-only round · worktree C:/b587 (RAN ALONE) · no new code written.**

Merged `checkpoint/BL-586` into `main` so the verification fix for the 114 stuck clippers deploys. No
money file or tracking.ts changed, no Apify actor run, no email sent, no env var changed, no
`prisma migrate`.

## STEP 0 — truth (with SHAs)
- `origin/checkpoint/BL-586` = **1075e0a9**. `origin/main` (before) = **bac606ac**.
- `git merge-base --is-ancestor origin/checkpoint/BL-586 origin/main` exited **1** ⇒ BL-586 was genuinely
  NOT on main. `git log origin/main..origin/checkpoint/BL-586` = the single commit 1075e0a9.
- merge-base(origin/main, BL-586) = **bac606ac** = origin/main itself ⇒ no divergence, so the merge brought
  ONLY BL-586's one commit. BL-586 touched 8 files (+363, -33): verify-handle.ts (new), accounts route,
  accounts/[id]/verify route, AddAccountFlow.tsx, schema.prisma, the migration SQL, BACKLOG, and its doc.
- **Live rounds left untouched:** `origin/checkpoint/BL-584` is NOT-merged (a live round holds it) and was
  NOT merged here. BL-583 / BL-585 / BL-588 are not on origin. Only BL-586 was merged.

## The merge
- `git merge --no-ff origin/checkpoint/BL-586` → **0 conflicts** (main == merge-base, so BACKLOG did not
  conflict and the `$2,000` capture-group corruption risk did not arise). No conflict markers anywhere.
- Merge commit **a62f226d**, parents p1=bac606ac (main) p2=1075e0a9 (BL-586).
- BACKLOG entry count: origin/main **82** → merged **83** (exact union, +1 for BL-586, no duplication).

## Schema (sanctioned path; migrate never run)
BL-586's five `clip_accounts` columns (`lastVerifyBio`, `lastVerifySource`, `lastVerifyFoundInBio`,
`lastVerifyBioBytes`, `lastVerifyAt`) were already applied to prod by BL-586 via `ADD COLUMN IF NOT EXISTS`.
Confirmed all five present via `information_schema` and **NOT re-run**. `npx prisma generate` (generate only)
regenerated the client so the merged code type-checks against them. `prisma migrate` was never run.

## Confirmed on the MERGED result
- **PART 1 persists per-attempt observations.** The verify POST writes the five `lastVerify*` columns
  best-effort (never gates the result), so "we could not see the bio" vs "the code was never in the bio" is
  now one SELECT. Present in the merged `src/app/api/accounts/[id]/verify/route.ts`.
- **PART 2 validates the profile link AT ENTRY.** `src/lib/verify-handle.ts` `normalizeHandleInput` extracts
  the real handle from a pasted/doubled URL (doubled links auto-correct) and rejects a handle with a space
  with a clear per-platform message; wired server-side in `POST /api/accounts` (authoritative) and
  client-side in `AddAccountFlow.tsx`. Of the malformed accounts BL-586 measured, doubled URLs auto-correct
  and space handles get a plain re-enter message.
- **PART 4 failure message is non-accusatory** and points to Discord (the channel that answers), never
  implying suspicion.
- **No TikTok per-profile vendor call was added.** PART 3 was deliberately not shipped (its source is
  per-video only and cannot meet the one-call-per-profile ceiling); the merged diff adds no LamaTok/TikTok
  profile call. YouTube key handling was already correct and is unchanged.

## Build (clean install, per brief; real exit codes from logs, not a pipe)
- `npm ci` exit **0** (clean install from lockfile). eslint binary **PRESENT** (hooks gate is real, not a
  silent no-op). `npx prisma generate` (generate only, never migrate) exit **0**.
- Standalone `npx tsc --noEmit` exit **0** (0 type errors).
- `npm run build` (`next build`) BUILD_EXIT=**0**: "Compiled successfully", "Finished TypeScript" (0 errors).
- Hooks gate: prisma-bypass **0 violations**, removed-fields **OK**, lint:hooks **11 problems (0 errors, 11
  warnings)** ≤ 11 ⇒ **pass**.

## After the push
Railway auto-deploys main. The verification unstick is then live: new accounts cannot save a link that can
never verify, every attempt records what each tier saw, and a stuck clipper gets a clear, kind message with
a working channel. The historical malformed accounts are fixable on re-add (doubled URLs auto-correct;
space handles get a clear message).

## Safety ledger
Merge only. No `prisma migrate`. No money file or tracking.ts changed (all 6 money files + tracking.ts
byte-identical by blob OID on both refs). No Apify actor run, no vendor call added, no email sent, no env
var changed. No conflict markers. BACKLOG unioned exactly (82 → 83). Nothing a live round holds (BL-584) was
merged. No clipper bio or handle appears in this report. main pushed and verified origin==local.
