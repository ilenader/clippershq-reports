# BL-645 — merge round for BL-641: NOTHING TO MERGE. It was already on main.

**2026-07-23** · Requested: merge `checkpoint/BL-641` into main. **Result: no merge performed, because BL-641 is already an ancestor of main.** No commit was created, no history rewritten. What this round did instead is re-verify the merged result end to end, on live data, and confirm nothing has drifted.

---

## STEP 0 — THE TRUTH, WITH SHAs

The brief said BL-641 was "verified on origin but NOT on main". **That is false.** BL-641 was merged into main earlier today, in the BL-641 round itself, and pushed.

| Ref | SHA |
|---|---|
| `origin/checkpoint/BL-641` | `e1f7d598aa3a2f37c4ec1374aaf06b4d6e819fd4` |
| `main` | `d6a5ca46bc2c4c80279043421ee045518608a827` |
| `origin/main` | `d6a5ca46bc2c4c80279043421ee045518608a827` |

```
$ git log --oneline main..origin/checkpoint/BL-641
(empty)

$ git merge-base --is-ancestor origin/checkpoint/BL-641 main
YES: BL-641 IS ALREADY AN ANCESTOR OF MAIN

$ git log --oneline -1 main
d6a5ca46 Merge BL-641: somesome moved to PAST, and a FINISHED campaign now reads
         100% to every clipper as a DERIVED status rule
```

`d6a5ca46` **is** the `--no-ff` merge commit for `e1f7d598`. Running `git merge --no-ff checkpoint/BL-641` again would have produced either "Already up to date" or, if forced, an empty commit misrepresenting work that was already done. **Neither was done.** Fabricating a merge to satisfy the brief would have been the dishonest option.

**Nothing a live round holds was merged.** Verified individually:

| Branch | On main? |
|---|---|
| `origin/checkpoint/BL-642` | **NOT on main** (untouched) |
| `origin/checkpoint/BL-643` | **NOT on main** (untouched, as instructed) |
| `checkpoint/BL-644` | does not exist on origin |

The primary working tree was clean of tracked changes throughout (only the three pre-existing untracked paths `.claude/`, `docs/BL-533-MERGE-REPORT.md`, `docs/SCRAPBADGER-TIKHUB-AUDIT.md`). Verification was run in an isolated worktree at the short path `C:/wt/bl641`, detached at `d6a5ca46`, with its own `node_modules`. **Never junctioned.**

---

## STEPS 2 and 3 — conflicts and BACKLOG

No merge, therefore **no conflicts to union**. Verified anyway on main:

* **Conflict markers:** `grep -rnE "^(<<<<<<<|>>>>>>>|=======$)" src/ scripts/ BACKLOG.md` → **0 hits.**
* **BACKLOG entries:** **89** `## BL-` headings; the `## BL-641` entry present **exactly once**, 11 lines, 10 bullets, all `- **`.
* **The "$2" capture-group corruption:** 73 occurrences of `$2` exist in BACKLOG.md, but **all 73 are legitimate dollar amounts** (`$2,000` ×15, `$2,000.00` ×9, `$25.86` ×6, `$20` ×6, `$2000` ×5, `$240` ×4, and so on). Only **2** are a bare `$2` not followed by a digit, and both are prose: `"owner cap $2/account/10d"` (line 358) and `"would have leaked $2 via campaignBudgetLiability path"` (line 7414). **Neither was introduced by BL-641** — `git diff pre-merge-BL-641 main -- BACKLOG.md | grep -c '\$2'` returns **0**. No corruption.

**Everything the BL-641 merge brought in, in full:**

```
 BACKLOG.md                                       |  12 ++
 scripts/migrations/BL-641-somesome-to-past.sql   |  49 +++++
 scripts/test-bl-641-finished-campaign-display.ts | 241 +++++++++++++++++++++++
 src/app/api/campaigns/spend/route.ts             |  21 ++
 src/lib/campaign-clipper-view.ts                 |  60 +++++-
 5 files changed, 380 insertions(+), 3 deletions(-)
```

---

## STEPS 4 and 5 — build gates, stated honestly

Run in the isolated worktree at `d6a5ca46`, in this order, each with a real exit code read from a log. **Never piped through tail.**

| Gate | Result |
|---|---|
| `npm ci` | **exit 0** (wipes the generated Prisma client, hence the next line) |
| `npx prisma generate` | **exit 0** — Prisma Client 7.8.0. No `migrate`, ever. |
| `npx tsc --noEmit` | **exit 0**, **0 lines of output** |
| `npm run check:prisma-bypass` | **exit 0** |
| `npm run check:removed-fields` | **exit 0** |
| `npm run lint:hooks` | **0 errors, 11 warnings** (at the ≤11 cap) |
| `npm run build` | **exit 0**, compiled in 20.3s, **61/61** static pages |

**eslint is genuinely present and executing: v9.39.4.** The hooks gate is not a silent no-op — it reported 11 real warnings, which a no-op could not produce.

---

## STEP 6 — the merged result, confirmed on main

`npx tsx scripts/test-bl-641-finished-campaign-display.ts` on `d6a5ca46` against live rows → **19 passed, 0 failed.**

```
somesome status=PAST budget=$9750.00 clipperSide=$2018.77 total=$5445.80
LIVE (status PAST) clipper sees $9750.00 of $9750.00 = 100%
PASS  LIVE somesome is FINISHED, so a clipper sees the full $9750.00
PASS  LIVE progress bar reads 100%
OWNER / ADMIN keep: $5445.80 of $9750.00, headroom $4304.20
control "Panic Baby" ACTIVE budget=$3000.00 spend=$158.31
  as-is: $158.31  ·  same row with status=PAST: $3000.00
PASS  the SAME row, money untouched, reads full the moment its STATUS is finished
campaigns whose clipper-facing figure CHANGED: 1  -> somesome [PAST] $5445.80 -> $9750.00
PASS  ZERO unfinished campaigns change display (BL-535 control property intact)
```

| Requirement | Confirmed on main |
|---|---|
| somesome is PAST | `status=PAST`, `pauseSource=AUTO`, `budget=9750`, `manualSpent=NULL`, `isArchived=false` |
| **and still VISIBLE to clippers** | `/api/campaigns/past` queries `{status:"PAST", isArchived:false, isTestCampaign:false}` (`past/route.ts:32`). Live, somesome matches and sorts **FIRST** by `updatedAt desc`, ahead of Gainzalgo, Hapday, Zhus and Grateful Songs |
| Clipper view reads 100% | **$9,750.00 of $9,750.00** |
| Owner and admin keep the true figure | **$5,445.80 of $9,750**, headroom $4,304.20, non-zero so the owner still sees this campaign is not fully consumed |
| Display is DERIVED, not hardcoded | Panic Baby is the control: the **same row**, money untouched, shows its real $158.31 while ACTIVE and the full $3,000 the moment its **status alone** is finished. `manualSpent` on somesome is still **NULL** — the 100% comes from the rule, not a stored number |
| No other campaign's clipper display moved | Across 10 campaigns with spend, exactly **1** moved and it is the finished one. **Zero** unfinished campaigns moved |

The other four PAST campaigns (Grateful Songs, Zhus, Hapday, Gainzalgo) have 0 clips and 0 agency rows, so they never enter the spend map at all; their cards prefer `manualSpent`, already equal to budget. There are still **0 COMPLETED** campaigns.

---

## STEP 7 — live re-verification after the fact

Every figure identical to the BL-641 round's post-write measurement, and to BL-638 and BL-570 before it.

| Set | Checksum | Verdict |
|---|---|---|
| Per-clipper earnings + base + bonus, all **84** clippers | `ed87165bb10201085ae54fde5fe991d8` | **identical** |
| All **953** clips (earnings, videoUnavailable, status) | `bbc08a0fafee555c5ecddfbcded6dc00` | **identical** |
| All **133** payout_requests (status, amount) | `754790413b8900853aab3d4ba147c931` | **identical** |

*(The brief said 104 clippers; the measured figure is 84, as recorded in BL-641 and BL-638.)*

| Invariant | Live reading |
|---|---|
| Era boundary minted? | **No.** Still `2026-06-22 03:07:29.621`; 33 status-change rows platform-wide, unchanged |
| eraExempt tripped? | **No. 0 of 33**, unchanged |
| Owner row re-derived? | **No.** 483 rows, **$3,427.03** — the $933.94 exposure never approached |
| Frozen clip unfrozen? | **No.** 433 videoUnavailable clips holding **$3,433.29**, unchanged. PAST is a *stronger* freeze than AUTO-pause; it adds a gate, it removes none |
| Pending withdrawal affected? | **No.** 2 REQUESTED **$104.70** + 3 UNDER_REVIEW **$166.45**, unchanged |

**No clipper's own earnings or balance changed by a cent.** `/api/earnings`, `/api/payouts` and `balance.ts` contain **zero** references to campaign status, so a campaign-level status flip is structurally incapable of moving them.

---

## Safety

**6 money files + `campaign-era.ts` BYTE-IDENTICAL by blob OID**, compared on **both refs** (`git rev-parse pre-merge-BL-641:<f>` vs `main:<f>`):

```
7aa6be48  clip-earnings-writer.ts      797e2098  earnings-calc.ts
e887f80a  balance.ts                   847dcf70  tracking.ts
61cef393  clip-earnings-invariant-middleware.ts
ef5cdae7  money-decimal.ts             106e16ad  campaign-era.ts
```

**None of them was deliberately changed by BL-641, so no diff needs justifying.** `tracking.ts` is not in the merge diff.

`origin/main == local main == d6a5ca46`, verified by `scripts/safe-push.mjs` (BL-288): *"VERIFIED PUSHED ✓"*. BL-642 and BL-643 untouched. BL-638's `/api/earnings:64` gap deliberately **not** touched — still its own round. No `prisma migrate`. NO dashes as bullets.

**Rollback (unchanged from BL-641):** `git revert -m 1 d6a5ca46` plus `UPDATE campaigns SET status='PAUSED', "updatedAt"='2026-07-20 11:28:37.103' WHERE id='cmoaa70j2000z0ppfyaco13mj';` — or `reset --hard pre-merge-BL-641`. The code revert alone returns somesome to $5,445.80 of $9,750 with no DB change, because the display is derived from status.
