# BL-1181 — the seven rules are built, the pinned posts are gone, and NO threshold clears the base rate

**Your 62 delivered pages are 17.7% good — 11 of 62 — [10.2, 29.0].** That is the number
behind "these leads are kinda mid", measured by hand against a standard written before a
single page was bought. All seven rules are implemented and each reports what it removes.
And the honest result is negative: **the best threshold reads 25.0% [14.2, 40.2], and its
lower bound does not clear the base rate's upper bound of 29.0.** By the bar set before the
data existed, nothing ships. Sections 6 and 7 say what to do instead.

**And the round nearly shipped a number that was an OCR artifact.** Scored with the reader I
built, the sweep peaked at 38.5% [17.7, 64.5] and looked like a real doubling. BL-1180 then
measured that the fixed top-45% crop both our rounds used *discards the poster's line* 18% of
the time. I re-read all 1,851 frames through the corrected `frame_text.py` — 260 reads changed,
67 lines recovered, 96 refused — and **the lift largely dissolved**: rule 3's removal fell from
22 pages to 14 and the sweep flattened to a 25% plateau. Both scorings are in §6. The
corrected one is the answer.

Every figure came from `Python311\python.exe` (3.11.9). Bare `python` is 3.13.14 here and
lacks the packages — the memory that says agents get 3.11 by default was wrong for this
session, and every command was pinned to the full path.

**Spend: $0.1182 of the $2.00 cap** (197 billed calls at $0.0006). OCR was free in money and
cost 2,006 seconds of wall clock.

---

## 0. A COLLISION I CAUSED, FIRST, BECAUSE IT COST SOMEBODY ELSE WORK

I checked `reports/BL-1178.md` was free before my first write. It was. That was not enough:
another session was already mid-round under the same id with untracked scratch files, and
**I overwrote two of them.** Neither had ever been committed, so git cannot restore either.

| path | theirs | what I did |
|---|---|---|
| `scratch/bl1178_probe.py` | a 19-call discovery-endpoint probe; their report bills it at $0.0114 | replaced with my 4-call pinned probe |
| `scratch/bl1178_score.py` | imported by their `bl1178_ablate.py`, `bl1178_agree.py`, `bl1178_bystyle.py` | replaced with my scorer |

Both contested paths are now **deleted rather than left holding my content**, so
`import bl1178_score` fails with a loud `ImportError`. Leaving my scorer there would have let
three of their live scripts import the wrong module and report plausible wrong numbers into
someone else's round — the worse failure by a distance. Their `scratch/bl1178_labels.json` is
untouched; my scorer had that exact path hard-coded as a read and would have overwritten it at
labelling time. This round is **BL-1181**; `bl1179` and `bl1180` are taken as well.

The rule already in memory, applied too narrowly: **checking the report id is not checking the
namespace.** Check every path you are about to write.

---

## 1. THE STANDARD WAS WRITTEN AND COMMITTED BEFORE THE PULL

`scratch/bl1181_standard.md`, committed at **41d2b7e**, before `bl1181_pull.py` bought
anything. It fixes the seven rules, the niche label set, the population, and — critically —
**the bar a threshold has to clear**, so that bar could not be moved afterwards to fit a
number. The two previous rounds both produced a figure that turned out to be its own base
rate; a standard that can be edited after the data arrives cannot rule that out.

Disclosure recorded in the standard: before writing it I had seen the `Page`, `Avg views` and
`Top text` columns of the 62 delivered rows while hunting for the ~200-view page. I had not
seen any post list, view distribution, pinned post, caption body, bio, or post count.

**Population** (80 pages): the **62 pages the shipped filter delivered** — its entire pass set,
287 walked, 62 passed — plus **18 drawn seeded-random from the 225 it rejected**, so false
negatives are visible too. Every new rule is a further restriction, so the new filter's output
is a subset of the old one's, and the old pass set is where precision has to be measured.

---

## 2. RULE 1 — THE VIEW GATE. THE PINNED FLAG IS A LIE; POSITION IS NOT

Probed live first (`scratch/bl1181_probe.json`, 4 calls, $0.0024). The vendor **does** return
`timeline_pinned_user_ids` — and it is `[]` on every item, on every account. `is_pinned` is
absent entirely. **The flag is unusable.**

What is usable: Instagram serves pinned posts at the **head** of the response and the rest in
reverse-chronological order. So a post in the first three positions whose `taken_at` is older
than something behind it is pinned. It is falsifiable — on a page with no pins the API order
*is* date order and the rule returns nothing. **It fired on 38 of the 80 pages, 90 posts in
all.** Pinned posts are not an edge case.

### The page you said averages ~200 views

`21.prio` — delivered to you, and this is its actual feed:

| pos | date | plays | |
|---|---|---|---|
| 0 | 2023-01-24 | **27,113,227** | PINNED |
| 1 | 2022-12-29 | **15,984,906** | PINNED |
| 2 | 2023-01-03 | **6,177,064** | PINNED |
| 3–11 | 2026-05-07 → 05-16 | 350, 430, **231**, 532, **252**, 483, 699, 737, **300** | |

**446 average excluding pinned. 4,927,817 including them — an 11,049× inflation.** Three posts
from 2022–23 carrying 49.3M plays were being averaged with nine recent posts running 231–737.
Your "~200" is posts 5, 7 and 11. `blitzedbuddy` is the same shape: 38.85M on one pinned post,
162–2,478 on everything since.

Pinned posts are now excluded from the view average **and** from the format share — a pinned
post is by definition the page's best ever, so counting it in either asks the wrong question.
The exclusion happens before the latest-ten slice, or three pins eat three of the ten slots.

`judge_page` now reports the inflation rather than hiding it:

> `average 446 views over 9 non-pinned post(s) older than 48h, below the 2000 floor;`
> `3 pinned post(s) excluded, which alone read 4927817`

The 48h grace you asked for is kept: a post younger than that is excluded, not counted as a
zero, so ten posts in the last hour cannot kill a page. A photo post carries no `play_count` at
all and is excluded as unknown rather than counted as zero.

---

## 3. RULE 2 — MINIMUM POSTS, AND WHY YOUR OWN EXAMPLES CAP IT AT 13

You asked for a floor justified from your examples. **The justification is uncomfortable and
you should see it.** The 33 accounts behind your 37 example reels:

| | media_count |
|---|---|
| minimum | **0** — `v4mp3rit409`, which you picked a 940,603-play reel from and which now reports zero posts |
| next lowest | **13** — `edm_whale`, 11,513 followers |
| then | 32, 48, 57, 98, 99, 125, 174, 257 |
| p10 / median | 32 / 401 |

So **13 is the highest floor your own examples permit**, and it rejects your twelve-post page
by exactly one post. Anything higher cuts a page you chose yourself. `buzz_lightning_mcqueen`
— 12 posts, 113 followers — is the page you meant, and 13 removes it.

The floor sweep is in the report data; it climbs (12→25.0%, 100→33.3%, 200→41.7%) but only by
throwing away most of the supply, and above 13 it is no longer derived from anything you
showed me.

**`media_count` also undercounts**, measured on 3 of 80 pages: `_neinaramwa` reports 1 against
10 posts actually served, `_iydys._` 2 against 6, `brad.da.rad` 7 against 11. A floor on the
raw field would reject a page for having fewer posts than we are literally holding, so the
floor is applied to `max(media_count, posts served)`. All four still fall.

This rule costs a billed call: `media_count` exists only on the profile payload, and
`user_medias` caps its page at 12 items (probed — asked for 24, got 12). So the gate order
changed: **profile first**, and a page under the floor is rejected before it buys its post
list. The bio and follower count that call returns are the ones contact resolution was going
to fetch anyway, so a page that passes costs exactly what it used to.

---

## 4. RULE 5 — THE NICHE, AND WHY IT NEVER REJECTS

You said `casinodubs` is "correctly meme format but gambling", and asked for a niche per page
so you can exclude categories rather than have your taste hard-coded. So the niche is
**reported and never enforced**: `casinodubs` is labelled **GOOD**, with `niche=gambling`
beside it. Five of five of its drawn lines are hooks. It is your call, not the filter's.

Niches across all 78 judgeable pages: general 28, film/tv 18, relationship 5, music 5,
football 3, kids/family 3, animals 2, anime 2, gambling 2, basketball 2, and one each of
travel, gaming, religious, motivational, tech, food, cars, sports.

**Caveat, stated plainly: the niche labeller is the weakest thing in this round.** It is
keyword scoring, and it put `21.prio` in `animals`. Treat the column as a hint to sort by, not
a fact. It costs nothing and rejects nothing, so it is safe to ship in that state — but do not
build an exclusion list on it without spot-checking.

---

## 5. WHAT EACH RULE ACTUALLY REMOVES

Over the 62 delivered pages, every rule evaluated independently — `judge_page` no longer
short-circuits, because a first-to-fire design cannot answer this question at all (a page
killed by language *and* views counts once for language and zero for views).

| rule | removes | of them BAD | **GOOD lost** |
|---|---|---|---|
| RULE 3 format must dominate | **14** | 13 | 1 |
| RULE 1 view gate, pinned excluded | **5** | 5 | 0 |
| RULE 2 minimum total posts | **4** | 4 | 0 |
| RULE 4 media / news pages | **3** | 2 | 1 — `1bornfire1` |
| RULE 6 film & TV promotion | **2** | 2 | 0 |
| RULE 7 English only | **0** | — | — |

**The view gate and the post floor are the only clean rules** — nine removals between them, not
one of them a page you'd have wanted. Format share still removes the most, but it is also the
only rule that costs you good pages, and its removal count fell from 22 to 14 once the OCR was
corrected (§6). Rule 7 removed **nothing**: this population is not foreign-language, it is
English pages with junk captions, and the four Indic-script pages the previous round leaked
were in a different sample.

### Two defects the labels exposed, both fixed

1. **`is_news_report` read only the drawn line**, so it could not fire on `799_media` — the
   page you *named* for that rule. Its drawn lines are ordinary ("What an incredible shot");
   what makes it a news page is that every caption is a `Facts: ●` bulletin. A rule that
   cannot fire on the example it was written from has not been implemented, only spelled.
2. **A frame word alone made a narration a hook.** "When a sold out stadium knew every word to
   a song they just made 3 hours ago" scored HOOK purely on "when", on a wholesome-reporting
   page. The frame now needs an anchor, a colon, or brevity — below the 69-character median of
   your 37 examples. All your own frame lines survive.

### One fix tried and measured back out — this is the important one

Treating `Facts:` / `Interesting fact:` / `Did you know` captions as reporting looked obvious
and made rule 4 fire on 7 pages. **Three of the seven were pages labelled GOOD**, and precision
dropped at every single threshold. The reason: `cartoon` (1.25M followers) and `brainfuel.meme`
open their captions with the *identical* boilerplate as `799_media` and `brainnourishment`. It
is algorithm-gaming caption spam common to both populations and **it does not separate them.**
Reverted, and recorded in the code so nobody re-adds it.

---

## 6. THE SWEEP — PRECISION, INTERVAL AND REJECTION COUNT AT EVERY THRESHOLD

**Base rate = the precision of what you actually received = 11/62 = 17.7% [10.2, 29.0].**
To ship, a threshold's lower bound must exceed **29.0**.

### 6a. The scoring that nearly shipped — and why it was wrong

Read with the top-45% band, the sweep looked like this:

| t | passed | rejected | GOOD | precision | recall |
|---|---|---|---|---|---|
| 0.35 | 31 | 31 | 9 | 29.0% [16.1, 46.6] | 82% |
| 0.45 | 26 | 36 | 8 | 30.8% [16.5, 50.0] | 73% |
| **0.55** | **16** | **46** | **6** | **37.5% [18.5, 61.4]** | 54% |
| **0.60** | **13** | **49** | **5** | **38.5% [17.7, 64.5]** | 46% |

17.7% → 38.5% reads like a doubling. It still failed the bar, so it would not have shipped —
but it would have been reported as "close, needs more labels", and that would have been wrong.

**BL-1180 measured the reader while this round was running.** On 82 hand-read clips, the fixed
top-45% crop — which my OCR used — was the defect: 11 of 13 failures were the poster's line
sitting *below* the band and being thrown away unread, and 6 clips had text *invented* off a
T-shirt or a bar sign. `frame_text.py` replaces the band with the largest paragraph on the
frame and refuses on frame disagreement: 18.1% → 3.2% failure, inventions 6 → 0.

Rule 3 reads exactly that field and does most of the removal, so I re-read all **1,851 frames**
(still in `%TEMP%`, so $0.00 and 1,037s). **260 reads changed, 67 posts gained a line that had
been discarded, 96 became refusals.**

### 6b. The corrected scoring — this is the answer

| t | passed | rejected | GOOD | precision | recall |
|---|---|---|---|---|---|
| 0.00 | 48 | 14 | 10 | 20.8% [11.7, 34.3] | 91% |
| 0.15 | 47 | 15 | 10 | 21.3% [12.0, 34.9] | 91% |
| 0.25 | 43 | 19 | 10 | 23.3% [13.2, 37.7] | 91% |
| **0.35** | **40** | **22** | **10** | **25.0% [14.2, 40.2]** | 91% |
| 0.45 | 32 | 30 | 8 | 25.0% [13.3, 42.1] | 73% |
| 0.55 | 24 | 38 | 6 | 25.0% [12.0, 44.9] | 54% |
| 0.60 | 21 | 41 | 5 | 23.8% [10.6, 45.1] | 46% |
| 0.65 | 11 | 51 | 3 | 27.3% [9.7, 56.6] | 27% |
| 0.75 | 5 | 57 | 2 | 40.0% [11.8, 76.9] | 18% |
| 0.85 | 2 | 60 | 1 | 50.0% [9.5, 90.5] | 9% |
| 0.95+ | 0 | 62 | 0 | nothing passed | 0% |

**No threshold clears, and the curve is now nearly flat.** Precision sits on a 25% plateau from
t=0.35 to t=0.55 while recall falls from 91% to 54% — that is the signature of a threshold that
is discarding pages roughly at random rather than sorting them. The best lower bound anywhere
is **14.2**, against a base-rate ceiling of **29.0**. The 40% and 50% rows rest on **five and
two pages**; that is why the rejection count sits beside every row.

**The honest reading: most of the apparent lift was the broken crop, not the filter.** The
drawn line separates these populations far less than it looked like doing an hour ago. This is
the fourth round in a row where a number dissolved on inspection — the difference is that this
time it dissolved *inside* the round instead of in front of you.

---

## 7. WHAT I'D DO NEXT, IN ORDER

1. **Ship rules 1 and 2 now, on their own.** Nine removals, zero good pages lost, no threshold
   involved and nothing to calibrate. The view gate alone would have kept `21.prio`,
   `blitzedbuddy`, `__anonymous_memer_`, `aimemeguy` and `4.udear` out of your inbox.
2. **Do not ship a format-share threshold.** On the corrected reader it buys almost no
   precision at any setting. Keep it at 0.35 as a demotion/sort key rather than a cut — at
   0.35 it still holds 91% of the good pages, so it costs nothing to order by it.
3. **Re-run the whole funnel through `frame_text.py` before anything else is measured.**
   `meme_finder` still calls its own top-45% reader. Every format number this project has
   quoted, mine included, came from a reader that discards the line 18% of the time.
4. **The measurement needs ~200 labelled pages, not 80.** At n=40 passing the interval is still
   ±13 points. The other live round found discovery runs at 27.5 new handles per billed call,
   so supply is not the constraint — labelling is, and OCR is the clock.
5. **Two false negatives are worth your eye**: `_10.eren` and `celluloidrealm` were both
   rejected by the shipped filter and are both, I think, pages you'd want — English drawn hooks
   under Japanese caption spam. If you disagree with either, tell me and the standard changes.
6. **The caption is now actively adversarial.** Roughly a quarter of this sample carries
   copy-pasted spam captions (the same One Piece PV block, the same Vogue World paragraph)
   bearing no relation to the video. Judging the drawn line is not merely better, it is the
   only thing that still works.

---

## 8. HONEST LIMITS

- **One labeller, no second opinion.** 78 judgeable pages, 2 unjudgeable (`arkanememes`
  returned 1 post of a claimed 3,348; `birkhoffd` is private). Borderlines I found genuinely
  hard: `barf`, `cartoon`, `alexdewittcomedy`, `brickcrip`. Every call and its reason is in
  `scratch/bl1181_labels.json` — argue with them directly.
- **`brickcrip` is the one that hurts.** Six of six drawn lines are textbook hooks and I still
  labelled it BAD, because its recent posts run 223–3,292 for a ~1,134 average. Right format,
  dead page. If you'd take that page, the 2,000 floor is wrong and I need to know.
- **OCR read a drawn line for 63 of 80 pages.** The other 17 were judged from captions and bio
  and are flagged `ocr_thin` in the labels; 11 of those were BAD on grounds having nothing to
  do with the drawn line (personal blog, business account, dead views).
- **The two defect fixes in §5 were found by looking at the labels.** They are correctness
  fixes, not threshold tuning, but the post-fix numbers are development-set numbers and carry
  an optimistic bias. They need a fresh population to confirm — which is point 4 above.
- **The OCR correction in §6 arrived from another round mid-flight and I applied it to my own
  result rather than shipping the number I already had.** That is the right call and it is also
  the reason this report says less than it could have. If `frame_text.py` itself turns out to
  be mismeasured, this round moves again.
- **Staleness is not one of the seven rules and probably should be.** Several pages pass every
  rule while having stopped posting months ago (`btw_arpit__`, 2025-10). I did not add a rule
  you did not ask for; flagging it instead.

---

## 9. THE RECORD

- Suites: **261/261 green, 7,031 checks, 243.5s under `--jobs 4`.** 70 tests in
  `test_meme_finder.py`, 29 of them new.
- Campaigns: **11, unchanged** — `git diff` on `run.py` is empty. `config.json` and
  `config.example.json` both parse; the funnel still exposes its six parameters and its export
  dataset.
- Commits: `41d2b7e` (standard frozen before the pull), `de4243a` (the seven rules),
  `c67a02d` (the namespace repair, with `Claim-Override: BL-1178` and `Index-Override`
  recorded on the record), `017ec29` (labels, defect fixes, score).
- Both scorings are kept: `scratch/bl1181_score.json` (top-45% band, superseded) and
  `scratch/bl1181_score2.json` (corrected reader, authoritative). Same labels, same rules,
  same code — only the OCR field differs.
- Data: `scratch/bl1181_standard.md`, `bl1181_labels.json`, `bl1181_score2.json`,
  `bl1181_pages.jsonl`, `bl1181_ocr.jsonl` + `bl1181_ocr2.jsonl`, `bl1181_probe.json`,
  `bl1181_example_accounts.json`, `bl1181_sheet.txt`.
