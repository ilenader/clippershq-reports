# BL-762 — The YouTube size gate now measures reach, not just followers: N = 2,000,000 views, measured on 200 channels across 4 niches

## N IS **2,000,000**, AND IT IS NOT A GUESS. On 200 channels over four niches, the 136 that fail the 50k subscriber floor have a view distribution of p50 154,505 / p90 2,102,000 / p95 9,191,912. At N=2M the rescued group's **median views-per-year jumps 3.1× (352,572 → 1,107,117)** versus N=1M, while the sub-50k channels below 1M views — the control, n=108 — sit at a median of **25,358 views/year, 44× lower**. 2M also lands just under the **p10 of channels that already pass on subscribers (2,718,523)**, so the clause admits nothing quieter than the bottom decile of today's intake: only **3 of 64 current passers (4.7%)** sit below it. The BL-748 rescue case reproduced exactly in an independent sample and now passes. **The description-email parse runs after the country gate, and that ordering is worth 19 of 25 raw hits**: 18 of those channels are dropped by a gate before the parse can matter. Two of the six addresses that survive come from channels the old subscriber floor would have deleted outright — so items 1 and 2 compound.

**2026-07-31 · BUILD + MEASUREMENT. Budget: $0.00 cash — the probe spends YouTube Data API quota (~404 of the 10,000/day free), not dollars. Backups taken before any write: `config.backups/{config.json,spend.json,master_leads.csv}.20260731_103305.bl753` plus a `PRESTATE` hash manifest. No credential is printed, logged or committed.**
**Assets:** `reports/BL-762-assets/` — the 200-channel sample, both analyses, the yield measurement, and all three scripts. **Raw `description` text is stripped from the published sample**: it contains third parties' email addresses, and none of the findings need it.

---

## PART 0 — RUN ALONE: NOT ACHIEVED. My own check gave a false all-clear.

**CORRECTED after publication.** The first version of this section said the window opened at
08:32:47 and broke later. **That was wrong, and the evidence contradicting it was sitting in a
monitor I had started and had not read.**

At 08:23 UTC the precondition plainly failed — two Claude session scripts plus
`tests/run_all.py` and `tests/test_spotify_serial_fix.py` were running, `master_leads.csv`
written 2 minutes earlier, `config.json` 5 minutes earlier. **No write was made.** I started a
continuous watcher (20 s sampling, resetting the clock on any repo write or repo-scoped
python process) and waited.

At 08:32:47 a **point-in-time** check showed processes clear and the files idle 873 s / 685 s,
so I took backups and began work. **That snapshot was a false all-clear.** The continuous
watcher recorded **40 resets across 109 samples**, including `testsun_all.py` running again
at **08:34:35 — 108 seconds after I declared the window open** — and further concurrent
`run_all.py` / repo-script activity at 08:38–08:44.

**The 10-minute quiet precondition was never satisfied before or during this work.** The first
genuine continuous quiet window began at **08:52:06** and completed at **09:02:22 UTC** —
after the implementation, the suite run and the original publication were all finished.

Timeline from the watcher (start 08:24:57, exit 09:02:22):

| time (UTC) | event |
| --- | --- |
| 08:26:20 | another agent: `tests/test_spotify_finder.py` |
| 08:30:48 – 08:34:35 | another agent: `testsun_all.py`, `tests/run_all.py`, `tests/test_funnel.py` — **spanning my 08:32:47 "clear" reading** |
| 08:38:45 – 08:43:17 | my `youtube_finder.py` writes, concurrent with another `run_all.py` |
| 08:44:35 | `WROTE: spend.json` — `probe:bl757_engagement`, 4 IG calls, $0.0024, another agent |
| 08:52:06 | last reset; continuous quiet begins |
| 09:02:22 | 600 s of quiet achieved |

**The lesson is mine, not the tooling's:** I ran a continuous monitor precisely because a
snapshot cannot prove a 10-minute condition, then published a 10-minute claim off the
snapshot while the monitor was still running unread. A point-in-time sample can only ever
disprove quiet, never establish it.

**What does NOT change — every integrity check was substantive and still holds:**

- `master_leads.csv` **byte-identical**, 131,191 rows, hash matched against the pre-state manifest.
- campaigns SHA **8e02f8d6f6307ae8 — MATCH**.
- `config.json` 161 keys, **only the `youtube_finder` block differs** from the backup.
- All **18 diff hunks** in `youtube_finder.py` verified to sit in regions this round edited.
- `spend.json`'s $0.0024 delta is attributable by ledger entry to another agent's probe; this
  round's probe writes nothing to it (verified in source).
- Suites **50/50, 2,250 checks** green.

So **no clobbering occurred and the shipped change is sound — but it was made in a contended
repo, not an isolated one.** If run-alone is a hard requirement rather than a preference, this
round does not satisfy it, and that is stated plainly rather than left to the reader to infer.

---

## PART 1 — the view floor, measured

**Method:** four queries, one per niche (fitness, gaming, podcaster, business), 50 channels each, reading the *same* `channels.list` response the funnel already pays for. 200 channels, ~404 quota units.

| niche | n | pass 50k | fail |
| --- | --- | --- | --- |
| fitness | 50 | 6 | **44** |
| gaming | 50 | 24 | 26 |
| podcaster | 50 | 19 | 31 |
| business | 50 | 15 | 35 |

**viewCount distribution among the 136 that FAIL the subscriber floor** — the population the OR-clause exists to sort:

| p10 | p25 | p50 | p75 | **p90** | p95 | p99 | max |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 985 | 25,476 | 154,505 | 785,569 | **2,102,000** | 9,191,912 | 24,544,829 | 59,250,673 |

**What each candidate N would rescue:**

| N | rescued | % of failures | median subs | median views/sub | **median views/year** |
| --- | --- | --- | --- | --- | --- |
| 1,000,000 | 28 | 20.6% | 16,250 | 201 | 352,572 |
| **2,000,000** | **14** | **10.3%** | **27,650** | **326** | **1,107,117** |
| 3,000,000 | 13 | 9.6% | 28,100 | 325 | 1,141,425 |
| 5,000,000 | 9 | 6.6% | 36,400 | 325 | 1,202,690 |

### Why 2M and not 1M or 5M

1. **1M is too low.** Median views/year of the rescued group is 352,572 — barely distinguishable from routine. Stepping to 2M triples it to 1,107,117. That step is the boundary between "has an audience" and "posted a lot once".
2. **The control separates cleanly at 2M.** Sub-50k channels below 1M views (n=108) run a median of **25,358 views/year**. The N=2M group runs 1.1M — a 44× gap. There is no need to climb higher to avoid junk.
3. **2M is calibrated against what already gets in.** Channels passing on subscribers have p10 = 2,718,523 views. N=2M sits just below that, so the clause is not a side door — it admits channels at least as watched as the quietest decile already flowing through. Only 3 of 64 passers (4.7%) are below 2M.
4. **3M and 5M buy nothing.** 3M rescues 13 instead of 14 for an identical median profile. 5M drops to 9, losing five live channels (e.g. 3.9M views / 1.5M per year) with the control already separated.

**The brief predicted N between 2M and 5M. The measurement puts it at the bottom of that band.**

### The rescued population, in full

Every sub-50k channel above 2M views — this is exactly who the old gate was deleting:

| niche | subs | views | views/sub | age | views/yr | cc |
| --- | --- | --- | --- | --- | --- | --- |
| fitness | 39,900 | 59,250,673 | 1,485 | 2.8 y | 20,889,294 | — |
| business | 22,400 | 24,544,829 | 1,096 | 17.7 y | 1,386,269 | US |
| **podcaster** | **26,900** | **16,843,033** | **626** | 5.8 y | 2,899,113 | DE |
| fitness | 38,400 | 12,475,207 | 325 | 10.9 y | 1,141,425 | US |
| gaming | 36,400 | 10,787,225 | 296 | 7.5 y | 1,436,920 | US |
| fitness | 27,200 | 9,532,614 | 350 | 7.9 y | 1,202,690 | US |
| fitness | 45,400 | 9,191,912 | 202 | 16.9 y | 543,172 | IN |
| gaming | 44,900 | 6,928,827 | 154 | 6.5 y | 1,072,808 | US |
| business | 28,100 | 5,298,179 | 189 | 11.3 y | 470,270 | IN |
| fitness | 20,400 | 3,923,332 | 192 | 2.6 y | 1,529,346 | US |
| gaming | 10,600 | 3,523,004 | 332 | 14.0 y | 252,210 | US |
| gaming | 10,500 | 3,442,645 | 328 | 11.9 y | 289,130 | RU |
| business | 40,200 | 3,398,143 | 85 | 9.0 y | 377,715 | IN |
| **fitness** | **1,360** | **2,102,000** | **1,546** | 2.3 y | 906,441 | — |

Row 3 is BL-748's case — **26,900 subs / 16,843,033 views / 626 views per subscriber — independently reproduced.** The last row is the strongest argument for the whole change: **1,360 subscribers, 2.1M views.** No subscriber-based gate at any threshold reaches that channel.

**Shipped:** `DEFAULT_MIN_VIEWS = 2_000_000`, config-editable via `youtube_finder.min_views`, 0 disables the rescue.

---

## PART 2 — description emails, parsed after the country gate

Measured on the same 200 channels, at the position the code now parses:

| | count |
| --- | --- |
| raw regex hit somewhere in the 200 descriptions | **25** (12.5%) |
| channels surviving the size + country gates | 45 |
| **usable address from description, post-gate** | **6** (13.3% of survivors) |
| personal / role | **6 / 0** |

**25 raw = 6 yielded + 1 guard-rejected + 18 gate-dropped.** The 18 break down as **non-target country 9** (IN 2, DE 2, FR 3, PK 1, **BD 1** — BL-750's exact case), **low subscribers 7**, too-many-subscribers 2.

**That is the ordering finding, quantified: 19 of 25 raw hits are addresses you cannot use.** Parsing before the gate would have reported a 12.5% fill rate against a population that never ships.

**Two of the six survivors exist only because of PART 1** — both sit below the old 50k floor and were kept on views:

| channel | subs | views |
| --- | --- | --- |
| Rajni Singh | 39,900 | 59,250,673 |
| Letz Play K | 44,900 | 6,928,827 |

**Items 1 and 2 compound: the view rescue does not just add channels, it adds contactable ones.**

### A guard gap found and closed

BL-750 measured the yield *through* `valid_email` + the junk blocklist + `PLATFORM_HOP_DOMAINS`. The helpers this funnel inherits (`_pick_email`/`_clean_email`, borrowed from the Google-Play finder) check regex shape and the asset-TLD trap but **deliberately never call `bio_parser.valid_email`** — on their own they pass `info@youtube.com`. A `_guarded_pick()` wrapper now applies the full chain at this call site. **`_clean_email` itself was left untouched**: it is shared with Google Play, and widening it would silently move that funnel's numbers.

A funnel-scoped `_OWN_PLATFORM_DOMAINS` set was added for the same reason — an address *at* the platform hosting the channel is boilerplate, never the creator's inbox — following the precedent `bio_parser` already sets for funnel-scoped hop domains, rather than widening the shared lists every funnel reads.

**A pleasing accident worth recording:** the one address rejected post-gate was **a role address at the `meri.et` domain (channel name withheld)** — the very channel BL-748 flagged as the self-declared-country false pass (declares `country=US`, Ethiopian domain). It was caught by `valid_email`'s TLD allowlist, **not** by any geo check. Do not mistake this for the country gate working.

---

## PART 3 — hiddenSubscriberCount

When YouTube flags the subscriber count as not public, that number is not evidence of smallness and can no longer be the reason for a cut. Such channels **skip the subscriber test entirely** and are judged on views; if views are unknown too they are **kept and flagged** (`size_flag = "subscriber count hidden"`) rather than dropped on a figure the API disowns.

**Honest limit: `hiddenSubscriberCount` was true on 0 of 200 channels in this sample.** The guard is correct by construction and is unit-verified, but this sample cannot demonstrate its real-world impact.

---

## PART 4 — niche presets and the new default

`NICHE_QUERIES` gains **fitness** and **gaming**, each with three queries. **The default is now `fitness`**, and the change is applied in all three places it lives, atomically:

| where | before | after |
| --- | --- | --- |
| `youtube_finder.DEFAULT_NICHE` | *(did not exist)* | `"fitness"` |
| `config.json` → `youtube_finder.niche` | `"podcaster"` | `"fitness"` |
| `control.py:2444` fallback | hardcoded `"podcaster"` | `yf.DEFAULT_NICHE` |
| `run_live` unknown-niche fallback | `NICHE_QUERIES["podcaster"]` | `NICHE_QUERIES[DEFAULT_NICHE]` |

The last row was found while implementing and matters: an unknown or typo'd niche previously fell back to **the measured-worst preset**, which is the exact failure this round removes.

**Why fitness over gaming**, given gaming scores higher in-band (82% vs 68%): gaming skews to entertainment channels that monetize through brand deals rather than by hiring editors, while fitness coaches sell a product and buy content. The in-band rates are close enough that fit-to-offer decides it. **`podcaster` remains selectable**, with a comment recording that it was the shipped default until today and why it was demoted — 4 of 5 top results Balkan, some with English titles the text language filter cannot catch, so only the country gate stops them.

---

## PART 5 — gates that were running on code defaults alone

`max_subscribers` was live at 5,000,000 via `DEFAULT_MAX_SUBS` with the key absent from `config.json`. **It is now written explicitly.** Auditing the rest of the funnel's knobs:

| gate | value | before | after |
| --- | --- | --- | --- |
| `max_subscribers` | 5,000,000 | **code default only** | **in config** |
| `min_views` | 2,000,000 | *(new)* | **in config** |
| `fetch_delay` | 0.3 s | **code default only** | still code-only |
| `block_halt` | 8 | **code default only** | still code-only |
| `max_seconds_per_channel` | 30 s | **code default only** | still code-only |
| `allowed_countries` | US/GB/CA/AU/IE/NZ | top-level `youtube_allowed_countries` | visible |
| `allowed_languages` | `["en"]` | top-level `youtube_allowed_languages` | visible |

**Three gates remain invisible to a config reader — `fetch_delay`, `block_halt`, `max_seconds_per_channel`.** All three are politeness/resilience controls rather than quality filters, so none changes who gets contacted; they are reported, not silently added, because writing keys nobody asked for is its own kind of surprise.

---

## PART 6 — customUrl and publishedAt, kept

Both arrive free in the same `channels.list` response and were being parsed and discarded.

- **`customUrl` → `tiktok_url`** (the generic profile-URL column) as `https://www.youtube.com/@handle`, falling back to the `/channel/<UC…>` form. The `@handle` link survives a channel rename; the id form is stable but opaque.
- **`publishedAt` → `quality_note`**, as `channel created YYYY-MM-DD`, alongside lifetime views and any size flag. **Deliberately not written to `last_post_date`**: that column means "last activity" and channel creation is the opposite end of the channel's life. Filing it there would have quietly corrupted every dormancy calculation downstream.
- `viewCount` also now surfaces in the xlsx (`Lifetime views`, `Channel created`) so the number driving the new gate is visible to whoever reads the sheet.

Fill on the sample: `viewCount` 200/200, `publishedAt` 200/200, `customUrl` 199/200.

---

## PART 7 — the country gate's self-declared field, recorded not fixed

`snippet.country` is **set by the channel owner and not verified by YouTube**. The measured case stands: a 236k-subscriber podcast declares `country=US` while its contact domain is `.et`. The gate therefore filters *stated* geography, not actual geography.

Recorded as a documented precision ceiling in the code, with the reason no fix is offered: there is no free corrective signal (a contact-domain TLD is weak on its own — plenty of US creators use `.co`/`.io`), and a paid geo lookup is precisely the cost this free gate exists to avoid.

---

## Verifications

| check | result |
| --- | --- |
| `tests/run_all.py` | **ALL GREEN — 50/50 suites, 2,250 checks, 81.6 s** |
| campaigns SHA (sha256[:16]) | **8e02f8d6f6307ae8 — MATCH** |
| `config.json` parses | **OK — 161 top-level keys**, only `youtube_finder` changed |
| `master_leads.csv` | **byte-identical**, 131,191 rows |
| RUN ALONE precondition | **NOT ACHIEVED** — see PART 0; work was done in a contended repo |
| `spend.json` | changed by **another agent** (`probe:bl757_engagement`, $0.0024 at 08:44:35), not by this round |
| 626-views-per-sub case | **passes**, `size_flag = "kept on views"` |
| dead 617-sub / 88k-view channel | **still cut** (`low_subscribers`) |
| hidden subs, views unknown | **kept + flagged**, never cut on subs |
| default niche | code `fitness` == config `fitness`; `podcaster` still selectable |
| `max_subscribers` in config | **5,000,000, visible** |
| `customUrl` / `publishedAt` | **persisted** to master + xlsx |

## Honest limits

- **200 channels, one query per niche.** The niche split is 50 each; the p90/p95 tails rest on tens of channels, not hundreds. N=2M is well-separated from the control, but the precise percentile is not pinned to ±0.
- **`hiddenSubscriberCount` fired 0/200** — the guard is verified by construction and unit test, not by field incidence.
- **The 6/45 post-gate email yield is a small denominator.** It is consistent with BL-750's 8.0%/6.0% but does not independently confirm it, and the 45 survivors reflect this sample's country mix.
- **"Genuinely good" is proxied**, not observed: views, views/sub and views/year are reach signals, not evidence a channel will hire or pay a clipper.
- **`fetch_delay`, `block_halt`, `max_seconds_per_channel` remain code-only** by choice.

**No credential appears in this document or in any committed asset. Raw description text is stripped from the published sample.**
