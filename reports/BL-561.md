# BL-561 — merge BL-538 (the Pause-and-Freeze undo) to main

**`origin/main` = `9e13650a`** · verified push (`origin == local`) · tags `pre-merge-BL-561` (5bcdbc58) / `post-merge-BL-561` (9e13650a).

There is now a way to undo a mistaken Pause and Freeze. BL-538 was finished and pushed but deliberately held by prior merge rounds because it is a 15-file schema-plus-migration change; it is now on main.

---

## STEP 0 — the truth per branch

| Branch | On origin | Already on main? | Evidence |
|---|---|---|---|
| `checkpoint/BL-538` | **YES** @ `044d2172` (matches stated SHA) | **NO — genuinely unmerged** | `merge-base --is-ancestor origin/checkpoint/BL-538 origin/main` false; `rev-list --count origin/main..` = **1** |
| `checkpoint/BL-558` | **NO — does not exist on origin** | n/a | `git rev-parse --verify origin/checkpoint/BL-558` fails |

Only BL-538 was merged. BL-558 does not exist, so there was nothing to merge for it. BL-538's merge-base with main was `b9147a92`, so main had commits BL-538 lacked (a real 3-way merge, not a fast-forward).

---

## The merge

`git merge --no-ff origin/checkpoint/BL-538`. **Only BACKLOG conflicted.** `prisma/schema.prisma` and `force-recalc-earnings/route.ts` auto-merged cleanly (verified below). 15 files, +2330/-27.

### BACKLOG union, corruption trap avoided

The conflict was not a simple append: HEAD carried BL-543 through BL-556, and BL-538's side carried its own `## BL-538` entry plus a duplicate `## BL-539`. I resolved it with **line-number deletes only — no regex, no `String.replace`, no replacement string** — so the `$2` in "$2,000" cannot be read as a capture reference. I kept all of HEAD, kept BL-538's unique `## BL-538` block, and dropped the duplicate BL-539 that HEAD already had.

Verified after:
* Headings: **65** = pre-merge main's 64 + BL-538's 1. Checked exactly.
* `## BL-538` present once; `## BL-539` back to one occurrence.
* Duplicated numbers: only **BL-536 x2** and **BL-541 x2**, both known-legit collisions from prior rounds. Nothing else.
* Literal `$2,000` present **13** times, intact. The "undo for Pause and Freeze" block appears **once** (no 11x explosion).
* Zero conflict markers anywhere in `src/`, `prisma/`, `scripts/`, `docs/`, BACKLOG.

---

## The schema — applied via the sanctioned path, `prisma migrate` never run

`scripts/migrations/BL-538-era-exempt-schema.sql` is purely additive: `ADD COLUMN IF NOT EXISTS` × 4 (`eraExempt` bool default false, `eraExemptAt`, `eraExemptByUserId`, `eraExemptReason`), one `CREATE INDEX IF NOT EXISTS`, one guarded FK. `run-schema-sql.js` permits exactly these shapes.

**Read-only check found the schema was ALREADY fully applied in production:**
* 4 `eraExempt*` columns present (`information_schema.columns`).
* Index `campaign_status_changes_era_lookup_idx` present (`pg_indexes`).
* FK `campaign_status_changes_eraExemptByUserId_fkey` present (`pg_constraint`).
* **All 32 status rows `eraExempt=false`** — exactly BL-538's claim that 0 live classifications moved.

So the additive SQL had already been run (by the BL-538 session). Re-running it would be a genuine no-op (every statement is `IF NOT EXISTS` or guarded), and I confirmed idempotently that the column, index and FK are all present. **What I ran:** `npm ci`, then `npx prisma generate` (which regenerated `src/generated/prisma` with the `eraExempt` field, verified present). **What I did NOT run:** `prisma migrate` (never), and the repair SQL (below).

### The optional repair SQL: EXISTS, and UNRUN

`scripts/migrations/BL-538-repair-falsified-audit-rows.sql` exists and is a **data write**. Its own header says "Claude Code did NOT run this". I did not run it. **Proof it is unrun:** the three rows BL-523/525 falsified (bees.n.honey, Panic Baby, STRAENGE) still have `fromPauseSource IS NULL` and `eraExempt=false` — a live SELECT returns **3** such rows. Running the repair before the code deployed would restore `fromPauseSource='AUTO'` and, against the old code that does not yet check `eraExempt`, **re-freeze all three**. BL-538's order is schema, then deploy the code, then optionally this. The code is now merged; whether to run the repair is the owner's decision, and it is safe only after Railway has deployed this merge.

---

## STEP 6 — scrutinising the diff

**BL-534's revert survives intact.** `campaign-era.ts` still matches **only** `toStatus:"ACTIVE"` AND `fromPauseSource:"AUTO"`, now AND `eraExempt:false`, in both query sites (lines 99-103 and 130-135). The BL-534 exemption symbols are gone: `resumeMintsEraBoundary`, `ERA_BOUNDARY_EXEMPT_TRIGGERS`, `ERA_EXEMPTION_EPOCH` all return 0 occurrences; the one `AUTO_BUDGET_INCREASE` is in an explanatory comment, not code. So a budget top-up still permanently freezes pre-existing clips, and MANUAL_OWNER still closes an era. The new `eraExempt` check is purely additive and defaults false, so it changes nothing until an owner presses undo.

**The never-decrease guard is in committed code.** `earnings-never-decrease.ts` is a new committed module (132 lines) exporting `decideNeverDecrease` / `logNeverDecreaseBlock`. `force-recalc-earnings/route.ts` imports and applies it (`BLOCKED (never-decrease) stored $X > recompute`, and "never-decrease ACTIVE — no clip was written down"). This replaces the prior guard that BL-528 proved existed only in an uncommitted script (`willIncrease` in a snapshot JSON, zero source files). **Force-recalc can no longer write a clip down.**

**No earnings maths changed.** No money file is in the merge diff at all.

**The behavioural guarantees, traced in the merged code:**
* Neutralises a boundary via the truthful `eraExempt` column, NOT by hand-flipping `fromPauseSource` (`campaign-freeze-undo.ts` header: "WHY NOT hand-flip fromPauseSource to NULL ... because it is FALSE").
* Resets BOTH cadence reasons: `FREEZE_CADENCE_REASONS = ["AUTO_PAUSED", "OLD_WINDOW"]` (the pair BL-523 half-fixed across 493 clips).
* Shows the blast radius before acting: `dryRun` drives both the preview and the execute, reporting clips/clippers/dollars.
* Idempotent: "recompute (guarded; absolute, so re-running is a no-op)" and an explicit `alreadyExempt` short-circuit. A second press changes nothing.
* Resume confirmation defaults to FROZEN: `unfreeze/route.ts` returns `defaultChoice: "KEEP_FROZEN"` with "Default FROZEN = the owner's rule holds by doing nothing" — the rule can only be surfaced, never altered.

---

## Safety

**6 money files byte-identical by blob OID**, via `git show`/`rev-parse` on **both** refs (pre-merge main and merged HEAD), never worktree-vs-git-show:
```
7aa6be48… clip-earnings-writer   797e2098… earnings-calc   f8499e18… balance
c2db8615… tracking               61cef393… invariant-middleware   ef5cdae7… money-decimal
```
BL-538 did not touch any money file (I checked against its merge-base: the two files whose blobs differ from current main — writer and tracking — were moved by MAIN after BL-538 branched, not by BL-538). **No clip's earnings changed, none decreased, no money was written by this merge.** `somesome` untouched. BL-534's revert intact.

## Gates (honest, every trap guarded)

* `npm ci` exit **0** (`omit` empty, `NODE_ENV` unset, dev deps installed).
* `npx prisma generate` exit **0**, run **before** tsc, `eraExempt` in the generated client.
* `tsc --noEmit` exit **0, 0 errors** — but I did not trust tsc alone.
* `npm run build` **actually run**, exit **0** read from a log with `$?` echoed directly, never piped to `tail`. **61/61** pages, **no OOM**, **no duplicate-symbol clash** (the failure BL-538's own build #1 hit). This is the gate the brief warned matters: BL-541 passed tsc and failed the build.
* eslint **confirmed present** (v9.39.4 binary) before the gate; hooks gate exit **0 → 0 errors / 11 warnings**, genuinely ran (not a silent no-op).
* No dashes.

Worked in an isolated worktree at `C:/bl561` (short path) because the primary tree is held by the live BL-559 round; the live BL-538, BL-549 and R-1 worktrees were untouched (BL-538's had its own node_modules, so `npm ci` could not break it).

## Rollback

`git reset --hard pre-merge-BL-561 && node scripts/safe-push.mjs main --force`. The additive schema can stay (harmless; all rows default false) or be dropped via the exact rollback block in the schema SQL — drop the code first.
