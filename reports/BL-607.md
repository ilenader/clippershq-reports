# BL-607 — Merge BL-605 to main (slideshow/carousel null-vs-0 fixes)

Date: 2026-07-20. Result: origin/main advanced `8ea418b0` to `286e386e` (verified,
origin==local). One `--no-ff` merge. Merge only, no code authored. 6 money files
byte-identical by blob OID; tracking.ts absent from the diff. Build fully green.

## STEP 0 — truth (with SHA)

`origin/checkpoint/BL-605 = 2168ac34d00d0619f49d9020365b07f35af8b177` (matches the
expected @ 2168ac3). `merge-base --is-ancestor BL-605 origin/main` = 1 (NOT an
ancestor); 1 commit ahead of main -> GENUINELY UNMERGED. Nothing a live round
holds was merged: only checkpoint/BL-605 was merged; BL-606 (the multiposting
build, a separate unmerged branch) was not touched. The primary main tree was
DIRTY with another round's staged file, so the merge ran in an ISOLATED worktree
(/c/bl607, short path, no node_modules junction) branched from origin/main, then
fast-forwarded onto origin/main.

## Money-file safety (git show on both refs)

Compared every money file on `origin/main` vs `origin/checkpoint/BL-605` and again
on the MERGED HEAD `286e386e` vs `origin/main`. All identical:
```
clip-earnings-writer 7aa6be48   earnings-calc 797e2098   balance f8499e18
tracking b311323c   clip-earnings-invariant-middleware 61cef393   money-decimal ef5cdae7
```
`tracking.ts` is NOT in the merge diff. BL-605 is a new-writes-only change; no
existing clip's stored views or earnings changed.

## Merge + BACKLOG

The merge was CLEAN (no conflicts — BL-605 branched from the current main, so
BACKLOG.md, clips/route.ts, and hikerapi.ts all merged without conflict). Zero
conflict markers in the tree. BACKLOG entry count is the exact union: main 77 +
BL-605's one entry = 78 `## BL-###` entries on the merged result (no capture-group
corruption; nothing to hand-resolve since there was no conflict).

Merge diff (3 files): BACKLOG.md, src/app/api/clips/route.ts,
src/lib/scraper-providers/hikerapi.ts. The two fix-file blobs on the merged HEAD
equal BL-605's exactly (route.ts `67e5b775`, hikerapi.ts `d8985793`), so the fixes
landed intact.

## STEP 6 — confirmations on the merged result

• Mixed carousel returns NULL not 0: `hikerapi.ts:490` reads
  `views: usedKey === null ? null : sum` — when no video child yielded a readable
  count (usedKey null) it returns null; a readable sum is returned otherwise.
• First ClipStat SKIPS on a null resolve: `clips/route.ts:1143`
  `if (resolvedFirstViews != null) { await tx.clipStat.create({ ... views:
  resolvedFirstViews ... }) }` — a null resolve writes NO first snapshot (never a
  fabricated 0), since `ClipStat.views` is a non-nullable Int.
• Normal video still writes a real count INCLUDING a real 0: `resolvedFirstViews =
  fetchedStats?.views`; a genuine 0 satisfies `0 != null` and is written, and any
  numeric count is written — only an unresolved null is skipped.

## Build honesty

`npm ci` clean (exit 0; wipes the generated Prisma client). `npx prisma generate`
(exit 0) run BEFORE tsc. `npm run build` exit 0 captured from a LOG file (not piped
through tail). prebuild gate ran with eslint PRESENT (real gate, not a silent
no-op) -> `11 problems (0 errors, 11 warnings)` = passes. `✓ Compiled
successfully` (tsc 0 errors). Next build fully ran: `Generating static pages
(61/61)`. The worktree needed `NEXTAUTH_URL` set for build-time page-data
collection (env trap, not code).

## Ship

origin/main = local HEAD `286e386e` (verified after push, origin==local). Tag
`merge-BL-607`. Rollback: `git push origin 8ea418b0:main` (the merge is the top
commit). No dashes used as bullets.
