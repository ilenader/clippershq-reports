# BL-594 — Supervised first run of the auto-retirement cron

**Status: DONE, SAFE.** One capped live run executed under supervision; 7 dead clips retired, 93 alive
correctly spared, earnings frozen not zeroed, fully reversible. No code changed (docs-only round; no
build run). 6 money files byte-identical by blob OID. No Apify actor run.

How it was run: CRON_SECRET is only in Railway, so the production HTTP endpoint could not be curled from
here. The route is a thin wrapper over `retireDeadClips({dryRun, minGone:3, maxPerRun:100})`; I invoked
that exact function via a throwaway tsx runner against the same production DB + HikerAPI key (identical
operation to the 06:00 cron, just watched). The runner was deleted after; nothing committed to src.

## PART 0 — schedule is live, has NOT fired yet
- `retire-dead-clips` is registered in `CRON_JOBS` (BL-593, daily 06:00 UTC) and the owner has it in
  `RAILWAY_NATIVE_CRON = lifecycle,watchdog,retire-dead-clips`, so it is in the effective allowlist.
- Has it fired? NO. Zero `cron_runs` rows of any `retire` kind (the route writes no heartbeat by design),
  and the only recent retirements (710 clips at 2026-07-18 19:09-19:10 UTC) are BL-584's one-off MANUAL
  sweep, not the cron. DB clock at check: 2026-07-19 21:07 UTC.
- **Next unattended fire: 2026-07-20 06:00 UTC = 08:00 Belgrade time** (CEST, UTC+2). No surprise at 06:00.

## PART 1 — dry run (dryRun=1, wrote nothing)
100 candidates probed (capped from 805), 100 live HikerAPI 404 re-probes:
- **would retire: 7** (fresh definitive 404 this run)
- **excluded as ALIVE: 93** (returned HTTP 200) — these keep polling, untouched
- ambiguous/skipped: 0 ; frozen-earnings sum: **$18.15**
- Full would-retire id list (all 7; earnings frozen, consecutiveGone):
  1. cmpbasm14002g0pqu61v9lytb  cg=1  $2.04  APPROVED
  2. cmpdy8xrp003p0plc003obf62  cg=1  $4.43  APPROVED
  3. cmpe3ewqf005u0plczzcfe8pw  cg=1  $4.35  APPROVED
  4. cmpbatb39002k0pqup72k7w6q  cg=1  $1.29  APPROVED
  5. cmpbapz0r002c0pqul5gmwspg  cg=1  $2.99  APPROVED
  6. cmpdy8a30003l0plcjy3u1z2k  cg=1  $3.05  APPROVED
  7. cmq8m7epm00050plm12eefsz7  cg=0  $0.00  REJECTED
- Probe cost disclosed: 100 HikerAPI /media-by-url probes (paced 1200ms). This is the first run clearing
  a backlog; later daily runs are small deltas.

## PART 2 — safety verified on real data (BL-584 proved 24/734 were alive, so the gate matters)
- No alive clip in the retire set: only httpStatus===404 retires; the 93 that returned 200 are excluded
  by construction. **93 of 100 candidates were alive** and the live gate spared every one; without it they
  would have been wrongly frozen. This is the re-probe gate demonstrably working.
- Every retire-set clip returned a fresh 404 THIS run. Per-run cap enforced (candidates=100 from 805).
  Earnings freeze not zero: the write touches only `videoUnavailable` + `videoUnavailableSince`.

## PART 3 — one supervised live run (dryRun=0), capped at 100
Result: candidates 100, reprobed 100, confirmedGone 7, revived 93, ambiguous 0, **retired 7**, frozen $18.15.
BEFORE/AFTER on the 7 (snapshots at 21:15:57 and 21:21:16 UTC):
- All 7 flipped `videoUnavailable` false to true, `videoUnavailableSince` stamped 21:16-21:18 UTC — they
  left the poll set (earnings-summing queries filter videoUnavailable=false; Apify stops polling them).
- earnings / baseEarnings / bonusAmount IDENTICAL before and after every id (sum $18.15 / $17.81 / $0.34).
  Frozen, not zeroed, not decreased. Invariant earnings ≈ base + bonus intact per clip.
- `trackingJob.isActive` still true for all 7 — trackingJob was NOT deleted (reversible).
- Exactly 7 clips newly retired in the window (verified by count) — no alive clip touched.
- Global earnings: $9374.24 (21:07) to $9374.32 (21:21). It rose $0.08 via normal tracking drift (clip
  count 4046 to 4047); it did NOT fall. Retirement wrote no earnings column, so it cannot move earnings.
- Backlog: 805 candidates to 798 after the run.

### Rollback SQL for THIS batch (reverses all 7 instantly)
```sql
UPDATE clips SET "videoUnavailable" = false, "videoUnavailableSince" = NULL
WHERE id IN ('cmpbapz0r002c0pqul5gmwspg','cmpbasm14002g0pqu61v9lytb','cmpbatb39002k0pqup72k7w6q',
             'cmpdy8a30003l0plcjy3u1z2k','cmpdy8xrp003p0plc003obf62','cmpe3ewqf005u0plczzcfe8pw',
             'cmq8m7epm00050plm12eefsz7');
```

## PART 4 — the unattended 06:00 UTC run is now proven safe to leave on
- The daily run is the SAME operation I just ran: same cap (100), same mandatory live 404 re-probe, same
  reversible videoUnavailable-only write, same earnings freeze. PART 3 proved one real run is safe, so the
  unattended run is safe.
- Watchdog: no false alarm. BL-593 deliberately left retire-dead-clips OUT of the watchdog's
  JOB_NAME_TO_HEARTBEAT_KIND, so it is unwatched and can never fire CRON_DOWN; the critical money-cron
  (tracking) watch is untouched.
- Backlog drains over a few daily runs: 798 candidates remain but ~93% are ALIVE clips on slow cadence
  (checkIntervalMin>=1440) that re-probe 200 and never retire; only the genuinely-dead (single digits per
  run at current density) get frozen each day. Nothing dead escapes permanently; nothing alive is harmed.
- Owner daily audit query (see exactly what the cron retired on any date, never a black box):
```sql
SELECT id, "videoUnavailableSince", earnings, status FROM clips
WHERE "videoUnavailable" = true AND "videoUnavailableSince"::date = CURRENT_DATE
ORDER BY "videoUnavailableSince" DESC;
```
- IG Apify cost: retiring a dead clip removes it from the Apify poll set, so its ghost polling stops. As
  the daily cron keeps the dead set retired, the DEAD-ghost portion of IG Apify spend trends to ~0. Honest
  caveat: the cron's own HikerAPI re-probes (~100/day, paced, prepaid) are a separate small cost, and the
  93 alive slow-cadence clips correctly stay in the Apify poll set.

## Safety ledger
Reversible (videoUnavailable only, never trackingJob.delete); live 404 re-probe before every retirement
(93 alive spared); earnings frozen never zeroed and never decreased; per-run cap enforced; unresolvable =
skipped (ambiguous 0 this run); 6 money files byte-identical by blob OID on both refs; tracking.ts and
campaign-era.ts untouched; every probe disclosed (100 dry + 100 live = 200 HikerAPI); no Apify actor run;
no code changed so no build claimed. branch checkpoint/BL-594 at main 3e35954a.
