# BL-573 — fix the fake growth feed, stop the bloat, and stage ONE safe trigger

**2026-07-18 · base main @ 94f3cb17 · branch checkpoint/BL-573 · worktree C:/b573 (RAN ALONE).**

No email was sent. No env var was changed by me. No cold trigger was enabled. No rows were deleted.
Four growth-only files changed (feed route, admin page, engine, config); the 6 money files, tracking.ts,
campaign-era.ts and the scraper paths are byte-identical by blob OID.

## The state today (live, verified read-only)
- Real growth emails ever: **3** (all FIRST_CLIP_CELEBRATE, email). Practice rows: **270,645** now
  (138,835 email + 131,810 in-app), up from BL-568's 225,080 — the table grows without bound.
- Rows written per day: 97,626 (07-16), 97,352 (07-17) — ~90-97k/day for 3 lifetime real sends.
- Only trigger with any real (dryRun=false) send is FIRST_CLIP_CELEBRATE ⇒ the `GROWTH_ENABLED_TRIGGERS`
  allowlist is exactly `FIRST_CLIP_CELEBRATE`.

## PART 1 — the feed now tells the truth
`api/admin/growth/events/route.ts` took every `email_event` with NO dryRun filter; the page labelled
rows Practice/Live but 270k practice rows drowned the 3 real sends ~90,000-to-1.
- Route: added a `scope` param — **default `live`** (`dryRun:false`), `practice` (`dryRun:true`),
  `all` (legacy unfiltered). It only narrows the WHERE; reads nothing else, writes/sends nothing.
- Page (`admin/growth/page.tsx`): the feed now defaults to **Sent** (live). A **Practice** tab is
  present but OFF by default (accessible two-`aria-pressed`-button group in a `role="group"`, min 44px
  targets, non-color-only active state, plus a persistent `aria-live="polite"` status line so a screen
  reader is told the list changed — pattern reviewed by the accessibility-lead agent).
- The identical-relative-time effect is gone in the default view: it was caused by each 15-minute sweep
  bulk-writing thousands of same-instant dry-run rows, so the feed's whole page was one sweep's wall of
  identical `createdAt`. Real sends are 3 rows spread over days (07-16 12:36, 07-17 01:22, 07-17 01:24),
  never bulk-written, so the Sent feed cannot show a same-instant wall. Confirmed.

## PART 2 — the table bloat is stopped at the source
`engine.ts` wrote one dry-run `EmailEvent` per candidate per channel per sweep (the ~90k/day). Both
dry-run writes are replaced by a per-trigger in-memory counter (`stats.dryRunByTrigger`), logged at
sweep end (`dryRunByTrigger={...}`) — the same "which trigger matched how many" insight, zero rows.
- **Safe because nothing reads dryRun=true rows:** the ladder/idempotency (`loadTriggerHistoryBatch`),
  the frequency cap (`countSentEmailsSince`), channel-engagement and nudge-attribution ALL filter
  `dryRun:false`; the only consumer of dry-run rows was the feed, which now defaults to Live. The LIVE
  in-app ledger write (`dryRun:false`, the ladder's real memory) is preserved.
- **No existing rows deleted.** Row-growth per day: **before ~90-97k dry-run rows/day → after 0
  dry-run rows/day** (only the handful of real sends + future Resend-webhook rows persist).

## PART 3 — enable STALLED_7D, and ONLY STALLED_7D (owner sets the env var; I did not)
- Lever: `GROWTH_ENABLED_TRIGGERS` (env; no deploy). **Exact current value: `FIRST_CLIP_CELEBRATE`.**
  **Exact new value for the owner to set in Railway: `FIRST_CLIP_CELEBRATE,STALLED_7D`.** I did NOT change it.
- Cohort re-verified against live prod (the engine's own current dry-run matches, which is exactly who it
  would send to): **22 distinct users** in the last 3h (20 in the latest single sweep). Redacted domain
  breakdown: 19 gmail.com, 2 no-email.local (in-app only — gate-blocked for email), 1 outlook.fr ⇒
  **~20 emailable warm users**. Consistent with BL-568's ~25; the cohort drifts daily as users cross the
  7-day-since-last-clip boundary. No handle or address is printed here.
- **STALLED_7D has 0 real sends ever** ⇒ enabling it does not double an existing send. It is a
  "you paused, come back" nudge to users who WERE active — a different message and audience from
  CLIP_APPROVED (which already sends, R-5: 1,931 sent / 1,719 read). It is NOT CLIP_APPROVED_POST_AGAIN
  (the redundant in-app duplicate BL-568 flagged), which is left untouched and OFF.

## PART 4 — safety rails
- Confirmed for STALLED_7D's ~20: `canSendMarketing` enforces the per-user chokepoint (banned →
  suppression → one-click unsubscribe `emailMarketingOptOut` → `FREQ_CAP_DAILY=1` / `FREQ_CAP_WEEKLY=3`
  from LIVE ledger rows only → engagement). One-click kill switch (empty the allowlist OR master
  `GROWTH_ENGINE_LIVE`≠"true") still stops every send with no deploy (proved below).
- The gap BL-568 named: NONE of the per-user rails cap the TOTAL volume of a cold burst — 1/day does
  nothing across 811 different users, and `SCAN_BATCH_SIZE=5000` lets one sweep queue them all. **Closed
  here:** new `MAX_LIVE_EMAILS_PER_SWEEP=50` hard-ceilings LIVE emails queued per sweep (in-app is
  exempt — no domain cost). The check+push is synchronous so the conc≤2 sweep cannot overshoot; at the
  cap the sweep HOLDS the rest (retries next sweep, fail-safe under-send). It ONLY restricts, never widens.
- **WELCOME must NEVER be enabled in one sweep.** Its 811 cold contacts would risk the domain that also
  carries payout + verification email. If it is ever enabled it must be throttled to a few dozen/day; the
  per-sweep ceiling is a backstop, not that throttle. WELCOME is NOT enabled here.

## PART 5 — proof (no email sent; no env changed; no rows deleted)
Pure-logic (14/14 PASS):
```
STALLED_7D dry-run under CURRENT allowlist (FIRST_CLIP_CELEBRATE)      => false
STALLED_7D LIVE under RECOMMENDED (FIRST_CLIP_CELEBRATE,STALLED_7D)    => true
FIRST_CLIP_CELEBRATE stays live under RECOMMENDED                     => true
WELCOME / NO_CLIP_7D / STALLED_14D / COMMUNITY_SOCIAL_PROOF /
  CLIP_APPROVED_POST_AGAIN stay DRY-RUN under RECOMMENDED             => false (each)
kill switch: empty allowlist => STALLED_7D dry-run                    => false
kill switch: master live!='true' => dry-run even if allowlisted       => false
cap: STALLED_7D's 22 all queue in one sweep                          => {queued:22, capped:0}
cap: a cold WELCOME blast of 811 is capped per sweep                 => {queued:50, capped:761}
```
- Feed truth: `SELECT dryRun,channel,COUNT(*) ... type='sent'` ⇒ **false/email=3**, true/email=138,835,
  true/in-app=131,810. The Live default shows the 3; Practice is opt-in.
- No re-send on a second sweep: idempotency is unchanged by this round. A live send writes a
  `dryRun:false` EmailEvent row; the next sweep's `loadTriggerHistoryBatch` (which reads ONLY
  `dryRun:false`) sees the trigger as fired and `decideNextTrigger` does not re-fire it, and
  `sendGrowthEmail`'s deterministic idempotency key collapses a same-window re-run at Resend. Stopping the
  dry-run rows cannot affect this because the ladder never read dry-run rows.
- Build clean on the committed content: `Compiled successfully`, `Finished TypeScript` (0 errors),
  prisma-bypass 0 violations, removed-fields OK, hooks gate `11 problems (0 errors, 11 warnings)` (≤11),
  `BUILD_EXIT=0`. eslint binary present (it produced the 11-problem report — no silent no-op).

## Handoff / what the owner does
1. Deploy this branch (merge to main). It changes only the feed + bloat + adds the per-sweep ceiling; it
   does NOT enable any trigger.
2. Set `GROWTH_ENABLED_TRIGGERS = FIRST_CLIP_CELEBRATE,STALLED_7D` in Railway (no deploy). Watch open +
   bounce rates on ~20 warm sends for a few days.
3. Only then consider wider audiences. WELCOME's 811 go in throttled daily batches, never one sweep.
   Kill switch if anything looks wrong: blank `GROWTH_ENABLED_TRIGGERS` (instant, no deploy).

## Safety ledger
No email sent. No env var changed by me (named for the owner). No cold trigger enabled (WELCOME OFF).
No `email_event` rows deleted. 4 growth files changed; 6 money files + tracking.ts + campaign-era.ts +
scraper paths byte-identical by blob OID. No handle or address in this report. Probes: read-only
`run-select.js` only; no external API called, no cost.
