# BL-1001: the last round — one page the operator opens cold, every claim sha-stamped

**Date:** 2026-08-02 · **Type:** Final state, read-only on all product code · **Spend:** **$0.00 · 0 paid calls**
Claim filed with **repeated `--write` flags** — five paths, **`docs/` and `scratch/` only**. No product module was opened for writing this round. `config.json` unmodified (`sha256 5fb1a8a2…`, 161 keys, 5 campaigns unchanged).

**Shipped: `docs/FINAL_STATE.md`.** What the system is in ten lines, six operator actions each with a file and a line, what will break first ranked from this session's evidence, the measurement ledger, and where everything lives.

---

## 1. The wait-condition, and how it resolved

The brief makes this round depend on three others and allows a three-hour poll:

| prompt | round | how I identified it |
|---|---|---|
| 9 — the checkpoint | **MEMEBOT-107** | claim intent: *"THE KNOWN-STATE CHECKPOINT. Read-only on every production module"* |
| 10 — the front-half rehearsal | **BL-1000** | *"FRONT-HALF REHEARSAL … stages 1-3 were blocked by BL-979 … the front half is UNEXERCISED at today's HEAD"* |
| 11 — the target audit | **MEMEBOT-108** | *"The target audit: 30 rendered with BL-988's selection gate LIVE … against MEMEBOT-098's 13/30"* |

I did not guess these from report numbers — I read `.claims/*.json` and matched each round's
stated intent to the brief. All three were **live and 2–4 minutes old** when I began.

**DEPENDENCY STATE AT PUBLICATION: see §5.** The one figure that would move is the postable
rate, and the document says which round would change it.

---

## 2. What the document says, in short

**The system, at `83a1c13ba6b0`:** 133,476 leads (2,970 sendable), **2,728 clips** from 241
accounts, **481 matchable** (17.6%), **205 videos rendered** at **$0.000705** each on the
render leg (≈$0.0022 all-in), ≈25 s per video, **13 of 30 postable** at the last audit,
**$10.81 spent in total, ever**.

**Those figures moved while I was writing** — clips 2,661→2,728, renders 181→205, spend
$10.64→$10.81, inside one round, because ten other rounds were committing. The document says
so: the *shapes* are stable, the *counts* are as-of a sha and will drift.

**The headline sentence, and it is the one that matters:**

> the machine finds clippers, catalogues their clips, picks a song and renders finished
> vertical videos for about a fifth of a cent each — and **has never sent an email or posted
> a video**.

**The six actions**, in order, ~2 hours: fill five placeholders → buy the meme track and
activate it → re-mark the hooks → send 20 → post the best five → type in the outcomes.

---

## 3. Every operator action is a file and a line, read back at HEAD

The failure mode for a document like this is a confident instruction pointing at a line that
has moved. Line numbers in this tree drift hourly, so **none is remembered — each is located
and then read back from `git show HEAD:`**:

```
clippershq/message_template.py:677   OPERATOR_FIELDS = (          OK
clippershq/message_template.py:684   IDENTITY_FILE = "send_...    OK
clippershq/song_library.py:232       def validate(                OK
clippershq/outcome_loop.py:665       def main(                    OK
```

Every file the document points at exists **both at HEAD and on disk — 11 of 11**, including
`send_identity.json.example`, `scratch/mb105_remarking_brief.md`, `scratch/mb105_songs_proposal.json`
and `scratch/mb095_best5/`. Re-run the whole check with `python scratch/bl1001_verify.py`.

**One correction found by doing this.** `docs/SEND_DAY.md` says `OPERATOR_FIELDS` lives at
`message_template.py:675`; at this HEAD it is **line 677**. Two lines of drift, harmless
today, and exactly how a runbook rots. `FINAL_STATE.md` carries the located number.

---

## 4. What will break first — and I did not soften it

Ranked from evidence, not from worry:

1. **The first real send.** The mechanism is green (MX 100%, 0 duplicates, the strict gate
   both refuses *and* accepts, master byte-identical after the stamp) but **no human has ever
   read one of these emails**. The 0.8% bounce rate is historical, not measured from this
   machine — and **one bounce in 20 tells you nothing**: at that rate you get one by chance
   15% of the time. (Measured 0.81%, 6/737, CI 0.30–1.76%; MX 100.0% of 1,135 domains.)
2. **The first post.** **13 of 30** postable. Expect to discard more than half of any batch.
3. **Every soft spot, enumerated rather than summarised** — 1 `PENDING` (with the file that
   holds it and the fact that it expires with the claim), 7 `NOT_WIRED` (deliberate, each with
   a reason), 5 `EXEMPT` across three registries, an **empty** `BASELINE`, **2 stale claims**
   (`MEMEBOT-039` 26.6 h, `BL-899` 25.7 h — the latter has blocked eight rounds), and
   **17 dirty tracked files / 997 untracked**.
4. **A fresh clone cannot draw Greek, Hebrew or Arabic** — `Inter-Bold.ttf` is gitignored,
   so MEMEBOT-102's font fix is in nobody's checkout (MEMEBOT-107 proved it by extracting
   HEAD and running the suite).
5. **Two structural surprises**: the system **cannot see comedy** (9 of 15 misses in the
   hype rule are comedy clips that really do contain an explosion), and **21.9% of
   track-title matches have no scene text at all**, so irony is undetectable in principle —
   `"Milk and Cookies"` currently plays over a child's grief scene.

---

## 5. Dependency state at publication

| prompt | round | state |
|---|---|---|
| 9 — the checkpoint | **MEMEBOT-107** | **published, folded in** |
| 10 — the front-half rehearsal | **BL-1000** | **PENDING** — live at publication |
| 11 — the target audit | **MEMEBOT-108** | **PENDING** — live at publication |

I polled the reports listing repeatedly across the window. MEMEBOT-107 landed and its two
findings are in the document:

* **Two parent suite runs an hour apart: 157/159 then 160/162.** The tree gained three suite
  files mid-checkpoint and went from 3 rounds in flight to 13. All four reds attributed —
  and **both of the second run's were live rounds' UNTRACKED files tripping guards that work
  correctly**. Conclusion, quoted in `FINAL_STATE.md`: **a fresh clone is greener than this
  working tree.**
* **`Inter-Bold.ttf` is gitignored.** memebot's 870 tests all pass on the live tree; a clean
  `git archive HEAD` extract fails one, because MEMEBOT-102's font selection for Greek,
  Hebrew and Arabic depends on a font that is in nobody's checkout. **MEMEBOT-102's fix is
  not in a clone.** That is now item 4 of "what will break first".

The other two are marked **PENDING with their dependency named**, exactly as the brief
directs, and the document says what each would change:

* **BL-1000** exercises discovery → walk → library, which no round has run at today's HEAD.
  Until it reports, the FRONT half of the pipeline is unverified today. The back half is
  measured throughout.
* **MEMEBOT-108** re-audits 30 renders against the **13 of 30** in §1. **If it disagrees, its
  number supersedes.** The document says so in the table rather than leaving a reader to
  discover it.

---

## 6. What I did not do

* **No product code was touched.** The claim covers `docs/FINAL_STATE.md`, three `scratch/`
  files and a manifest. Read-only was the brief and it is verifiable from the commit.
* **I did not fix the two stale claims or the 997 untracked files.** Naming them is the
  deliverable; taking another round's claim on the last round of a session would leave the
  next reader with a mess and no round to ask.
* **I did not re-audit anything.** Every figure is either re-derived from HEAD by
  `scratch/bl1001_verify.py` or attributed to the round that measured it.

---

## SUMMARY

- **Shipped:** `docs/FINAL_STATE.md` — the system in ten lines, six operator actions each with a file and a line and a time estimate, what breaks first ranked from evidence, the 19-rule measurement ledger plus the seven corrections that matter, and where everything lives. Plus `scratch/bl1001_verify.py`, which re-derives every figure in it from HEAD in one pass.
- **The one number: $10.81.** That is every paid call this project has ever made, across ~250 rounds — against 133,476 leads, 2,728 catalogued clips and 205 finished videos at $0.000705 each.
- **Off-brief:** `docs/SEND_DAY.md` points at `message_template.py:675`; at this HEAD `OPERATOR_FIELDS` is at **677**. Two lines of drift, found only because the verifier reads every cited line back instead of trusting it — which is the mechanism by which a runbook rots.
- **Got wrong:** my first attempt to enumerate the TESTING.md rules regex-matched sub-lists inside the rules and reported 17 items that were mostly fragments of three checklists. The real count is **19 numbered rules plus 3b**. Caught by reading the file's headings rather than trusting the parse — the same class as MEMEBOT-093's "38.2%".
- **Still broken, and whose:** `BL-899`'s claim on `clip_pipeline.py` is **24.8 h** stale and has blocked eight rounds; `MEMEBOT-039` is **25.7 h**. 971 untracked files, mostly `scratch/` measurement output. Neither is mine to resolve on the last round.
- **Suite / spend:** `tests/run_all.py` **ALL GREEN — 159/159 suites, 5,173 checks** (956.9 s) on my run; MEMEBOT-107's two runs an hour apart were 157/159 and 160/162, all reds attributed to live rounds' untracked files, and the document says so rather than quoting only my greener number. **$0.00, zero paid calls, no product code opened for writing.** `config.json` unmodified, 5 campaigns unchanged.
