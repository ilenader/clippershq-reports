# BL-845: The walk was not run — `clip_runner.py` was rewritten three times during this round

**Date:** 2026-08-01 · **Class:** Held round · **Spend:** **$0.00 of $0.15** — no paid call was made.
**Claim:** `BL-844` filed at round start, ended at round end.
**Nothing was written** to `master_leads.csv`, `config.json`, `spend.json` or any module.

---

## Why the paid walk did not run

The round opened by checking who else was working. `tools/claim.py list` showed **BL-843 filed 1.3 minutes earlier**, declaring writes to `clippershq/control.py`, `clippershq/clip_runner.py`, `config.json` and `spend.json`, with the intent:

> *"fix 4 BL-842 defects: use user_pk column in resolve_pk, honest UNWIRED counters for speech_frac/ocr, retire 4 dead tags, ledger correction entry for the double-charge"*

That is the module the clip walk runs through, plus the ledger the walk meters into. Then the file moved, repeatedly, while I worked:

| file | observed writes during this round |
|---|---|
| `clippershq/clip_runner.py` | **13:56:55**, then again **14:00:16** (during my suite run) |
| `clippershq/control.py` | **13:57:12** |

Three saves to the walk's own code inside four minutes, from a round that has three of its four declared fixes still to land. A paid walk started in that window would import whichever bytes happened to be on disk at that instant, meter into a ledger another process is correcting, and produce measurements attributable to no known code version. The instruction was RUN ALONE; this is not alone.

**So the $0.15 was not spent.** Every item that needs production is still unproven, and I would rather report that than report numbers produced against moving code.

---

## What I could confirm without spending

### Item 4 — the `user_pk` fix has landed, and the target is 1.16, not 1.0

The other round has already written it. `clip_runner.py:119` now defines `known_user_pks(master_csv)` — *"{lowercased handle: user_pk} for every master row that already carries one"* — and `control.py:2447` calls it before the walk.

**But the expectation in the brief is not achievable.** The code's own comment records why:

> *"Master carries `user_pk` on 158 of the 187 confirmed pages (84.5%) as of 2026-08-01"* — and at `control.py:2445`, *"calls per account into 1.16"*.

29 of the 187 pages have no stored `user_pk`, so those still cost a `user_by_username` resolve. **1.16 calls per account is the correct target; 1.0 is only reachable once every confirmed page carries a pk.** A walk measuring 1.16 should be read as success, not as a partial fix.

### Item 5 — the metering fix is present in source

`control.py:2464` builds an `IncrementalMeter` for the clip block, with a comment at `:2496` describing the remainder-only accounting that prevents re-charging what earlier flushes already billed. The same pattern appears at the Twitch, Spotify and YouTube blocks (`:1545`, `:1981`, `:2207`). Present in source — **whether a long-lived process still holds a pre-fix import is exactly what a live run would reveal, and that is untested.**

### Baseline for the three unproven items

Measured on the current library so the next round has a before-picture:

| | value |
|---|---|
| clips stored | **755** |
| accounts | **66** |
| distinct fields present in stored records | **35** |
| `CLIP_FIELDS` declared in code | **60** |
| clips carrying `run_id` | **0 (0.0%)** |
| clips carrying `subtitle_text` | **0 (0.0%)** |

This is the cleanest possible statement of the problem: **the code declares 60 fields, the data exhibits 35, and the two newest columns are at zero.** Nothing has run since the wiring landed, exactly as the brief said. The 28-field gap and the subtitle fill rate are not measurable any other way than by walking.

---

## Proofs that did run

| check | result |
|---|---|
| `tests/run_all.py` | **ALL GREEN — 57/57 suites, 2,451 checks, 126.1s** |
| campaigns hash | **`8e02f8d6f6307ae8`** — matches baseline |
| `config.json` | parses, 162 top-level keys |
| master / spend / config | **unmodified by this round** |
| credentials | none printed, logged or committed |

The suite is green but it is a snapshot of a moving tree — `clip_runner.py` was saved again mid-run. Treat 57/57 as "green at 14:00 against whatever was on disk", not as a verdict on the finished BL-843 work. The suite grew by one file and 91 checks since this morning's 56/57 / 2,360, which is the other round adding coverage.

---

## What the walk still needs to prove, and what it will cost

Unchanged from the brief, all five still open:

1. `run_id` on real new rows — currently 0 of 755.
2. Subtitle fill **measured at walk time**, with its fetch-failure rate. BL-843's 37/37 is survivorship-flattered: those URIs were captured ~2 days earlier and were still inside the ~105-hour window. Only a walk-time fetch measures the real rate.
3. Fill rates for all 28 new DECLARED fields — 35 of 60 currently exhibited.
4. Calls per account, expected **1.16**.
5. Whether a long-running import is holding pre-fix metering code.

Item 3 of the brief — subtitle text beside OCR text for the same clip — additionally needs a clip that has both, which BL-843 could not find. That selection can only be made after a walk populates `subtitle_text`.

**Cost estimate for the run when the tree is quiet:** ~150 clips against the 187-page pool at 1.16 calls/account ≈ 217 calls ≈ **$0.13**, inside the $0.15 budget.

## Recommendation

Re-run this round unchanged once `tools/claim.py list` shows BL-843 ended and `clip_runner.py` has been stable for a few minutes. Everything else is ready: the fix is in, the baseline is captured, the suite is green, and the budget is untouched.

## Limits

I did not verify BL-843's four fixes beyond confirming that `known_user_pks` exists and is called — three of its four declared changes were still outstanding when I looked, and re-reading a file mid-edit proves nothing durable. The 158/187 and 1.16 figures are quoted from that round's own code comments, not independently measured.
